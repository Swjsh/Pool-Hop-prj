# Build Lessons

Running log of hard-won, non-obvious findings — bugs, tool quirks, workarounds. **Newest first.** One entry per lesson. Keep entries concrete (what happened, why, what to do). Cross-reference skills in [`.claude/skills/`](../.claude/skills/) where a lesson generalizes into a reusable procedure.

---

## 2026-07-03 (menu opens but mouse is uncontrollable) — **`SetInputMode_UIOnly` does NOT show the OS mouse cursor — `bShowMouseCursor` is a completely separate PlayerController flag, defaulting to `false`, and nothing in this project ever set it.** User report: main menu appears on Play, but there's no way to click Play/Quit at all.

Confirmed via `find_nodes(title:"Mouse")` on `BP_PlayerController`'s EventGraph returning **zero results** — the whole graph (main-menu-open, pause-open, pause-resume) only ever calls `SetInputMode_UIOnly`/`SetGamePaused`, never touches the cursor. `get_properties` on the CDO confirmed `bShowMouseCursor: false` (engine default). This is the same family of bug as the earlier "menu doesn't pause the game" fix (`SetInputMode` changing *which system* receives input, without touching an orthogonal, separately-defaulted-off property) — worth checking for this shape of gap whenever a UI-open path uses `SetInputMode_UIOnly` alone.

**Fix, in both directions, in both places that open a UI-only mode:** added `Variables|MouseInterface|SetShowMouseCursor` (creatable in a Blueprint's own graph as this exact type_id; from ANOTHER Blueprint's graph — e.g. `WBP_MainMenu` acting on the fetched `PlayerController` reference — the creatable form is instead `Class|PlayerController|SetShowMouseCursor`, same cross-object-accessor naming split documented elsewhere in this project) spliced immediately after every `SetInputMode_UIOnly`/before every `SetInputMode_GameOnly`:
- `BP_PlayerController`: main-menu-open (`SetInputModeUIOnly → SetShowMouseCursor(true) → SetGamePaused(true)`), pause-open (`SetInputModeUIOnly → SetShowMouseCursor(true) → SetGamePaused(true)`), pause-resume (`SetInputMode_GameOnly → SetShowMouseCursor(false) → SetGamePaused(false)`).
- `WBP_MainMenu`'s Play-button chain (a *different* Blueprint that also calls `SetInputMode_GameOnly` on the fetched PlayerController when closing itself): `SetInputMode_GameOnly → SetShowMouseCursor(false, self=PlayerController) → SetGamePaused(false)`.
- `WBP_PauseMenu` needed no change — confirmed via `find_nodes(title:"Input Mode")` returning empty — its Resume button delegates back into the controller's own toggle logic rather than calling `SetInputMode` itself, so the controller-side fix already covers it.

All three splices compiled clean and were re-verified via `get_node_infos` (both the new node's own wiring and that the original downstream target — `SetGamePaused`/`PrintString` — still receives the chain, not just a "compiled OK" check). Not yet confirmed with a real live click — the honest next check is the user actually clicking Play/Quit, since MCP has no way to simulate a mouse click on a UMG button.

---

## 2026-07-03 (crouch-walk/slide/swim anims + fence retile + climb mechanic) — **Built all three missing procedural animations, retiled all 20 grey-cube fence panels to the real Kenney mesh, and added a tall-obstacle climb mechanic — then caught a real bug in the climb tuning via independent geometry traces before ever calling it done.**

**Animation work**: extended `CR_Crouch` (Control Rig) with a walk-phase sine bob on the pelvis (new `WalkPhase` float var, `GetDeltaTime`→accumulate→`sin`→scale→add to the existing crouch-drop Z). Built two new Control Rigs from scratch — `CR_Slide` (simple deeper pelvis-drop, −60 Z, scoped down from an originally-planned forward-lean rotation for time) and `CR_Swim` (phase-accumulator driving two out-of-phase sines into `hand_l`/`hand_r` world-Z offsets, a paddle motion via translation rather than risking bone-local rotation-axis guessing). Wired `SlideAlpha`/`SwimAlpha` float vars into `ABP_Unarmed`'s EventGraph (cast to `BP_PlayerCharacter`→`GetIsSliding`/`Movement|IsSwimming`→`SelectFloat`→`Set*Alpha`, mirroring the existing `CrouchAlpha` pattern exactly), then added both as new `Misc.|ControlRig` AnimGraph nodes chained in series (`Slot→CR_Crouch→CR_Slide→CR_Swim→FootIK→Output`), each gated by its own Alpha getter. Compiled clean, round-tripped via `read_graph_dsl` to confirm the full chain.

**Fence retile**: all 20 pre-existing grey-box fence actors (`SB_Fence_*`, `SB_VaultFence_B_MC`, `SB_PlaygroundFence_*_MC`, `SB_DogPen_Fence` — a stretched `/Engine/BasicShapes/Cube` each) swapped to the real `SM_Fence_2x2` Kenney mesh via a `ProgrammaticToolset` batch script: computed each panel's world footprint from its existing cube scale (`scale*100`), divided by the fence mesh's native bounds (87.5×83.75×27) to get the new non-uniform scale, and shifted `location.z` down by the old half-height (cube pivot is center, fence mesh pivot is base) so the footprint lands in the exact same place. Verified one panel's `get_actor_bounds` before/after matched exactly (330×20×130 either way). This was explicitly flagged as a deferred follow-up in an earlier session's note ("retiling the fence panels... meaningfully riskier than the low-risk material swaps done here") — the footprint-preserving scale math made it a mechanical, low-risk operation after all.

**New climb mechanic + the bug it caught**: added a genuinely tall (216-unit) fence obstacle to the sandbox (5× `SM_Fence_2x2` panels, non-uniform scale, next to the existing 100-tall vault wall for a clear short-vs-tall contrast) and rewrote `BP_PlayerCharacter.TryVault` to branch: the existing low forward trace still gates vault-vs-jump as before, but a NEW second trace at `loc + (0,0,ClimbCheckHeight)` decides vault (short) vs climb (tall) — climb uses a slower/higher `LaunchCharacter` (350 fwd / 650 up vs vault's 500/400) and a new `AM_Climb` montage (duplicated from `AM_Vault`, still wrapping `MM_WallJump` as the closest available generic pose). **Before declaring this done, independently verified both branches via `SceneTools.trace_world` (a raw line trace against the actual placed geometry, not the Blueprint's own execution) at both check heights against both obstacles — and this caught a real, would-have-shipped bug**: `ClimbCheckHeight` is added to `GetActorLocation().Z`, which is the CAPSULE CENTER (~90 units above ground when standing, confirmed via `CollisionCylinder.capsuleHalfHeight=90`), not the ground itself. The first tuning (`ClimbCheckHeight=180`) put the absolute trace height at ~270 — above the 216-tall fence's own top, meaning the climb branch would never have fired for the exact obstacle built to test it. Fixed to `ClimbCheckHeight=90` (absolute trace Z≈180, comfortably within the fence's 0–216 span and above the vault wall's 100-tall span) and re-verified: vault wall trace hits only at the low check (Z=90, distance 80) and misses at the climb check (Z=180, null) → vault branch; new fence hits at BOTH checks (distance 36.25 each) → climb branch. **Generalizes: any height-gated trace on a Character must account for `GetActorLocation()` returning the capsule center, not the feet/ground — a bare "add N units and trace" is silently wrong by the capsule half-height.** Real dynamic (real-keypress) confirmation is still the one open item, per this project's usual honest-flagging pattern for physics-launch functions — the geometry-trace check used the same collision query the Blueprint's own `LineTraceByChannel` calls make, which is strong but not identical evidence.

---

## 2026-07-03 (AI Watcher re-test) — **The AI Watcher movement bug flipped which instance is affected: last session found sandbox-stuck/MapleCourt-fine, this session found the exact opposite (MapleCourt-stuck/sandbox-fine), reproduced fresh with a stronger evidence bar than before (exact position+rotation frozen across 35s+ real time, survives a level reload). This is NOT a fixed property of one Blueprint instance — treat it as an intermittent NavMesh-build-data issue, not "Watcher X is broken."**

Re-checked §5 fresh per the plan doc's own "don't trust a stale claim" rule. Sampled both Watchers' live `CharMoveComp.Velocity` and `get_actor_transform` across real time gaps (verified via `Bash sleep 15-20` between reads, not just consecutive tool calls): the sandbox Watcher (`BP_WatcherController_C_0`) moved substantially between every pair of reads (confirmed nonzero, changing velocity each time) — healthy. The Maple Court Watcher (`BP_WatcherController_MC_C_0`) read the **exact same location AND rotation to many decimal places** (`x:32900, y:300, yaw:-158.83874018317172`) across a fresh PIE session, a 20s wait, a full `SceneTools.load_level` reload + new PIE session, and another 15s wait — genuinely frozen, not just slow. This is the opposite of the prior session's finding for these same two actors.

**Ruled out via live checks (not assumed):** (1) `NavMeshBoundsVolume` geometrically covers the frozen position (`bounds [27500,34500]x[-3000,3000]x[-300,600]` contains `(32900,300,98.65)`) — not an out-of-bounds issue. (2) `homeLocation` (set once in `BeginPlay`) reads `(32900,300,105.27)`, matching current position almost exactly — proves `BeginPlay` ran successfully (it's the thing that sets this value), so this is not a "controller never initialized" case. (3) `watcherProfile` is correctly assigned (not `None`). (4) Teleporting the player into the Watcher's sight cone did NOT trigger `bCanSeePlayer`/`alertState`/`detectionMeter` — but this alone is inconclusive (the facing-direction math for where to stand was a rough estimate, not confirmed against the Watcher's actual view cone, and no time was spent debugging that specific probe further given the larger point — exact rotation staying frozen — was already conclusive on its own).

**Also fixed a real, generalizable tool-usage gap while investigating:** `find_actors(name="NavMeshBoundsVolume")` returned only ONE of the two `NavMeshBoundsVolume` actors in the level — the second one's Actor Label apparently doesn't literally contain that substring. Using `find_actors(actor_type={refPath:"/Script/NavigationSystem.NavMeshBoundsVolume"})` (the class-filter form) found both. This is the same "name-substring search misses actors whose Label doesn't match" gotcha already documented elsewhere in this project (Maple Court pools, playground props) — worth defaulting to the `actor_type` filter over name substring whenever you need a COMPLETE list of actors of a known class, not just "the one I already know the name of."

**Why this wasn't pursued further this session:** no MCP tool exists anywhere in `list_toolsets`'s full output to trigger `Build → Build Paths`, rebuild NavMesh tiles, or read tile/build-status — confirmed again this session (same conclusion as prior sessions). The one available lever that's historically helped with a similar WP-actor-staleness class of bug (`SceneTools.load_level` reload) was tried and did NOT fix it. **This needs the human**: open the editor, `Build → Build Paths` once (or toggle nav-mesh display via `P` to visually confirm empty/broken tiles first), matching the exact remediation this project has documented and successfully used before for this bug class. Given the instance affected flips between sessions, it's not safe to assume "fixing Watcher X" fixes the class — the human's one `Build Paths` click most likely fixes both, as it did the first time this bug was resolved.

---

## 2026-07-03 (real asset import) — **The user asked "where's the grass, the houses, the characters" and was right: `ASSET_CREDITS.md` claimed the Kenney City Kit (Suburban) was already downloaded and staged — it was not, anywhere, confirmed via a direct filesystem search. The "houses" were literal `/Engine/BasicShapes/Cube` actors with flat near-black materials. Fetched the real kit from kenney.nl (CC0, same source as 4 other packs already in this project), imported real building meshes, and replaced all 4 Maple Court houses. Also found `ground_grass.fbx` already sitting unused in the already-staged nature kit.**

**Root-caused the "not styled" complaint with primary evidence, not by re-reading old docs:** `get_components`/`get_properties` on one of the "house" actors returned `staticMesh: /Engine/BasicShapes/Cube`, `overrideMaterials: MI_Greybox_Door` — a flat-colored engine cube, not a modeled house. `MI_Greybox_Ground`'s `BaseColor` was `(0.02, 0.03, 0.07)` — near-black navy, not green. `_ThirdPartyStaging/ASSET_CREDITS.md` claimed the Suburban City Kit lived at `_ThirdPartyStaging/Kenney/kenney_city-kit-suburban/` — a `find` across the whole staging tree proved that path, and any `.zip` for it, never existed. Classic "a claim nobody re-verified became load-bearing" — the doc was written aspirationally (or for a plan that got dropped) and nobody ever checked it against disk again.

**Downloaded the real kit live this session**: found the actual CC0 download link by fetching the kenney.nl page HTML and grepping for the `lity-pop` inline-download div (`kenney_city-kit-suburban_20.zip`, ~3MB, `Content-Type: application/zip` confirmed via a HEAD request before downloading) — Kenney's download button is a JS-triggered link inside a hidden modal, not a plain `<a href>`, so a naive `grep -i zip` on the page misses it; search for the `lity-pop`/`initiateDownload` pattern instead. Extracted into `_ThirdPartyStaging/Kenney/kenney_city-kit-suburban/`, matching the existing convention for the other 4 packs.

**Imported via `StaticMeshTools.import_file`** (4 building variants + fence + driveway), reusing the first import's auto-created `colormap` Material/Texture for subsequent imports (`import_materials=false` after the first) rather than re-importing textures 4 times. Placed all 4 at the existing house lots' exact base-footprint coordinates (surveyed via a `ProgrammaticToolset` batch script — far cheaper than 28 individual `get_actor_transform` calls), scaled ~5x to roughly match the old cube footprint, then deleted all 28 old cube sub-pieces (door/window/roof/foundation/base per house) only AFTER confirming the new mesh rendered correctly underneath — verify-then-cleanup, not delete-then-hope.

**Real, reusable finding: a newly-imported FBX static mesh's own auto-generated material can render as flat/uncolored even when a real, differently-colored `vectorParameterValues` exists on it** — the Kenney nature-kit `ground_grass.fbx` import produced a `grass` slot material that, at this project's night-lighting levels, was visually indistinguishable from the near-black ground in every screenshot despite genuinely different underlying values. Rather than debug the auto-imported material's internal node graph, swapped the slot to an existing, already-proven `M_GreyboxToon`-based Material Instance (`MI_Greybox_Grass` — found already existing in the project, built in some earlier session, correct green values, but **never actually assigned to anything** — another stale-unused-asset find) via `StaticMeshTools.set_material` on the mesh asset itself, which retroactively re-colors every already-placed instance at once (confirmed: 91 grass tiles across 4 yards all picked up the change from one call, no per-instance touch needed).

**Screenshot-based color verification was a dead end for the ~40 minutes spent on it, and the reason why is now doubly confirmed, not just theorized:** the exact same house, from two near-identical camera setups (one taken right after placement, one taken ~15 minutes later after unrelated edits), rendered once as a warm, clearly-colored building and once as a flat near-black silhouette — same geometry, same material, same light rig, no code change to the house or its lighting in between. This can only be viewport auto-exposure reacting to subtle framing/GPU-state differences between calls, not a real material change — and it's the **same phenomenon this file already flagged in an earlier styling session** ("night-mode screenshots read wildly differently by camera distance/angle — trust property readback over screenshot exposure-hunting"). Don't re-litigate this every session: for real-money verification of a night-scene material/color change in this project, `get_vector_parameter`/`get_material`/`get_bounds` reads are the reliable signal; `CaptureViewport` is for rough framing/composition sanity checks only, never for "is this color right."

**Also restored 4 warm `PointLight` window-glows** (one per house) that were deleted along with the old cube pieces — the original per-house "someone lives here" window light was real design intent (documented in the styling-marathon session), not decoration to discard just because it happened to be parented to a cube. Intensity 3000, warm color `(1.0, 0.75, 0.4)`, 400-unit radius, matching the pattern of the ones removed.

**CRITICAL save-mechanism gap found while wrapping up, worth its own paragraph: `AssetTools.save_assets(["/Game/_Project/Maps/L_Sandbox_Movement"])` silently did NOT persist any of this session's scene edits (99 new actors: 4 houses, 91 grass tiles, 4 lights; 28 deletions) — despite returning `true` every single time it was called, across many calls, with zero error.** Caught this only by cross-checking with a plain filesystem `find -newer`/`-mmin` on `Content/__ExternalActors__/_Project/Maps/L_Sandbox_Movement/` (this project's World Partition level stores actors as One-File-Per-Actor packages there, NOT in the `.umap` itself) — the newest file in that folder was ~9.5 hours old, meaning nothing from this whole session had actually reached disk. `AssetTools.is_dirty("/Game/_Project/Maps/L_Sandbox_Movement")` also read `false`, confirming the level's own lightweight container package genuinely has nothing to flush — for a WP level, new/deleted actors dirty their OWN individual OFPA packages, not the map package, so passing the map's path to `save_assets` saves the wrong (already-clean) thing. Also tried `SceneTools.save_actor` directly on one new actor — it failed outright (`"Asset does not exist: /Game/__ExternalActors__/.../BFOGNNB1ZTMW709RZAS88X"`), meaning the per-actor save path doesn't work either for a brand-new-this-session OFPA actor (it expects the package to already be registered). **The fix that actually worked: `AssetTools.save_assets([])` — an empty list, which the tool's own docstring says means "save all dirty assets" project-wide.** That single call produced exactly the expected file diff (99 new external-actor files, 28 deletions, matching the session's actor count exactly) confirmed via a fresh `find -mmin -5` immediately after. **Generalizes: for this project's World Partition level, never trust `save_assets([map_path])` or `save_actor()` to persist newly created/deleted scene actors — always finish a scene-editing session with a bare `save_assets([])` sweep, and verify with a filesystem timestamp check (not just a `true` return value) before trusting the work is safe from an editor crash or restart.** This should have been caught much earlier — every actor-placement session this project has ever done was implicitly assuming targeted saves worked; it's worth a fresh `find -mmin` sanity check the next time any session does heavy scene placement work, since this could easily have been silently losing WP actor placements for multiple past sessions too, not just this one.

**Scope note, stated plainly:** this pass covers Maple Court's 4 houses + yards only. The sandbox course's own ground/walls (a separate, smaller test area near the origin) were not touched — same "grey-box now, real assets later" gap may still apply there if the user plays that area instead. Fence and driveway meshes were imported but not yet placed (ran out of session time after the house+grass+lighting work) — `SM_Fence_2x2` and `SM_Driveway_Long` sit ready in `Content/ThirdParty/Kenney/CityKitSuburban/Meshes/` for a future pass.

---

## 2026-07-03 (first live human playtest of the overnight build) — **Two real bugs, both found within the first minute of an actual human playing: `SetInputMode(UIOnly)` alone does NOT stop Enhanced Input from driving gameplay — you also need `SetGamePaused(true)`. And `WorldSettings.KillZ` is a trap: enabling it doesn't "add a floor," it destroys the pawn outright via the default `FellOutOfWorld` handler, which is worse than the bug it would fix.**

**Bug 1 — the new main menu (built last session) didn't actually block movement.** The user reported being able to walk around freely with the main menu still on screen. Root cause: the menu's `BeginPlay` chain called `SetInputMode_UIOnly` but never `SetGamePaused(true)` — and `SetInputMode` alone governs where mouse/keyboard *routing* goes for UI focus purposes, it does NOT stop `EnhancedInputComponent` from continuing to deliver bound Action events to the possessed pawn. The already-working pause menu (built the same session, `IA_Pause`) had ALWAYS included `SetGamePaused` alongside `SetInputMode` — the main menu was built without cross-checking that reference pattern, and the gap was invisible to every verification done at the time (compiles clean, widget creates, no accessed-none — none of that proves gameplay is actually gated). **Fix:** added `SetGamePaused(true)` right after `SetInputMode_UIOnly` in `BP_PlayerController`'s menu-open chain, and `SetGamePaused(false)` right after `SetInputMode_GameOnly` in `WBP_MainMenu`'s Play-button chain — mirroring the pause menu exactly. **Verified for real, not just re-compiled:** read `GameState.NightTimeRemaining` twice across a real 12-second gap while the menu was up — it stayed at exactly `600` both times, proving the whole world (not just visual/input routing) is genuinely frozen. This is a MUCH stronger signal than "input mode changed" ever was, and it's the same class of test this project's own LESSONS already recommends (frozen state across a real wall-clock gap, not a same-tick read).

**Generalizes:** any time you gate gameplay behind a menu/UI state in this project (or likely any Enhanced-Input UE5 project), `SetInputMode` and `SetGamePaused` are BOTH required — one is about where input routes, the other is about whether the world simulates at all. Verifying "the widget appeared, no errors" is NOT sufficient evidence that gameplay is actually blocked; the only real test is checking whether world state that should be frozen (a timer, a tick counter) actually stays frozen across a real time gap.

**Bug 2 — walking off the street dropped the player into an infinite fall, because `WorldSettings.KillZ` was at the engine's default (`-1048575`, i.e. "so far down it never matters" = effectively disabled).** The obvious-seeming fix — set `KillZ` to a real threshold like `-2000` — was tried and then DELIBERATELY REVERTED after realizing the actual mechanism: `AActor::FellOutOfWorld` (the engine's default handler for crossing `KillZ`) applies massive damage and then unconditionally calls `Destroy()` on the actor. For a player-controlled `Character` with no health/damage-immunity setup, this means crossing `KillZ` would **permanently destroy the player pawn** — a strictly worse outcome than an endless fall (at least a stuck player can be told "you're stuck, reload"; a destroyed pawn likely soft-locks the whole session with no possessed actor at all). **Correct fix instead:** a new `BP_PlayerGameMode.CheckFallCatch` function, called every tick (spliced into the front of the existing `EventTick`, same safe insert-and-reconnect pattern used elsewhere in this project), that gets the player character's location, and if `Z < -300`, teleports them back to the stash zone's location — a soft, non-destructive catch that fires WAY before any `KillZ` threshold would ever need to. Left `KillZ` at its disabled default; the Tick-based catch is the only active safety net. **Red-proofed for real:** teleported the player to `Z=-1000` (deliberately worse than the fix's own `-300` threshold, to prove the catch fires with margin to spare), confirmed on the very next read the player was back at the stash's ground position — not just "compiles clean," an actual before/after state-correction proof, matching this project's own established verification bar for this class of self-healing fix (see the swim-collision self-heal entry).

**A practical testing wrinkle worth keeping:** both fixes needed the WORLD to be genuinely unpaused to verify (frozen-world testing can't observe a Tick-driven fix, by definition). Since the main menu now pauses the game at `BeginPlay` with no MCP-reachable way to simulate a real button click, verifying `CheckFallCatch` required temporarily flipping the pause literal's pin value to `false`, testing, then flipping it back to `true` before saving — a clean, reversible way to get a genuinely-ticking world for a single test cycle without touching the actual shipped behavior.

---

## 2026-07-03 (crouch animation) — **Built a genuine procedural Control Rig crouch (pelvis-drop) via `ControlRigTools`, discovering the RigVM unit-node naming convention (`RigUnit_<Name>`) and sub-pin path connection (`Node.Transform.Translation.Z`) live, with zero introspection tool for RigVM node types. Compiles clean end-to-end; the ONE thing not confirmed is whether it looks good, because screenshot verification at this world's scale can't resolve it.**

`ControlRigTools` has `create_node(node_type, node_name)` but **no `find_node_types` equivalent** for RigVM units (unlike `BlueprintTools`) — discovered the naming convention by trial: the `add_event_node(FORWARD_SOLVE)` call returned a node named `RigUnit_BeginExecution`, which suggested the pattern `RigUnit_<UnitName>`; guessed `RigUnit_GetTransform`/`RigUnit_SetTransform`/`RigUnit_MathFloatMul`/`RigUnit_MathFloatAdd` and all four succeeded on the first try. **A struct-typed pin's sub-fields are directly connectable/settable by dotted path without any explicit "split pin" call** — `GetPelvis.Transform.Translation.Z` and `SetPelvis.Value.Translation.Z` both worked as `connect_pins`/`get_pin_value` targets straight away, and `RigElementKey` pins take a `set_pin_value` string format of `(Type=Bone,Name="pelvis")` (confirmed by reading the default value's own stringified format first, then writing the same shape back with a real bone name — read-then-write-same-format is a reliable way to discover a struct pin's expected string grammar for ANY unfamiliar RigVM/Blueprint struct pin, not just this one).

**Design decision that avoided a live regression risk:** initially planned to expose the rig's internal `CrouchAlpha` variable as an ABP-visible pin on the `Misc.|ControlRig` AnimGraph node (setting `node.controlRigAssetReference.blueprintRigClass` via `ObjectTools.set_properties`) — but setting that property directly (rather than through the details-panel UI) does NOT trigger the reconstruct step that scans the rig's public variables and exposes them as pins (confirmed: `get_node_infos` showed zero new pins even after a full `compile_blueprint`). Rather than chase the UI-only reconstruct hook, **pivoted to a simpler design**: made the rig always compute a fixed "full crouch" pelvis offset (no internal variable at all), and drove the wrapper `AnimGraphNode_ControlRig`'s own pre-existing `Alpha` pin (a genuine ordinary Blueprint float pin, works with plain `connect_pins` like any other AnimGraph node) from the character's native `ACharacter.bIsCrouched` via a `Math|Float|SelectFloat` node (1.0/0.0). This is a strictly better design than the original plan, not just a workaround — it reuses the exact same blend mechanism the pre-existing foot-IK Control Rig node already relies on, and it means **the rig's effect is fully gated to zero whenever not crouching**, so even if the pose itself needs tuning later, normal gameplay animation is provably unaffected by this addition.

**Cross-object member access on a Character-typed pin uses `Class|Character|GetIsCrouched` for `create_node`, NOT the `Variables|Character|GetIsCrouched` string that `find_node_types` returns when given that same pin as context** — the two tools disagree on the creatable type_id for what is otherwise the identical member. When `find_node_types(context_pins=[...])`'s returned string 404s on `create_node`, try the `Class|<ClassName>|<Member>` form (matching the blueprints skill's already-documented cross-object-call convention) before concluding the node type doesn't exist.

**Honest gap, flagged for a human playtest or a future session with better tooling:** the mechanism (pelvis lowered by a fixed 32 units, gated by a 0/1 blend on crouch) is compiled, saved, and structurally sound, but **whether it actually looks like a good crouch — as opposed to legs stretching/clipping since there's no two-bone leg IK repositioning the feet — was never visually confirmed.** Every `CaptureViewport` attempt this session (for this AND the prior player-skin task) produced a character too small/distant in frame to judge a ~32-unit local pose change, even after 3-4 rounds of camera-math correction. This is a real, load-bearing limitation of screenshot-based verification at this project's world scale, not a one-off mistake — a future session should either get much closer with a corrected transform (worth a fresh, focused attempt, not a continuation of this session's guesses) or defer to an actual human Play session.

---

## 2026-07-03 (later, player re-skin) — **`CaptureViewport`/`get_actor_bounds` are not reliable for framing a moving PIE pawn — 4 camera-positioning attempts across two sessions all failed to visually confirm a material change that was already provably correct via property readback. Accept property-level verification as sufficient when screenshot framing repeatedly fails; don't burn more cycles chasing a nice-to-have.**

Built `MI_PlayerBody` (duplicated from `MI_WatcherBody`, same `M_GreyboxToon` parent) with distinct stealth-suit tones — `BaseColor(0.05,0.08,0.11)`, `ShadowColor(0.02,0.03,0.045)`, `RimColor(0.25,0.55,0.65)` cyan-teal, `EmissiveColor(0,0,0)` — applied to both material slots of `BP_PlayerCharacter`'s CDO `CharacterMesh0.overrideMaterials`, confirmed via direct property readback (values round-tripped exactly), confirmed no placed level instance exists to also patch, saved to disk. **Visual confirmation via `CaptureViewport` failed 3 times this session** (on top of a prior session's attempt): computing camera position from the player's own `get_actor_transform` (yaw-based forward-vector math, placing the camera in front of the character's face and rotating to look back at it) produced a wide street/pool establishing shot with no visible character both times: once too high (z=200, framed over the character's head onto distant geometry) and once at a corrected eye-height (z=160) that STILL missed — likely because the "stealth suit" colors (near-black BaseColor ~0.05, zero emissive) are deliberately close to the night skybox's own dark blue tone, so even a correctly-framed capsule could read as near-invisible against the background, compounding the framing problem. A follow-up `get_actor_bounds()` call meant to nail down exact capsule extents for a tighter shot returned a ~2020×2020×2020 unit box — 20+ meters across, nowhere near a player capsule's real size — indicating this tool doesn't return trustworthy tight bounds for this actor (possibly picking up a level-streaming or root-component-inclusive bound rather than the actual mesh bounds). **Decision: stopped after the 3rd same-session attempt** rather than continuing to iterate on camera math — this is the same discipline as the DSL orphan-node / IsValid-guard entries elsewhere in this file: once a verification METHOD (not the underlying claim) is the thing failing repeatedly, time-box it and fall back to the strongest evidence that DOES work (direct property readback on the exact fields the visual would confirm), rather than treating "I couldn't get a good screenshot" as equivalent to "the change might not have worked." Contrast with the earlier `MI_WatcherBody` skin, which WAS successfully screenshot-verified in the same session it was built — the difference there was a stationary/simpler framing case, not a fundamentally different verification requirement.

---

## 2026-07-03 (late) — **The editor-Python-console route (SlateInspector Cmd box) is unavailable when the editor has zero registered Slate windows — confirmed via `Windows(list)` returning `[]` AND `CaptureEditorImage` failing ("Failed to capture any editor windows"), while every dedicated MCP toolset (BlueprintTools, SceneTools, MaterialTools, UMGToolSet) kept working fine.** This blocks anything that specifically needs the Python console (IK Rig/IK Retargeter creation — no dedicated toolset exists for these, per the buildability gaps list — Control Rig graph edits via `ControlRigTools` are a dedicated toolset and NOT affected). The engine itself is confirmed alive and ticking (`get_current_level`, blueprint compiles, PIE start/stop all still work) — this is specifically about the editor's Slate UI having no visible/registered top-level window right now (possibly the window was closed/minimized while the underlying process + MCP server kept running), not a connection or engine-health problem. **Don't burn retries on this if it recurs** — check `Windows(list)` once; if it comes back empty, the Python-console-dependent tasks (IK retargeting, anything else that needs `py "path"` in the status-bar Cmd box) are blocked until a human brings the editor window back, and everything else should proceed via the dedicated toolsets instead. Pivoted the "wire a Mixamo player skin" task to a material-based re-skin of the existing Manny mesh instead (same visual-differentiation goal, zero Python-console dependency) — full mesh+animation swap is parked until the window issue clears.

---

## 2026-07-03 (loot system build) — **Two new MCP gotchas: `Array|Add`/`AppendArray` fed by a variable-getter doesn't reliably write back through this MCP's node-creation path (needs an explicit follow-up `Set`), and newly-placed World Partition actors can spawn at `(0,0,0)` in PIE until the level is reloaded. Also: `PrimitiveTools.add_cube/sphere/cone/cylinder` adds a REAL persistent SCS component when called on a Blueprint's CDO — much simpler than the `SubobjectDataSubsystem` Python route for primitive meshes.**

**Gotcha 1 — array by-ref write-back is not reliable via MCP-created nodes.** Standard Blueprint pattern: wire a `Get MyArrayVar` node's output directly into an impure function's by-ref array parameter (e.g. `Array|Add`'s `TargetArray`, `AppendArray`'s `TargetArray`) and the compiler auto-detects "this ref traces back to a plain variable" and writes through to the actual variable memory — no explicit `Set` needed, this is extremely common in hand-authored Blueprints. **Built and wired this exact pattern twice tonight (loot pickup's `CarriedLoot` Add, GameMode's `BankedLoot` AppendArray) and the write-back did NOT persist either time** — `get_node_infos` confirmed the wiring was textbook-correct (Get→TargetArray, impure node, proper exec chain to a working `DestroyActor`/score-set afterward), the exec chain provably ran to completion, yet a live PIE property read showed the array unchanged. **Fix: add an explicit `Set<ArrayVar>` node immediately after the Add/Append, self=same target object, value=the same `Get` node's output (safe to fan out a pure node's output to a second consumer).** This is now a required extra step for EVERY array mutation built via MCP node creation in this project — don't trust the implicit by-ref write-back the way you would in the graphical editor. Confirmed fixed for the `CarriedLoot` case (live PIE re-test showed the item correctly present after the fix); the `BankedLoot` case has the identical fix applied and compiles clean but wasn't independently re-confirmed live due to a separate, compounding test-methodology issue (see below) — treat it as "same fix, same confidence," not "unverified."

**Gotcha 2 — newly-MCP-placed World Partition actors can read `(0,0,0)` in a PIE session instead of their authored transform.** Placed 3 `BP_LootPickup` instances via `SceneTools.add_to_scene_from_asset`, verified their transforms correctly via the PERSISTENT-level actor ref (matched what was set) — then started PIE and read the SAME actor via its `UEDPIE_0_`-prefixed PIE-world duplicate: `(0,0,0)`, not the authored position. This is presumably a WP streaming/registration gap for freshly-added external actors (same family as the already-documented "AI patrol points need the NavMesh rebuilt" and "SupportedAgents needs an editor restart" issues — WP/nav data has its own build/cache step separate from the plain asset save). **Fix: `SceneTools.load_level(same_level_path)` to force a reload before the next `StartPIE`** — confirmed this resolves it (re-checked the same actor's PIE-world transform after reload: matched the authored position). **Do this reload as a matter of course after placing/repositioning ANY actor via MCP, before trusting its transform in a PIE test** — cheap, and the alternative is a confusing "my object is at the origin for no reason" chase.

**`PrimitiveTools` adds a real persistent component directly on a CDO — a much simpler path than the documented `SubobjectDataSubsystem` Python recipe, for the specific case of a primitive-shape mesh.** Calling `PrimitiveTools.add_cube(actor=<CDO ref>, name=...)` (and presumably `add_sphere`/`add_cone`/`add_cylinder`) on a Blueprint's `Default__<Class>_C` CDO reference returned a `<Name>_GEN_VARIABLE`-suffixed component ref — the same naming signature the `SubobjectDataSubsystem` recipe produces, confirming it's a genuine persistent SCS component, not a CDO-only override. Tried the raw Blueprint asset ref first (`/Game/.../BP_Foo.BP_Foo`) — rejected ("not valid Actor for property 'actor'") — the CDO ref (`BP_Foo.Default__BP_Foo_C`, from `BlueprintTools.get_default_object`) is what's needed. **Use this for any primitive-mesh component need (a pickup's visual+collision mesh, a trigger-shaped mesh, etc.) before reaching for the full Python `SubobjectDataSubsystem` dance** — that recipe is still needed for non-primitive component types (skeletal meshes, widget components, custom mesh assets), but not for basic shapes.

**Also confirmed:** `add_function_param` only accepts primitives/basic structs (`bool int float byte name string text Vector Rotator Transform Vector2D LinearColor`) — no object-reference types, despite existing GameMode functions (`Server_BankAtRisk`, `HandleDetain`) clearly having `BP_PlayerState`-typed parameters (built in an earlier session via a mechanism not currently exposed, or since changed). **Workaround: splice new logic directly into an EXISTING function that already has the object parameter you need in scope**, using exec-chain-tracing (`find_nodes` + `get_node_infos`, follow `then` pins to the true unconnected terminal) to find a safe append point, rather than trying to create a new function with its own object parameter.

---

## 2026-07-03 (post-reconnect session) — **Swim CLOSED OUT: `MOVE_Swimming` confirmed on 2 pools live. A single teleport does NOT trigger a physics-volume re-check on the very next read — this cost a false "still broken" reading before a follow-up move revealed it works fine. Also: `bUseDefaultCollision` has NO Blueprint setter, so the self-heal targets `SetCollisionEnabled` instead — and it's genuinely more general as a result.**

**MCP reconnected clean this session** — `list_toolsets`/`IsPIERunning`/`get_current_level` all returned real, live data on the first real calls (not just a `ToolSearch` hit), confirming last session's diagnosis was right: the prior session's client just never got a live handle because the editor was down at its own startup; a plain Claude Code restart (with the editor+port already up) was the whole fix, no special recovery needed.

**The false alarm, and the actual mechanism:** teleported the player into the sandbox pool's `PhysicsVolume` bounds via `set_actor_transform`, immediately read `CharMoveComp.MovementMode` → got `MOVE_Walking`, THREE reads in a row, despite `get_actor_transform` confirming the pawn really was sitting deep inside the volume and `bWaterVolume=true` on the volume itself. Nearly wrote this up as "swim behaviorally still broken despite correct collision." One more probe first (per the debugging discipline — don't theorize past the evidence): sent a SECOND, small position update (a few units of Z) and re-read — `MOVE_Swimming` immediately. Repeated on a second pool (Maple Court A) with the identical two-step pattern: same result both times. **Mechanism: `AActor::UpdatePhysicsVolume`-style membership checks don't necessarily re-run on the exact frame a hard `SetActorTransform`/teleport lands — they need a subsequent transform update to trigger re-evaluation**, which is a non-issue for a real player (every tick of `AddMovementInput`-driven motion IS a transform update) but WILL fool a single-teleport-then-immediate-read test every time. **Generalizes: any MCP-driven "teleport then immediately read volume-membership/overlap state" test should do a teleport + one small follow-up nudge + THEN read, or it risks a false negative on the very mechanism it's trying to verify.** Also confirms the historically-recurring collision bug (`bUseDefaultCollision`) is genuinely still fixed — verified fresh via `bWaterVolume=true` + successful swim entry on 2 separate pools, not just an old claim.

**Self-heal for the recurring collision regression — redesigned mid-build once a real MCP limitation was found, and the result is better than the original plan.** The original plan (`12_Overnight_Session_Plan.md` §0) called for forcing `bUseDefaultCollision=False` on `BeginPlay`. `find_node_types` on a `Collision` filter turned up `Collision|SetCollisionEnabled` but **no setter for `bUseDefaultCollision` anywhere in the node catalog** — it's evidently not `BlueprintReadWrite` at all (an authoring-time-only flag), so no Blueprint graph can touch it, full stop. Pivoted to a mechanism that doesn't need to: `BP_PlayerGameMode.EventBeginPlay` now does `GetAllActorsOfClass("/Script/Engine.StaticMeshActor")` → cast each → compare its `StaticMeshComponent.GetStaticMesh()` against a new `WaterSurfaceMesh` object variable (set to `/Engine/BasicShapes/Plane`) → `SetCollisionEnabled(NoCollision)` on every match. A runtime `SetCollisionEnabled` call overrides whatever the initial body-setup produced regardless of what `bUseDefaultCollision` did at spawn — so this achieves the same functional goal (water never blocks the player) without needing write access to the property that's actually causing the historical regression. **Bonus: this is mesh-identity-based, not per-pool-wired** — it automatically covers all 5 current water surfaces (and any future ones) with zero per-pool setup, versus the originally-planned per-`BP_PoolVolume` reference wiring, which would have needed 5 separate correctly-paired variable assignments and doesn't exist as an owned relationship in the actual architecture (water surfaces are independently-placed `StaticMeshActor`s, not components of `BP_PoolVolume`).

**Verified as a real guard, not just a happy-path check (RED-PROOF pattern):** before trusting this, deliberately broke one water surface's `CollisionEnabled` to `QueryAndPhysics` on the EDITOR (persistent-level) actor, started a fresh PIE session, and read the LIVE PIE-world copy of that same component — it came back `NoCollision`, proving the `BeginPlay` logic actually corrects a broken state at runtime rather than merely leaving an already-correct one alone. This is the class of verification this exact bug's history has been missing every prior time it was "fixed."

---

## 2026-07-03 (overnight session) — **User reported "we can't swim" despite the previous session's own "SOLVED" entry below. Re-diagnosed MCP-free via the standalone probe: the exact historically-recurring root cause (`bUseDefaultCollision`) is confirmed NOT present right now — but full behavioral confirmation (does `MOVE_Swimming` actually trigger) is still an honest gap, not a false "fixed" claim.**

**Context: `unreal-mcp` was unreachable this whole session** — the editor+port 8000 were down when this Claude Code session started, so the MCP client never got a live handle to the server (confirmed: the tool didn't appear anywhere in the session's available/deferred tool list, and three different `ToolSearch` queries for it came back empty — a stronger signal than a single failed call). Started the editor fresh (port 8000 now up, process responding), but per this project's own documented transport behavior, THIS session's client needs a Claude Code restart to pick up the now-live server — that's a user action, not something an agent can trigger on itself. Rather than block all diagnosis on that, used the MCP-free [`unreal-input-probe`](../.claude/skills/unreal-input-probe/SKILL.md) path instead.

**New diagnostic technique (worth keeping): query the exact property a recurring bug hinges on directly via console `GetAll`, instead of trying to navigate to the repro location blind.** Rather than trying to dead-reckon a player to "Pool_A" with no known coordinates (the map has no placement/coordinate doc — flagged separately in `09_Design_Review_Punchlist.md` §C), launched standalone and ran `GetAll StaticMeshComponent bUseDefaultCollision` + `GetAll StaticMeshComponent BodyInstance` immediately at spawn — no navigation needed at all. This dumps every static mesh component's value with its full object path to the log. Found exactly 5 unlabeled `StaticMeshActor_UAID_...` entries (matching the "5 water surfaces" count and, circumstantially, the exact count of external-actor files the original collision-fix commit touched) all reading `bUseDefaultCollision = False` + `BodyInstance.CollisionEnabled = NoCollision` — the precise, correct, previously-documented fixed state. **This is genuinely stronger evidence than a repeat editor-session property read**: it's a brand-new, fully cold-launched standalone process, with zero shared memory/state with any editor session that might have "fixed" it earlier this same sitting — exactly the class of check the project's own LESSONS says is the only one that can catch this bug ("only a genuinely fresh PIE session's live property read" — a cold separate process is a strictly stronger version of that same test). **Generalizes:** when a bug's root cause is already known and pinned to a specific property, don't re-derive "is it broken" by trying to reproduce the full user-visible symptom (which may need precise navigation/state you don't have) — go straight at the property via `GetAll <Class> <Property>` and grep the dump. Much cheaper, and for a recurring bug with a known mechanism, just as conclusive.

**What this does NOT prove:** whether the player actually enters `MOVE_Swimming` on contact with the water — that needs either (a) precise pool coordinates (don't exist in any doc) to navigate there and check `CharacterMovementComponent.MovementMode` post-overlap, or (b) MCP's `set_actor_transform` teleport (the method every prior successful verification in this project actually used). One bounded attempt to dead-reckon there (walk forward, tap vault, toggle crouch, walk further) landed the character on an unrelated raised platform/ramp (`AT RISK` stayed `0`, `MovementMode` stayed `MOVE_Walking` throughout) — inconclusive, not a negative result, just a miss. Did not keep grinding blind attempts (per the debugging-discipline "don't repeat a failing approach" rule) — logging the gap honestly instead of overclaiming either "fixed" or "still broken."

**Bonus finding from the same MCP-free toolkit, unrelated to swim: the sandbox AI Watcher looks stuck again, while the Maple Court one is confirmed moving — a specific, dated data point for whoever next reads the long, contradictory "is AI Watcher movement broken" thread in `CLAUDE.md`.** Sampled `CharacterMovementComponent.Velocity` for both Watchers 3 times across ~2 real minutes (t=0, t=18s, t=~110s) via the same `GetAll`-in-a-fresh-process technique. `BP_WatcherCharacter_MC_C` (Maple Court) showed a different nonzero velocity vector each time (≈472 u/s, then ≈600 u/s, clearly patrolling/turning). `BP_WatcherCharacter_C` (sandbox) read exactly `(0,0,0)` all three times. Three consecutive zero reads spanning 2 minutes is not a wait-timer coincidence — this is a real, specific "sandbox Watcher not moving" signal, isolated to one instance while its near-identical sibling (same `TickBrain` shape, per `CANON.md`'s "controller variables mirror... 1:1" duplication) works fine. Points at something instance-specific (patrol point array empty/misconfigured for the sandbox one, a spawn issue, or a nav-mesh-bounds gap specific to that region) rather than a systemic `TickBrain` bug — check the sandbox Watcher's gathered `PatrolPoints` array and its `NavMeshBoundsVolume` coverage first once MCP is back, before re-touching the shared brain logic both classes share.

**Working theory for what the user actually hit, given the collision property is confirmed correct:** something introduced by the very next commit after the collision fix (`d6a907d`, same session: slide mechanic + ~130-unit pool depth increase on all 5 basins/physics-volumes + player buoyancy 1.0→0.92) is the more likely suspect now — it touched a *different* set of external-actor files than the collision fix (verified via `git show --stat`, zero hash-path overlap), so it didn't undo the collision property, but it DID touch the physics volumes' depth and the character's buoyancy, either of which could plausibly affect swim-entry or swim-feel without touching collision at all. **Next session with MCP: check the 5 `PhysicsVolume` (`bWaterVolume`) actors' bounds actually still overlap their (now-deeper) basin correctly, and live-teleport-test entry on at least 2 of the 5 pools (not just one) before declaring this fixed** — teleport + `CharMoveComp.movementMode` read is the proven method, do that fresh regardless of what this entry found.

---

## 2026-07-03 — **`Montage_Play` is a real workaround for the AnimGraph ceiling — it's a regular EventGraph function, not an AnimGraph node, so it's fully reachable via BlueprintTools even though the AnimGraph itself isn't.**

Given zero animations exist anywhere in the project for swim/vault/slide/crouch, and Control Rig can't be wired into the AnimGraph (confirmed 2026-07-03 earlier entry), the practical middle ground: wrap an EXISTING, tonally-adjacent AnimSequence into a new `AnimMontage` asset (`unreal.AnimMontageFactory` with `source_animation` set — same factory-based creation pattern used for materials elsewhere, works cleanly, no gotchas hit), then call `Animation|PlayAnimMontage` from a normal Blueprint function (`TryVault`, `StartSlide`) exactly like any other function call. This sidesteps the AnimGraph-node wall entirely, because Montage playback is a *procedural* pose override layered on top of whatever the AnimGraph outputs — it's driven from gameplay code, not graph wiring. Used `MM_Dash` (0.97s) as a stand-in for both slide-start and had `MM_WallJump` (0.47s) stand in for vault-launch — neither is a bespoke animation for the action, but both give real triggered motion instead of the character sliding/launching in a static pose. **This is the same trick documented for the AI Watcher's catch/detain flow conceptually (a system reacting via EventGraph triggers, not AnimGraph rewiring) — worth remembering as the general pattern whenever "the character needs to visibly react to X" and the AnimGraph itself is off-limits: look for an EventGraph-callable animation hook before assuming the whole feature is blocked.**

**Reused `ResolveSpeed` instead of building a parallel Tick system for the slide's momentum decay.** The character already had one per-tick speed-resolution function computing `MaxWalkSpeed` from sprint/crouch/swim state. Rather than add a second per-tick handler (risking two systems fighting over the same property, and Blueint only allows one `EventTick` anyway), the slide decay (`FInterpTo` from a boosted speed down to `WalkSpeed`, auto-clearing `bIsSliding` once it settles) was spliced into that SAME function as an early-exit branch, leaving the original sprint/crouch/swim logic completely untouched in the `else` path. **General pattern: before adding a new per-frame handler for a new movement state, check whether an existing per-tick function already owns the property you need to override — extending it is lower-risk than a second competing writer.**

**Deepening a pool's water is safe to do via Z-scale/reposition alone — it's the X/Y footprint changes that risk neighbor overlaps (already learned the hard way once this project, see the enlarged-pool-overlapped-the-bush entry).** Extended each pool's basin + physics volume ~130 units further down (bottom face moves, top/water-line Z stays fixed) with zero risk of colliding with fences/bushes/houses at the surface level, since nothing about the pool's outline in the XY plane changed. A useful distinction going forward: "make the pool feel bigger/deeper" splits into a low-risk Z move and a higher-risk XY resize, and they don't need to be bundled.

---

## 2026-07-03 — **SOLVED: the swim-collision bug that recurred 3+ times across this project's history. Root cause was `StaticMeshComponent.bUseDefaultCollision`, not the `collisionEnabled` property everyone (including two attempts earlier this same session) kept re-fixing.**

Full mechanism: every water surface (`MI_Water_{A,B,C,D}_MC` + sandbox `PoolWaterSurface`) uses the shared engine mesh `/Engine/BasicShapes/Plane`. That mesh asset's own `BodySetup.DefaultInstance` is hard-baked to `BlockAll`/`QueryAndPhysics` — a totally reasonable default for a generic plane primitive used all over the engine for walls/floors/etc. Every fix attempt (this session's first two, and per this file's own earlier entries, at least two from prior sessions) set the **instance-level** `StaticMeshComponent.BodyInstance.CollisionEnabled` to `NoCollision` — which is the *correct, normal* way to override collision on a placed instance, and it DID work... in the sense that reading the property back immediately in the editor always showed `NoCollision`, exactly as set. **But every one of those water surface components also had `bUseDefaultCollision = True`.** That flag makes `UPrimitiveComponent` ignore its own instance-level `BodyInstance` override entirely at *runtime* (`BeginPlay`/PIE-start physics-body creation) and pull collision straight from the mesh asset's `BodySetup.DefaultInstance` instead. Net effect: the instance override was real, verifiable, and **completely inert** — a plausible-looking fix that could never have worked, no matter how many times someone re-applied it, because the actual runtime physics body was never built from it in the first place.

**Why this took 3+ sessions across this project's history to catch, and why an in-editor property readback can never catch it:** reading `bodyInstance.collisionEnabled` on the placed actor in the editor (the verification method used every previous time, including my own first two attempts this exact session) shows the *instance's stated intent*, not what the runtime physics engine will actually use — those are two different data paths that only converge if `bUseDefaultCollision` is false. **The only way this bug was findable at all was a genuinely fresh PIE session's live property read** — checking the SAME component, mid-Play, and seeing `QueryAndPhysics` despite a saved-and-reloaded editor state reading `NoCollision`. That specific "editor says X, live PIE says NOT-X, on an asset nobody touched in between" signature is what pointed at "something is regenerating collision from a different source at BeginPlay" rather than "the fix didn't save" (which was the wrong hypothesis for 2 straight attempts).

**Generalizes to ANY future "the collision fix isn't sticking" mystery on a component using a shared/engine primitive mesh** (Cube, Plane, Cylinder, Sphere from `/Engine/BasicShapes/`): check `bUseDefaultCollision` FIRST, before re-touching `collisionEnabled` again. If it's `True`, the asset's own `BodySetup.DefaultInstance` collision (queryable via `mesh.get_editor_property("body_setup").get_editor_property("default_instance")`) is what actually governs runtime behavior, and any instance-level override is cosmetic until that flag is flipped to `False`.

**Also confirms/extends the earlier same-day finding**: `EditorActorSubsystem.get_all_level_actors()` returns **zero actors** while PIE is active (not just "doesn't see PIE-world actors" — it returns nothing at all, even for the persistent editor level). The direct MCP `SceneTools.find_actors`/`ActorTools.*` calls DO resolve during PIE, but only to the `UEDPIE_0_`-prefixed transient PIE-world duplicate — reads are trustworthy (reflects live game state, genuinely useful for exactly this kind of live diagnosis), but writes to those refs are lost the moment PIE stops. To make a PERSISTENT fix, PIE must be fully stopped first; there is no way to reach the editor's real persistent-level actors for writing while a Play session is running.

---

## 2026-07-03 — **The pool-water-blocks-swimming bug is now a THIRD confirmed occurrence — this is a recurring regression with an unknown root cause, not a one-off. A live human playtest caught what property-based "verification" missed twice already.**

Sequence across this project's history: (1) first playtest found it, fixed via `NoCollision` on the water surface mesh; (2) a later session found "all 4 Maple Court pools" had independently regressed back to `BlockAll`/`QueryAndPhysics` despite the fix being documented as applied; (3) this session — after an entire styling marathon that never touched collision — the user did a live human playtest immediately after being told "swimming works," stood on top of the water in Maple Court, and a fresh property read confirmed **all 5 pools** (not just the one tested) were back to blocking collision. None of this session's own commits plausibly caused it (every pool-related change this cycle was material-only, verified via commit history), which means something outside normal Blueprint/material editing is resetting this property — a stale template default reasserting on some editor/save operation, a World Partition external-actor regeneration path, or something else not yet identified. **Flagging clearly for whoever hits this a 4th time: don't just re-fix it and move on — instrument WHEN it happens** (checkpoint collision state before/after specific operations — level reload, `save_dirty_packages` on unrelated assets, editor restart — to isolate the trigger) rather than continuing to blind-fix the symptom indefinitely.

**Bigger meta-lesson: automated/property-based "verification" has now been wrong about this exact bug multiple times, while a live human playtest catches it in seconds.** This session told the user "swimming should work now... verified via automated property checks" — true at the time of checking, but the ground had shifted again by the time they actually pressed a key. For anything touching physics/collision specifically (not materials, not lighting — those don't seem to exhibit this drift), a "verified" claim should be treated as time-bound, not durable, and re-checked fresh immediately before telling the user it's fixed rather than trusting an earlier check in the same session.

---

## 2026-07-03 — **AnimGraph nodes are not reachable through this project's current MCP surface — confirmed via two independent negative probes, not just an assumption.** `AnimBlueprint` objects don't expose `ubergraph_pages` as a readable property (`Failed to find property 'ubergraph_pages'`), and `BlueprintEditorLibrary` has no `get_all_graph_names` method in this engine build. This means the "source a crouch animation" ask can't be solved by scripting a procedural bone-modifier pose into `ABP_Unarmed`'s AnimGraph the way `BP_WatcherController`'s EventGraph logic gets scripted — AnimGraphs are a fundamentally different graph type from Blueprint EventGraphs/FunctionGraphs and this MCP toolset's node/graph tools were built against the latter. **Don't re-attempt AnimGraph scripting without first finding a DIFFERENT entry point** (e.g., Control Rig tools, which ARE exposed via `animation_toolset.toolsets.controlrig.ControlRigTools` per `list_toolsets` and already proved capable of real work this project — see the BigVegas IK retarget entries — might be a viable alternate path for a procedural crouch pose, unlike raw AnimGraph node manipulation). Also confirmed in the same investigation: **the stock UE5 "Simple" Mannequin animation set (`/Game/Characters/Mannequins/Anims/...`) is entirely combat-oriented (Pistol/Rifle/Attack/Death/HitReact/Jump/Dash) with zero crouch-anything** — a genuinely missing category, not a search miss — and **no crouch-related FBX exists anywhere in `_ThirdPartyStaging` either**, so this is a real asset gap, not a wiring gap. The fastest realistic close is a human downloading one Mixamo crouch-walk animation (2 minutes on Mixamo's web app, which isn't fetchable via automation) for retargeting through the already-proven BigVegas IK-retarget pipeline.

---

## 2026-07-02 — **Styling marathon: real Kenney trees/bushes, grey-box street lights, house siding variety, pool glow lights, fence/shed/chair materials — plus two verification-methodology lessons.**

User handed the whole visual-styling pass to this session directly ("style the game... houses, pools, siding, light poles, bushes, trees, characters... loop until complete") and stepped away. Worked it as a sequence of independently-committed chunks:

- **Real geometry replacing grey-box**: imported 7 static meshes from `Kenney/kenney_nature-kit` (trees, bushes, rock), materialed with new `MI_Greybox_{Bark,Stone,Foliage}` instances, and swapped them in for the old primitive-cube trees on Maple Court's 6 tree spots plus `BP_BushHide`'s hiding bush (was a bare engine Cube). Kenney FBX imports land at ~1/16 scale — confirmed a **second time** on this kit (trees needed ~38x scale-up), and material slot names are atlas-region names that differ *per mesh within the same kit* (`SM_Tree_Oak` uses `leafsGreen`/`woodBark`, `SM_Tree_PineTallA` uses `leafsDark`/`woodBarkDark` — a blind copy of slot names across meshes throws "slot not found").
- **Street lights**: no lamp-post asset exists in either staged Kenney kit (furniture-kit is indoor-only), so grey-boxed 5 pole+head+`SpotLight` groups from primitives, built to `design/01`'s exact spec (cone ~70°/`AttenuationRadius` 900/`Intensity` 5000lm/`LampWarm` color) rather than guessing values.
- **House siding**: all 4 Maple Court houses shared one identical `MI_HomeBase_Wall` cream material. Gave B/C/D distinct siding tones (slate-blue/sage/terracotta) and added window+door+foundation-trim geometry to all 4, positioned by deriving each house's actual front-wall world Y from its pre-existing window `PointLight`'s position (`sign(light.y - house.y)`) rather than guessing which side faces the street.
- **Pool glow — a genuine spec gap, not just an upgrade**: `design/01`'s own lighting table specs a "Pool glow" `PointLight` (radius 300, `PoolTurquoise`) as the reward beacon. It existed on **zero** of the 5 pools — only the water surface material carried the turquoise tone. Added the missing light to all 5.
- **Fences/sheds/chairs**: all three were silently reusing unrelated existing materials (`MI_Greybox_Cover` — the same tone as the vault wall — for fences and chairs; `MI_HomeBase_Wall` for sheds), not a deliberate placeholder. Gave each its own tone (warm wood fence, weathered shed, off-white lounge chair) — then separately upgraded the pool chairs a second time from primitive boxes to a real Kenney `loungeChairRelax` mesh.
- **Sandbox "unstyled" sweep found two real, previously-invisible-to-doc-notes gaps**: a material-name sweep (checking every static mesh component project-wide for `Prototype`/`Placeholder`/`BasicShape` substrings, not just re-checking places already suspected) turned up **20 `SM_Cube`/`SM_QuarterCylinder`/`SM_Ramp` instances AND the entire main sandbox ground plane** (the actor literally named `Floor`, 4000×4000 units — the single largest visible surface in the level) still on Unreal's own stock `MI_PrototypeGrid_Gray` blockout material, untouched by the earlier "re-skin 33 leftover template props" pass. **A targeted sweep by mesh/actor NAME had already missed these** (they don't match any of the naming patterns previously checked) — the material-name-based sweep is what actually found them. **Generalizes: when auditing "is everything styled," search by what a placeholder actually IS (material name containing `Prototype`/`Default`/`Placeholder`) rather than by where you already expect placeholders to be — name-based/location-based sweeps have blind spots that a property-based sweep doesn't.**

**GOTCHA — a hand-rolled `unreal.AssetImportTask` + `unreal.FbxImportUI` import does NOT necessarily apply the same unit-scale conversion as whatever built earlier Kenney imports in this project, even for the same source kit.** Importing `loungeChairRelax.fbx` this way and then applying the already-established "Kenney needs ~16x" scale convention (confirmed correct for both nature-kit trees and the earlier furniture-kit sofa/coffee-table) produced a **127-meter-long chair** — a ~700x error. The fix was not "always use 16x for Kenney" — it was measuring the ACTUAL post-scale world bounds via `get_actor_bounds()` and back-solving the correction factor from that measurement (`target_real_world_size / current_full_size × current_scale`), which landed on ~0.23x for this specific mesh via this specific import path. **Never assume a previously-confirmed scale constant transfers to a new asset without a fresh bounds readback** — the constant is a property of the *import pipeline*, not the source kit, and different import call-sites (a dedicated MCP tool vs. a hand-built `FbxImportUI`) can silently disagree on unit conversion. Always verify with `get_actor_bounds()` immediately after any new mesh import + scale, before considering the placement done.

**GOTCHA — `MaterialInstanceConstantFactoryNew` has no Python-settable `initial_parent` property**, despite that being the obvious guess (it's how the C++/editor-UI factory works). `factory.set_editor_property("initial_parent", parent)` throws `Failed to find property 'initial_parent'`. **Use `unreal.EditorAssetLibrary.duplicate_asset(existing_mi_path, new_path)` instead** — duplicate an existing `MaterialInstanceConstant` of the same parent material and re-tune its parameters. This is the same pattern already used project-wide for every `MI_Greybox_*` variant (Bark/Stone/Foliage/Fence/ShedWood/etc.) — don't reach for the factory class at all when a same-parent sibling MI already exists to duplicate.

**METHODOLOGY — a screenshot that reads as blown-out-white or crushed-to-black is not necessarily a bug; it depends on camera distance from emissive sources, and this project's whole night palette is DELIBERATELY dark outside direct light pools.** While QA-checking the new house siding, three `CaptureViewport` attempts at different distances/angles of the *same* house produced three wildly different reads: point-blank (334cm) blew the two window-emissive planes to a flat white blob that erased all wall/door/trim color; a medium oblique shot from a shadowed pocket between the patio wall and house showed everything crushed near-black; a wide high overhead shot showed the roof as a dim, cool-tinted navy rather than any recognizable material hue. None of these were the material data being wrong — property readback (the `BaseColor`/`ShadowColor`/`RimColor` values actually being set, and the correct MI actually being assigned to the correct component) had already independently confirmed success before any screenshot was attempted. **Rule of thumb going forward: when a night-mode screenshot looks "wrong," check property values FIRST (cheap, unambiguous) before spending multiple round-trips hunting for a camera angle that "looks right" — this game's art direction means most camera angles at night are *supposed* to read dark, and only pockets near an actual light source are supposed to pop.** Did use one legitimate finding from the screenshot pass: the window emissive magnitude (9.0) was tuned down to 5.0 after the close-range shot showed it clipping to a shapeless white blob at typical inspection distance — that particular tuning WAS a genuine improvement, just not evidence that anything else was broken.

---

## 2026-07-02 — **When "copy this layout to N other spots" is the ask, don't copy fixed coordinates — the spots are rarely actually identical.** Dressing Maple Court's Lots A/B/C with a fence+shed+chairs+trees package (user request, post-playtest) started as "build it once on Lot A, then paste the same relative offsets onto B and C." That would have been wrong: **the 4 Maple Court pools are different sizes** (Pool A ext=350×250, B=325×250, C=300×225 — deliberate score-tier variants, not a bug), and **Lot C's house sits on the OPPOSITE side of its pool from A/B** (mirrored street layout, both sides of a road facing inward). A fixed-offset copy would have either clipped through Lot C's smaller pool or put the fence/shed on the wrong side entirely. Fixed by writing the builder as a function of each lot's OWN measured geometry (pool water-surface `get_actor_bounds()` for size, `sign(house.y - pool.y)` for orientation) rather than hardcoded deltas from the first instance. **General rule: before replicating a hand-tuned layout N times, check whether the target spots are actually congruent (same size/orientation) — "looks like the same kind of thing" in a level often means "same category, different dimensions."** A quick `get_actor_bounds()` sweep across all N targets before writing the replication script would have caught this in one round-trip instead of after building Lot A's version once.

---

## 2026-07-02 — **First real human playtest: 6 bugs found and fixed in one pass (pool/swim/score, costume cylinder, vault FX, crouch hold-vs-toggle, missing loudness tier, bush placement). Two new MCP-safety gotchas earned the hard way.**

**The bugs (all playtest-sourced, all fixed same session):**
1. **Pool water surface blocked swimming AND scoring** — `PoolWaterSurface`'s mesh had `collisionEnabled=BlockAll`, so the player physically rested on top of the water instead of entering the `PhysicsVolume` beneath it. One collision fix (`NoCollision`) resolved both "can't swim" and "score not counting" simultaneously, since both depended on the player actually reaching the water volume. **This bug had regressed on ALL 4 Maple Court pools too** despite CLAUDE.md claiming it was already fixed there — a fix that apparently never actually persisted to disk (or was silently reverted). Always re-verify a "fixed" claim with a fresh property read before trusting it, especially collision/physics state on placed level actors.
2. **Costume "block" in the water** — `DA_QuietShoes.AttachMesh` was still the raw engine `Cylinder` placeholder from initial system-building; cleared to `None`.
3. **Vault "explosion"** — `NS_NoiseRing`'s `Spawn Count` was still the RadialBurst template default (100) — never retuned down (unlike its sibling `NS_PoolSplash`, which did get a count reduction). 100 amber sprites bursting at 1800-2600 u/s next to the character reads as violent. Cut to 20 + shrunk max sprite size 6→4.
4. **Crouch was hold-only, not a toggle** — `IA_Crouch`'s `Started`→`Crouch`/`Completed`→`UnCrouch` wiring is literally what a first-pass "make crouch work" implementation looks like, but the design intent (and the user's explicit ask) was a toggle so the player can walk around crouched. Rewired to a `Branch` on `IsCrouching` (read through `CharacterMovementComponent`, the pure node's real target type) — `Started` now flips crouch state, `Completed` is fully disconnected.
5. **No loudness distinction between crouch-walking and upright-walking** — both fell through to the same "not sprinting" branch and decayed to the same `IdleFloor`. The design doc (`06_Core_Systems_TechSpec.md`) already speced crouch as "intentionally silent, relies on IdleFloor" — the actual gap was a missing WALK tier *above* idle. Added `WalkBand` (20, between `IdleFloor`=5 and `SprintBand`=55) and restructured the tick to a real 3-tier ladder: crouch→idle floor, walk→WalkBand, sprint→SprintBand.
6. **Requested bush never encountered** — `BP_BushHide` already existed in the sandbox, but on a separate raised platform near the map origin, nowhere near the vault/crouch/pool cluster the user actually walked. Not a bug — a level-layout miss. Spawned a second instance directly in the played path.

**Also attempted and correctly reverted:** enlarging the pool (user: "make the pool a lot bigger or longer"). A straightforward XY-scale-up on all 3 water actors (kept Z untouched to preserve water-level alignment) worked mechanically, but a bounds check afterward showed the new footprint would overlap BOTH the just-placed bush and the vault wall's bounding box. Reverted rather than ship a new collision regression to chase a "would be nice" ask — this needs a real layout pass (repositioning neighbors), not an in-place resize. **The general lesson: after ANY scale-up of a placed actor, bounds-check it against nearby geometry before considering the change done — a resize that "looks bigger" in one capture can silently eat into a neighbor's space that isn't in frame.**

**NEW GOTCHA — `get_node_type_pins()`'s auto-created preview node is NOT a safe, real graph member; wiring to it can corrupt the graph's compiled frame.** Calling `get_node_type_pins(graph, type_id)` to discover a node's pins creates an actual node in the graph as a side effect (documented behavior, used routinely to get real, wireable node refs). But when this was done for `Utilities|FlowControl|Branch` and the resulting node was then wired into via `connect_pins`, the blueprint **failed to compile** with a wall of `ICE SetVariableOnPersistentFrame - No property found` errors covering EVERY event's data pins (Axis, Action Value, Delta Seconds, Prev Movement Mode — not just the ones touched), plus a genuine engine `ensure` in the log: `UEdGraphPin::MakeLinkTo Pin 'execute' on node 'Branch' has a different outer than pin 'then' on node 'Assign'`. The probe-created node apparently doesn't share the same graph "outer" as legitimately-placed nodes, and linking across that boundary corrupts the whole Ubergraph's persistent-frame compilation, not just the touched nodes. **Fix: never trust a `get_node_type_pins` preview node as a real node to wire into. Use `create_node(graph, type_id, pos)` explicitly instead** — same type_id, but properly parented into the graph; wiring to those nodes compiled clean on retry. Both attempts targeted the exact same node type in the exact same graph, so this is reliably about how the node was created, not what it was.

**NEW GOTCHA — recovering from a bad in-memory Blueprint edit without ANY disk risk: `EditorLoadingAndSavingUtils.reload_packages([pkg], unreal.ReloadPackagesInteractionMode.ASSUME_POSITIVE)`.** When the compile above failed on `BP_PlayerCharacter` — a shared, gameplay-critical file — with the asset never having been saved that session, the safest recovery was discarding ALL in-memory changes and reloading from the last-saved disk state, rather than trying to surgically un-break a corrupted graph. `reload_packages` with `ASSUME_POSITIVE` (not `INTERACTIVE`, which would block on a modal) does exactly this with zero prompts. Verified: post-reload, `compile_blueprint` succeeded clean and `get_node_infos` showed the exact original (pre-edit) wiring restored — full confirmation the revert was real, not just "no error." **This is now the standing recovery move any time a Blueprint edit produces a hard compile error (not just a warning) on a not-yet-saved asset** — don't try to patch forward through a corrupted graph state; discard and redo with a corrected technique.

**Smaller API-naming gotchas hit this session (all Python-side, not engine bugs):** `HitResult`'s fields (`location`, `impact_point`) are "protected" and throw on `get_editor_property` — use `.to_dict()` instead, which returns everything including nested structs. `Character` Python wrapper exposes `crouch()` but the uncrouch counterpart is `un_crouch()`, not `uncrouch()`. `Actor.set_actor_location()` needs all 3 positional args (`location, sweep, teleport`) — calling with 2 throws "required argument 'teleport' not found", not a default-fill. A `read_graph_dsl`-displayed node/accessor name is sometimes cosmetic-only and differs from what `write_graph_dsl`/`create_node` actually accept as a type_id (hit three separate times on one function rewrite: `FInterpTo_Constant` read-display vs `FInterptoConstant` real ID; `|SetwithNotifyCurrentLoudness` read-display vs plain `Variables|Default|SetCurrentLoudness` real call; `|GetbIsInWater` read-display vs `Variables|Default|GetIsInWater` real accessor, consistent with the already-documented b-prefix-stripping rule). **When a DSL write fails with "type does not exist" for a name that `read_graph_dsl` just showed you, don't assume the feature is unavailable — search `find_node_types` for the real ID before giving up.**

---

## 2026-07-02 — **Adding a PERSISTENT component to a Blueprint: BlueprintTools has NO `add_component` — use the editor-Python `SubobjectDataSubsystem`. (This is how the vision cone finally shipped as a mesh, and how the decal/icon components were originally added.)**

BlueprintTools does variables/functions/events/nodes/graphs but **cannot add an SCS component** (confirmed by listing its tools). The path that works, via the editor Python console:
```python
subsys = unreal.get_engine_subsystem(unreal.SubobjectDataSubsystem)
bp = unreal.load_asset("/Game/.../BP_Foo")
handles = subsys.k2_gather_subobject_data_for_blueprint(bp)   # handles[0] = root
params = unreal.AddNewSubobjectParams()
params.set_editor_property("parent_handle", handles[0])
params.set_editor_property("new_class", unreal.StaticMeshComponent)
params.set_editor_property("blueprint_context", bp)
new_handle, fail_text = subsys.add_new_subobject(params)      # fail_text '' = ok
subsys.rename_subobject(new_handle, "VisionFanMesh")
comp = unreal.SubobjectDataBlueprintFunctionLibrary.get_object(
           subsys.k2_find_subobject_data_from_handle(new_handle))  # the SCS template
comp.set_static_mesh(mesh); comp.set_material(0, mat)
comp.set_editor_property("relative_scale3d", unreal.Vector(...))
comp.set_editor_property("relative_location", unreal.Vector(...))
comp.set_editor_property("relative_rotation", rot)
comp.set_collision_enabled(unreal.CollisionEnabled.NO_COLLISION)
unreal.EditorAssetLibrary.save_loaded_asset(bp, only_if_is_dirty=False)
```
Worked first try. Gotchas: the created object is named `<Name>_GEN_VARIABLE` (a template, not the runtime instance) — set properties on IT and they serialize into the BP; `compile_blueprint` after so **placed instances pick up the new component** (SCS components DO propagate to placed instances, unlike CDO material/visibility overrides); make the add idempotent by scanning existing handles for the name first (re-runs then reconfigure instead of duplicating). This is **editor-visible**, so you can iterate the transform with the cheap non-PIE `CaptureViewport` loop.

**Applied: the mesh vision cone that the decal couldn't do.** Decal projection bled onto walls (see prior entries); a **flattened Engine `Cone` mesh** as an SCS component doesn't — it's placed geometry. Recipe: `M_VisionConeFan` (Surface/Translucent/Unlit/TwoSided, `FanColor`+`FanOpacity` params), `relRot pitch=-90` lays the cone axis flat (local-X→world-Z, so `scale.x=0.05` flattens it to a slab; y=lateral, z=length), **`yaw=180` flips the apex to the eye** (apex-forward otherwise), `relLoc (150,0,-88)` drops it to the floor, no collision, parented to root → follows facing. Clean floor FOV cone, verified via non-PIE overhead capture. Two red herrings while iterating: (1) the flatten axis — flattening local-Z gives a thin vertical *blade*, not a floor fan (must flatten the axis that maps to world-Z after the pitch); (2) a persistent "mystery cyan square" in captures was just the **sandbox pool's translucent water surface**, not vision debris (an `identify by material` sweep confirmed only one actor used the vision material).

---

## 2026-07-02 — **Visual QA pass caught the home-base starting room blown to solid white — a bounded interior PP volume's `AutoExposureBias` had overshot to +3.0 EV. Also: a non-PIE editor `CaptureViewport` DOES reflect PostProcessVolume exposure, so exposure/lighting QA needs no PIE.**

**The bug:** the home-base room got a bounded `PP_HomeBase_WarmInterior` volume to lift it out of the level's dark exterior exposure lock (the main unbound PP volume runs Manual/locked EV for the moonlit exterior). That override was `AutoExposureBias = +3.0` (8× brighten) — and combined with the room's own lights (ambient 1500 cd, floor lamp + CRT rect at 3000 cd each) it blew the entire room to solid warm-white, zero detail. Dropping the bias to **0.0** fixed it (cozy warm room, all furniture/colors visible). **Generalizes: a bias added to escape a locked exterior exposure is very easy to overshoot — the interior lights are already bright relative to a moonlit exterior, so you often need little or no positive bias. Always re-capture and check for clipped highlights after adding interior exposure compensation.**

**Two reusable capability facts confirmed:**
1. **A non-PIE editor `CaptureViewport` (with an explicit `captureTransform`) applies the scene's PostProcessVolume settings** — both the non-PIE and the `bSimulate:true` captures of the room showed the identical blown-white, and both showed the identical fixed result. So **you can QA lighting/exposure/materials with a plain editor capture — no need to StartPIE** (PIE is only needed to observe gameplay/AI/animation). Cheaper loop: edit a light/PP/material → `CaptureViewport` → assess → iterate, all without PIE.
2. **Reading level actors during PIE/Simulate throws `"The Editor is currently in a play mode"`** from `EditorActorSubsystem.get_all_level_actors()` — it wants the editor world. Stop PIE (or use `UnrealEditorSubsystem.get_game_world()` + `GameplayStatics.get_all_actors_of_class`) before enumerating/editing level actors.

**QA-pass workflow that works (repeatable):** enumerate actors → bucket by world-space region (the neighborhoods live at large X offsets: home-base ≈ X−6000, Maple Court ≈ X+30000) → get a landmark's `get_actor_bounds` → position the camera inside/above it (for an enclosed interior, the camera MUST be inside the local PP volume's bounds or the interior exposure won't apply) → `CaptureViewport` → read the PNG. Maple Court passed clean this way (night-exposed, blue-water pools, house shells, warm accent lights all correct); only the home-base needed the exposure fix.

---

## 2026-07-02 — **The per-tick `Accessed None VisionConeMID` error was crash-recovery-specific, not a normal-play bug — and a DSL `(if (Utilities|IsValid x) …)` guard does NOT lower to a working branch (it dead-ends + orphans the guarded nodes).**

**Diagnosis first (before "fixing" it):** live PIE reads showed `VisionConeMID` is reliably created by BeginPlay in fresh sessions (valid `MaterialInstanceDynamic_0` in two independent fresh runs; `create_dynamic_material_instance()` also succeeds). The per-tick errors only appeared during the **post-editor-crash recovery churn** (rapid PIE restarts after a crash), not on clean cold loads. So the "per-tick error" was lower-severity than it looked — a crash-recovery race, and on a decal that's **hidden/mis-built anyway** (see the vision-cone-decal entry below). Lesson: read the actual failure distribution before assuming a logged error is a normal-play bug — timestamp-cluster the occurrences against what else was happening (here: they all fell inside the crash-recovery window).

**The DSL guard gotcha (cost two rewrites):** trying to guard the null case with `(if (Utilities|IsValid _mid) (SetScalarParameterValueByInfo _mid …))` did NOT produce a working `IsValid → Branch → SetScalar` shape. `IsValid` is a **K2 macro** (`K2Node_MacroInstance` with `Is Valid`/`Is Not Valid` exec outputs), and the DSL lowering left **both macro outputs unconnected** (a dead-end after the prior node) while separately creating an orphaned `IfThenElse` + the `SetScalar` nodes unreachable from the entry. Net effect: the guarded scalars simply never ran (which happened to achieve the goal — no error — but via a mess, not a guard). This matches the earlier catch/detain finding that `Utilities|IsValid` in DSL lowers unreliably. **Takeaways:** (1) don't express an `IsValid` guard in `write_graph_dsl` and trust it — verify the macro's `Is Valid` output is actually wired via `get_node_infos` (`read_graph_dsl` renders the macro as a bare `(Utilities|IsValid …)` and hides the breakage); (2) if you need a real guard, do it surgically (create a `K2Node_MacroInstance` IsValid + wire its outputs) or restructure so the important path doesn't depend on the guard.

**What I shipped:** rewrote both `BP_WatcherCharacter` + `_MC`'s `UpdateVisionConeParams` to **icon-only** (`SetAlertState → CastToWBP_WatcherStateIcon → UpdateIcon`), dropping the decal scalar-param updates entirely — they were pointless while the decal is hidden, and their absence is structurally error-proof (no `VisionConeMID` access remains). Both compile clean (warnings-as-errors). The decal's alert-param plumbing gets re-added when the decal itself is reworked (project-down + UV re-align, already a flagged task). **Residual (accepted, documented):** the DSL rewrites left disconnected orphan nodes in these two functions — zero compile warnings once fully disconnected, zero behavior impact (per the write_graph_dsl-orphans entry in the blueprints skill); the decal rework rewrites the function and clears them. Reordering the important work FIRST and the fragile/guarded work LAST is the robust pattern when a DSL construct might mis-lower — the icon path here runs before anything that could dead-end.

---

## 2026-07-02 — **MIXAMO CHARACTER INTEGRATION DONE via editor Python — the "IK Retargeter needs a human" ceiling is BROKEN. Both Watchers now wear an animating Mixamo `SK_BigVegas` (distinct heavyset silhouette vs the light-Manny player). Four stacked gotchas, each of which alone produced a broken result.** Overturns the prior-entry claim (below, 2026-07-02 Watcher-skin) that these meshes "still need a human in-editor IK Retargeter."

**THE BREAKTHROUGH — the editor Python console is reachable via the Slate Cmd box, and it has the FULL `unreal` API** (unlike `execute_tool_script`, which is stdlib-only and can't `import unreal`). Type `py "C:/abs/path/script.py"` into the status-bar Cmd box (SlateInspector ref `tb2`) and it runs editor Python with `unreal.IKRigController`, `unreal.IKRetargeterController`, `unreal.IKRetargetBatchOperation`, asset factories, etc. This makes **IK Rigs + IK Retargeters + batch anim retargeting MCP-buildable** — the long-standing "no MCP toolset creates IKRetargeter/IKRigDefinition, so it needs a human" wall (still true for the *dedicated toolsets*) is bypassed entirely through Python. The whole pipeline (2 IK rigs, 1 retargeter, 22 retargeted anim assets, mesh+ABP wiring) ran with zero human editor clicks.

**The working retarget pipeline** (scripts in `Saved/PythonProbe/`): (1) `IKRigController.get_controller(rig).set_skeletal_mesh(mesh)` then **`apply_auto_generated_retarget_definition()`** — UE5.8's auto-characterizer assigns IDENTICAL standard humanoid chain names (Spine/Neck/LeftArm/LeftLeg…) to BOTH Manny AND Mixamo skeletons despite different bone names (`pelvis`/`spine_01` vs `Hips`/`mixamorig:Spine`), so no manual chain mapping. (2) `IKRetargeterController`: `set_ik_rig(SOURCE, manny_rig)` + `set_ik_rig(TARGET, vegas_rig)` + `auto_align_all_bones(TARGET)` + `auto_map_chains(FUZZY, True)`. (3) `IKRetargetBatchOperation.duplicate_and_retarget([abp_assetdata], manny_mesh, vegas_mesh, retargeter, suffix="_BV", target_path=…)` → duplicates the ABP + all referenced sequences/blendspaces onto the target skeleton. **Gotcha: `assets_to_retarget` needs `AssetData` structs, NOT loaded objects** — `areg.get_assets_by_package_name(pkg)` returns `Array[AssetData]` directly (don't pass the loaded AnimBlueprint, and don't use `get_asset_by_object_path(SoftObjectPath)` — it wants a plain Name string).

**GOTCHA 1 (the big one) — a retargeted ABP silently forces a T-POSE if it carries a post-process Control Rig authored for the source skeleton.** `ABP_Unarmed`'s AnimGraph ends with a `ControlRig` node running `CR_Mannequin_FootIK` (Manny foot IK). The batch retarget correctly repointed every anim-sequence/blendspace reference to the `_BV` versions (verified via `AssetRegistry.get_dependencies`), BUT it left the Manny `CR_Mannequin_FootIK` reference in place. On the BigVegas skeleton that Control Rig can't characterize (load-time warning: *"Control Rig — Hierarchy discrepancy for bone 'Head' — different parents on Control Rig vs SkeletalMesh"*) and outputs the **reference pose**, overriding the correctly-animated base locomotion underneath. Symptom: a live `anim_instance` of the right class, `is_playing=True`, valid retargeted sequences with real frame data — yet a static T-pose. **Fix: set the `ControlRig` AnimGraph node's `Alpha` input pin `1.0 → 0.0`** (`set_pin_value` on the node's Alpha pin, index 1), blending the foot-IK rig fully out so the base pose passes straight through. Costs foot IK (negligible on flat ground); gains a properly animating character. **Generalizes: after retargeting ANY UE-template ABP, check for a post-process `ControlRig` node and either bypass it (Alpha=0) or rebuild an IK rig for the target skeleton — the base locomotion can be perfect while the final Control Rig quietly stamps ref pose over it.**

**GOTCHA 2 — off-screen runtime bone reads are UNRELIABLE for verifying animation; Simulate-mode `CaptureViewport` IS reliable.** `SkeletalMeshComponent.get_bone_transform(name)` (component-space, via `make_relative_transform` vs the component xform) returned the **reference pose** regardless of the live evaluated pose — the CPU-side bone array isn't refreshed during synchronous Python-console reads while the editor is off-foreground (the documented render/anim throttle). It read identical values before AND after the Alpha=0 fix, even though the render changed. **Also dead ends:** `get_socket_transform(bone, RTS_COMPONENT)` returns `(0,0,0)` for bone names (only true sockets resolve); `get_delta_transform_from_ref_pose(bone)` returned 0 for every bone; `get_bone_transform` takes a NAME not the index from `get_bone_index`. **What WORKS:** `StartPIE(bSimulate=True)` (AI runs, free editor camera), freeze the pawn (`character_movement.max_walk_speed=0` + `stop_movement_immediately()` so it plays idle in place), then `EditorAppToolset.CaptureViewport` with an explicit `captureTransform` aimed at the pawn — **the viewport DOES render during Simulate even off-foreground** (contra the "editor renders black" assumption; `was_recently_rendered` returned True). A T-pose (arms straight out to the sides) vs a natural idle (arms down) is unmistakable in the silhouette. This is now the honest way to verify a character animates without a human at the keyboard. (Extract the spilled base64: `json.load` the tool-result file → `base64.b64decode(rv['image']['data'])` → PNG → Read it.)

**GOTCHA 3 — `EditorAssetLibrary.save_loaded_asset(bp)` SILENTLY NO-OPS if the package isn't marked dirty** (returns `True`, writes nothing, mtime unchanged, git shows no diff). A `set_pin_value`+`compile_blueprint` edit didn't flag the package dirty, so the save claimed success but the Alpha=0 fix lived only in editor memory — a cold load would have reverted to the T-pose. **Fix: `save_loaded_asset(bp, only_if_is_dirty=False)` (or `EditorAssetLibrary.save_asset(path, only_if_is_dirty=False)`) to force the write.** Always confirm the on-disk mtime/git-diff actually changed after saving a Python-edited asset — this is the same dirty-memory-vs-cold-disk trap as the IMC saga, in a new guise. (`Package.set_dirty_flag` does NOT exist; the method is `mark_package_dirty()`, but forcing the save is simpler.)

**GOTCHA 4 — importing one character at a time.** `SkeletalMeshTools.import_file` cross-contaminates material slots if you rapid-fire imports with `import_materials=true` (documented earlier). BigVegas came in with 4 slots (`Elvis_*`); the Watcher's `overrideMaterials` was sized 2 (for Manny). After a mesh swap, **resize the override array to the new mesh's slot count** or the main body slot renders with the default material. Set `MI_WatcherBody` on all 4 slots.

**Mechanics that held up:** the CDO mesh/anim-class swap (`cdo.get_editor_property("mesh").set_editor_property("skeletal_mesh_asset"/"anim_class", …)`) DID propagate to already-placed instances for mesh+anim (unlike `overrideMaterials`/`bVisible`, which per the entry below do NOT — those still need per-instance setting). Editor **crashed once** mid-session (engine-level, unrelated to BigVegas — PIE ran stable ~2min first); the self-heal restart reconnected THIS session's MCP the moment port 8000 came back (commit before restarting; clear `Saved/Autosaves/PackageRestoreData.json` so the boot modal doesn't block the MCP game thread). Assets: `IK_Manny`/`IK_BigVegas`/`RTG_Manny_To_BigVegas` in `_Project/AI/Retarget/`, `ABP_Unarmed_BV` + 21 anims in `_Project/AI/BigVegas/`.

**Known-open, observed this session (NOT caused by the above — recompile of both Watcher BPs produced zero on-disk diff, proving their logic is unchanged):** `BP_WatcherCharacter` spams a per-tick `Accessed None ... VisionConeMID` runtime error in PIE. `UpdateVisionConeParams` reads `VisionConeMID`, which is created `OnBeginPlay` via `VisionConeDecal.CreateDynamicMaterialInstance`; it's None at tick time → either the pawn's BeginPlay isn't firing from a cold load (the documented BP-cold-load disease) or the decal's base material is None. Non-fatal (the ground vision-cone decal just won't update its color; the overhead `WBP_WatcherStateIcon` + HUD `DetectionBar` still carry alert readability). **Robust fix for a follow-up: make `UpdateVisionConeParams` lazily create the MID if `!IsValid(VisionConeMID)` instead of relying on BeginPlay.** Pre-existing; flagged, not yet fixed.

---

## 2026-07-02 — **"GREEN MYSTERY" FULLY CLOSED: the green in playtest screenshots was NEVER navmesh — it was (1) the vision-cone decal on the Watcher [fixed prior] and (2) 33 leftover ThirdPerson-TEMPLATE props wearing the template's green `MI_ThirdPersonColWay` material, scattered across the sandbox.** Don't reflexively blame "navmesh viz" for green in this project.

**What it actually was:** `L_Sandbox_Movement` was built by duplicating `/Game/ThirdPerson/Lvl_ThirdPerson`, and the duplicate KEPT all of the template's decorative geometry — 33 `SM_Cube`/`SM_Cylinder`/`SM_QuarterCylinder`/`SM_Ramp` actors on the green `MI_ThirdPersonColWay` material, sitting on the central spawn mound, the map perimeter, and right beside the course (e.g. two quarter-cylinders at `(10, 1900)` — the recurring "green cylinder"). These render in the actual game (they're real static meshes, not a debug overlay), so they'd absolutely show in a playtest. Re-skinned all 33 to `MI_Greybox_Cover` via a batch `set_properties` on each StaticMeshActor's mesh component's `overrideMaterials` (filtered by "which actors have a `ThirdPersonColWay` material"). Kept the geometry (non-destructive — some may be traversal/cover); a full removal is a separate call.

**How the misattribution held for cycles:** the template's `MI_ThirdPersonColWay` is a striped green that looks *exactly* like Recast navmesh viz, and there genuinely WAS navmesh viz toggled on too, so "it's all navmesh" seemed to fit. **Decisive identification that finally cracked it:** a `CaptureViewport` with `annotations.maxLabelDistance` set returns a `labeledActors` array (name + screenPosition + worldLocation) — parse THAT (no need to even read the image) to map an on-screen blob to a specific actor, then read that actor's mesh + `overrideMaterials`. Navmesh has no actor at the blob's screen position; a real prop does. This turned "is it navmesh or a prop?" from a guess into a one-query fact. **Also:** `ShowFlag.Navigation 0` (via the Slate Cmd box) did not remove this green — correctly, because it isn't navmesh — which was itself the tell.

**Generalizes:** any level duplicated from an Epic template (ThirdPerson, FirstPerson, etc.) inherits ALL the template's decorative props on the template's own materials. Audit + re-skin (or delete) them as part of styling — grep placed StaticMeshActors for `/Game/ThirdPerson/` or `/Engine/` materials. And when hunting a stray on-screen color, use the labeled-capture → actor-lookup path instead of assuming it's a debug overlay.

---

## 2026-07-02 — **The "green everywhere" that confounded 3 cycles of screenshots was the VISION-CONE DECAL, not navmesh — its box is huge + mis-oriented so it projects onto the Watcher's own body and nearby walls instead of a clean floor cone.** Also: Watcher skinned dark (distinct-from-player readability); CDO material/visibility overrides do NOT propagate to already-placed level instances.

**The decal bug (root cause of a long-running visual mystery):** `BP_WatcherCharacter`'s `VisionConeDecal` has `relativeRotation (0,0,0)` (never rotated to project downward), `relativeLocation (0,0,0)` (centered ON the watcher, not in front on the floor), and `decalSize (128,256,256)` (a large box). A `UDecalComponent` projects along its local **−X** and fills a box of ±decalSize; with no downward rotation and a big box centered on the pawn, it smears the `M_VisionCone` material (which reads greenish at the editor-default/AlertState-0 param) across the Watcher's own mesh, the ground, and adjacent walls. **This is what I kept misreading as "navmesh" for three cycles** — the striped green on the character was the decal, and hiding it (`bVisible=false`) instantly revealed a clean dark Watcher. **Decisive disambiguation:** capture in the **editor viewport** (not PIE) with `ShowFlag.Navigation 0` — real navmesh viz is off there, so any green that remains on a *character* must be a decal/material, since navmesh never projects onto skeletal meshes. **Fix chosen (pragmatic):** hid the decal on both Watchers (CDO + placed instances) — it was actively making the Watcher look broken, and the overhead `WBP_WatcherStateIcon` (`?`/`!`) + HUD `DetectionBar` already carry alert readability. The decal's proper rework (pitch to project down, shrink the box to a floor slab, offset forward, and re-align the material's fan-mask UV to the new orientation) is a focused future task — genuinely fiddly to do blind because the fan-mask geometry is coupled to the decal's orientation; don't rabbit-hole it in a general cycle.

**CDO-vs-instance gotcha (cost one confused PIE pass):** setting `overrideMaterials` (and `bVisible`) on a Blueprint's CDO component (`Default__BP_X_C:CharacterMesh0`) does **NOT** apply to actors already **placed** in a level — placed instances serialized their own (empty/old) component values when they were dropped, and those stick. PIE spawns the *placed* actor, not a fresh CDO instance, so the CDO change is invisible in play. **Set the property on BOTH the CDO (for future spawns) AND every placed instance (`find_actors` by class → set on each instance's component).** Verified via a live PIE read: after CDO-only edit the instance still showed `overrideMaterials: []`; after also editing the instances it showed the override and rendered correctly.

**Watcher skin (the deliverable):** created `MI_WatcherBody` (dup of `MI_Greybox_Cover`, parent `M_GreyboxToon`) tinted near-black charcoal — `BaseColor (0.015,0.018,0.028)`, `ShadowColor (0.004,0.005,0.009)`, `RimColor (0.14,0.18,0.30)` cool moonlit rim so the silhouette edge reads in the dark, `EmissiveColor 0` (no glow — alert glow belongs to the icon/cone, not the body). Applied to both material slots of both Watchers' `CharacterMesh0`, keeping the animating Manny mesh + `ABP_Unarmed`. Result (confirmed in PIE): the Watcher is a dark menacing figure, clearly distinct from the light Mannequin player — the design's "heavy dark silhouette contrasts the light/low player" read (`05_Characters` §1.2), achieved via the doc's own endorsed `OverrideMaterials` path (no mesh swap, no regression). **`BP_PlayerCharacter` stays untouched** (movement lane's file); the Mixamo character *models* still need a human in-editor IK Retargeter to animate (confirmed again this session: no IK rigs/retargeters exist and no MCP toolset creates them — swapping a raw Mixamo mesh would be a T-pose regression, strictly worse than the animating Manny).

---

## 2026-07-02 — **THE CATCH/DETAIN LOOP WORKS — closing the last broken core mechanic. Verified live: patrol → detect → chase → catch at CatchRadius → detain → at-risk lost → 2s → respawn at stash → Watcher resumes patrol.** Resolves the "caught/detain unreachable in play" design gap (the old "3 candidate fixes need a human decision" flag) under the hands-off mandate.

**The design rule implemented:** catch fires when the Watcher is Suspicious-or-Alert AND the target is within the profile's own `CatchRadius` (120) — **independent of current sight**. This was necessary because of a cleanly-reproduced engine behavior: **AISense_Sight goes blind at point-blank range** (observed: the Watcher standing 68uu from the player — capsules touching — with `bCanSeePlayer=false` and the meter decaying; the eye-to-target trace ends inside the target's own capsule). Any catch logic gated on live sight can therefore never fire at contact distance. The overlap-based catch was doubly dead: move completion leaves the pawn at AcceptanceRadius+capsules (~108-123uu), so capsule overlap (68uu) is never reached either. Distance-check-at-alerted is the standard stealth-game resolution and uses only numbers the profile already specs (`CatchRadius`, `DetainRespawnDelay`).

**A DSL structuring gotcha cost one broken iteration:** placing the catch block BEFORE the movement dispatch, gated as `(if (and (!= alert 0) (Utilities|IsValid target)) ...)`, silently killed the whole main exec path when the target was null (patrol froze at spawn again — caught by re-running the patrol check before the catch test, which is now the standing pattern: **after ANY TickBrain edit, verify patrol still runs BEFORE testing the new feature**). The working structure: catch block AFTER the movement dispatch, gated only on `(!= alert 0)`, using the impure `CastToBP_PlayerCharacter` itself as the null gate (a failed cast dead-ends only the catch block's own tail, not the dispatch). Avoid `Utilities|IsValid` inside `and` expressions in DSL — whichever overload the writer picks, the lowering broke main-path exec here.

---

## 2026-07-02 — **AI WATCHER FULLY ENABLED, HANDS-OFF: the "movement regression" was THREE stacked causes, not one — and the long-standing "needs a human Build Paths click" diagnosis was only ever a third of the story.** Both Watchers now verified patrolling their full loops + chasing + investigating, live in PIE, zero human clicks.

**The three stacked causes (each one alone kept the Watcher frozen; all three had to fall):**
1. **PIE registered ZERO navigation data** — the map's RecastNavMesh was a stale actor with `bIsWorldPartitioned=false` inside a World Partition map. The interactive editor world adopted it (rebuilds logged "RebuildAll building NavData... agent radius 34"), but every runtime-style world (PIE, the WP builder commandlet) silently refused to register it — so `MoveToLocation` instant-failed forever, regardless of generation mode, rebuilds, or baked tiles. **Diagnostic that cracked it: type `RebuildNavigation` into the editor's status-bar Cmd box WHILE PIE IS RUNNING** — it executes in the PIE world; "Build started... 0.00s" with NO "RebuildAll building NavData" line = nothing registered. Fix: delete the stale actor (`SceneTools.remove_from_scene`), `load_level` to reload (auto-spawns a fresh NavData because bounds volumes exist — the nudge-a-volume trick did NOT trigger a spawn; a reload did), set `bIsWorldPartitioned=true` + `runtimeGeneration=Dynamic` on the fresh actor (verify by readback), then run the editor's real **Build → Build Paths via SlateInspector menu clicks** — which finally routed through the WP-aware build and emitted 4 `NavigationDataChunkActor`s (the WP nav data this map never had). Verified: the per-PIE-start "Unable to find RecastNavMesh instance while trying to create UCrowdManager" warning disappeared, and chase movement worked.
2. **Every patrol point floated beyond the nav agent's vertical projection extent.** Sandbox points at z=100, Maple Court at z=90, over ground at z≈0 — and the supported agent's `DefaultQueryExtent` is (50,50,50). A `MoveToLocation` goal >50 units above the mesh fails to project → **instant Failed, no log output even at `log LogPathFollowing VeryVerbose`** (the request dies before path-following ever sees it — verbose PF logging shows nothing, which itself is the diagnostic signature distinguishing "no request issued/projected" from "request failing"). This is why chase (goal = the player, on the ground) and investigate (goal = LastKnownLocation, at the player's feet) both worked while patrol never did. Fix: trace ground at each point's XY, set z = ground+10 — all 10 points (4 sandbox + 6 MC). Two sandbox points also had to be RELOCATED in XY (their traces landed on the raised course platform at z=200 and on top of a wall at z=400 — unreachable-from-floor placements authored under a flat-ground assumption; the template floor also ENDS at y≈2000, traces beyond it hit nothing).
3. **The arrive-advance arithmetic could never trigger.** Patrol used `MoveToLocation(point, AcceptanceRadius=60)` + "advance index when distance < 80" — but a pawn completes its move at acceptance radius + capsule radius ≈ 84-94 from the point CENTER, which is > 80 → both Watchers walked to their first point and parked forever. Fix: advance threshold 150 (> worst-case stop distance). **Generalizes: an arrive-check radius must exceed AcceptanceRadius + agent capsule radius + slack, or arrival never registers.**

**Also fixed en route:** both `TickBrain`s were rewritten clean via `write_graph_dsl` (the sandbox controller's graph had 300+ nodes of which ~2/3 were dead debris from 2-3 prior rewrite generations — the write_graph_dsl orphan-accumulation disease at scale, making node-level tracing hopeless and read_graph_dsl actively misleading). Read-DSL output is NOT directly writable: `(|GetbCanSeePlayer)` must be written as `(Variables|Default|GetCanSeePlayer)` (b-prefix strip), `Math|Byte|Equal(Byte)` doesn't exist for writing (use `==`/`!=` sugar → PromotableOperators), `Variables|Controller|GetPlayerState` renders with an arg but creates argless (use `Class|Pawn|GetPlayerState target` instead), and a cross-class function rendered as `Class|BPWatcherCharacterMC|...` actually needed `Class|BPWatcherCharacter|...` (wrong-class render on same-named functions).

**New capabilities proven this session (all previously believed impossible):** (a) **SlateInspector can click real editor menus** — Observe(window) → menus appear as m1-m9 refs in the title bar (a fresh Observe pass may be needed; the first snapshot missed them) → Click the menu → popup appears as a new top-level window → Observe it → Click the item. This retires the "MCP cannot trigger Build → Build Paths" ceiling. (b) **The status-bar Cmd textbox is typeable** (Click it first to focus, then Type with submit) — opening the whole console-command surface: `RebuildNavigation`, `ShowFlag.Navigation 1` (visualize navmesh in captures!), `log <Category> VeryVerbose`, and during PIE these run in the PIE world. (c) **A deliberately self-triggered editor restart reconnects fine** — kill editor via PowerShell, relaunch with the MCP flag, poll port 8000, next tool call works (the last untested reconnect scenario, now proven; the editor-restart caution in older entries is fully retired).

---

## 2026-07-02 — **ROOT CAUSE FOUND + FIXED: the "black screen / can't see the game" was TWO stacked lighting-authoring bugs, NOT a render/GPU/focus problem — and the long-standing "editor renders black when not OS-foreground" lesson partially mis-covered for it.** The user could not see ANY geometry in the viewport ("Lit" mode, only wireframes/gizmos on black); every MCP capture came back the same.

**The two bugs (both in saved level content, which is why restarts didn't help):**
1. **Degenerate exposure lock on the unbound night PP volume:** `AutoExposureMinBrightness=0` AND `AutoExposureMaxBrightness=0` both *overridden* (a nonsense clamp), on top of `AEM_Manual` metering with bias −1. With the project's `r.DefaultFeature.AutoExposure.ExtendDefaultLuminanceRange=True`, those fields are EV100 units — the combination crushed the entire exterior to *mathematically pure black*, not "dark night."
2. **Palette token misused as a light color:** `BP_Moon`'s DirectionalLight `lightColor` was set verbatim to `DA_ArtPalette.nightBlueKey` `(0.04, 0.07, 0.16)` — a token authored as an on-screen *surface/UI* color. As a *light* color it multiplies intensity: 1.8 lux × ~0.16 ≈ 0.3 lux of near-black navy. Effectively no moon at all. **Rule: palette tokens give lights their HUE (normalize max channel → 1.0), never their brightness — brightness lives on `intensity`.**

**The fix (verified visually via before/after captures):** on the unbound PP volume — `AEM_Histogram`, bias 0, min/max brightness overrides re-locked to EV100 = 0/0 (a *sane* lock: consistent stealth-readability exposure with no adaptation lag); on the moon — `lightColor=(0.25, 0.44, 1.0)` (nightBlueKey hue at full brightness), `intensity=3.0`. Result: full moonlit-blue night everywhere; home-base room (own +3EV bounded volume) unaffected and still correct.

**Why this evaded diagnosis for a day+ (the meta-lessons, more valuable than the bug):**
- **A plausible documented lesson became a thought-terminating explanation.** Every black capture since ~July 1 evening was attributed to the (real, but narrower) "editor renders black when not OS-foreground" lesson. That lesson is about `CaptureEditorImage` returning stale/throttled *window* frames — but `CaptureViewport` with an explicit transform does a real scene render regardless of focus, and it was black for a *different* reason. **When a known gotcha "explains" a symptom, check whether its mechanism actually covers the observation before filing it away.**
- **`CaptureAssetImage` thumbnails can serve a STALE CACHED image** — an early "the renderer is healthy, look, the material sphere renders" proof was unsound (thumbnails may predate the breakage). Don't use asset thumbnails as evidence about live scene rendering.
- **The unverified-write gotcha applies to DIAGNOSTIC experiments too.** A disable-the-PP-volume test "failed" (scene stayed black) and nearly exonerated the true culprit — the `set_properties(bEnabled=false)` write was never verified by readback (the project's own documented rule, applied everywhere *except* mid-diagnosis). One hypothesis → one change → one test only works if the change actually applied.
- **What finally cracked it:** capturing the home-base room (which has its own bounded +3EV exposure-override volume + unlit emissive CRT) rendered *perfectly* while the exterior was black — same world, same renderer, same materials. That isolated the mechanism to exposure in one capture. **When "everything is black," find or build one region with independent exposure/lighting and compare.** Also: a black *mesh* still depth-occludes editor wireframes behind it — visible occlusion of lines proved meshes were rendering (as black) rather than missing.
- **Eliminations that were still worth doing (cheap, config-level):** show flags (`EditorShowFlagsString` all `=1`, `ShowFlag.*` cvars unoverridden at `2`), viewport fixed exposure (`bFixed=False`), Preview Rendering Level (inactive), `r.ScreenPercentage=0` (means "defer to default 100", NOT zero-res), shader compile queue (no workers, no pending jobs), GPU/TDR events (none), hidden actors (`bHidden=false`, comp `bVisible=true`), WP loading (163/163 loaded, 101 in frustum).

**Capability un-block for future sessions:** `CaptureViewport` with an explicit `captureTransform` DOES produce real, full-color renders regardless of OS focus — the styling lane can now visually verify work again. The "can't screenshot color, editor unfocused" claim in older entries below is superseded by this entry wherever the capture in question was `CaptureViewport`.

---

## 2026-07-02 — **MAJOR FINDING: `write_graph_dsl` does NOT clean up the previous version's nodes when it rewrites a function — dead nodes accumulate across every re-edit of the same graph, invisibly.** Also confirms `NiagaraToolset_System` can bind a module input to a User Parameter (closes the `NS_NoiseRing` loudness-color-lerp gap).

**The accumulation bug:** rewrote `BP_LoudnessComponent.Server_ReportAction` via `write_graph_dsl` to add a loudness-driven color lerp on `NS_NoiseRing`'s spawn. Ran `find_nodes` afterward to verify — got back **35 nodes** for an 8-line function that should produce ~18. `get_node_infos` tracing from `K2Node_FunctionEntry_0`'s exec pin showed the LIVE chain only reached 18 of them; the other 17 (three separate `IfThenElse`/`GetActionInstantBump`/`HasAuthority` mini-chains, `Self` reference nodes, etc.) were completely disconnected from the entry point — no incoming exec connection at all — yet still physically present in the saved graph. Cross-checking `FunctionEntry_0`'s `ActionTag` **data** pin showed it fanned out to THREE separate generations of "GetActionInstantBump"-shaped calls (data pins can freely fan out to dead exec territory) — proof this function had been rewritten via `write_graph_dsl` at least 2-3 times across prior sessions, and EVERY rewrite left the previous version's nodes physically in place, just orphaned. `compile_blueprint(warnings_as_errors=true)` returned clean both before and after — UE's compiler does not flag this as a warning, so it is **completely silent** unless you explicitly walk the graph from `FunctionEntry`.

**Not the same bug as the get_node_type_pins concern it first looked like:** before this, `get_node_type_pins` was called 5 times against this same graph for schema discovery (a normal, expected pattern per the `unreal-mcp-blueprints` skill). Checked whether THOSE calls also leave garbage — they don't: none of their probe node IDs (`K2Node_CallFunction_18/19/20/21`, `K2Node_MakeStruct_0`) appeared in the post-edit `find_nodes` listing. `get_node_type_pins` cleans up after itself (or its nodes never persist to the saved graph); `write_graph_dsl` does not clean up the graph it's replacing.

**Fix applied:** traced the live chain exhaustively via `get_node_infos` (every pin, both directions) before touching anything, confirmed the 17 orphans had zero data or exec connection to the live 18, then `delete_node`'d all 17. Re-verified: `find_nodes` now returns exactly 18, `read_graph_dsl` round-trips to the identical intended function body, `compile_blueprint` still clean. **Generalizes — this likely affects every Blueprint function in the project that's been through more than one `write_graph_dsl` rewrite across sessions** (which, per this project's own history, is most of the actively-iterated gameplay/AI Blueprints). Not urgent to mass-sweep (dead nodes don't affect compiled behavior or runtime), but **any session about to `find_nodes`/`get_node_infos` a graph for analysis must trace reachability from `FunctionEntry` rather than assuming every returned node is live** — and a full project-wide orphan sweep is a good, low-risk future cleanup task. Added to the `unreal-mcp-blueprints` skill.

**The loudness-color-lerp feature itself (previously flagged as deferred in CLAUDE.md):** `NS_NoiseRing` had zero User Parameters. Added `User.RingColor` (LinearColor, default = the existing amber `(0.91, 0.70, 0.23, 1.0)`) via `NiagaraToolset_System.AddUserVariables`, then rebound `InitializeParticle`'s `Color` input from its hardcoded literal to a **Linked** value pointing at `User.RingColor` via `SetStackInputData` (`inputData.struct = NiagaraExt_StackInputData_Linked`, `value.linkedVariable = {name: "User.RingColor", type: {classStructOrEnum: {refPath: LinearColor}}}` — the exact same mechanism Niagara already uses internally for engine-provided links like `Emitter.Age`/`Engine.Owner.Scale`, discovered by reading an existing linked input's value shape off `GetScriptStackInputValues` rather than guessing). Verified via independent `GetStackInputData` readback + `GetSystemCompileState` (`UpToDate`, zero errors/warnings) before touching Blueprint. On the Blueprint side, `Niagara|SpawnSystemAtLocation`'s `ReturnValue` (the spawned `NiagaraComponent`) was previously discarded — captured it, then called `Niagara|SetNiagaraVariable(LinearColor)` on it with a `Math|Color|Lerp(LinearColor)` between the amber default and `DA_ArtPalette.readAlert` (`1.0, 0.15, 0.12`), driven by `GetLoudness01()` read AFTER `AddLoudness` so it reflects the post-bump level. Standard Niagara idiom confirmed safe here: setting a user parameter on a `bAutoActivate=true` component immediately after spawn (same Blueprint frame, before any Tick) reliably lands before the emitter's first update — no visible pop risk for a burst effect.

---

## 2026-07-02 — Collision/material bug-hunt across Maple Court CLOSED OUT clean: patio walls + playground props are correctly solid, no fixes needed

Continuing the systematic sweep from the pool-water/BushHide/HedgeSqueeze/QuietShoes fixes (below), checked the last two unaudited Maple Court prop categories:

- **`SB_PatioWall` ×5** (Pool D's wall ring): all `BlockAll` collision + correct `MI_Greybox_Cover` material. **Correct as-is** — per `02_Neighborhood_MapleCourt.md` §3.5's "greed vs. safety sightline puzzle," this ring is a deliberately SOLID perimeter forcing entry through 2 designed gaps (hedge-squeeze + streetlight-lit). Same category as `BP_HedgeSqueeze`'s intentionally-solid hedge halves — don't confuse "blocks movement" with "bug" when the design explicitly wants a barrier.
- **Playground props** (`SB_PlaygroundFence_N/S/W_MC`, `SB_PlaygroundPad_MC`, `SB_MonkeyBars_MC`, `SB_Slide_MC`): all `BlockAll` + correctly materialed (`MI_Greybox_Cover`, pad on `MI_Greybox_Stash`). **Correct as-is** — playground equipment and its ground pad are meant to be solid props a player walks around/on, not through, unlike the pool-water/bush/hedge-gap/pickup class of "must pass through this" bugs.

**Finding methodology note:** `find_actors(name="Playground")` only returned 4 of the actual 6 playground props (missed `SB_MonkeyBars_MC` and `SB_Slide_MC`, whose labels don't contain "Playground") — the exact substring-completeness gotcha already documented in the `unreal-mcp-scene-building` skill. Caught it by re-querying with a `bounds` box around the known playground area instead of a name guess, which is the skill's own prescribed workaround and correctly surfaced both misses (plus `SM_SkySphere`/`SB_GroundPlane_MC`/`SB_StreetStrip_MC`, correctly excluded as out-of-category).

**This closes the collision/material bug-hunt thread across the whole project.** Every prop category has now been audited: real bugs found+fixed = Pool_A + all 4 Maple Court pool water surfaces (blocking collision), `BP_BushHide`, `BP_HedgeSqueeze` materials, `BP_ItemPickup_QuietShoes`; correctly-solid-by-design and left alone = vault/crouch course props, verticality props, house shells, patio walls, playground props. No further sweep needed unless new props are added.

---

## 2026-07-02 — **MAJOR CORRECTION: in-session `unreal-mcp` reconnect DOES work** (at least for this scenario), overturning the long-standing "no in-session reconnect, must restart Claude Code" doctrine that's driven real caution all project. Full empirical trail below — see `unreal-mcp-session` skill §1 for the corrected guidance.

**The sequence, observed directly across several consecutive /loop cycles:**
1. A `unreal-mcp` tool call failed with `"Unable to connect. Is the computer able to access the url?"` — a genuinely different error shape than the usual "tool not found," worth distinguishing (connection-refused vs. never-registered).
2. Checked evidence before acting (per this project's own debugging discipline): `tasklist` showed `UnrealEditor.exe` was NOT running at all. Read the log tail — a clean, orderly `LogExit` shutdown sequence (including the MCP's own `HttpListener stopping listening on Port 8000` message), not a crash (no callstack, no "Fatal error", no abrupt truncation). Concluded the human had closed the editor themselves.
3. **Correctly did NOT restart the editor unilaterally** — per the (then-believed-accurate) doctrine that a mid-session restart wouldn't restore this session's access anyway, and because relaunching something the user may have deliberately closed risks interfering with whatever they're doing. Flagged this clearly in `CLAUDE.md` instead and stood down.
4. Over the next few `/loop` cycles, `tasklist` showed the editor process came back (twice, different PIDs each time — the user restarting it), but `Test-NetConnection 127.0.0.1 -Port 8000` stayed `False` both times — consistent with the editor being launched without the `-ExecCmds=ModelContextProtocol.StartServer` flag (a normal double-click launch, not the MCP-enabled one).
5. On an explicit fresh `/loop` invocation, armed a **persistent `Monitor`** polling `Test-NetConnection` for port 8000 (the correct tool for "wake me when this external condition changes" rather than manual polling every cron cycle).
6. The monitor fired: **`Port 8000 is now listening`**. Rather than assume this meant nothing for the current session (per the old doctrine) or assume it fixed everything, **tested empirically**: called `list_toolsets` directly. **It succeeded immediately** — full toolset list returned, no Claude Code restart involved. Verified NOT a fluke with a second, independent call (`IsPIERunning`) right after — also succeeded.

**Why this matters beyond just "cool, it worked":** this directly undercuts the reasoning behind at least one standing, deliberately-parked decision — the MegaLights/Lumen re-test was explicitly deferred specifically because "restarting the editor would kill MCP access for the entire rest of this autonomous run... recoverable only by the user manually restarting Claude Code" (see the flag CLAUDE.md carried for most of this session). That specific fear may be overstated. **However — important scope limit, don't over-correct:** what was proven is "the server died and came back for reasons external to this session, then a tool call succeeded." What was NOT tested: an already-connected session *deliberately* triggering its own server's shutdown-and-relaunch (e.g., to test a `DefaultEngine.ini` change) and then reconnecting. That's a different enough scenario (this session's own in-flight state, timing, whatever object handles it was holding) that it deserves its own explicit test before fully retiring the caution — don't treat this as blanket permission to restart the editor freely.

**Best-guess mechanism (not confirmed, just the most parsimonious explanation):** the transport is plain HTTP (`http://127.0.0.1:8000/mcp`). If each tool call is a fresh, stateless HTTP request rather than a held-open session/handshake, then "reconnection" isn't really reconnection at all — it's just that a request against a dead port fails, and a request against a live port succeeds, with no session-affinity concept in between. This would mean the original "no in-session reconnect" claim was likely either over-generalized from a narrower original incident, or accurate for how a different piece of the stack behaved at the time it was written, rather than a hard architectural limit of the current setup.

**Generalizes:** when a tool call fails with a connection-refused-style error (not a "tool not found"/schema error), don't assume the session is permanently dead — check whether the underlying server process is actually reachable right now, and if it comes back, **just try a real tool call** (not `ToolSearch`, which returns cached schemas without proving live connectivity) before concluding a full Claude Code restart is needed. It might just work.

---

## 2026-07-02 (BushHide HideVolume re-investigation + aborted live-movement probe) — Independently rediscovered the same root-component `RelativeLocation` confusion the styling lane found the same day (see the `PickupVolume` entry below) via a different actor; also found that teleporting a `Character` while its own `AddMovementInput` loop is actively running doesn't reliably stick

**The false-alarm rediscovery:** CLAUDE.md flagged `BP_BushHide.HideVolume` as floating ~300uu above `BushMesh` (world Z≈600 vs Z≈300). Before "fixing" it, checked `ActorTools.get_root_component` — `HideVolume` IS the root, so its `relativeLocation.z=300` already equals the actor's own world Z (confirmed identical to a direct `get_actor_transform` read), not an additive offset. `BushMesh` (child, relative Z=0) sits at the same world Z=300. Both components were correctly aligned the whole time; ground at that XY traces to Z=210 (not the main area's Z=0 — the level isn't flat everywhere), matching the combined actor bounds (210–390) exactly. Attempted a `set_properties` write to "fix" `HideVolume.relativeLocation` to `(0,0,0)` before catching this — it silently failed to apply (readback showed the write never took), which incidentally protected against what would have been a real regression (moving the whole actor). **Two independent sessions hit the exact same category of mistake on the same day** — this is now solidly generalized (see the `PickupVolume` entry below); don't re-investigate any component's `RelativeLocation` as a "position mismatch" without checking `rootComponent` first.

**The aborted live-movement probe:** tried to verify the pool/bush `BlockAllDynamic→NoCollision` collision fixes with genuine `AddMovementInput`-driven movement (not a teleport, which bypasses collision resolution) — added a temporary, fully-disconnected `Test_AutoWalkProbe` custom event to `BP_PlayerCharacter` (per the "isolated test event" diagnostic pattern), self-triggering via `DoOnce` off `EventTick`'s dangling `ResolveSpeed.then` pin, looping via `SetTimerByFunctionName` calling `AddMovementInput` every 0.05s. It worked as a movement generator (the pawn walked ~1858 units continuously with no visible snag), but **`ActorTools.set_actor_transform` did not reliably relocate the pawn while this loop was actively driving it** — repeated re-teleports either silently failed to stick or were overridden within a fraction of a second, making it impossible to precisely position the pawn for a controlled before/after collision test. Root cause not pinned down (plausibly `CharacterMovementComponent` re-asserting its own tracked position/velocity against an external `SetActorLocation` while simulating) — not worth over-investigating for a one-off test. **Cleaned up completely**: all 5 temporary nodes deleted, `ResolveSpeed.then` confirmed dangling again via `get_node_infos`, `BP_PlayerCharacter.uasset`'s save produced only an LFS-pointer-churn diff (`git checkout --` to avoid a no-op commit). **Generalizes:** if you need to test real-movement collision behavior via an `AddMovementInput` probe, either (a) let it run from an uninterrupted fresh `BeginPlay` and reason about relative displacement rather than trying to reposition mid-run, or (b) drive the probe's target *direction* dynamically from a hardcoded destination (steer-to-point every tick) instead of relying on a single teleport-then-walk setup — don't try to `set_actor_transform` a `Character` that has an active movement loop already running and expect it to stick.

---

## 2026-07-02 — **A component's `RelativeLocation` is NOT always "offset from a sibling/parent" — check `rootComponent` FIRST, because for the actor's ROOT component, `RelativeLocation` effectively IS the actor's world transform.** Nearly "fixed" a non-bug by moving an actor 300uu.

**What happened:** investigating `BP_ItemPickup_QuietShoes` for the same collision-blocking bug class as the pool/bush fixes (previous entries), found `PickupVolume.relativeLocation` = `(300, 1700, 208)` — suspiciously identical to the actor's own world position. Assumed this was a copy-paste bug (someone pasted world coords into a relative-offset field) that would place the pickup's trigger volume ~3800uu away from its own visual mesh (`PickupMesh`, at relative `(0,0,0)`). Attempted to "fix" it to `(0,0,0)`.

**What actually happened:** `PickupVolume` is the actor's `rootComponent` (checked via `ObjectTools.get_properties(actor, ["rootComponent"])` — do this BEFORE assuming any position bug). For a root component, there is no parent to be "relative to" — `RelativeLocation` on the root directly defines (or heavily constitutes) the actor's own world transform. So `PickupVolume.relativeLocation == ActorWorldLocation` was never a bug — it's the **expected, correct** relationship. `PickupMesh` (a genuine child, attached to the root) was ALREADY correctly co-located with `PickupVolume` by construction (its own relative `(0,0,0)` places it exactly at the root's position) — there was no mismatch to fix in the first place.

**The damage and the recovery:** the "fix" attempt (`set_properties(PickupVolume, {relativeLocation: {0,0,0}})`) partially succeeded — only the X component actually applied (see the `set_properties` partial-struct-write gotcha, same entry family), moving the WHOLE ACTOR from world X=300 to X=0 (since moving the root moves the actor). Caught immediately via `get_actor_transform` readback (not just the `set_properties` return value), and fully reverted with `ActorTools.set_actor_transform` back to the actor's original, correct `(300, 1700, 208)`. Re-verified after reverting. No lasting damage, but this could have shipped a genuinely displaced actor if the revert-check habit had been skipped.

**Generalizes — do this check BEFORE touching ANY component's transform for a suspected "position mismatch" bug:** `ObjectTools.get_properties(actor, ["rootComponent"])`, compare its `refPath` against the component you're investigating. If they match, that component's `RelativeLocation` is the actor's own placement — don't reason about it as "this should match a sibling's relative offset," and if you DO need to reposition it, use `ActorTools.set_actor_transform` on the ACTOR (which is the semantically correct operation), not `set_properties` on the component's `relativeLocation` (which is the same underlying value but invites exactly this category of confusion).

---

## 2026-07-02 — **`ObjectTools.set_properties` can silently apply only PART of a single struct-valued property**, not just silently drop whole properties from a multi-property call (see the entry below) — even a single `relativeLocation: {x,y,z}` write can land only `x` and leave `y`/`z` unchanged, with the call still returning `true`

Found while investigating the `PickupVolume` position (see the root-component entry above): two SEPARATE, isolated `set_properties` calls, each specifying `relativeLocation: {x:0, y:0, z:0}` as the ONLY property in the call, each returned `true`, and each time a follow-up `get_properties` showed only `x` had actually changed — `y` and `z` silently stayed at their original values, identically, both times (not a random/flaky result — the same partial outcome twice in a row). This is a more granular version of the already-documented "multi-property call can silently apply only some properties" gotcha — this time it's WITHIN a single struct-typed property, not across multiple top-level ones.

**Generalizes:** for any struct-valued property write (`Vector`, `Rotator`, `Transform`, etc.) that matters, re-read ALL of its sub-fields after the write, not just a couple. A `true` return, and even a partially-correct-looking readback, is not proof the whole struct landed. If a field doesn't stick after a retry, stop trying to fight it via `set_properties` — reconsider whether you're editing the right thing at all (see the root-component entry above: in this case the "stuck" fields were actually a signal something more fundamental was misunderstood, not just a flaky write).

---

## 2026-07-02 — **`ObjectTools.set_properties` can silently apply only SOME of multiple properties in one call** — always verify every property independently after a multi-property set, not just the call's `true` return

**What happened:** fixing `BP_BushHide`'s `BushMesh`, called `set_properties` ONCE with both `bodyInstance` (collision fix) and `overrideMaterials` (material fix) in the same `values` dict. Call returned `true`. Re-read via `get_properties`: `bodyInstance.collisionProfileName` correctly showed `"NoCollision"` — but `overrideMaterials` was STILL the original empty array, completely unchanged. Split into two separate single-property `set_properties` calls; the material one then applied correctly and stayed applied on a final combined re-read.

**Generalizes:** this is the same family as the already-documented "never trust a mutating call's `true`/`null` return alone" discipline, but specifically for MULTI-property calls — a call can partially succeed (first property applied, rest silently dropped) while still reporting overall success. **When setting more than one property on the same object, either set them in separate calls, or verify EVERY property in the `values` dict via a follow-up `get_properties`, not just one of them.**

---

## 2026-07-02 (found via the same pattern as the pool water bug) — `BP_BushHide`'s `BushMesh` had blocking collision + a missing art/theme material; `HedgeHalfA`/`B` had the missing material only (their blocking collision is correct by design); `HideVolume` sits ~300uu above `BushMesh` in world space — flagged, not fixed

Same investigative pattern that caught the Maple Court pool water collision bug (previous entries): checked the other "player must physically enter/pass through this" mechanics for the same class of issue.

- **`BP_BushHide.BushMesh`**: had `collisionProfileName="BlockAllDynamic"` — would prevent a player from ever walking INTO the bush to trigger the hide overlap, exact same bug shape as the pool water surfaces. Fixed to `NoCollision`, matching the now-corrected water-surface pattern. Also had `overrideMaterials=[]` (raw engine `Cube` default, predating the art/theme pass — same shape as the vault-wall/crouch-doorway gap from 2 cycles ago). Fixed to `MI_Greybox_Cover`.
- **`BP_HedgeSqueeze.HedgeHalfA`/`HedgeHalfB`**: architecturally DIFFERENT from bush/water — this mechanic is "squeeze BETWEEN two solid hedge walls," not "pass fully through one volume." Their `BlockAllDynamic` collision is correct by design (confirmed via their relative positions: two 140uu-wide blocks centered at x=∓110, leaving an ~80uu gap between them for the squeeze path) — did NOT touch their collision. Both DID have the same missing-material gap as everything else this session (`overrideMaterials=[]`); fixed to `MI_Greybox_Cover`.
- **New finding, NOT fixed — needs a design decision, not just a pattern-match:** `BP_BushHide`'s `HideVolume` component sits at world Z≈600 while `BushMesh` sits at world Z≈300 (computed from the actor's own world transform `{x:0,y:0,z:300}` plus each component's relative Z: `BushMesh` relative Z=0 → world 300; `HideVolume` relative Z=300 → world 600). A ~300uu (3m) vertical gap between the visual bush and its own hide-trigger volume — well above where a standing player's capsule would ever be relative to the bush. **Deliberately NOT repositioned this session**: unlike the collision/material fixes, there's no directly-analogous "confirmed-correct reference instance" to pattern-match against (nothing else in the project has this exact geometric relationship to copy), and moving a gameplay trigger volume's placement is more design-sensitive than matching an established material/collision convention. Flagged in `CLAUDE.md` for the movement/gameplay lane (who owns the hide mechanic's feel) or the human to resolve.
- **Context that makes this MORE plausible, not less:** `CLAUDE.md` already documents an earlier session's "clean, zero-displacement enter/exit toggling... an early false 'collision is blocking' reading turned out to be a stale/wrong Z coordinate in the test itself, not a bug" verification for this exact system. That test — like every other pool/hide verification this whole session — almost certainly used `set_actor_transform` (teleport), which can place a pawn at ANY Z coordinate, including one that happens to fall within a misplaced, floating trigger volume, without that placement making sense for a real walking player. A positive teleport-based "it works" result doesn't rule out this kind of placement mismatch — see the teleport-blindness lesson two entries up.

---

## 2026-07-02 (potentially significant, needs real-movement confirmation) — all 4 Maple Court pool water surfaces had blocking collision (`BlockAll`), while the sandbox's `Pool_A` correctly had `NoCollision` — every pool-scoring test this session used teleport, which can silently bypass exactly this class of bug

**What was found:** following up on the Pool_A water-material fix (previous entry), checked the SAME 4 Maple Court water surfaces' `bodyInstance.collisionProfileName` while I had the actors open. Result: all 4 = `"BlockAll"`. `Pool_A`'s own surface (the one most heavily tested this whole session) = `"NoCollision"`, matching the project's own documented recipe (`unreal-mcp-scene-building` skill: "Disable a decorative plane's collision so players pass through").

**Why this matters:** checked each water plane's Z-position against its paired pool's basin bounds (`ActorTools.get_actor_bounds` on the `BP_PoolVolume`). E.g. `Pool_MC_A`: basin Z range `[-88, 168]`, water surface at flat Z=20 — squarely inside the range, not off to the side or floating above the traversable area. A `BlockAll` plane sitting there would act as a solid floor a player's capsule cannot descend through, physically preventing entry into the deeper basin from above.

**Why this wasn't caught by any of this session's "verified live in PIE" pool-scoring claims:** every one of them used `ActorTools.set_actor_transform` to place the pawn INSIDE the pool directly (documented as necessary because "spawn-transform override fails on collision, but a post-spawn teleport correctly re-triggers overlaps" — see the Step 3 entries in CLAUDE.md). A hard `SetActorLocation`-style teleport can interpenetrate solid geometry without being blocked or resolved the way a player WALKING or SWIMMING into the same space would be. This means the scoring Blueprint LOGIC was genuinely, correctly verified — but the test method itself was blind to a collision setup that would stop a real player from ever reaching that logic through normal gameplay.

**Fix:** matched `Pool_A`'s confirmed-correct config exactly (`collisionProfileName="NoCollision"`) on all 4 Maple Court water surfaces. Verified via property readback, not just the `set_properties` return value.

**Honest confidence level — NOT the same as the NavMesh regression's rigorous isolated-test diagnosis:** this fix is well-reasoned (matches an established, proven-correct reference config; geometrically the collision plane does sit in the entry path) but was **not confirmed via an actual live movement test**, because doing so would require adding a temporary movement probe to `BP_PlayerCharacter`, which is this lane's explicitly excluded file. Don't treat "Maple Court pool entry now works" as proven — it's a strong inference from static config + geometry, not a live-tested fact. **The movement/gameplay lane (which owns `BP_PlayerCharacter` and CAN test real movement) or the human should confirm this with an actual playtest.**

**Generalizable lesson:** teleport-based verification (`set_actor_transform`) proves overlap/scoring LOGIC works, but is fundamentally blind to collision-blocking bugs that only a real walking/swimming entry would hit. When a system's only verification path has been teleport-based, a "does this work" claim should be read as "the logic is correct," not "a real player can reach this."

---

## 2026-07-02 (independent re-confirmation of the NavMesh regression) — Fresh 20s wall-clock test reproduces zero Watcher movement exactly; three more plausible config-level culprits checked and ruled out, strengthening the existing "needs a human Build Paths click" conclusion rather than finding a new fix

**Why re-tested:** an earlier cycle this same session (before the regression was flagged) had watched the sandbox Watcher move from `X=100` to `X=330` across a live PIE window — seemingly contradicting the later "zero movement" finding. Needed to resolve the conflict with a rigorous test rather than trust either data point.

**Test:** fresh `StartPIE`, read both Watchers' transforms, `sleep 20` (real wall-clock, not just several MCP round-trips), read again. Both the sandbox and Maple Court Watcher showed **byte-identical position** before and after — confirmed zero movement over a genuine 20s window, matching the prior investigation exactly. The earlier "X=100→330" observation was almost certainly a short BeginPlay-time settling/impulse (or movement recorded before whatever broke the pathing took effect), not sustained patrol — consistent with an even older LESSONS entry describing the Watcher "patrolling a short distance then freezing at a consistent X-coordinate."

**Additional culprits checked and ruled out** (none of these were in the original evidence chain, so worth recording even though none panned out): `NavigationSystem` project config's `bGenerateNavigationOnlyAroundNavigationInvokers=false` (would silently zero out Dynamic-generation tiles if true with no invokers registered — it's false, not the cause); `WorldSettings.NavigationSystemModuleConfig_0.bStrictlyStatic=false` (would override the per-navmesh `runtimeGeneration=Dynamic` at the module level — false, not the cause); the ground floor's `StaticMeshComponent0.bCanEverAffectNavigation=true` (geometry is correctly nav-relevant). Combined with the existing investigation's findings (`SupportedAgents` correct, bounds volumes correct, `runtimeGeneration=Dynamic` + `bForceRebuildOnLoad=true` both set, `bAutoCreateNavigationData=true`, `bAutoSpawnMissingNavData=true`, `MoveToLocation` returns `Failed` even for a hardcoded valid destination), every config knob that would normally explain "Dynamic generation configured correctly but zero tiles ever built" checks out clean. This is a genuinely stubborn case, not a config an autonomous session missed — **the CLAUDE.md top-level flag's "needs a human Build → Build Paths click" conclusion stands, confirmed from a second independent angle.** A future session should NOT re-check any of the properties named above — they're settled.

---

## 2026-07-02 (regression ROOT CAUSE FOUND, fix needs the human) — **AI Watcher movement: `AIController.MoveToLocation` returns `Failed` even called directly with a valid, in-bounds destination — confirmed via an isolated test that bypasses `TickBrain` entirely. This is genuinely a NavMesh build-data problem, not a Blueprint logic bug, and MCP has no tool to trigger the fix.**

**The decisive test:** rather than keep reading `TickBrain`'s logic (already confirmed correct twice), built a temporary, fully isolated test on `BP_WatcherController`: a new `TestForceMove` custom event (zero connections to any existing exec chain) containing a single `AI|Navigation|MoveToLocation` call to a hardcoded, real patrol waypoint (700,1850,100 — 884uu from spawn, confirmed inside the `NavMeshBoundsVolume`), with its `ReturnValue` (`EPathFollowingRequestResult`) captured into a temporary byte variable. Triggered via `SetTimerByFunctionName` appended to the tail of `BeginPlay`'s existing chain (its `then` pin was already dangling/unconnected — zero risk to existing logic, no `break_pins` needed). Result: **`TestMoveResult = 0 = Failed`**, every time, including after two different fix attempts (see below). This conclusively isolates the bug to pathfinding/NavMesh itself — not `TickBrain`'s dispatch logic, not perception, not the splice history.

**Two targeted fix attempts, both tried and both did NOT resolve it (don't re-try these first):**
- `RecastNavMesh.runtimeGeneration`: was `Static` (no auto-rebuild as level geometry changes) → changed to `Dynamic`. Kept the change (strictly more correct for a level under active script-driven construction) but it alone didn't fix pathfinding.
- `RecastNavMesh.bForceRebuildOnLoad`: was `false` (engine skips the first nav update after a successful load by default) → set to `true`. Re-tested with extra real-time buffer (PIE `warmupSeconds=3` + an additional real 10s wait) in case the rebuild was just slow — `MoveToLocation` still returned `Failed`.

**Why this points at "no nav data was ever actually baked," not a config error:** `SupportedAgents` matches exactly between the project config and the active `RecastNavMesh` instance (`AgentRadius=34, AgentHeight=192` both sides — ruled out a mismatch). The `NavMeshBoundsVolume` geometrically covers both the start and destination. `bAutoCreateNavigationData=True`. Despite all of this being individually correct, an explicit, isolated `MoveToLocation` call still fails — the remaining explanation is that the RecastNavMesh actor and its bounds volume exist as configuration, but the underlying tile/polygon navigation DATA was never generated (or was invalidated and never regenerated) for this level, and neither of the two runtime-generation-related properties available via MCP reflection was able to force that generation to happen.

**Why MCP can't finish this fix:** the standard, definitive resolution — the editor's own "Build → Build Paths" action (or the console `RebuildNavigation` command it wraps) — is not exposed anywhere in the MCP toolset surface (confirmed: no dedicated navigation-build toolset exists per `list_toolsets`; `ProgrammaticToolset.execute_tool_script` only wraps OTHER MCP tools, it can't call arbitrary editor subsystem functions like `UNavigationSystemV1::Build()`). Nor is there a reflectable property that reports "tile count" or "has valid generated data" to confirm the theory beyond the behavioral `MoveToLocation`-fails signal. **This needs the human**: open the editor, check the `Navigation` viewport mode / press `P` to toggle nav mesh display (should show green if valid data exists over walkable areas, nothing if it doesn't — this alone would confirm or refute the theory in seconds), and if it's missing, `Build → Build Paths` (or `Build → Build All`) once. That single click is very likely to fix AI Watcher movement across the whole project (both the sandbox and Maple Court Watchers use the same navigation system).

**Reusable technique — isolate "does X work at all" from "is X being invoked correctly":** when a Blueprint's decision logic looks correct on inspection but the expected effect doesn't happen, don't keep re-reading the logic — build a minimal, fully-disconnected test (a new custom event, zero wiring to existing chains, triggered via a one-shot `SetTimerByFunctionName` off any conveniently-dangling exec pin) that calls the suspect operation directly with hardcoded-known-good inputs, and capture its return value (if it has one) into a temporary variable you can read afterward. This is far more decisive than more code reading, and — done this carefully — carries near-zero risk to existing logic. **Clean up fully afterward**: delete every temporary node (`delete_node` on each), remove the temporary variable (`remove_variable`), then `read_graph_dsl` the whole graph again and diff it by eye against what you had before the test — confirming byte-for-byte identical output is strong evidence the cleanup was complete, not just "looks deleted."

---

---

## 2026-07-02 (regression suspected, UNRESOLVED) — **Both AI Watchers show ZERO patrol movement in a fresh PIE session** — contradicts the standing "✅ Movement now confirmed working for BOTH Watchers" claim. Documented with full evidence rather than silently left standing, but root cause NOT found this session — a fresh session should pick this up before trusting the old "movement confirmed" claim again.

**The test (all measurements used real Bash `sleep` for the wait, never in-script `time.sleep()` — see the throttle-vs-stalled differential below for why that distinction mattered):** `StartPIE` → read both Watchers' `CharMoveComp.Velocity`/`MovementMode` and world position → real 8s wait → re-read: **bit-for-bit identical position, `Velocity=(0,0,0)` on both**, `MovementMode=MOVE_Walking` (capsule physics is fine, just no move command being issued or executed). Extended to 30+ real seconds (well past the doc's own ~2s dwell time at a waypoint) — still exactly zero movement on both the sandbox AND Maple Court Watcher simultaneously.

**Ruled out, with evidence (don't re-check these first):**
- **Not a render/simulation throttle.** The editor genuinely was running at ~3fps real-time (confirmed via log frame-counter deltas across clean Bash-`sleep`-bracketed windows — not a script-blocking artifact, see below), matching the already-documented "throttles when not OS-foreground" behavior. But a **decisive differential test proved simulation itself ticks fine**: teleporting the player pawn to z+500 and waiting 5 real seconds, it fell back to the correct resting z (92.15) — full, correct gravity/physics simulation in well under the window. If general physics ticks normally but the AI never moves, the cause is specific to AI movement/pathfinding logic, not a global engine stall. (This also means the earlier-established "the game ticks fine underneath render throttle, just check position" doctrine needs a caveat: it's true for physics, not proven true for AIController pathfinding specifically.)
- **Not a script-blocking artifact.** First measured a zero-movement result using `ProgrammaticToolset.execute_tool_script` with in-script `time.sleep()` between polls — correctly distrusted this, since a Python script on the game thread could itself stall ticking and produce a false zero-movement reading. Re-tested with the wait happening in a real Bash `sleep` OUTSIDE the game thread entirely, bracketing direct (non-scripted) `get_actor_transform` calls — same zero-movement result. This rules out the confound.
- **Not `bThrottleCPUWhenNotForeground`** — already confirmed `False` in an earlier session.
- **Not a missing NavMesh** — `RecastNavMesh` + both `NavMeshBoundsVolume`s (sandbox + Maple Court) exist in the live PIE world (`find_actors(name="NavMesh")` returned all three). An old `LogCrowdFollowing: Unable to find RecastNavMesh instance` warning in the log turned out to be from a much earlier PIE session (timestamp ~17 min before this one, based on the frame-counter wrap), not this one — don't over-read stale log lines without confirming which PIE session they belong to.
- **Not an obviously severed exec chain at the one known TickBrain splice point.** `BP_WatcherController.TickBrain`'s vision-cone splice (`Cast→UpdateVisionConeParams`, added earlier this session) was the most plausible suspect given this exact session already found `connect_pins` can silently sever an exec chain (see the entry above). Checked directly via `get_node_infos`: `UpdateVisionConeParams`'s `then` pin correctly connects onward to `IfThenElse_11`, not orphaned. This specific splice is NOT the cause (though the function has other splice points added across the session that weren't individually checked — see below).
- **Not a perception-blocking state** — `AlertState=0`, `DetectionMeter=0`, `bCanSeePlayer=false`, `TargetActor=None` on the sandbox Watcher throughout. This is a normal "Unaware, no target" state that should still be actively patrolling per the state-machine design (Unaware → patrol between waypoints), not one of the states that would legitimately halt movement.

**What wasn't checked (ran out of cost-effective options this session):** `TickBrain` has grown to **300+ nodes** across many incremental splices this session (vision cone, AlertState mirror, icon push, plus whatever the movement/gameplay lane added independently) — too large to exhaustively `get_node_infos` without burning excessive context. The actual per-`AlertState` movement-dispatch logic (the `IfThenElse` chain that should call `AIController` `MoveTo*` for Unaware/Suspicious/Alert) was never located or inspected this session. Also not checked: whether an explicit, manual `MoveToLocation` call (bypassing `TickBrain` entirely) succeeds — would cleanly separate "pathfinding itself is broken" from "`TickBrain` just isn't issuing the command right now," but no MCP tool exposes a direct AIController-`MoveTo` call, and Blueprint doesn't have a bare debug function for it without adding a temporary node.

**UPDATE (2026-07-02, next cycle) — reproduced again cleanly, ruled out more, found a genuinely new lead, still UNRESOLVED.** Re-ran the exact test fresh: same result, bit-for-bit identical position after a real 8s wait. Not transient/environmental — this is real and reproducible.

- **`read_graph_dsl` on `TickBrain` actually worked this time and gave a complete, sane picture** — contrary to the "300+ nodes, too large" note above, the DSL reconstruction is compact and readable (the raw node count includes many pure-node re-reads of the same variables, not 300+ distinct pieces of logic). The movement dispatch reads exactly as expected: `if AlertState==2 → MoveToActor/MoveToLocation (chase)`, `elif AlertState==1 → MoveToLocation (investigate)`, `elif patrolPoints.Length > 0 → MoveToLocation(patrolPoints[patrolIndex]) + advance index when within 80uu`. **The logic itself is correct** — no missing branch, no obviously-wrong condition. **Lesson: try `read_graph_dsl` on a large function BEFORE assuming it's too big and resorting to raw `get_node_infos` node-by-node — the lossiness warning elsewhere in this doc is real for some freshly-rewired nodes, but doesn't mean the whole function reads empty.**
- **Confirmed `PatrolPoints` is correctly populated at runtime** (4 valid `TargetPoint` refs on the sandbox Watcher, `PatrolIndex=0`) and **waypoint 0 is a real 884uu away** (not a coincidental already-there case) — so the `elif patrolPoints.Length > 0` branch's condition is true and should be actively calling `MoveToLocation` toward a real, distant point every tick.
- **Confirmed the `NavMeshBoundsVolume` bounds geometrically cover both the Watcher's spawn and waypoint 0** (volume center (400,1500,100) scale (12,12,4) → bounds X:[-800,1600] Y:[300,2700] Z:[-300,500]; both points fall inside).
- **New finding: the active `RecastNavMesh` had `runtimeGeneration="Static"`** — meaning it does NOT auto-regenerate nav data as level geometry changes, only using whatever was baked at the last explicit build. Given MCP has no "Build Navigation" tool and this project adds geometry constantly via script, this looked like a strong candidate (stale/never-built nav data → pathfinding silently fails → zero movement, while physics/perception are unaffected). **Changed to `"Dynamic"` via `ObjectTools.set_properties`, saved, re-tested in a fresh PIE session — did NOT fix it.** Same exact zero-movement result. Ruled out as the (sole) cause, but leaving the change in place since `Dynamic` is strictly more correct for a level under active construction and caused no regression.
- **CORRECTED (2026-07-02, next cycle) — the "second, orphaned `RecastNavMesh`" finding below was an overstatement; there was nothing to clean up.** Original finding: a SECOND `RecastNavMesh` actor (`RecastNavMesh_UAID_...1DEA02`, different from the active `...26EA02` one) fails registration on every PIE boot: `LogNavigation: Warning: NavData RegistrationFailed_AgentNotValid... will be removed. This warning will disapear once the map is saved.` Flagged at the time as "genuine mess worth a deliberate cleanup pass." **Follow-up check**: `find_actors(name="RecastNavMesh")` in the plain (non-PIE) editor world returns ONLY the active `...26EA02` instance — the `...1DEA02` one does not exist as a persisted level actor at all. It's a **transient candidate the navigation system spins up fresh during PIE startup** (most likely while resolving the sandbox's own `NavMeshBoundsVolume_UAID_...1DEA02...`, whose ID lineage matches), gets rejected because a valid `...26EA02` already serves the "Default" agent slot, and is discarded — exactly matching the log line's own "will be removed" framing. This is ordinary UE navigation-system startup noise, not a leftover actor; **there is nothing to delete, don't go looking for it.** Doesn't change the core `MoveToLocation=Failed` diagnosis, which is independent of this side-finding.
- **Dead end: `PathFollowingComponent.Status` (the `EPathFollowingStatus` that would directly show Idle/Moving/Paused/Waiting) is not exposed via the reflection-based property system** — `list_properties` on the component shows only generic `ActorComponent` fields, no path-following-specific ones. No way to directly ask "is a move request even active" via MCP; would need a temporary Blueprint node (e.g., `GetMoveStatus`) wired to a debug print, not attempted this session.
- **Confirmed the file-level "1 insertion 1 deletion" diff on `BP_WatcherController.uasset` after this investigation is a benign compile-touch resave, not a logic change** — re-ran `read_graph_dsl` after the unexpected dirty flag appeared and got byte-identical output to the earlier read in the same session. Pure read/inspect calls (`find_nodes`, `get_node_infos`, `read_graph_dsl`) can apparently still mark a Blueprint package dirty (likely UE's compile-on-load path), even with zero graph changes — **don't assume an unexpected dirty/changed `.uasset` after a read-only investigation session means something broke; verify via a DSL/property re-read before treating it as suspicious.**

**Next steps for whoever picks this up (updated 2026-07-02):** items (1) and (3) below were the real remaining leads; item (2) (cleaning up a supposedly-orphaned duplicate NavMesh) turned out to be based on a misread — see the CORRECTED note above, nothing to clean up there. (1) actually verify nav DATA exists (tile count > 0) for the covered area, not just that a correctly-configured NavMesh ACTOR exists — no MCP-reflectable property was found for this; (3) [DONE, see the "ROOT CAUSE FOUND" entry above] an isolated `MoveToLocation` call confirmed pathfinding itself returns `Failed` — root cause is NavMesh build data, needs the human to `Build → Build Paths` in-editor. This whole investigation is otherwise CLOSED pending that human action; don't re-open it without new information (e.g., the human reporting they built paths and it's still broken).

**Why this matters / next steps for whoever picks this up:** `CLAUDE.md`'s Step 4/5 narrative and the vision-cone-decal paragraph both assert Watcher movement as independently confirmed working (including "observed live, patrolling and moving from its spawn point on its own" and a specific `x=32900→32669` MC-Watcher displacement). Those may have been true AT THE TIME under different conditions (shorter test windows, possibly less-throttled editor state, or before some later change) — this entry does NOT claim those were false, only that a fresh, careful, real-wall-clock-verified test just now shows zero movement. **Before trusting "movement works" again, re-run this exact test** (bare position/velocity reads bracketed by a real Bash `sleep`, not a screenshot or a short round-trip gap) fresh. If it reproduces, bisect `TickBrain`'s splice history (`git log -p` on `BP_WatcherController.uasset` won't show Blueprint graph diffs usefully since it's binary — instead walk the splice points named in this file/CLAUDE.md chronologically and `get_node_infos` each one's `then`-chain continuity, the same check that cleared the vision-cone splice above) or consider asking the human to eyeball it live (this is exactly the kind of "close the loop with a real Play session" gap noted elsewhere in this doc).

---

## 2026-07-02 (NS_PoolSplash) — **`connect_pins` does NOT support exec-pin fan-out** — calling it a second time on the same output exec pin silently REPLACES the existing connection instead of adding a parallel one, unlike the graphical Blueprint editor where dragging a second wire off one output pin is completely normal. Nearly shipped a broken pool.

**What happened:** wanted to add a new "spawn splash VFX" call that fires ALONGSIDE `BP_PoolVolume`'s existing `EventActorBeginOverlap → IfThenElse_0` scoring logic, without touching that existing chain at all — reasoning that since real Blueprint exec output pins support multiple outgoing wires (both fire, in wire order, when the event triggers), I could just `connect_pins` a NEW target onto the SAME already-connected output pin as a safe, zero-risk addition. **This assumption was wrong for this MCP tool specifically.** After the call, `get_node_infos` on the event node showed `then` connected ONLY to my new node — the original connection to `IfThenElse_0` was gone. Re-checking `IfThenElse_0.execute` directly confirmed it: `connected_pins: []`, completely disconnected. **This would have silently killed the entire pool's scoring path** (`PoolScoreComp.Server_EnterPool` and everything downstream) had it shipped un-caught.

**Fix, and the correct general pattern:** treat `connect_pins` as **single-connection-per-output-pin, always** — never assume a second call adds a parallel branch. To add new logic that should fire alongside (not instead of) an existing chain, **splice linearly**: `break_pins` the existing link, connect the event's output to your NEW node's execute input, then connect your new node's OWN "then" output to the ORIGINAL target. (`Event → YourNewNode → OriginalTarget`, not two independent listeners on `Event`.) This is the exact same "splice at the front" pattern already used successfully elsewhere this session (the vision-cone `TickBrain` splice, the `AlertState` mirror splice) — the difference here is realizing it's not just the SAFEST option, it's the ONLY option this tool actually supports for this case.

**How this was caught before it shipped:** re-ran `get_node_infos` on BOTH ends of the connection immediately after the `connect_pins` call, instead of trusting the `{"returnValue":null}` success response. The very first re-splice attempt (reconnecting the original link) then ALSO silently disconnected my new node in turn — proving conclusively (not just suspecting) that this is single-connection-per-pin behavior, not a one-off glitch. Confirmed fully fixed via a live PIE test reading real game state (`PlayerState.IndividualScoreAtRisk` climbed 0→51 on pool entry, exactly as before this change) — not just a clean compile, which would NOT have caught this class of bug (a disconnected-but-otherwise-valid exec pin is not a compile error, just silently-dead logic). **Generalizes hard: `{"returnValue":null}` / `true` from a mutating MCP call is not proof the mutation did what you intended — for anything touching an EXISTING, already-load-bearing exec chain, re-read both the source and destination pins immediately after, every time, not just on suspicion.**

---

## 2026-07-02 (AttachedMeshComp) — Re-read the actual design-doc spec before trusting your own earlier paraphrase of it; `Game|AddComponentbyClass`'s type-pins preview under-reports its real pins; a dedicated "attach to component/socket" node isn't reachable via `find_node_types` or direct `create_node` guesses on this MCP surface

**The paraphrase trap:** an earlier session's summary said costume mesh-swap was blocked "until the styling lane wires an accessory attach point (`AttachedMeshComp`)," worded in a way that reads like it needs a NEW component added to `BP_PlayerCharacter` (an excluded file this lane must stay out of). Re-reading `06_Core_Systems_TechSpec.md` §4.3/4.4 directly showed `AttachedMeshComp` is a **member variable of `BP_CostumeComponent` itself** (a `StaticMeshComponent` reference, populated at RUNTIME by `ApplyCostumeVisual`, not a design-time SCS component) — `BP_CostumeComponent` was already attached to the character by an earlier session, so none of this needed touching the excluded file at all. **Generalizes: when a summary phrase makes a task sound blocked by a boundary, check the actual source doc before accepting that — paraphrases lose the exact mechanism, and the mechanism is what determines whether a boundary actually applies.**

**`Game|AddComponentbyClass`'s `get_node_type_pins` preview showed only 3 input pins** (execute, self, Class) **but the REAL node, once `create_node`'d, has 5** (adds `bManualAttachment` and `RelativeTransform`) — confirms a pattern worth assuming generally: **the type-pins preview for a node can under-report its real pin set; if a node's behavior seems too limited, create it for real and re-run `get_node_infos` before concluding the capability doesn't exist.** Not exploited this session (already had a working root-attach + manual-offset solution by the time this was noticed) but worth using next time a "spawn + attach at socket" need comes up — `bManualAttachment=true` + a supplied `RelativeTransform` may get a proper socket-relative spawn in one call instead of the two-step fallback below.

**No dedicated "attach component to component/socket" node was reachable** — tried `Class|SceneComponent|AttachToComponent`, `Transformation|AttachToComponent`, bare `AttachToComponent`, `Components|AttachToComponent`, and a `find_node_types` substring search for `"AttachTo"` (which matched several UNRELATED nodes — `CustomizableObjectInstanceUsage|AttachTo`, `AttachtoPlayerPawnCameraNode|*` — proving the search index does cover "Attach*" broadly, just not this specific function). **Fallback used instead: `AddComponentByClass` with no manual-attachment args (auto-attaches to the owning actor's ROOT component) + a separate `Transformation|SetRelativeLocation` call afterward with a hand-picked offset approximating the target socket's height.** This is a real fidelity trade-off — the spawned mesh sits at a fixed offset from the root and won't track bone animation (a true socket attach would follow the foot through a walk cycle) — acceptable for this MVP-scoped grey-box placeholder, worth revisiting with the `bManualAttachment`/`RelativeTransform` pins noted above, or a proper socket attach, before any real art pass.


## 2026-07-02 (WBP_WatcherStateIcon) — `WidgetComponent` (a 3D-world-attached UMG widget) confirmed MCP-buildable on the first attempt, and the "cosmetic AlertState mirror" search-index-lag block from an early session is finally resolved — same fix pattern as `GetControlledPawn` two sessions ago, now confirmed general

**The gap:** `04_AI_Watcher.md` §11 specs an overhead `?`/`!` `WidgetComponent` icon reading a pawn-local `AlertState` mirror — distinct from and complementary to the ground-projected vision-cone decal (both are explicitly called for as separate signals, not redundant). `CANON.md` already documented that `BP_WatcherCharacter.AlertState` was added as a variable but the controller's setter call was never actually wired, because "the cross-object setter wasn't indexed by `find_node_types` yet when this was attempted (fresh-asset DB-refresh timing)" — i.e. the exact same search-index-lag class of issue as the `GetControlledPawn` block from two sessions ago, just never revisited.

**Resolved the same way**: don't wait for `find_node_types` to index a freshly-created cross-class function/component/widget-class — guess the type_id from this project's already-established naming conventions (`Variables|Default|Get<ComponentName>`, `Utilities|Casting|CastTo<AssetName>`, `Class|<ClassNameNoUnderscores>|<Member>`) and call `create_node` directly. Every guess landed correctly on the first try: `Variables|Default|GetStateIconWidget` (a component added earlier the SAME turn), `Utilities|Casting|CastToWBP_WatcherStateIcon` (a widget class also created the SAME turn), `Class|WBPWatcherStateIcon|UpdateIcon` (a function on that same brand-new widget). **Generalizes into a firm rule, not a one-off workaround: `find_node_types` search-index lag is NEVER a real capability gap for anything created earlier in the same session — always try `create_node` with the conventionally-guessed type_id before concluding something "can't be built yet."**

**`WidgetComponent` itself — genuinely new capability territory this session, now confirmed.** Unlike `WBP_HUD` (a screen-space overlay via `CreateWidget`+`AddToViewport` on the PlayerController), a `WidgetComponent` is a `SceneComponent` attached to any actor that renders a UMG widget in 3D space (billboarded overhead icons, floating nameplates, etc.) — a different mechanism entirely. Added via the same `ActorTools.add_component` path already proven for the vision-cone `DecalComponent` (`component_type: {refPath: "/Script/UMG.WidgetComponent"}`), then configured via `ObjectTools.set_properties` with `space` (`"Screen"` or `"World"`), `widgetClass` (a `Class@/Script/UMG.UserWidget` ref), `drawSize` ({x,y} IntPoint), and standard `relativeLocation`. Reading the live widget INSTANCE it creates at runtime (as opposed to the class/config properties) turned out to be a dead end via `ObjectTools.get_properties` — a plain `"widget"` property read fails ("could not be read"), and guessing a sub-object path pattern (`<Actor>.<Component>.<WidgetClass>_C_0`, matching how `MaterialInstanceDynamic` sub-objects appear) also failed ("not valid Object"). **Fell back to the same multi-signal verification already validated for the NS_NoiseRing VFX case**: the pawn-local mirror variable it feeds updates correctly tick-by-tick (proves the write side of the chain runs), and a log scan for `Accessed None`/cast-failure patterns came back clean (proves the Cast→UpdateIcon call isn't silently dead-ending) — sufficient without needing to directly read the runtime widget instance.

---

## 2026-07-02 (DetectionAlpha live-verify session) — Closed the last open gap on the DetectionAlpha sync: full 0→1→0 lockstep confirmed live; the earlier "couldn't reliably trigger bCanSeePlayer=true" difficulty was a distance-picking mistake, not a new bug

**What was open:** the previous session's `TickBrain` → `PlayerState.DetectionAlpha` sync (commit `1368af9`) was compile-verified and structurally correct, but never observed live with a nonzero value — multiple teleport attempts at 74/200/400/500 units failed to trigger `bCanSeePlayer=true`.

**Resolution:** with a fresh PIE session, the sandbox Watcher was mid-patrol at `X≈330` moving toward `+X` (yaw 0). Teleporting the player to `X=400` (only ~70 units ahead) reproduced the known **close-range sight-loss gap** (see the two entries below dated 2026-07-01/07-02 on this same issue) — `bCanSeePlayer` stayed `false`. Re-teleporting to `X=930` (~600 units ahead, dead-on the Watcher's forward axis, comfortably inside the `1400` sight radius and outside the close-range failure zone) triggered detection cleanly within ~2s: `bCanSeePlayer=true`, `DetectionMeter=1.0`, `AlertState=2`, and **`PlayerState.DetectionAlpha` read exactly `1`, matching `DetectionMeter` 1:1**. Teleporting the player far away (`X=-800`, behind/out of cone) let it decay; after ~3s both `DetectionMeter` and `DetectionAlpha` had independently settled back to `0` in lockstep.

**Takeaway:** when a live-detection test doesn't trigger and the Watcher is *patrolling* (not stationary), always re-read its CURRENT transform right before picking a teleport distance — a distance chosen against its spawn position can silently become a close-range test if it has since patrolled toward that point. Prefer a healthy mid-range distance (400-800 units, dead-on the facing axis) over anything under ~150 units, which reliably hits the documented close-range sight-loss gap instead of testing normal detection.

---

## 2026-07-02 (flagged, NOT executed — needs a session that can restart+verify+revert in one flow) — Strong evidence the Lumen/MegaLights/VSM/reflections strip in `Config/DefaultEngine.ini` was based on the SAME debunked "frozen" measurement as the AnimClass=None saga, not a real Blackwell perf problem — but testing it safely requires an editor restart, which forfeits this session's own MCP connection mid-verification, so left as a documented plan rather than acted on blind

**The finding:** `Config/DefaultEngine.ini` disables `r.DynamicGlobalIlluminationMethod`/`r.ReflectionMethod`/`r.Shadow.Virtual.Enable`/`r.MegaLights.EnableForProject`/`r.VolumetricCloud`/`r.VolumetricFog` (both in `[/Script/Engine.RendererSettings]` and a `[SystemSettings]` override), with a comment dated 2026-07-01 justifying it: *"RTX 50-series (Blackwell) + UE5.8 renders the full game path at ~1-2 FPS with Lumen + MegaLights + Virtual Shadow Maps (the engine frame counter hit only 809 over 6.5 min standalone = ~2 fps)."* **This measurement technique is EXACTLY the one the same day's later `✅ RESOLVED` entry (`CLAUDE.md` "Current state") explicitly discredits**: a long-window frame-counter read is unreliable/wrap-prone, and the *actual* "frozen" cause that session chased for hours turned out to be nothing to do with rendering at all — it was `AnimClass=None` making Manny a motionless statue, with the game secretly running fine at ~120fps the whole time (confirmed via a correct <1s tight frame-counter delta). The "809 frames / 6.5 min standalone" figure was almost certainly ALSO the OS-focus-throttle artifact (`t.IdleWhenNotForeground`, fixed in the same config file's `[SystemSettings]` block right below the Lumen/MegaLights strip) rather than a genuine Lumen+MegaLights+VSM-on-Blackwell slowdown — the standalone process sat unattended for 6.5 minutes, almost certainly losing OS foreground and idling the GPU to ~3% util, exactly the mechanism later diagnosed and fixed by `t.IdleWhenNotForeground=0`.

**Why this matters now:** `01_Art_And_Style_Direction.md` §6 explicitly designs the whole "lighting IS the level design" Maple Court readability system around MegaLights ("dozens of shadow-casting local lights without the classic stealth-game light perf collapse"), and its own roadmap note (`08` Phase 4 actions) says to "stand up the MegaLights vocabulary" for exactly this phase — which is now. Also found: the Satara night HDRI (`01` §5) the same phase calls for is **already staged on disk** at `_ThirdPartyStaging/PolyHaven/satara_night_4k.exr` (not a pending download — `find` confirmed it's there) — importing it is a normal in-engine asset import, not an external-fetch action. So two of Phase 4's biggest visual-quality action items (MegaLights, Satara sky) are both more buildable right now than their surface description suggested, gated only by this stale-perf-concern question.

**Why NOT executed this turn:** these `r.*` settings are load/scalability-group values with no live-toggle tool in this MCP surface (checked `EditorAppToolset` — only `SearchCVars` exists, read-only; no `SetCVar`/`ExecuteConsoleCommand`). Actually testing requires editing the `.ini` and **restarting the editor process**, and per the `unreal-mcp-session` skill's #1 gotcha, "there is no in-session reconnect" — restarting the editor kills THIS session's MCP connection for the rest of the session, so I would not be able to measure the post-restart fps, decide pass/fail, or revert if it's bad, all within the same turn. Committing an unverified render-quality change and then walking away (with the user asleep and the next check-in ~15 minutes out via the recurring loop) is exactly the kind of irreversible-while-unsupervised risk this project's standing safety rules are for — a real regression could sit live for a full cron cycle, or worse, be the user's own first exposure if they launch the game before any session re-checks it.

**The plan for the next session that picks this up (ideally one starting fresh, so the whole restart→verify→revert cycle fits in one continuous flow):**
1. In `Config/DefaultEngine.ini`, remove/comment out the `[SystemSettings]` overrides for `r.DynamicGlobalIlluminationMethod`, `r.ReflectionMethod`, `r.Shadow.Virtual.Enable`, `r.MegaLights.EnableForProject` (and the matching `[/Script/Engine.RendererSettings]` copies) — **leave `r.RayTracing=False` and `t.IdleWhenNotForeground=0` untouched**, those are separate, still-valid, independently-reproduced fixes (HWRT SBT deadlock; OS-focus throttle), not part of this same debunked narrative.
2. Commit that config change alone, on its own, before restarting — so a bad result is a trivial single-file `git revert`.
3. Restart the editor (`unreal-mcp-session` skill §1 command), let it boot, confirm MCP reconnects.
4. Immediately measure fps with the CORRECTED methodology: two `get_properties`/log-read calls **less than 1 second apart** on the frame counter (or a frame-to-frame pixel diff), NOT a long unattended window — this is the exact mistake that produced the bogus "809 frames/6.5min" figure the first time.
5. If fps holds up reasonably (doesn't need to hit the lightweight profile's ~120fps, just needs to stay comfortably playable), keep the change, then proceed with `01` §5/§6's Satara HDRI import + MegaLights vocabulary (streetlamps/windows/sensor floods) for Maple Court. If it's genuinely bad even measured correctly, `git revert` the config commit and leave a note that the Blackwell concern is real, not a measurement artifact.

---

## 2026-07-02 (CLAUDE.md staleness audit) — Found a THIRD stale "known issue" this session (Manny materials, PoolID) beyond the SensorLight false alarm — CLAUDE.md's "Current state" narrative drifts from reality because fixes landed in later sessions never looped back to correct the original problem description

Two more cases, same shape as the SensorLight false alarm below: **(1) "Manny's material instances... still 0-byte from the original corruption"** — false; a project-wide `find Content -name "*.uasset" -size 0` returned zero results, and `SKM_Manny_Simple`'s two material slots resolve correctly to `MI_Manny_01_New`/`MI_Manny_02_New` (parented to the real `M_Mannequin`) — the `Mannequins/`+`Input/Touch/` tree restore from 2026-07-01 already fixed this, the note just never got updated afterward. **(2) "`PoolID` couldn't be set on the placed instance... low priority"** — also false; a LESSONS entry from 2026-07-01 documents the actual fix (`BP_PoolVolume.PoolID` needed `set_variable_instance_editable`) and verifies it on Maple Court's Pool B, but the ORIGINAL sandbox `Pool_A` instance was never revisited to apply it, so it sat at `PoolID="None"` for a full extra day of sessions while CLAUDE.md kept describing the class-level bug as still-open. Fixed this session (`PoolID` set to `"Pool_A"`, verified via readback).

**The common failure mode: a fix lands in LESSONS.md (the detailed log) and/or gets applied to ONE instance of a class, but the higher-level "Current state" summary in CLAUDE.md — which is what gets skimmed for planning, including by this very autonomous loop — never gets corrected to match.** LESSONS is append-only and newest-first, so the true fix is always IN there somewhere, but CLAUDE.md is a hand-maintained narrative that silently drifts. This project has been running many-hours-long autonomous sessions with a recurring identical prompt (`/loop`) — exactly the condition where nobody naturally revisits old "known issue" paragraphs once the immediate task that created them is done.

**Generalizes into a standing practice, not a one-time cleanup: when a `/loop` iteration runs out of obviously-new work, treat CLAUDE.md's "known issues" / "not yet done" / "low priority" flags as a checklist to re-verify against current live/on-disk state before assuming they're still true** — cheap (a `get_properties` readback or a `find -size 0` scan, not a rebuild) and has had a 100% hit rate so far this session (3 for 3 stale claims found and corrected). Don't trust a prior session's "still broken" note more than you'd trust your own untested assumption — the auto-memory system's own rule ("a memory that names a specific fact is a claim that it was true when written, not that it's true now") applies just as much to this project's own internal docs as to Claude's cross-session memory.

## 2026-07-02 (SensorLight false-alarm closure) — The "light visibility toggle never works" issue (open since the prior session, marked low-priority/unresolved in LESSONS) was NEVER a bug — it's the exact same "decay races round-trip latency" measurement trap as the heat-bump and VFX entries below, just not recognized as that family at the time

**Root cause, found by reading the actual graph instead of re-running the same style of test:** `BP_SensorLight.HandleSensorOverlap` calls `Rendering|SetVisibility(SensorSpotLight, true)` on trip, immediately followed by `Utilities|Time|SetTimerbyFunctionName(self, "TurnOffLight", LightOnDuration, ...)` — a **4-second** (`LightOnDuration` CDO default) delayed call to a function (`TurnOffLight`) that itself calls `Rendering|SetVisibility(SensorSpotLight, false)`. The prior investigation's "ruled out" list (stale/duplicate event nodes, collision-profile mismatch, stray `EventTick`/`EventBeginPlay` logic, `Mobility`) never considered a **timer-scheduled delayed function call** as a source of an auto-off — reasonable, since it isn't an event node at all, but it's functionally identical to one. Any test methodology that takes more than ~4 seconds of wall-clock between triggering the overlap and reading `bVisible` (very easy to do across several "exhaustive verification" MCP round-trips, each of which has real latency) would reliably observe `bVisible:false` and conclude the toggle "never works" — exactly what happened.

**Confirmed by testing with a MINIMAL round-trip** instead of a thorough one: read baseline `bVisible` (`false`) → one `set_actor_transform` teleporting the player into the `TriggerBox` → **immediately, next call, zero other calls in between** → `get_properties(SensorSpotLight, ["bVisible"])` → **`true`**. A few calls later (comfortably past 4s of real elapsed round-trip time), read again → back to `false` — the full on→off cycle, exactly as designed. **The light, the trigger, the timer, and the visibility toggle are all completely correct and were probably correct the entire time** — this was purely an artifact of how thoroughly (i.e., slowly) the previous session tested it. **Generalizes, and sharpens the standing "decay races round-trip latency" lesson (see the NS_NoiseRing/heat-bump entries below): before spending a long investigation on "X is set but doesn't stick", grep the surrounding graph for ANY delayed-execution node (`SetTimer*`, `Delay`, a dispatcher scheduled from `Tick`) that targets the same property — a thorough, multi-step live test is not automatically more trustworthy than a fast one; here, the SLOW test was the one that was wrong.**

## 2026-07-02 (vision-cone decal wiring) — Getting an `AIController`'s possessed pawn is `Pawn|GetControlledPawn`, not `Class|Controller|GetPawn` / `Variables|Default|GetPawn` (both genuinely don't exist); and where you splice new per-tick logic into an existing function matters when it reads a value that function itself just computed

**The node:** needed the AIController's own pawn from inside `BP_WatcherController.TickBrain` (to cast it to `BP_WatcherCharacter` and push live `AlertState`/`DetectionMeter` into its new vision-cone decal material). Two intuitive guesses both failed outright with `"...does not exist"`: `Class|Controller|GetPawn` and `Variables|Default|GetPawn`. **The real type_id is `Pawn|GetControlledPawn`** — found via `find_node_types(type_id_filter: "ControlledPawn")` after the obvious guesses failed. It's a **pure** node (no exec pins) with a `self` input pin typed `Controller Object Reference` and one output `ReturnValue` typed `Pawn Object Reference` — wire it straight into a cast, no exec plumbing needed. (A stray, unrelated error message earlier in the session had referenced `CallFunc_K2_GetPawn_ReturnValue`, which looked like a promising lead/real function name — it wasn't; don't chase engine-internal `K2_*` names surfaced in incidental error text, they don't necessarily map to a callable `find_node_types` entry.)

**The splice-placement decision:** `TickBrain`'s very first existing node is `SetDetectionMeter` (this tick's freshly-computed fill/decay value being written to the member var). The new "update the decal" logic needed both `AlertState` and `DetectionMeter` as CURRENT-tick values. Splicing the new `GetControlledPawn → Cast → UpdateVisionConeParams(GetAlertState, GetDetectionMeter)` chain in **before** `SetDetectionMeter` (i.e., at the very front of the function, the "obvious"/laziest insertion point right after `FunctionEntry`) would have read `DetectionMeter` **one tick stale**, for the exact reason already documented in the Step 3 "pure node re-evaluation" gotcha (`unreal-mcp-blueprints` skill): a pure `Get` node re-evaluates fresh at each consumption point in exec order, so reading it BEFORE the tick's `Set` gets the OLD value, not the one this tick just computed. **Fix: spliced right AFTER `SetDetectionMeter` instead** (`break_pins` the `SetDetectionMeter.then → IfThenElse.execute` link, insert the new chain between them, reconnect the tail) — guarantees `GetDetectionMeter` downstream reads the just-written value. `AlertState` may still be transition-frame-stale by one tick if it's set later in the same function (not chased further — imperceptible for a cosmetic decal, self-corrects within one frame). **Generalizes: before picking WHERE to splice new logic into an existing per-tick function, check whether the values you're about to read are computed earlier in that SAME function — if so, splice after that computation, not reflexively at the front.** Verified correct live in PIE (not just by compiling clean): teleported the player into the Watcher's cone and watched `AlertState` 0→2 and `DetectionMeter` 0→1 progress through the exact same exec path the splice now sits in, proving both that the values are live-fresh and that the new Cast doesn't silently dead-end the function (a failed `Cast` would have left `CastFailed` unconnected and permanently blocked all downstream state-transition/movement logic every tick — worth an explicit regression check any time a `Cast` is inserted into a mandatory, must-always-continue exec chain).

## 2026-07-02 (DetectionAlpha sync session) — Two findings while wiring `BP_WatcherController.TickBrain` → `PlayerState.DetectionAlpha`: a "No then pin found" warning cascade is pre-existing/benign, and the sandbox Watcher's `AIController` instance intermittently disappears from `find_actors` after a few minutes in the SAME PIE session

### `LogBlueprint: Warning: No then pin found on node ...TickBrain.K2Node_*` — checked, confirmed harmless and pre-existing, NOT something this edit introduced
After rewriting `TickBrain` (a full, faithful reconstruction via `write_graph_dsl` to add the new sync call — same technique used for the earlier `AcceptanceRadius` fix), starting PIE logged **294 "No then pin found" warnings**, all pointing at `TickBrain`'s pure nodes (`VariableGet`, `PromotableOperator` i.e. `+`/`-`/`*`/`/`). Alarming at first glance — enough volume to suspect real corruption. **Cross-checked by timestamp**: `EditorToolset.LogsToolset.GetLogEntries` showed the exact same warning pattern ALSO logged at `03:33:18`, hours earlier, during the session that made the (separately, independently verified-working-live) `AcceptanceRadius` fix. Since that version of `TickBrain` was later confirmed correct in live PIE, this warning is a **pre-existing, benign artifact of how `write_graph_dsl`-authored functions compile** (almost certainly the compiler's generic node-audit pass touching every pure node while looking for exec continuations, which pure nodes never have by design) — not a sign of anything wrong with a specific edit. **Generalizes: before treating a compile-time warning spike as a regression, check whether the SAME warning already existed in an earlier, independently-verified-working compile of the same file** (a log timestamp search across the whole session, not just after your latest edit, is enough to settle it).

### The sandbox `BP_WatcherController_C_0` instance vanishes from `find_actors`/`get_properties` after roughly 1-2 minutes of the SAME PIE session, while its pawn (`BP_WatcherCharacter`) persists and (initially) keeps patrolling
Reproduced twice, across two separate fresh `StartPIE` sessions: immediately after `StartPIE`, `find_actors(actor_type=Controller)` correctly lists `BP_WatcherController_C_0` alongside the Maple Court variant and the player controller; after roughly 1-2 minutes of continued MCP tool calls (teleports, property reads), it silently disappears from every enumeration (`find_actors` by exact class, by base `AIController`/`Controller` class, and by partial name — all three empty), while the **pawn it was controlling remains present and had visibly patrolled a short distance before freezing at the exact same X-coordinate (~226.7) in both repro runs** — a suspiciously specific, reproducible stopping point. **Not caused by this session's edits**: reproduced on a freshly-restarted PIE both before and after the `TickBrain` change, and the pawn's freeze-point was identical across restarts, suggesting a pre-existing, deterministic failure (possibly hitting the same NavMesh/pathing snag every time, or a controller-level exception on a specific tick that this project's Blueprint-based "custom event → HasAuthority function" substitution swallows silently instead of logging). **Left as an open, unresolved finding — needs a human-in-editor session with `stat ai`/breakpoints, or the AI perception close-range design gap fix, whichever comes first.** Practical consequence for testing: if a Watcher's controller property reads suddenly start failing with "not valid Object" mid-PIE-session, don't assume your own last action broke it — restart PIE and retry near the start of the session before investigating further, and budget for the controller becoming unreachable again after a couple of minutes.

## 2026-07-02 (cross-lane handoff session) — Verifying a fire-and-forget, fast-auto-destroying VFX spawn (`Niagara|SpawnSystemAtLocation`) live in PIE: property-polling can't catch it, use log-absence-of-errors + confirmed exec-chain-reachability instead

Wired `BP_LoudnessComponent.Server_ReportAction` to spawn `NS_NoiseRing` (a `bAutoDestroy=true`, `0.6s`-lifetime Niagara system) whenever an instant loudness bump occurs. `find_actors` for `NiagaraActor` after triggering it (via walking into `BP_HedgeSqueeze`) found nothing — **not a bug**: by the time an MCP round-trip completes, the effect has almost always already auto-destroyed (real wall-clock cost of even 2-3 chained tool calls routinely exceeds 0.6s, the same "decay races round-trip latency" class of trap as the heat-bump measurement-error entry above, just for a VFX lifetime instead of a decay rate). **Cross-checked correctness a different way**: (1) `EditorToolset.LogsToolset.GetLogEntries` showed zero `Accessed None`/`Blueprint Runtime Error`/`SpawnSystemAtLocation` warnings anywhere in the session log after triggering — a null/invalid `SystemTemplate` reference would have logged a warning; silence here is a real (if indirect) positive signal; (2) confirmed the exec chain actually REACHED the spawn call by checking a side effect earlier in the same function that IS observable via property read (`SqueezeSpeedMultiplier` updating proves `EnterSqueeze` → `Server_ReportAction` executed with a >0 bump, which is the exact branch guarding the spawn). **Generalizes:** for any transient, fast-expiring, or otherwise hard-to-directly-observe effect (VFX, one-shot SFX, a `Delay`-gated callback), don't try to catch the effect itself — verify (a) no error/warning logged for the specific failure mode that would occur if the reference/args were wrong, and (b) a nearby, durable side effect proves the surrounding exec chain actually ran.

## 2026-07-02 (styling session, verification pass) — CONFIRMED BUG, SILENTLY WRONG FOR AN ENTIRE SESSION: `LightComponent.lightColor` is a 0–1 clamped `LinearColor`, NOT a 0–255 `FColor` — every light color set this session as `{r:255,g:158,b:64,a:255}`-style values silently clamped to pure white

**The mistake:** every `PointLight`/`RectLight` color this entire session was set using the same 0–255 byte-range pattern proven correct for OTHER color-bearing fields elsewhere in this project (e.g. `WorldOutliner`-style FColor fields on some components). Every one of those `set_properties` calls returned `{"returnValue": true}` — no error, no warning — so each was trusted without a readback. **They were all wrong.** `ObjectTools.list_properties` on a real `LightComponent` shows `lightColor`'s actual schema: `{"type":"object","title":"LinearColor","properties":{"r":{"minimum":0,"maximum":1}, ...}}` — a **HDR-capable LinearColor, hard-clamped 0.0–1.0 per channel**. Sending `{r:255, g:158, ...}` doesn't error; every channel independently clamps to its max (1.0), silently producing pure white `(1,1,1,1)` regardless of the intended tint. **Caught only by accident**, during an unrelated verification pass, when a `get_properties` readback on `PointLight_HouseD_Window_MC` showed `lightColor: {r:1,g:1,b:1,a:1}` instead of the warm tone that had been sent.

**Blast radius: 8 lights, the entire session's worth of "warm lamp glow" / "cool CRT blue" mood lighting** — the home-base room's `RectLight_HomeBase_CRT` (TVGlow) + 2 `PointLight`s (LampWarm, WindowGlow), Maple Court Pool D's streetlight (LampWarm), and all 4 house window lights (WindowGlow) were ALL rendering as neutral white this entire time, not the intended night-mood palette tones — a real, meaningful styling regression that had gone completely undetected across many "verified via screenshot" passes, because a scene lit by white lights isn't visually alarming the way a totally-dark or garishly-wrong-hued scene would be — it just quietly loses the warm/cool contrast that was the entire point.

**Fix:** re-set every affected light's `lightColor` using the SAME 0–1 `LinearColor` values already read from `DA_ArtPalette` earlier in the session (`LampWarm={1,0.62,0.25,1}`, `WindowGlow={1,0.78,0.42,1}`, `TVGlow={0.3,0.55,1,1}`) — i.e., the exact values the material system (`M_GreyboxToon`, `M_CRT`) had been using correctly the whole time, since material `VectorParameter`s use the same 0–1 convention and I'd always used it there. **Verified fixed via `get_properties` readback (no longer `1,1,1,1`) AND a fresh `CaptureViewport`** — the CRT's key light visibly changed from a neutral glow to a distinct blue pool of light on the home-base room floor.

**Generalizes, and is now a standing check:** `ObjectTools.list_properties` (or a `get_node_type_pins`-style min/max check) on the SPECIFIC component/property BEFORE assuming a color convention — this project has at least two different color conventions in play (0–255 `FColor` on some fields, 0–1 `LinearColor` HDR on others, confirmed for `LightComponent.lightColor` specifically) and neither the property name nor a successful-looking `set_properties` call distinguishes them. **A `set_properties` call returning `true` is NOT proof the value landed as intended — silent clamping is a real failure mode, not just silent rejection.** For anything visually load-bearing (colors, especially), always `get_properties` readback immediately after set, the same discipline already established for text/enum values. A useful tell in hindsight: FColor's channel range in a schema dump reads `0–255`/no explicit range (byte), LinearColor explicitly shows `"minimum":0,"maximum":1` in its JSON schema — that's the fast way to tell them apart before writing, not after.

---

## 2026-07-02 (styling session, NS_NoiseRing) — Niagara IS MCP-buildable (resolves the "(unconfirmed)" flag), but ONLY by basing off a real template — a from-scratch/near-empty template hits a wall the API can't self-heal

`NiagaraToolsets.NiagaraToolset_System` (`CreateNiagaraSystem`/`AddEmitter`/`AddModule`/`AddRenderer`/`SetStackInputData`/etc.) is real and functional — confirms Niagara off the CANON/skill "(unconfirmed)" list, same family as the UMG/SkeletalMesh/StaticMesh discoveries earlier this session. **But there's a real gotcha in HOW you start:**

- `CreateNiagaraSystem`/`AddEmitter` both **require** a `templateSystem`/`templateEmitter` (no "create truly blank" option) — same "duplicate a working thing" pattern that's paid off all session for levels/Blueprints.
- **Picking a too-minimal template backfires.** Started from `/Niagara/DefaultAssets/Templates/Systems/MinimalLightweight` (sounded like the safest, simplest base) — `GetEmitterTopology` showed **zero modules in all 4 script stacks AND zero renderers**, seemingly a truly blank slate. But `AddModule` calls targeting `EmitterUpdateScript`/`ParticleSpawnScript`/`ParticleUpdateScript` all failed with `"Script '<X>' not found in stack reference"` — the underlying script/stage objects don't exist on this emitter at all (unlike a normal emitter where all 4 stages always structurally exist, just empty), and there's no tool in this toolset to CREATE a missing stage from nothing. `AddRenderer` also failed separately (`"emitter data is invalid"`). This template is a dead end for AI-driven building via this API surface, whatever it's useful for in the human editor.
- **Fix: pick a template that's a real, complete effect and prune it, instead of a "blank" one and building up.** Switched to `/Niagara/DefaultAssets/Templates/Systems/RadialBurst` (3 emitters: two ribbon-trail, one `OmnidirectionalBurst` sprite burst) — `GetEmitterTopology` on `OmnidirectionalBurst` showed a FULL, already-wired stack (`SpawnBurst_Instantaneous`, `InitializeParticle`, `ShapeLocation`, `AddVelocity`, `GravityForce`, `Drag`, `ScaleSpriteSize`, `ScaleColor`, `SolveForcesAndVelocity`, plus a real `NiagaraSpriteRendererProperties` renderer) — exactly the module set the design doc's spec needed (spawn once, size-over-life, color-over-life), zero `AddModule` calls needed. `RemoveEmitter(emitterToRemove: {...})` (note the param name — NOT `emitterRef` like every read-only lookup call) dropped the two unwanted ribbon emitters, leaving one clean, already-functional burst emitter to retune. **Generalizes: when adapting a stock/engine template via this toolset, prefer "start rich, delete what you don't need" over "start minimal, add what you do need" — deletion (`RemoveEmitter`/`RemoveModule`/`RemoveRenderer`) is confirmed reliable; the reverse (building up a stage-less emitter) hit a real, currently-unworkaroundable API gap.**
- **Script-stage names for `NiagaraExt_StackItemReference.scriptName` are the `ENiagaraScriptUsage` enum values** (`EmitterSpawnScript`/`EmitterUpdateScript`/`ParticleSpawnScript`/`ParticleUpdateScript`), not friendly short names (`"EmitterUpdate"` etc. — tried first, rejected with a message that happens to list valid examples, saving a lookup).
- **`SetStackInputData`'s `inputData.struct` must exactly match the input's OWN struct type as shown by a prior `GetModuleInputValues` read** (e.g. `/Script/Niagara.NiagaraFloat` for a plain float input, `/Script/CoreUObject.LinearColor` for a color) — read before you write, same discipline as everywhere else in this MCP surface, and target one input at a time via `inputNameStack: ["Exact Display Name"]` rather than trying to resend a whole module's input list.

**Built with this pattern**: `Content/_Project/UI/FX/NS_NoiseRing` (from `RadialBurst`, pruned to the one `OmnidirectionalBurst` emitter, retuned `Lifetime Min`/`Max` to `0.6` and `Color` to the `ReadWarn` amber palette token). `GetSystemCompileState` confirmed `bHasErrors:false` before saving; 331 KB on disk (not a stub). **Not yet done, deliberately deferred**: wiring an actual spawn call (`Niagara|SpawnSystemAtLocation` from `BP_LoudnessComponent`'s noise-report path) — `BP_LoudnessComponent` has shown up modified by the concurrent movement/gameplay session multiple times this session (Pool D chunk, home-base chunk), so it's currently a "hot" file best left to that lane rather than risking a collision; the FX asset itself is complete and ready for whoever wires the trigger. Also not retuned to spec: `ShapeLocation` (doc wants a literal ring shape — inherited default is whatever `RadialBurst` used, unverified), `AddVelocity`'s magnitude (doc wants "0→HearingRadius", currently whatever the template's burst-explosion tuning defaults to), and the loudness-driven `lerp(ReadWarn,ReadAlert)` color (currently a static amber, not dynamic) — all straightforward follow-ups once someone verifies the look live in PIE.

---

## 2026-07-02 (styling session, HUD) — Step 7 HUD built via UMGToolSet end-to-end; several new widget-specific DSL/node gotchas, and BeginPlay DOES fire (a prior "maybe it doesn't" concern is resolved for PIE)

Built `WBP_HUD` (loudness bar, at-risk/banked score, neighborhood alert `?`/`!`, air bar, hide pip) fully via `UMGToolSet` + `BlueprintTools`/DSL, per `07_Movement_And_UI.md` §3 with names corrected against `CANON.md`'s drift table (`TeamScoreBanked`/`IndividualScoreAtRisk`/`AlertLevel`/`CurrentLoudness`, not the doc's stale short names). New widget-authoring gotchas, all found via trial-and-error against real tool errors (never guessed silently):

1. **A widget blueprint's OWN member-variable accessor prefix is `Variables|<BlueprintName>|Get<Var>`, not `Variables|Default|Get<Var>`** — the pattern used successfully all session for every OTHER Blueprint class. `Variables|Default|GetLoudnessBar` errors "does not exist"; `Variables|WBP_HUD|GetLoudnessBar` works. Likely because widget "variables" here are actually named child-widget bindings (`ToggleWidgetAsVariable`), not plain `add_variable` members — the DSL's accessor-name resolution keys off the owning Blueprint's name specifically for this category. Check `find_node_types(graph, "Variables|")` on the ACTUAL widget graph before assuming the `Default` prefix carries over from regular-Blueprint work.
2. **UMG's plain "Text" widget (`/Script/UMG.TextBlock`) SetText/SetColorAndOpacity show up under DISAMBIGUATED type_ids you have to pick correctly among near-duplicates**: `Widget|SetText(Text)` (bare "Text" widget = TextBlock) vs `Widget|SetText` (bare, no suffix — actually `RichTextBlock`, confirmed by its self-pin reading "Rich Text Block Object Reference" when checked via `get_node_type_pins`) vs `Widget|SetText(TextBox)`/`(EditableText)`/etc. **Always verify via `get_node_type_pins` — read the `self` pin's type name — before trusting a type_id name alone**; "no suffix" is not "the plain/default one," it's whichever widget class happened to register first. Same family: `Class|Text|SetColorandOpacity` (not `Class|TextBlock|...` — the friendly class name in this node-DB is "Text", matching the palette label, not the C++ class name).
3. **Building "LABEL: value" HUD text is much easier as two separate TextBlocks (static label + dynamic value) than one concatenated string.** No `Concat_StrStr`/plain-text-`+` node was found registered under obvious names; rather than sink time into discovering Unreal's actual Text-concatenation node, split every "prefix + live number" HUD element into a static label TextBlock (set once, never touched again) plus a value-only TextBlock updated via `Utilities|Text|ToText(Integer)` straight into `SetText`. Simpler graph, zero string-building risk, and arguably better UMG practice anyway (independent Bind-ability, matches the source doc's own "many small `WBP_` widgets" philosophy).
4. **`ProgressBar.SetPercent`'s param is `Percent`, not the property name `InPercent`/`percent`** you'd expect from the property JSON schema — property names (used with `ObjectTools.set_properties`) and FUNCTION parameter names (used with `write_graph_dsl`/`create_node` keyword args) are DIFFERENT namespaces that don't always match 1:1. Same for `Widget.SetVisibility`'s enum param, which is `InVisibility` (property is `visibility`). Verify function param names via `get_node_type_pins`, don't reuse the property-schema name blind.
5. **`Class|X|SetY` type_ids returned by `find_node_types` can report a misleadingly bare `type_id` field back from `get_node_type_pins`/`get_node_infos`** (e.g. `Class|ProgressBar|SetFillColorandOpacity` node's own `.type_id` reads back as just `|SetFillColorAndOpacity`, prefix stripped) — this is cosmetic/how the tool reports self-class setters, not a sign your `create_node`/DSL call used the wrong type_id. Don't "fix" a call that already succeeded because the echoed type_id looks different from what you passed.
6. **A Blueprint can have only ONE `Event BeginPlay` node per graph** (it's a single-dispatch virtual-function override, NOT a repeatable delegate-bound event like `EventTick` felt like it might be) — `create_node("AddEvent|EventBeginPlay", ...)` on a graph that already has one **returns/relocates the EXISTING node** (confirmed via `get_node_infos`: same existing "then" connections, just moved to the new requested position) rather than erroring OR creating a second one. Always `find_nodes`+`get_node_infos` to check for an existing singleton-event node before assuming `create_node` made you a fresh one — verify by checking whether its output pins already have connections you didn't just make.
7. **To add new logic to an existing `EventBeginPlay` chain without touching/understanding its full existing content: `break_pins` the ONE link right after the event node, splice your new chain in between, then reconnect your chain's tail to whatever the event used to connect to directly.** This is the safe "insert at the very front" pattern — you don't need to trace the rest of the chain at all, and you provably can't break anything downstream since its first input is unchanged, just now sourced from your tail instead of the event directly.
8. **Widget UMG `(event EventTick ...)` DSL sugar fails** with `AddEvent|EventTick does not exist` — for `UserWidget` graphs specifically, Tick is namespaced `AddEvent|UserInterface|EventTick` (unlike a regular Actor/Component graph where bare `EventTick`/`EventBeginPlay` DSL sugar works per the blueprints skill's existing guidance). The DSL's `(event Name ...)` shorthand doesn't take a category-qualified name — fall back to raw `create_node`/`connect_pins` for any event whose `AddEvent|...` type_id has a category segment.
9. **Verified BeginPlay DOES fire reliably in PIE** — added a `PrintString` immediately after a (possibly-stale-bytecode-suspected, see the `BP_PlayerController` cold-load entries below) existing `EventBeginPlay`, and `LogsToolset.GetLogEntries` showed it firing with zero `Accessed None`/runtime-error entries across the whole PIE session afterward (confirms the new HUD-creation chain, and by extension the chain it was spliced in front of, both execute). **This is PIE evidence only, not cold-disk proof** — the project's own established rule (only a fresh process tests on-disk truth) still applies; a cold standalone relaunch is the next honest check if the "cold load" symptom resurfaces.

---

## 2026-07-02 (costume integration session) — CRITICAL: a stub function created by another session as a Custom Event (not a plain function graph) silently resists `add_object_function_param`/`write_graph_dsl` edits forever, even though every call reports success and compiles clean

### The finding
`BP_CostumeComponent.Server_EquipCostume` had been stubbed by an earlier (different) session with a signature that LOOKED like a normal function (`read_graph_dsl` printed `(fn Server_EquipCostume () (|ExecuteUbergraphBPCostumeComponent 0))`). Tried repeatedly to add a `NewCostume` parameter (`add_object_function_param` — reported success each time, returning a valid-looking pin) and write a real body (`write_graph_dsl` — reported success, no errors). Every single time, a subsequent `compile_blueprint` (success, no errors) silently reverted the function back to zero parameters and the original one-line stub body — confirmed via `get_node_infos` on the actual `K2Node_FunctionEntry_0` node (ground truth), not the possibly-lossy `read_graph_dsl` printer. **Root cause: `Server_EquipCostume` was never a real function graph at all — it was a `K2Node_CustomEvent` living inside the shared `EventGraph`.** The tell: `BlueprintTools.list_graphs` (lists genuine top-level function graphs) did NOT include it, even though `read_graph_dsl`/`get_node_infos` could reference it by the same name. Unreal compiles every Custom Event as a thin dispatch stub into one combined `ExecuteUbergraph_<ClassName>` function — which is EXACTLY the body every failed edit kept reverting to, because compiling regenerates that stub fresh from the *real* Custom Event node's actual (unparameterized) signature in `EventGraph`, discarding whatever the function-editing tools had just written to a synthetic/transient view.
**Diagnosis recipe if this happens again:** if `add_function_param`/`write_graph_dsl` on a "function" report success but a subsequent `compile_blueprint` + `get_node_infos` on the ACTUAL entry node shows your change reverted, immediately check `BlueprintTools.list_graphs(blueprint)` — if the function name is absent from that list, it's a Custom Event, not a function, and none of the function-editing tools will ever stick.
**Fix:** find the real node (`AddEvent|Custom|<Name>` type, discoverable via `get_node_infos` once you know roughly where it lives) and `delete_node` it, then `add_function_graph` to create a genuine replacement with the same name. **Gotcha while doing this:** immediately after `delete_node`, `add_function_graph` with the exact same name still auto-suffixed to `_0` (name still reserved) — one `compile_blueprint` in between let the deletion fully register, after which the exact original name was free again. Once rebuilt as a real function, `add_object_function_param` + `write_graph_dsl` behaved normally and the parameter survived every subsequent compile.

### Related, smaller finding: `Class|X|Y` self-context pin ORDER for a cross-object call is `(self, ...dataArgs)`, not `(...dataArgs, self)` — despite property Setters being the other way around
Confirmed again this session: setter-style nodes for Blueprint VARIABLES (`Class|BPPlayerState|SetAir`, etc.) take positional args as `(value, self)` — matching `Class|CharacterMovementComponent|SetMaxWalkSpeed(value, self)` from earlier sessions. But a plain cross-object FUNCTION call (`Class|BPCostumeComponent|ServerEquipCostume`) takes `(self, dataArg)` — self FIRST. There is no way to guess which convention applies from the node's name alone; always call `get_node_type_pins` before writing a multi-arg cross-object call, exactly as the project's existing "always check pins for CallFunction nodes" rule already says — this is a second, independent reason that rule exists (not just the "extra self pin swallows position 0" issue documented earlier), so don't assume all self-inclusive nodes share one fixed argument order.

## 2026-07-02 (styling session, home-base room) — CONFIRMED BUG: `ActorTools.set_actor_transform` with a PARTIAL `xform` resets the UNSET fields to identity, not "leave unchanged" as documented

The tool schema explicitly says of `xform`'s fields: "Unset fields mean 'identity' when creating objects and 'don't change' when modifying existing ones." Called `set_actor_transform(actor, {xform: {scale: {16,16,16}}})` (location/rotation omitted) to rescale 5 already-placed furniture actors — a `get_actor_transform` readback immediately after showed **`location` reset to `(0,0,0)` and `rotation` reset to `(0,0,-0)`**, not preserved. This silently teleported 5 furniture actors from the home-base room (built at world offset X≈-6000) to the world origin — which is inside/near the SANDBOX area the concurrent movement/gameplay session actively tests in. Caught immediately via a bounds check that showed nonsensical coordinates (`min.x=-128, max.x=0` instead of the expected `~-6128..-6000`), not by the visual result — **always sanity-check `get_actor_bounds`/`get_actor_transform` after ANY transform-mutating call whose xform is partial**, don't trust the "don't change" doc claim. **Fix: never pass a partial `xform` to `set_actor_transform` — always specify `location`+`rotation`+`scale` together, even when you only intend to change one of them** (read the current transform first with `get_actor_transform` if you need to preserve the other two exactly). Recovered cleanly here only because the original placement transforms were still in the conversation's own recent tool-call history to copy back from — if they hadn't been, this would have required re-deriving positions from the design doc's coordinate table.

## 2026-07-02 (styling session, home-base room) — Kenney `kenney_furniture-kit` FBX imports land at ~1/16th the correct real-world scale via `StaticMeshTools.import_file`

Imported 6 furniture meshes (sofa, coffee table, TV cabinet, CRT/computer screen, floor lamp, rug) at `scale:{1,1,1}` and placed them at doc-plausible room coordinates — `get_actor_bounds` afterward showed a "sofa" only **~9.8×8.2×4.6 units**, a "floor lamp" only **~8.6 units tall**, etc. — roughly 15-20× too small across every item (a real ~2m sofa should read ~200 units; the imported one read ~10). This is a units mismatch baked into the Kenney kit's FBX export (their source scale doesn't match UE's "1 unit = 1cm" assumption at a 1:1 import ratio), not a one-off — **every mesh from this kit exhibited the same order-of-magnitude undersizing**, though the exact per-mesh factor varied (16-20× for most items, ~10× effective for the rug once its own additional placement scale was factored in), so treat "roughly 15-20×, verify per mesh" as the starting assumption rather than a single universal constant. **Fix: after importing ANY Kenney-kit mesh (this kit specifically, likely others), immediately `get_actor_bounds` a test placement at `scale:1` and compare against the item's expected real-world size BEFORE dressing a whole scene at the wrong scale** — cheap to catch early (one bounds check), expensive to catch late (had to re-derive and reapply correct scale + full transforms — see the adjacent `set_actor_transform` partial-xform bug — across every placed instance). Static meshes expose no "reimport at corrected scale" shortcut via `StaticMeshTools`; the fix is a per-instance `set_actor_transform` scale correction after the fact, same as done here (settled on a uniform `16` for most props, `10` for the rug after its own prior scale multiplier).

## 2026-07-02 (styling session, home-base room) — A scene built at a NEW world-space offset can render pitch-black even with correctly-configured lights, if it falls inside an existing `bUnbound=true` PostProcessVolume with Manual exposure

Built the home-base room (per `01_Art_And_Style_Direction.md` §7, "warm tungsten... exposure up vs the night's exposure down") with correctly-colored/intensity'd lights (a `RectLight` CRT key + warm `PointLight`s), but a `CaptureViewport` screenshot came back **completely black** — no visible geometry, only the debug label callouts. Before assuming a lighting or material bug, checked the level's existing `PostProcessVolume` (`find_actors` filtered by `actor_type: /Script/Engine.PostProcessVolume`): it has **`bUnbound: true`** (applies everywhere in the level, with no bounding box) and **`autoExposureMethod: "AEM_Manual"` + `autoExposureBias: -1`** — a fixed, deliberately dark exposure tuned for the night exterior (Step 7's art layer). Manual exposure mode doesn't locally adapt to a bright interior; it applies the SAME fixed EV everywhere in the level, so a fully-lit interior room built inside that volume's reach still renders at the exterior's dark exposure. **Fix: added a second, BOUNDED (`bUnbound:false`) `PostProcessVolume` sized to just the room's interior, higher `priority` than the global one, overriding just `AutoExposureBias` (+3, i.e. ~4 stops brighter than the exterior's -1) + `BloomIntensity`/`VignetteIntensity`/`SceneColorTint` to taste** — this is the standard UE pattern for "one exposure mood outside, another inside," not a workaround. **Second gotcha, easy to misdiagnose as "the fix didn't work": PostProcessVolume blending is keyed to the CAMERA's position, not what's on screen** — a verification screenshot taken from a camera positioned OUTSIDE the new bounded volume (e.g., standing in the doorway looking in, for a wider establishing shot) still samples the OLD unbound volume's settings even though the lit room fills the frame. Always place the verification camera **inside** the new volume's bounds (matching where a player actually would be) before concluding an exposure fix didn't take effect.

---

## 2026-07-02 (styling session, Pool D + verticality) — Maple Court's new Watcher didn't move in PIE, but the control comparison shows it's a pre-existing, editor-process-wide condition, NOT a bug in the new content

Placed a second `NavMeshBoundsVolume` (`/Script/NavigationSystem.NavMeshBoundsVolume`, confirmed via `ObjectTools.search_subclasses(base_class="/Script/Engine.Volume", class_name="NavMeshBounds")`) spanning `x:[27500,34500] y:[-3000,3000] z:[-300,600]` to cover Maple Court — the level's only pre-existing `NavMeshBoundsVolume` covers just `x:[-800,1600] y:[300,2700]` (the original sandbox), so the AI literally had no navigable surface to path onto out there. After placing it, `save_assets([])`, `StartPIE` (4s warmup), the new `BP_WatcherCharacter_MC` instance still hadn't moved from its exact spawn transform. **Before concluding "NavMesh needs a manual rebuild" or reaching for anything invasive, ran the control: checked the ORIGINAL sandbox `BP_WatcherCharacter` instance (untouched by this session, previously verified moving/patrolling correctly) in the same live PIE run — it was ALSO frozen exactly at its `HomeLocation`, zero displacement.** Since the sandbox watcher's behavior is identical (stationary) despite this session never touching it, the cause can't be anything about the new volume/tag/waypoints — it's an **editor-process-wide condition**, most likely the exact one already flagged at the tip of `git log` by the concurrent session: `c3213aa fix(nav): add missing SupportedAgents entry; untested pending editor restart`. `Config/DefaultEngine.ini` does have the `SupportedAgents`/`SupportedAgentsMask` entries — but per that commit message and CLAUDE.md's own Step-4 history ("the `SupportedAgents` config fix from the previous session took effect after an editor restart between sessions"), **NavMesh/AI-agent config changes in this project have a documented history of requiring a full editor restart to actually take effect on an already-running process**, not just a re-save or a new PIE session.

**Did NOT restart the editor to test this** — a restart would drop this session's MCP connection (no in-session reconnect, per the session-orientation skill) AND kill whatever the other concurrent Claude session is doing in the same live editor (a PIE run from that session was observed active earlier this exact session). Restarting a shared, actively-used editor process unilaterally to satisfy one diagnostic check is exactly the kind of cross-session-disruptive action the established coordination protocol exists to prevent.

**Generalizes:** when a newly-placed/newly-configured thing "doesn't work" in live PIE, always check whether a known-working CONTROL exhibits the same symptom in the same live process before debugging the new thing — it can immediately distinguish "I broke something" from "this whole process is in a stale/pending state that predates my change," which calls for a completely different (and here, deliberately deferred) response. **Follow-up for whoever next restarts this editor** (for any reason — don't restart solely for this): re-verify both Watchers patrol correctly afterward; the Maple Court NavMeshBoundsVolume + `PatrolPoint_MC`-tagged waypoints + `BP_WatcherCharacter_MC` are all believed correctly configured (patrol-point gather already verified correct pre-restart: exactly 6 Maple Court points, zero sandbox contamination) and should just start working once NavMesh generation is actually live.

## 2026-07-02 (styling session, Pool D + verticality) — tool-signature gotchas: `add_to_scene_from_asset` takes a plain string `asset_path`, not an `{refPath}` object; generated-class names in actor refPaths are NOT the pool letter

**Two small but time-costing param-shape mistakes, both worth documenting so future calls get it right first try:**
1. `SceneTools.add_to_scene_from_asset` and `add_to_scene_from_class` take `asset_path: string` (a bare content-path string like `/Engine/BasicShapes/Cube.Cube`), **not** `asset: {refPath: "..."}` — unlike almost every other tool in this MCP server, which uses the `{refPath}` object form for object/class references. `find_actors` similarly has 3 *required* params (`name`, `tag`, `collision_channels`) even though you only want to filter by one — pass `tag:""` and `collision_channels:[]` explicitly rather than omitting them (omitting throws a schema error naming the NEXT missing required param one at a time, costing a round-trip each). Always read the schema error's embedded JSON schema rather than guessing a fix from memory.
2. **A placed actor's generated-class segment in its `refPath` reflects the *Blueprint's* generated class name, not anything about which instance it is.** Every `BP_PoolVolume` instance (Pool A, B, C, D) shows up as `BP_PoolVolume_C_UAID_<guid>_<n>` in its refPath — the trailing `_C` is Unreal's standard "this is the Blueprint-generated class" suffix (`BP_Foo` → class `BP_Foo_C`), applied identically to every instance regardless of which pool it is. Don't pattern-match "_C" in a refPath as "this must be Pool C" — always confirm the actual actor via `find_actors(name=..., tag:"", collision_channels:[])` and read back a distinguishing property (`poolId`) rather than inferring identity from the class-name substring.

## 2026-07-02 (styling session, Pool D + verticality) — Actor "Folder" (outliner organization) is NOT a reflected UProperty — use `SceneTools.set_actor_folder`, not `ObjectTools.set_properties`

Tried `ObjectTools.set_properties(actor, '{"folderPath": "MapleCourt/PoolD"}')` to file new actors under an outliner folder (matching the pattern apparently used for Pools A–C) — failed with `"the following properties could not be set: folderPath"`, and `list_properties` on the actor confirms no `folder`/`folderPath` key exists in the reflected property set at all. **Root cause: the editor "Folder" shown in the World Outliner is `FActorFolders` editor-only state, not a UPROPERTY** — it isn't reachable through the generic get/set-properties reflection path that works for every normal property. **The actual tool is `SceneTools.set_actor_folder`** (referenced in the 2026-07-01 "no Level creation tool" LESSONS entry as already in use for Maple Court's earlier pools) — use that dedicated tool for outliner organization, never `ObjectTools.set_properties`. Purely cosmetic/organizational (doesn't affect gameplay or save correctness), so it's safe to defer if the dedicated tool call isn't available, but don't waste a retry loop on the generic property setter for it.

## 2026-07-02 (styling session, Pool D + verticality) — Watcher patrol points are gathered by ACTOR TAG at BeginPlay, not a manually-wired array property — placing new waypoints for a new area is just "place + tag `PatrolPoint`", no Blueprint wiring needed

Per CANON.md's Step-4 substitution note, `BP_WatcherController.TickBrain`'s Unaware-state movement "cycles an ordered `TargetPoint` array **gathered by tag `\"PatrolPoint\"` at `BeginPlay`**" — i.e. the controller does a `GetAllActorsWithTag` (or equivalent) itself; there is no per-instance "patrol route" array property to populate via `ObjectTools.set_properties`. **Consequence for building a new area's patrol route (e.g. Maple Court's 6 `SB_WP*` waypoints per `02_Neighborhood_MapleCourt.md` §4):** place `TargetPoint` actors (`/Script/Engine.TargetPoint`) at the documented locations, `ActorTools.add_tag(actor, "PatrolPoint")` on each, then place a `BP_WatcherCharacter` instance anywhere in the same level — no graph edits, no array wiring, nothing Blueprint-side to touch. **UPDATE, same session, before shipping this for Maple Court:** actually doing this naively would have been a real regression — `GetAllActorsWithTag("PatrolPoint")` is a single hardcoded literal shared by EVERY `BP_WatcherController` instance in the level (confirmed via `get_node_infos` on the actual node — the `Tag` pin is a bare literal `"PatrolPoint"`, not wired to any variable), so a second Watcher placed for a second region would pick up BOTH regions' waypoints in one un-scoped array, likely causing the original (already-verified-working) sandbox Watcher to path toward unreachable Maple-Court points 30,000 units away. **Fix shipped instead: duplicated `BP_WatcherController`→`BP_WatcherController_MC` and `BP_WatcherCharacter`→`BP_WatcherCharacter_MC` (`AssetTools.duplicate`), `set_pin_value`'d the duplicate controller's `GetAllActorsWithTag` `Tag` pin from `"PatrolPoint"` to `"PatrolPoint_MC"`, and pointed the duplicate character's `AIControllerClass` CDO property at the duplicate controller.** Zero edits to the original two classes — full isolation by construction, not by convention. Verified live in PIE: the new controller's `PatrolPoints` array contains exactly the 6 new Maple-Court-tagged points (matching refPaths one-to-one), and the original controller's `PatrolPoints` still contains exactly its original 4 sandbox points — no cross-contamination either direction. **Generalizes:** before tag-gathering ANY per-instance "route/config" data via a hardcoded literal in a shared Blueprint class, check whether the gather is actually scoped per-instance or global-per-tag — if global, a second instance of that class anywhere in the same level WILL merge data pools unless you duplicate-and-repoint (cheap, safe, zero risk to the original) or properly parameterize the tag through a variable (more work, more risk if the graph is unfamiliar — the duplicate approach is the pragmatic default when in doubt and short on time).

---

## 2026-07-02 (movement/hiding/swimming session) — `ObjectTools.set_properties` cannot set ANY property (not just `RepNotify` bools) on an actor instance that has already run `BeginPlay` in a live PIE session — confirmed with a plain, non-replicated local bool

### The finding
The earlier LESSONS entry this session ("`set_properties` refuses to set RepNotify bool properties on a LIVE PIE actor") undersold the scope of the restriction. Tried to directly poke `BP_PlayerCharacter.bWantsToSprint` — a brand-new, purely local, **non-replicated** bool with no `RepNotify`, added specifically to test the new speed-resolution pipeline without needing a real keypress — and got the identical `the following properties could not be set: bWantsToSprint` failure. **This rules out "it's a replication-safety guard" as the mechanism** (there is nothing to replicate here) and points instead to a blanket restriction on writing ANY property, of any replication mode, on an actor instance once it has begun play in a running PIE session — plain reads (`get_properties`) work fine throughout.
**Practical consequence, generalized:** don't reach for `set_properties` to drive a test scenario on a LIVE PIE actor for ANY variable, replicated or not — budget for a real input path instead (a teleport into a trigger volume, an `unreal-input-probe` keypress, or a CDO-level default change before `StartPIE`). When a variable genuinely can't be exercised this way in a given session (e.g. it's gated behind a physical key that only real `SendInput` can trigger in a floating PIE window), it's fine to fall back to "graph-verified but not live-input-tested" and say so explicitly — this project has a running precedent for that gap (Sprint/Vault from earlier steps; now also Dive's toggle and Interact's hide-trigger).

### A safe pattern for extending an EXISTING Enhanced Input action without touching its original event node
Needed `BP_PlayerCharacter` to also react to `IA_Sprint` presses (to track a new `bWantsToSprint` intent flag for the speed-resolution formula) without risking the original Sprint-handling `EnhancedInputActionIA_Sprint` node, whose exact internal exec-continuation structure couldn't be safely re-derived (`read_graph_dsl` shows Enhanced Input event bodies as empty — a known limitation — and hunting for the right `K2Node_EnhancedInputAction_N` index by trial probing wasted several calls before this approach was chosen instead). **Unreal allows multiple independent `K2Node_EnhancedInputAction` listener nodes bound to the SAME `InputAction` asset in the same graph — both fire on the same key press, fully independently.** Created a second, brand-new `Input|EnhancedActionEvents|IA_Sprint` node via `create_node` (never touching the original), wired only its `Started`/`Completed` pins to a new dedicated function. This is a good general pattern whenever you need to react to an input action a second time and the existing handler is too risky/opaque to safely re-edit.

## 2026-07-02 (styling session, Pool D + verticality) — tool-signature gotchas: `add_to_scene_from_asset` takes a plain string `asset_path`, not an `{refPath}` object; generated-class names in actor refPaths are NOT the pool letter

**Two small but time-costing param-shape mistakes, both worth documenting so future calls get it right first try:**
1. `SceneTools.add_to_scene_from_asset` and `add_to_scene_from_class` take `asset_path: string` (a bare content-path string like `/Engine/BasicShapes/Cube.Cube`), **not** `asset: {refPath: "..."}` — unlike almost every other tool in this MCP server, which uses the `{refPath}` object form for object/class references. `find_actors` similarly has 3 *required* params (`name`, `tag`, `collision_channels`) even though you only want to filter by one — pass `tag:""` and `collision_channels:[]` explicitly rather than omitting them (omitting throws a schema error naming the NEXT missing required param one at a time, costing a round-trip each). Always read the schema error's embedded JSON schema rather than guessing a fix from memory.
2. **A placed actor's generated-class segment in its `refPath` reflects the *Blueprint's* generated class name, not anything about which instance it is.** Every `BP_PoolVolume` instance (Pool A, B, C, D) shows up as `BP_PoolVolume_C_UAID_<guid>_<n>` in its refPath — the trailing `_C` is Unreal's standard "this is the Blueprint-generated class" suffix (`BP_Foo` → class `BP_Foo_C`), applied identically to every instance regardless of which pool it is. Don't pattern-match "_C" in a refPath as "this must be Pool C" — always confirm the actual actor via `find_actors(name=..., tag:"", collision_channels:[])` and read back a distinguishing property (`poolId`) rather than inferring identity from the class-name substring.

## 2026-07-02 (styling session, Pool D + verticality) — Actor "Folder" (outliner organization) is NOT a reflected UProperty — use `SceneTools.set_actor_folder`, not `ObjectTools.set_properties`

Tried `ObjectTools.set_properties(actor, '{"folderPath": "MapleCourt/PoolD"}')` to file new actors under an outliner folder (matching the pattern apparently used for Pools A–C) — failed with `"the following properties could not be set: folderPath"`, and `list_properties` on the actor confirms no `folder`/`folderPath` key exists in the reflected property set at all. **Root cause: the editor "Folder" shown in the World Outliner is `FActorFolders` editor-only state, not a UPROPERTY** — it isn't reachable through the generic get/set-properties reflection path that works for every normal property. **The actual tool is `SceneTools.set_actor_folder`** (referenced in the 2026-07-01 "no Level creation tool" LESSONS entry as already in use for Maple Court's earlier pools) — use that dedicated tool for outliner organization, never `ObjectTools.set_properties`. Purely cosmetic/organizational (doesn't affect gameplay or save correctness), so it's safe to defer if the dedicated tool call isn't available, but don't waste a retry loop on the generic property setter for it.

## 2026-07-02 (styling session, Pool D + verticality) — Watcher patrol points are gathered by ACTOR TAG at BeginPlay, not a manually-wired array property — placing new waypoints for a new area is just "place + tag `PatrolPoint`", no Blueprint wiring needed

Per CANON.md's Step-4 substitution note, `BP_WatcherController.TickBrain`'s Unaware-state movement "cycles an ordered `TargetPoint` array **gathered by tag `\"PatrolPoint\"` at `BeginPlay`**" — i.e. the controller does a `GetAllActorsWithTag` (or equivalent) itself; there is no per-instance "patrol route" array property to populate via `ObjectTools.set_properties`. **Consequence for building a new area's patrol route (e.g. Maple Court's 6 `SB_WP*` waypoints per `02_Neighborhood_MapleCourt.md` §4):** place `TargetPoint` actors (`/Script/Engine.TargetPoint`) at the documented locations, `ActorTools.add_tag(actor, "PatrolPoint")` on each, then place a `BP_WatcherCharacter` instance anywhere in the same level — no graph edits, no array wiring, nothing Blueprint-side to touch. Worth double-checking post-placement that the Watcher actually cycles the NEW area's points specifically (not a mix of sandbox + Maple Court points, since tag-gathering is presumably level/world-wide, not scoped to "nearest" or "same region") — if the sandbox's original 4 sandbox waypoints are still tagged `PatrolPoint` too, a Maple-Court-placed Watcher may wander toward the sandbox origin. Flag this to whoever does the first live-PIE verification of the Maple Court patrol.

---

## 2026-07-01 (styling session) — ✅ RESOLVED: the "PoolID can't be set on a placed instance" limitation (and its siblings) — the variable just wasn't marked Instance Editable

**Root cause found, retroactively resolving a Step-3 limitation documented in CANON/CLAUDE.md as low-priority-but-unfixed.** Placing a second `BP_PoolVolume` instance (Maple Court's Pool B) and setting `BaseScorePerSecond` via `ObjectTools.set_properties` silently failed exactly like `PoolID` always had (the call errors `"the following properties could not be set"`, but a `get_properties` immediately after shows the OLD/default value, not the one just "set"). **Fix: `BlueprintTools.set_variable_instance_editable(blueprint, variable_name, true)` then `compile_blueprint` then `save_assets`** — after that, `ObjectTools.set_properties` on a PLACED INSTANCE of that variable works correctly (verified: `BaseScorePerSecond` went from reading `10` — the untouched class default — to a confirmed `13`; `PoolID` from unsettable to a confirmed custom value). **Root cause: a Blueprint member variable defaults to NOT instance-editable unless explicitly toggled** (the checkbox is off by default in the editor's Variables panel too — this isn't MCP-specific, it's normal UE Blueprint behavior that the earlier session just didn't know to check). `add_variable` doesn't set it either, so any variable added purely via `add_variable` (i.e. everything built by MCP so far) needs this call before per-instance overrides will ever stick.

**Generalizes — audit every other "instance-level tuning" var across the project for the same silent-failure pattern**, not just `BP_PoolVolume`. Prime suspects: anything meant to differ per-placed-instance (`AlertDirector` thresholds if ever instanced twice, per-Watcher `AIP_WatcherProfile` overrides, any future per-pool/per-actor tuning field). The class CDO / defaults always read+write fine regardless of this flag — it's specifically placed-instance overrides that need it. Cheap to apply proactively: `set_variable_instance_editable(bp, var, true)` for any var you expect a level designer (human or agent) to tweak per-placement, right after `add_variable`, before the first `compile_blueprint`.

## 2026-07-01 (styling session) — Cannot create or duplicate `.umap` Level assets via MCP; no "new level" tool exists at all
## 2026-07-02 (movement/hiding/swimming session) — `BP_BushHide` overlap testing: an hour lost to a wrong Z coordinate that LOOKED exactly like a collision bug — always re-`trace_world` the ground height at a NEW X/Y before reusing an old Z value

### The finding
Teleporting the player into a freshly-built `BP_BushHide.HideVolume` (a `QueryOnly`/`OverlapAllDynamic` BoxComponent — the same profile already proven to work fine for `BP_PoolVolume`/`BP_StashZone`) produced a bizarre, highly reproducible symptom: `bIsInBushVolume` stayed `false`, and `get_actor_transform` showed the capsule had been physically shoved sideways by a suspiciously exact amount every time (`box half-extent + capsule radius + ~1.125 skin`, e.g. `75+34+1.125=110.125`) — the shove direction always matched whichever axis had the SHALLOWEST penetration (a textbook minimum-translation-vector depenetration). This reproduced at four different X/Y target locations, survived even setting the box's `collisionEnabled` to `NoCollision` on the LIVE instance (a red herring — reflection-based property writes on a running physics body don't call `RecreatePhysicsState()`, so the old collision silently stayed active), and looked exactly like "this box is secretly blocking, not overlapping." **The actual cause: every single test used a stale/copied Z coordinate from a DIFFERENT location's ground-height trace** (reusing `PlayerStart`'s Z, or an earlier spot's already-traced Z, at a NEW X/Y without re-tracing) — the capsule was landing with its top a couple units UNDER the actual ground surface at the new spot, and it was **the ground**, not the bush, driving the depenetration; the bush's box (whose Z-span happened to closely match the same embed region) just made the resulting push look bush-shaped. The instant a fresh `trace_world` was used to compute the correct ground Z for the exact new X/Y (`ground_Z + capsule_half_height`), the exact same box, at the exact same size, produced a perfectly clean, zero-displacement overlap with `bIsInBushVolume` flipping `true`/`false` correctly on enter/exit.
**Generalizes:** when a teleport-based MCP test shows a suspiciously EXACT, reproducible displacement (not a random jitter), suspect the test's OWN coordinates before the Blueprint's collision setup — specifically, never reuse a Z value traced (or eyeballed) at one X/Y for a teleport to a different X/Y; call `SceneTools.trace_world` fresh for every new horizontal position. A "the box must be blocking" hypothesis is falsifiable in one call (`get_actor_transform` right after the teleport — if X/Y also drifted, not just Z, the real cause is a horizontal, not vertical, embed) and should be checked before spending time on collision-profile theories.

## 2026-07-01 (styling session) — Cannot create or duplicate `.umap` Level assets via MCP; no "new level" tool exists at all

Tried to start `L_MapleCourt` (per `02_Neighborhood_MapleCourt.md` §7 step 1) via `AssetTools.duplicate("/Game/ThirdPerson/Lvl_ThirdPerson", "/Game/_Project/Maps/L_MapleCourt")` — returned `false` (silent failure, no error text). Tried duplicating the project's own already-working `L_Sandbox_Movement` instead — also `false`. Tried `SceneTools.load_level("/Game/ThirdPerson/Lvl_ThirdPerson")` — errored `"level does not exist"`, even though `AssetTools.find_assets` finds that exact path and the `.umap` is confirmed on disk. **`AssetTools.exists` also incorrectly reports `false` for known-good, on-disk Level assets** (both the source and a real, currently-loaded level) — so `exists`/`load_level`/`duplicate` all seem to mis-handle the Level/World asset class specifically, while `find_assets` handles it fine. Checked every toolset with a plausible "create new level" tool (`SceneTools`, `EditorAppToolset`) — **none exists.** This is a new, confirmed MCP gap: **you cannot create a brand-new empty Level, and you cannot duplicate an existing one, via MCP at all.** Same family as the enum/struct/BT/UMG-formerly gaps — a whole asset class the generic tool factory doesn't cover.

**Workaround used:** build new "areas" as a distinct, clearly-offset region of world-space *within* an already-open, already-working level (`L_Sandbox_Movement`), rather than a separate map file. Picked a large offset (+30000 on X for Maple Court) so the new content's bounding box can't overlap the existing sandbox content, and used a dedicated outliner folder (`SceneTools.set_actor_folder`) to keep it organized and select-able as a unit. This sidesteps the tooling gap entirely and inherits the host level's already-tuned GameMode override, night lighting rig, and NavMesh for free — a genuine win, not just a workaround. **Downside:** doesn't match the design docs' literal "separate `L_MapleCourt` map asset" instruction — flag this to the human; splitting the offset region into its own map file later is a normal in-editor operation (select actors → right-click → "Move Selected Actors to New Level"), a 2-minute manual step once someone confirms that path works, or a good next MCP spike (a dedicated level-creation tool may exist under a toolset name that wasn't obviously named "level" or "world").

## 2026-07-01 (styling session) — PIE blocks ALL scene-placement calls outright (not just risky ones) — and it can be a concurrent session's test, not yours

`SceneTools.add_to_scene_from_asset` (and presumably the other `SceneTools`/`ActorTools` mutators) fail immediately with `"Cannot create actors while PIE is active"` — this is a hard block, not a warning, and it fires even for completely unrelated new actors far from anything PIE-relevant. Confirmed via `EditorAppToolset.IsPIERunning()` that PIE was active when this happened despite this session never having called `StartPIE` itself — **when multiple Claude sessions share one live editor (see the "concurrent session" thread this session), a PIE block you hit may be the OTHER session's test run, not yours.** Per the coordination handoff established this session: don't unilaterally `StopPIE()` a run you didn't start — wait it out (do other non-scene work in the meantime: docs, data-only asset creation, git) and retry `IsPIERunning()` periodically instead.

---

## 2026-07-01 (styling session) — `SkeletalMeshTools.import_file` EXISTS and works (another wrong "needs the human" assumption) — but rapid-fire imports with `import_materials=true` cross-contaminate material slots

**New capability, not previously documented anywhere:** MCP can import raw `.fbx` files as real `SkeletalMesh` assets — `editor_toolset.toolsets.skeletal_mesh.SkeletalMeshTools.import_file(folder_path, asset_name, source_file, skeleton=None, import_materials, import_textures, import_animations, create_physics_asset)`. Used it to bring 4 Mixamo character FBX files (downloaded via the browser, see below) into `Content/ThirdParty/Mixamo/Characters/` as `SK_BigVegas`/`SK_Kaya`/`SK_Josh`/`SK_Michelle`, each with its own auto-created `Skeleton` asset (`skeleton=None`). This closes another capability gap nobody had actually tested — same family as the UMG discovery earlier this session. Always check `describe_toolset` on the plausible toolset before assuming "MCP can't do X."

**Gotcha found the hard way — verify with `get_vertex_count`/`get_material_slots`, not just "the call returned success":** importing all 4 characters back-to-back with `import_materials=true, import_textures=true` produced SkeletalMesh assets that reported `get_vertex_count() == 0` and `get_section_count() == -1`, and whose `get_material_slots()` pointed at completely unrelated characters (Big Vegas's slots read `Elvis_Mouth_MAT1`/`Character_Fat_Elvis_body_color1`; Kaya's read `Character_Rasta_boy_body`) — a real cross-contamination between the 4 rapid, back-to-back import calls, not just cosmetic naming (ruled out below). **Root-caused by isolating the variable:** deleted the whole folder, re-imported ONE character at a time with an explicit `AssetTools.save_assets([])` between each import, and this time with `import_materials=false, import_textures=false`. Every mesh came back with a real, plausible non-zero vertex count (Big Vegas 4644, Kaya 5401, Josh 32814, Michelle 16278). **Conclusion: importing multiple FBX files in rapid succession without a save/settle between each risks the importer's internal material-slot-matching state bleeding across calls — always import character meshes one at a time with a save in between, at least until this is spiked further.**

**Red herring ruled out — don't be alarmed by "wrong-looking" material slot NAMES alone:** even with `import_materials=false` (so no Material asset is created at all), Big Vegas's slot label still reads `Elvis_Mouth_MAT1` etc. This proves the name comes from the FBX's own embedded internal slot-name strings, not from a created/mismatched asset reference — Mixamo's public catalog names ("Big Vegas") apparently don't match the character's internal authoring name in every case. Harmless. The real signal to check for actual corruption is `get_vertex_count`/`get_section_count`, not slot name vibes.

**Follow-up, not yet done:** real materials/textures for these 4 meshes (currently mesh+skeleton only, default/no material — consistent with this project's existing "grey Manny renders fine for grey-box" precedent). If re-attempting `import_materials=true`, do it one character at a time with a save between, and re-verify vertex counts after.

## 2026-07-01 (styling session) — Mixamo web app: screenshot/read_page tools permanently fail on it (continuous WebGL render defeats "document_idle"); drive it via `javascript_tool` DOM manipulation instead

The Mixamo character/animation browser (`mixamo.com`) never reaches whatever "document_idle" heuristic `computer`/`read_page`/`find` (claude-in-chrome) wait on — its live WebGL character-preview viewport renders continuously, so every screenshot/read_page/find call fails with `"Page still loading (executeScript waited 45000ms for document_idle)"`, indefinitely, even minutes after the page is fully interactive. **`document.readyState` is `"complete"` the whole time** — confirmed via `javascript_tool`, which is NOT gated by the same check and works fine throughout. **Workaround: drive the whole flow through `javascript_tool` DOM queries + synthetic events** instead of the normal screenshot-driven `find`/`computer` flow: locate elements by `textContent` match (`Array.from(document.querySelectorAll(...)).find(...)`), `.click()` them directly, use the native-setter + `dispatchEvent(new Event(...))` trick for React-controlled `<input>`/`<select>` elements (`Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype,'value').set.call(el, value)` then dispatch `input`/`change`). Mixamo's character grid: click "Characters" nav → `<select>` "96 Per page" (same native-setter trick) to get the full catalog on one page → find a tile by name → click → a "Proceed with this new character?" confirm modal appears (click "USE THIS CHARACTER") → click "Download" → a SEPARATE `.asset-download-modal` appears (may take a second click of "Download" if the first click raced the character-switch) → its Format/Pose fields are real `<select>` elements (already default to `FBX Binary(.fbx)` / `T-pose`) → click the modal's own "Download" button.
- **Chrome will silently block the 2nd+ automatic download** with a native "download multiple files?" permission bar that is OUTSIDE the page DOM — invisible to `javascript_tool`, and NOT clickable via computer-use either (browsers are granted read-only/"read" tier for computer-use by design; this is a real security boundary, not a bug — ask the human to click Allow).
- **The tool response for any `javascript_exec` call gets replaced with `"[BLOCKED: Cookie/query string data]"` or a masked `"{}"`** whenever the returned value (or, seemingly, page state at call time) contains something that looks like session/cookie data — hit this when trying to read `location.href`/`location.hash` or raw `fetch()` response bodies from the page. This is a deliberate safety filter (prevents exfiltrating session tokens through the JS-eval channel) — don't fight it, just avoid returning raw URLs/fetch bodies; query specific DOM text content instead, which passes through fine.
- **A completed download can land with a generic filename** (`character.fbx`) instead of the character's name, especially after a retry — don't assume the filename matches the last character you selected; verify via the in-page preview panel text (`document.querySelector('.product-preview-holder').innerText`) *before* clicking Download, and rename the downloaded file yourself afterward.

---

## 2026-07-02 (movement/hiding/swimming session) — Building `BP_BreathComponent`: five `write_graph_dsl` node-wiring gotchas, plus a real logic bug caught only by live PIE (`bIsInWater` vs `bIsSubmerged`)

### `(event EnhancedInputActionIA_X ...)` DSL syntax can only be used to relate to an EXISTING Enhanced Input event node — it cannot CREATE one
Trying to add brand-new `IA_Dive`/`IA_SwimUpDown` handlers via `write_graph_dsl`'s normal `(event EnhancedInputActionIA_Dive (...) ...)` form (which is how `read_graph_dsl` prints existing ones) fails: `AssertionError: AddEvent|EnhancedInputActionIA_Dive does not exist`. **Fix:** create the node the low-level way — `create_node` with `type_id: "Input|EnhancedActionEvents|IA_<ActionName>"` (find it via `find_node_types` filtered to `EnhancedActionEvents|IA_<Name>`), then `get_node_infos` on the new node to see its real exec pins (`Triggered`/`Started`/`Ongoing`/`Canceled`/`Completed`, plus a plain-typed `ActionValue` pin — Boolean for a Digital action, Float for Axis1D, no struct-unwrapping needed), then `connect_pins` by hand to a `CallFunction|YourHandler` node also made with `create_node`. This is the same "must delete+recreate with the correct `Input|EnhancedActionEvents|IA_X` type" rule from the original input-repair session, now confirmed to also apply to first-time creation, not just rebinding.

### `CallFunction|X` nodes for a custom Blueprint function sometimes expose an explicit "self" input pin that swallows your first positional arg
`(CallFunction|TickBreath DeltaSeconds)` (one arg, matching the function's one parameter) failed with `Could not connect pin DeltaSeconds to self` — `get_node_type_pins` showed the real pin order was `(execute, self, DeltaSeconds)`, i.e. TWO input pins beside exec, so the single positional arg bound to the wrong one. This contradicts the `Move`/`Aim` precedent from the ThirdPerson template (`(CallFunction|Move Axis_X Axis_Y)` — no self needed) — those nodes were built by the original `create_node`/`connect_pins` era of this project, not this DSL, so their "no self pin" shape isn't representative of what fresh `CallFunction` nodes for NEW functions look like. **Rule going forward: always call `get_node_type_pins` on a `CallFunction|X` node type before trusting positional args, and default to explicit keywords (`:self self :ParamName value`) for any freshly-added custom function** — cheap insurance against this exact silent-shape-mismatch class of error. Same fix applied to `Pawn|Input|AddMovementInput` (4 real input pins: self/WorldDirection/ScaleValue/bForce — the Move() function's 2-arg call was, again, pre-DSL legacy shape).

### Sequential top-level `switch` statements in one event/function body raise a false "unreachable code" error — `switch` has no single unified continuation pin the way `if` does
Adding a second, unrelated `switch` statement after an existing one in `EventOnMovementModeChanged` (checking `PrevMovementMode` right after already checking `NewMovementMode`) failed to write at all: `Unreachable code after branch/return`. A `switch` node's cases each dead-end on their own case pin — there's no single "next" pin for a following top-level statement to chain from, so the DSL compiler (correctly, if confusingly) refuses to treat anything after it as reachable. Adding an empty `(:Default)` case didn't help (this enum's switch has no Default pin — `SwitchOnEMovementMode`'s cases are exhaustive: `MOVE_None/Walking/NavWalking/Falling/Swimming/Flying/Custom`, no catch-all). **Fix: don't chain two switches — use `if (== EnumVar (Utilities|Enum|LiteralenumETypeName "EnumValue")) ... )` instead**, chaining as many `if`s in a row as needed (this form DOES support a normal single continuation, proven throughout this session in `TickBrain`/`ApplyMovementTuning`). Find the literal-maker node type via `find_node_types` filtered to the enum name (e.g. `EMovementMode` → `Utilities|Enum|LiteralenumEMovementMode`), and note the quoted-string-literal-as-enum-value trick from the DSL docs applies as the ARGUMENT to that literal node, not directly as an operand to `==`.

### Real bug caught only by live-PIE property inspection, not by clean compiles or code review: `TickBreath` drained Air based on `bIsInWater` instead of `bIsSubmerged`
Wrote `BP_BreathComponent.TickBreath` gating the drain/refill branch on `Class|BPPlayerState|GetIsinWater` — compiled clean, DSL round-tripped correctly, looked right on paper. Live PIE caught it immediately: teleporting the player into the water (which sets `bIsInWater=true` via `CharacterMovementComponent`'s own `MOVE_Swimming` switch, well before any Dive keypress) showed `Air` had already silently drained from 100 to ~16 and `bBreathCritical=true`, despite the player never having dived — `bIsSubmerged` (the actual "head under" state, toggled only by `IA_Dive`) was still `false` the whole time. **Fix:** re-gate `TickBreath`'s branch on `GetIsSubmerged` instead of `GetIsinWater` — `bIsInWater` is only meant to gate WHETHER a dive request is honored (`Server_SetSubmergeIntent` checks it), it has nothing to do with whether breath should drain. This is exactly the kind of "two similarly-named booleans, wrong one wired" bug the project's own debugging discipline warns about — the fix was a one-line variable swap, but only live PIE reads (not the clean compile, not eyeballing the DSL) surfaced it, reinforcing "verify via live PIE reads, not just compile" as more than boilerplate advice.

### `ObjectTools.set_properties` refuses to set `RepNotify` bool properties on a LIVE (already-`BeginPlay`'d) PIE actor instance
Tried to directly poke `bIsSubmerged`/`bBreathCritical`/`bDetained` (all `RepNotify` bools) on a running `BP_PlayerState_C_0` instance to drive a test scenario without real input — every attempt failed with `the following properties could not be set: <name>`, while the exact same properties read back fine via `get_properties`. Confirmed NOT specific to the new breath vars — `bDetained` (pre-existing, unrelated) fails identically. Root cause unconfirmed but consistent with the tool refusing to bypass replication semantics for `RepNotify`-flagged properties on a live networked actor (a plain non-replicated bool, e.g. `bIsInWater`, was never tested but is a reasonable next thing to check if this recurs). **Practical consequence: to drive a scripted MCP test scenario for any `RepNotify` bool, you cannot shortcut it with `set_properties` — you must go through the actual gameplay path** (a real keypress via `unreal-input-probe`, or a function call chain that legitimately sets it, e.g. teleporting into a trigger volume). Recompiling a Blueprint mid-PIE also silently re-instances actors and resets all instance vars to CDO defaults — a convenient reset mechanism if you need one, but a trap if you assume state persists across a compile.

## 2026-07-02 (movement/hiding/swimming session) — `ObjectTools.set_properties`: to create a NEW instanced sub-object inline (e.g. an `InputModifier` on an `InputMappingContext` mapping), pass the **class path** as the `refPath` value directly — do NOT wrap it as `{"instance": "<ClassPath>"}`

### The finding
Adding `IA_SwimUpDown`'s negative-axis key mapping (`Left Ctrl` → `-1`) to `IMC_Default` needed a brand-new `InputModifierNegate` instance in the mapping's `modifiers` array (existing modifiers on other mappings are pre-existing instanced sub-objects like `IMC_Default.IMC_Default:InputModifierNegate_0`, created once, long ago, presumably in-editor). The tool's own schema doc says "For instanced sub-object properties, pass a class path as the instance member" — read literally, this means wrap it as `{"instance": "/Script/EnhancedInput.InputModifierNegate"}`. **That produced a silent no-op**: `set_properties` returned `true`, but a follow-up `get_properties` showed the modifiers array element as the literal string `"None"` — no new object was created, no error was raised either.
**What actually works:** pass the class path as the plain `refPath` value, exactly like an existing-instance reference: `{"refPath": "/Script/EnhancedInput.InputModifierNegate"}`. The tool detects the path resolves to a *class* rather than an object instance and auto-instantiates a brand-new sub-object of that class (owned by the target asset), returning it as a real new instance (confirmed: `IMC_Default.IMC_Default:InputModifierNegate_1` appeared after save, verified via `get_properties` immediately after and again via a **cold standalone process** reading `GetAll InputMappingContext DefaultKeyMappings` from disk).
**Generalizes:** whenever an MCP tool's schema description mentions a special JSON shape for "creating" something inline, don't trust the described key name at face value — verify with a `get_properties` (or equivalent) round-trip immediately after the call. A `true` return and no error do NOT mean the operation did what you intended; this is the same family of trap as the `set_pin_value`-doesn't-mark-dirty lesson below (success signals that don't mean success).

## 2026-07-02 (post-MCP-reconnect session) — CRITICAL, SILENT-DATA-LOSS RISK: `BlueprintTools.set_pin_value` does NOT mark the containing package dirty, so `save_assets` silently skips it — a pin edit can affect live runtime behavior for an entire session while never reaching disk

### The finding
Fixed a verified, high-value bug (`TickBrain`'s chase `MoveToActor` `AcceptanceRadius=120` → `55`, see the caught/detain entry below) using `set_pin_value` on 3 duplicate nodes, then `compile_blueprint` (succeeded), then `save_assets([])` (returned `true`). **The fix demonstrably took effect at runtime** — re-tested live in PIE multiple times and the Watcher's chase behavior genuinely changed (walked to ~69 units instead of stopping at 120+). Only when preparing the end-of-session commit did a `git status` check reveal **the `.uasset` file on disk was byte-identical to a commit from Step 4, hours earlier** — the fix had never been written to disk despite `save_assets` returning `true` and despite the change being real and observable in every subsequent PIE session (because the *editor process* never restarted, so its in-memory compiled Blueprint kept the fix even though the file didn't). Confirmed directly: `AssetTools.is_dirty("/Game/_Project/AI/Watcher/BP_WatcherController")` returned **`false`** immediately after the `set_pin_value` + `compile_blueprint` + `save_assets` sequence — Unreal's own dirty-tracking never saw this edit as a package modification, so `save_assets`' "save all dirty" (or even an explicitly-targeted path — tried both, neither worked) had nothing to write.
**The fix:** re-apply the same change via a full `write_graph_dsl` rewrite of the containing function instead of a surgical `set_pin_value`. Immediately after, `is_dirty` returned `true`, and `save_assets` + a `git status` check confirmed the file actually changed on disk this time. **`write_graph_dsl` reliably marks its target dirty; `set_pin_value` (at least for this pin/node type) does not.**
**Generalizes to a new, permanent verification habit: after ANY `set_pin_value` call (not just `write_graph_dsl`), check `AssetTools.is_dirty(path)` before trusting `save_assets`'s return value, and/or check `git status`/file mtime after saving.** A `save_assets() == true` return is NOT proof anything was written — it can mean "nothing needed saving" just as easily as "saved successfully," and there is no way to tell the two apart from the return value alone. This is a more insidious version of the project's existing "verify against cold on-disk state" rule: previous instances of that rule were about the EDITOR's dirty in-memory state vs. disk; this one is about the MCP TOOL's own reported success vs. disk, which is a new, narrower trap — the tool didn't lie about compiling or even about "saving," it just had nothing to save because the earlier edit never registered as a change worth saving.

## 2026-07-02 (post-MCP-reconnect session) — CRITICAL: `write_graph_dsl` on an event ALREADY PRESENT in a shared EventGraph accumulates orphaned nodes instead of cleanly replacing them; the live exec chain is still correct (rewired to the newest nodes) but the graph balloons with dead nodes on every re-edit

### The finding
Iteratively editing `BP_SensorLight`'s `EventActorBeginOverlap` handler in-place (4 successive `write_graph_dsl` calls, each adding one more diagnostic line) grew the shared `EventGraph` from ~20 nodes to 100+. Confirmed via `get_node_infos` on the `Event` node itself: `OtherActor`'s output pin had **5 separate consumers** (one `ActorHasTag` call per edit generation), and the event's single `then` exec pin was correctly repointed to only the NEWEST chain each time — so the graph was *functionally* still correct (only the latest logic actually runs), but stuffed with 80+ dead, disconnected nodes from every earlier edit. **This is unlike editing a plain FUNCTION graph** (`(fn Name ...)`), which cleanly full-replaces on every `write_graph_dsl` call — confirmed by contrast, e.g. `BP_AlertDirectorComponent:PollWatcherAndPush` and `:NotifyDetection` stayed at a small, stable node count across many re-edits. **The difference is specifically about editing an EVENT that already exists in a graph shared with other events** (`EventBeginPlay`/`EventTick` also live in `BP_SensorLight`'s `EventGraph`) — the tool seems to append a new node cluster and only rewire the single-owner exec/delegate pins, not diff-and-replace the event's whole body.
**Practical fix, and the pattern to use from now on for ANY event that might need iterative re-editing (diagnosis, tuning):** don't repeatedly edit the event body directly. On the FIRST pass, make the event a one-line dispatcher — `(event Category|EventName (Params...) (CallFunction|MyHandler :Params Params...))` — and put all real logic in a plain function (`MyHandler`). Every subsequent iteration edits the FUNCTION (which cleanly replaces every time), never the event again. This is exactly the fix applied to `BP_SensorLight` this session (`HandleSensorOverlap` extracted from the messy `EventActorBeginOverlap` body) — verified via `get_node_infos` that `HandleSensorOverlap` stayed a clean ~22 nodes even after adding/removing diagnostics twice more, while the EventGraph's dispatcher call itself (necessarily re-edited once to install the dispatcher pattern) still added its own round of clutter in the process — an unavoidable one-time cost, not a recurring one once the dispatcher pattern is in place. **Orphaned nodes are harmless at runtime** (disconnected = never executes) but make `read_graph_dsl` and any manual graph reading unreliable/misleading — always trust `get_node_infos` traced from the live entry point, never assume `find_nodes`' full list represents "what runs."

## 2026-07-02 (post-MCP-reconnect session) — Heat-bump "bug" fully RESOLVED: root cause was decay racing MY OWN test round-trip latency, not the runtime; SensorLight's functional side effects (heat bump, Watcher noise-alert) confirmed working via the same technique; light visual toggle remains an unresolved, isolated, low-priority cosmetic issue

### Heat-bump magnitude — RESOLVED, was pure measurement error
Following up the previous session's "UNRESOLVED" entry: after reconnecting MCP and re-verifying `PollWatcherAndPush`'s graph wiring (which had reverted to an unfixed state — see below), the bump STILL read wrong (~0-9 instead of ~33) even on structurally-correct, freshly-reverified code. The actual cause: **`HeatDecayPerSecond=3` fully erases even a +25 bump within ~8.3 seconds, and a chain of 2-4 sequential MCP tool calls (`set_actor_transform`, `get_properties`, `GetLogEntries`, `sleep`) routinely costs MORE than that in real wall-clock round-trip time** — so every test was checking well after the bump had already decayed to 0, regardless of whether any `sleep` was used. **Proof:** temporarily set `HeatDecayPerSecond=0.05` for testing (eliminates the race), reran the identical detection cycle, got `AccumulatedHeat≈32.55` — matching the expected `8+25` almost exactly. Reverted decay to 3 afterward. **This generalizes: any time a tuning value decays/expires on a timescale comparable to a multi-call MCP round-trip chain (single-digit seconds), do NOT trust a "check right after triggering" read — either temporarily slow the rate for the test (edit the value, verify, revert), or correlate against a diagnostic log's own absolute wall-clock timestamps instead of trusting intended `sleep` durations.** The same technique caught and resolved an apparent "SensorLight doesn't bump heat" false alarm later in this same session, for the exact same reason (`SuspicionHeatBump=8` decays even faster, in ~2.7s — well within typical round-trip cost).

### One session-boundary wrinkle found and refixed while re-verifying
After the MCP reconnect, `PollWatcherAndPush`'s graph had reverted to having `SetLastSeenWatcherAlertState` run **unconditionally right after the cast succeeded, before the branch condition read `lastState`** — i.e. the exact bug class this project calls out repeatedly (pure/condition reads must happen before any write to the same variable). This was almost certainly the last (unsaved) diagnostic edit from the pre-disconnect session never having been written to disk, leaving a different, older, broken version live. Re-applying the known-correct fix (`Set` nested inside each branch, strictly after the condition check) and verifying via `get_node_infos` (not the lossy `read_graph_dsl`) resolved it immediately — this is now confirmed correct and saved to disk.

### SensorLight refactored into a clean dispatcher + function (see the accumulation entry above) — functional effects confirmed working; light visibility toggle still unresolved
With the dispatcher pattern in place and using slowed-decay testing, confirmed: `BP_WatcherController.LastKnownLocation` updates correctly to the sensor's position, and `AccumulatedHeat` rises correctly on trip — **the full functional contract of `BP_SensorLight` (alerting the Watcher, contributing to neighborhood heat) works.** **Not resolved:** `SensorSpotLight.bVisible` was never observed to read `true` despite exhaustive verification that `Rendering|SetVisibility(light, true)` is correctly wired and unambiguously reached (proven because everything AFTER it in the same exec chain — the Watcher notification, the GameMode cast, the heat bump — all demonstrably execute). Ruled out: stale/duplicate event-node execution (traced the live chain end-to-end via `get_node_infos`), collision-profile mismatch (matched to the known-working `BP_StashZone`/`BP_PoolVolume` `OverlapAllDynamic` profile, no change), stray `EventTick`/`EventBeginPlay` logic resetting it (both are empty stubs), and light `Mobility` (`Movable`, correct). **Left as an open, low-priority, purely-cosmetic issue** — the dressing light doesn't visibly turn on, but the gameplay effect (alerting the AI, raising heat) fires correctly regardless. If revisited: try reading `bVisible` via a fresh, never-before-queried Blueprint graph (per the *other* major lesson this session about per-graph accessor staleness) in case `ObjectTools.get_properties` itself has a similar staleness quirk for this specific property/component-type pairing that hasn't been characterized yet.

### Caught → detain is currently UNREACHABLE by design: the Watcher loses sight of the player at close range, so `AlertState` drops before the chase ever closes to physical touching distance — a real AI-perception/design gap, confirmed via both teleport AND genuine chase movement (not a testing artifact)
`BP_WatcherCharacter.CollisionCylinder` confirmed `bGenerateOverlapEvents=true` — collision is fine. `HandleDetain`/`FinishDetain` re-verified structurally correct via `get_node_infos` (small, clean, unchanged) — **the detain LOGIC itself was never the problem.** Root-caused in two steps:

1. **`AcceptanceRadius=120.0` on the Alert-branch `MoveToActor` call (`TickBrain`) was larger than the actual physical catch range (~68, the sum of the Watcher's and player's capsule radii).** Watched the Watcher chase live: it walked from spawn to within 120 units of the player, then **stopped** (pathfinding considers itself "arrived"), leaving a permanent ~50-unit dead zone it could never close. **Fixed this session:** lowered the chase `MoveToActor` `AcceptanceRadius` in `TickBrain` from `120.0` to `55.0`. First attempt used `set_pin_value` on 3 duplicate accumulated nodes — this took effect at runtime but **never actually reached disk** (see the `set_pin_value`-doesn't-mark-dirty entry above); **the fix that's actually saved and confirmed on disk is a full `write_graph_dsl` rewrite of `TickBrain`** with `AcceptanceRadius=55.0` baked in from the start, re-verified via `is_dirty`/`git status` this time.
2. **After that fix, the Watcher chased all the way down to ~69 units — and `AlertState` STILL dropped to `0` before contact.** This ISN'T a teleport artifact (confirmed via a clean test: position the player once at proven detection range, ~300 units, then let the chase run completely untouched with no further teleports — same result). **Root cause: `TickBrain`'s hysteresis has no protection against sight LOSS at close range** — as the Watcher's own capsule closes in on the player, the sight trace (from the Watcher's eye location to the player) apparently starts failing at some point inside roughly 70-100 units (plausibly self-occlusion by one or both capsules, or a raycast-at-near-zero-distance edge case in `AIPerceptionComponent`'s sight sense), `bCanSeePlayer` flips false, and the existing `TickBrain` logic immediately treats that as "lost the target" and decays the meter / drops `AlertState`, with no grace period for "I'm mid-chase and about to touch them." **This is a genuine, unresolved gameplay-design gap, not a bug in anything built this session** — the catch mechanic (`HandleDetain`) is provably correct and ready, but the Watcher can currently never survive long enough at close range to trigger it. **Next session's fix options (needs human/design judgment, not just an MCP fix):** (a) add a "chasing" grace window to `TickBrain` that ignores momentary `bCanSeePlayer=false` for N seconds once already in `Alert` and within some `ChaseLockRange`, only truly losing the target if sight stays lost longer than that; (b) or drive the Alert→lost-target transition off distance-to-`LastKnownLocation` exceeding a threshold instead of raw `bCanSeePlayer`, which is more forgiving of near-field trace noise; (c) or check whether `AIPerceptionComponent`'s sight sense has a `MinAge`/near-clip config that's causing the near-range failure directly, if so tune that instead of touching the hysteresis logic at all — cheapest fix if it's really just a sight-sense config gap rather than a design gap.

---

## 2026-07-01 (new session) — MAJOR CAPABILITY UPGRADE: `UMGToolSet` now exists and genuinely authors UMG widget trees — the "HUD needs the human" ceiling is LIFTED

Every prior doc (`CANON.md` "Known MCP buildability gaps", the `unreal-mcp-session` skill §6) states UMG widget-tree authoring is not MCP-buildable. `mcp__unreal-mcp__list_toolsets` this session shows a live **`UMGToolSet.UMGToolSet`** toolset ("UMG widget toolset for AI-driven widget creation and tree manipulation") that was either added to the MCP plugin between sessions or simply never checked directly before (the MCP is Experimental — API drift is expected, in either direction). **Verified for real, not just schema-deep:** `CreateWidgetBlueprint` → `AddWidget` (TextBlock) → `ObjectTools.set_properties` (set `text`) → `CompileWidgetBlueprint` (returned `true`) → `save_assets([])` → confirmed **23,748-byte** `.uasset` on disk (not a 0-byte stub) via a direct filesystem check. Tools available: `CreateWidgetBlueprint`, `AddWidget`, `RemoveWidget`, `MoveWidget`, `WrapWidgets`, `ReplaceWidgetWith{Template,NamedSlot,Child}`, `SetNamedSlotContent`, `GetNamedSlots`, `RenameWidget`, `ToggleWidgetAsVariable`, `BindToEventProperty` (wires a Blueprint graph node to e.g. `UButton::OnClicked`), `AddUIComponent`/`RemoveUIComponent`/`MoveUIComponent`, `GetWidgets`/`GetWidgetDescription`/`GetWidgetTreeDepth` (tree introspection), `ListWidgetClasses`/`GetWidgetClassInfo`, `CompileWidgetBlueprint`. Same workflow discipline as everything else in this project: `ObjectTools.list_properties` on the returned widget/slot BEFORE `set_properties` (property names vary per widget class and are not guessable — confirmed, e.g. `TextBlock.text` is lowercase-first, not `Text`). **One quirk found:** immediately after `CreateWidgetBlueprint`, the new asset is visible to `AssetTools.find_assets` but NOT to `AssetTools.exists`/`can_edit_asset`/`get_referencers`/`save_assets` (with an explicit path) — all four say "Asset does not exist" until `save_assets([])` (empty array = save-all-dirty) runs at least once; use the empty-array form, not a targeted path, for a just-created widget blueprint. Also, `AssetTools.delete` on such an asset failed silently (returned `false`, twice) even after saving — fell back to deleting the `.uasset` file directly via shell for an uncommitted scratch asset; **do not do this for anything with committed history or real references, only same-session scratch files.** **Action for next session: re-attempt Step 7's `WBP_HUD`/`WBP_AlertIcon` build (previously deferred to the human) via this toolset before assuming it needs a human — update `08_Implementation_Roadmap.md` Step 7 and the `unreal-mcp-session` skill §6 once a real widget with bound gameplay state is verified live in PIE.**

---

## 2026-07-01 (autonomous session, continued) — Step 5 built (AlertDirector/SensorLight/caught-detain); **NavMesh pathfinding CONFIRMED WORKING** (the Step 4 "UNRESOLVED" issue is resolved); a genuine heat-bump magnitude bug left mid-diagnosis when unreal-mcp disconnected

### ✅ MAJOR STATUS CHANGE: the Watcher paths now. The Step 4 "MoveToLocation/MoveToActor always Failed" issue is RESOLVED.
Mid-session, a live PIE check found the Watcher **had moved from its spawn point** (100,1200,90 → 1306,844,90) and was actively patrolling/rotating between checks — proof `MoveToLocation` is succeeding. This was not something I did this session (no new NavMesh edits were made before this was observed) — the most likely explanation is the 5th-attempt fix from the prior session (`Config/DefaultEngine.ini`'s `NavigationSystem.SupportedAgents`, committed `c3213aa`, previously untested pending an editor restart) finally took effect via an editor restart that happened between sessions. **Do not re-investigate the NavMesh/SupportedAgents issue** — it's fixed. Caught/detain and chase behavior can now be tested against REAL movement, not just teleport-simulated proximity.

### `ObjectTools.set_properties` cannot write to ANY live PIE actor/component instance — reads work, writes silently fail
Attempted to directly poke `LoudnessComp.currentLoudness`, `LoudnessComp.bIsSprinting`, `LoudnessComp.SprintBand` (non-replicated!), `PlayerState.IndividualScoreAtRisk`, and `BP_AlertDirectorComponent.HeatDecayPerSecond` (also non-replicated) on **live, PIE-spawned instances** — every single one failed with `"the following properties could not be set"`, while `get_properties` on the exact same instances/properties always succeeds. This is **not** a replication-only restriction (confirmed by testing a plain non-replicated float) — it appears to be a blanket restriction on mutating any live PIE-world instance via this tool. **Reads are reliable; writes are not, for ANY live spawned instance.** This invalidates a testing shortcut I assumed from earlier sessions' phrasing ("verified via live PIE" in prior CANON/CLAUDE.md entries meant *reading* state after *real gameplay-driven* changes — e.g. teleporting a pawn into a trigger volume — never a direct property poke). **Going forward: to test runtime behavior, always drive a REAL gameplay trigger** (teleport into/out of overlap volumes, wait out timers, cause a real state transition) **and only ever READ afterward.** CDO/class-default `set_properties` (pre-PIE, editing the asset) is unaffected and works fine — this restriction is PIE-instance-specific.

### The cross-object `find_node_types` accessor index is frozen **per already-opened graph**, not per-Blueprint or globally-time-based
Refines the earlier "DB needs a save to refresh" lesson (which was about a fresh *target* asset) — this session hit a **different, stricter** version: a graph that was **already queried/edited earlier in the same MCP session** (e.g. `BP_WatcherController.TickBrain`, `BP_WatcherController.EventGraph`, both touched during Step 4) permanently could not see brand-new functions added to ANOTHER class (`BP_PlayerGameMode.Server_NotifyDetection`, `Server_NotifySensorTrip`, `HandleDetain`) later in the SAME session — not after one save, not after ten, not after 20+ seconds of real-time waiting, not after recompiling either side. **Proof:** a genuinely **brand-new** scratch Blueprint (`BP_ScratchIndexTest`, created fresh, never touched before) immediately saw every one of those same functions via `find_node_types` on its first-ever query. **Conclusion: each graph's node-type index is built once, on first touch, and never invalidated for the rest of the session — new capability added to OTHER classes after that point is permanently invisible to it.** **Workaround that generalizes:** if an old/already-touched graph needs to call something added later, don't fight it — restructure the call direction so the NEW code (a freshly-created function/component, whose own graph hasn't been touched yet) reaches OUT to the old, already-indexed target instead (old targets stay visible forever; it's only *new* targets-from-old-queriers that go stale). This is exactly why `BP_AlertDirectorComponent.PollWatcherAndPush` (brand new this session) polling `BP_WatcherController.GetAlertState` (old, pre-existing) works, while the original design (old `BP_WatcherController.TickBrain` pushing to new `BP_PlayerGameMode.Server_NotifyDetection`) silently could never resolve the call.

### ✅ RESOLVED (next session): the "heat-bump magnitude" issue was never a code bug — it was decay racing MY OWN diagnostic round-trip latency, not the runtime
The previous session's `get_node_infos` verification was right: the graph was 100% correctly wired the whole time. (One session-boundary wrinkle: after the editor restart, `PollWatcherAndPush` briefly reverted to an *unfixed* ordering — the `SetLastSeenWatcherAlertState` call ran unconditionally right after the cast succeeded, BEFORE the branch condition checked `lastState`, so the condition always read the just-written value. This was almost certainly the diagnostic-PrintString edit's write never having been saved before the disconnect. Re-applying the same fix — `Set` nested inside each branch, after the condition — and reverifying via `get_node_infos` immediately made it structurally correct again.) But even with the graph *provably* correct, live reads kept showing `AccumulatedHeat` around 0–9 instead of the expected ~33 (8+25). **The actual cause:** my original "decay math doesn't hold" reasoning was wrong — I'd sanity-checked it in the abstract (a 25-heat bump should stay visible >8s at `HeatDecayPerSecond=3`) but never accounted for how much real wall-clock time a chain of MCP tool round-trips actually costs. Added a `PrintString` diagnostic (`"DIAG cur=X last=Y"`, logged every tick) and cross-referenced its **absolute wall-clock timestamps** against when I actually read `AccumulatedHeat` — the gap between the Alert-transition tick and my read was routinely 15–25 real seconds (each `get_properties`/`GetLogEntries`/`set_actor_transform` call plus the intentional `sleep`s all add up), more than enough to fully erase even a 33-point bump at decay=3/sec. **Proof:** temporarily set `HeatDecayPerSecond=0.05` (on the SCS template — see next entry for why the class CDO alone wasn't enough), reran the identical test, and `AccumulatedHeat` read back **32.55** — matching the expected 8+25 almost exactly. Reverted decay to 3, removed the diagnostic, resaved. **Takeaway for future runtime-magnitude testing:** when a tuning value decays/expires within roughly the same order of magnitude as a multi-call MCP round-trip chain (seconds, not minutes), either (a) temporarily slow the relevant rate for the test and revert after, or (b) correlate against the log's own absolute timestamps rather than trusting `sleep` durations to reflect actual elapsed time.

### An SCS-instanced component's own template holds an independent copy of its defaults — editing the class CDO after the fact does NOT propagate to it
Setting `HeatDecayPerSecond` on `BP_AlertDirectorComponent`'s class CDO (`Default__BP_AlertDirectorComponent_C`) had no effect on the live PIE instance spawned on `BP_PlayerGameMode` — it kept reading the old value. The fix was to edit the **SCS template** instead, at `/Game/_Project/Core/BP_PlayerGameMode.BP_PlayerGameMode_C:AlertDirector_GEN_VARIABLE` (note the `_GEN_VARIABLE` suffix — dropping it gives an invalid-object error). This makes sense in retrospect: `ActorTools.add_component` copies the class CDO's values into a new, independent SCS template at attach-time; from then on the template is its own archetype and no longer tracks the class CDO. **Generalizes:** to change a tuning default for an SCS-attached component after it's already been added to an owner Blueprint, edit the owner's `<Component>_GEN_VARIABLE` object, not the component class's own CDO.

### unreal-mcp disconnected mid-session — no in-session reconnect (re-confirms the standing constraint)
The `mcp__unreal-mcp__*` tools vanished mid-turn (server disconnected) while mid-diagnosis of the above heat-bump issue. Per the project's established constraint (`unreal-mcp` attaches once at Claude Code session start, no in-session reconnect), this is unrecoverable within the current session regardless of whether the editor is still running. **Next session: verify port 8000 is listening, restart the editor+MCP if not** (command in `CLAUDE.md`), then resume from the `DIAG` log-reading step above.

---

## 2026-07-01 (autonomous session) — Step 4 AI Watcher: BT/BB confirmed blocked, built a Blueprint state-machine substitute; perception/detection fully verified live; movement/pathfinding hit an UNRESOLVED NavMesh issue — root-caused as far as possible, needs a human with eyes on the editor

### The substitution: no BT_Watcher/BB_Watcher, so the Watcher's brain is a plain Controller Tick function
`aimodule_toolset...BehaviorTreeTools` is confirmed **inspect-only** (get_blackboard/list_nodes/get_children/get_root_decorators — no create/add tool anywhere), and there's no dedicated Blackboard-creation toolset either — matches CANON's existing "BT/Blackboard authoring: inspect-only" gap, now hit directly instead of just cited. **Substitute:** `BP_WatcherController.TickBrain(DeltaSeconds)` — a single Blueprint function replicating `04_AI_Watcher.md §7.3`'s `BTS_UpdateDetection` pseudocode (fill/decay meter, hysteresis state transitions Unaware/Suspicious/Alert) PLUS the `§7` Selector's per-state movement dispatch (Alert→MoveToActor/MoveToLocation, Suspicious→MoveToLocation, Unaware→patrol-point cycling), called every tick. This is a real, if less debuggable, substitute for BT_Watcher — no visual debugger, no EQS, no per-node breakpoints — but it drives the exact same observable behavior the roadmap's DoD asks for. When the human eventually authors `BT_Watcher`/`BB_Watcher`, `TickBrain`'s logic can be redistributed into BT Services/Tasks reading/writing the same-named Blackboard keys; the Controller variables I built (`AlertState`, `CanSeePlayer`, `TargetActor`, etc.) already mirror `06`'s Blackboard key list 1:1, so the port is mostly mechanical.

### AIPerceptionComponent sense configs (Sight/Hearing) ARE settable via ObjectTools — a new capability, not previously known
`AIPerceptionComponent.SensesConfig` is an array of **instanced sub-objects** (`AISenseConfig`, not inline structs). `ObjectTools.set_properties` on the component with `{"sensesConfig": [{"refPath": "/Script/AIModule.AISenseConfig_Sight"}, {"refPath": "/Script/AIModule.AISenseConfig_Hearing"}]}` **actually instantiates real sub-objects** in place (confirmed via `get_properties` echoing back real refs like `PerceptionComp_GEN_VARIABLE.AISenseConfig_Sight_0`) — then `set_properties` on THAT specific sub-object refPath sets its fields (`sightRadius`, `peripheralVisionAngleDegrees`, `detectionByAffiliation.bDetectNeutrals`, etc.) directly. This is the documented mechanism the `set_properties` tool description alludes to ("for instanced sub-object properties, pass a class path as the instance member") — first time actually exercised in this project. Generalizes to any component with an array-of-instanced-UObject property.

### DSL: cross-object accessor discovery is genuinely per-graph and per-timing — don't assume, always `find_node_types` fresh
Confirmed (again) that `Class|BP<Name>|<Member>` accessors only appear in `find_node_types` **after the target Blueprint has been saved at least once** — a freshly-created-and-edited class (e.g. `BP_WatcherCharacter` right after adding `AlertState`) returned zero hits for its own cross-object accessors until `AssetTools.save_assets` ran, even though the variable already compiled clean. Cost: skip a "nice to have" cosmetic mirror (Controller→pawn `AlertState` for future visuals) rather than spend another round-trip chasing the DB refresh — documented as deferred, not broken.

### `GetSenseClassForStimulus` + `ClassIsChildOf` — how to branch on AIPerception stimulus TYPE without a `Type` field
`Utilities|Struct|BreakAIStimulus` does NOT expose a comparable "which sense" field (only `Age`, `ExpirationAge`, `Strength`, `StimulusLocation`, `ReceiverLocation`, `Tag`, `bSuccessfullySensed`). To tell Sight from Hearing: `AI|Perception|GetSenseClassForStimulus(Stimulus) -> AISense Class Reference` (impure — bind it as its own statement), then compare with **`Utilities|ClassIsChildOf(TestClass, ParentClass)`**, NOT `==` — plain `==` between a class-typed pin and a quoted class-path string fails ("Could not connect pin ReturnValue to B") for the exact same reason enum `==` failed in Step 2 (no implicit string→Class conversion for a binary compare node). `ClassIsChildOf` is the correct idiom for "is this dynamic UClass this known class" in DSL, same as it would be in hand-authored Blueprint.

### Wiring logic onto an existing COMPONENT-BOUND event (e.g. `OnTargetPerceptionUpdated`) — DSL can't touch it directly, even freshly created
`add_component_bound_event(component, event_name, graph)` creates a real `K2Node_ComponentBoundEvent` whose `type_id` shows as `AddEvent|OnTargetPerceptionUpdated(PerceptionComp)` — but `write_graph_dsl`'s `(event OnTargetPerceptionUpdated(PerceptionComp) ...)` fails ("does not exist") even immediately after deleting the bare node and recreating it fresh; the parenthesized component-qualifier suffix apparently isn't a valid DSL event name token (parens read as S-expression syntax). **Workaround that generalizes:** don't fight the DSL for component-bound events. Build the actual logic as a plain custom function (`add_function_graph` + params matching the event's output pins) where DSL's implicit `self`/full authoring power works natively, then do ONE small surgical wire-up (`create_node` a `CallFunction|<YourFunctionName>` node, `connect_pins` the bound event's `then`/data outputs into it) — 3-4 tool calls instead of fighting node-by-node surgical construction of the whole logic tree.

### UNRESOLVED: `AIController::MoveToLocation`/`MoveToActor` return `Failed` (0) every tick despite an existing, correctly-sized `NavMeshBoundsVolume` + generated `RecastNavMesh` — root cause not found, needs a human
**Signature:** `TickBrain`'s perception/detection half is 100% verified live (sight cone detects the player, `DetectionMeter` fills to 1.0, `AlertState` flips to Alert, `TargetActor` locks correctly) — but the pawn's world position **never changes**, confirmed via `CharMoveComp.Velocity` reading exactly `(0,0,0)` and a debug `PrintString` of `MoveToLocation`'s own `EPathFollowingRequestResult` return value showing **`0` (Failed) every single tick**, with distance-to-target frozen at the exact spawn-time value (proving zero movement, not just slow movement).
**What was tried, in order, each a genuine one-hypothesis-one-test cycle (none fixed it):**
1. No `NavMeshBoundsVolume` existed at all initially → added one (`-2000..2000` in X/Y matching the floor) → hit `LogNavigation: Warning: ... 3 tile limit reached!` (the volume was accidentally 2× oversized — `scale=40` against an assumed-100uu default half-extent actually gave `4000uu` half-extent, not `2000uu`).
2. Shrunk the volume to the intended `-800..1600 / 300..2700` footprint → tile-limit warning didn't recur on the next PIE start (good sign) but movement still didn't work.
3. `RecastNavMesh.bFixedTilePoolSize=true, tilePoolSize=4096, tileSizeUU=2000` (directly acting on the warning's own suggested remedy) → triggered a `Recreating dtNavMesh instance` log line (proof the setting change WAS picked up) → still `Failed`.
4. Found and fixed a genuine mismatch (`RecastNavMesh.agentHeight=144` vs the Watcher's actual capsule `2×88=176`, `agentRadius=35` vs capsule `34` — close but height was clearly wrong) → bumped to `agentHeight=192, agentRadius=42` → still `Failed`.
**Not yet tried (next session's starting points, in likely-value order):** (a) ~~check Project Settings → Navigation Mesh → Supported Agents~~ **done, see attempt 5 below — most promising untested lead, needs an editor restart to verify**; (b) verify with the in-editor NavMesh visualization (`P` key in viewport, or `EditorApp` console command `show Navigation`) whether the generated navmesh geometry visually covers the Watcher/patrol-point locations at all — this is a 10-second check for a sighted human, versus many blind property-inspection round trips for MCP; (c) check whether `AutoPossessAI=PlacedInWorldOrSpawned` + a Blueprint-Character-based nav agent needs an explicit `bCanEverAffectNavigation`/nav-relevant collision profile that a fresh Character class doesn't have by default; (d) try `AI|Navigation|SimpleMovetoLocation` (found alongside `MoveToLocation` in `find_node_types` — an older/simpler nav request path) as a diagnostic swap, since a difference in behavior between the two would narrow the cause. **Do not repeat steps 1-4 — they're exhausted hypotheses, per the debugging discipline.**

5. **(Same session, later) Found `Config/DefaultEngine.ini`'s `[/Script/NavigationSystem.NavigationSystemV1] SupportedAgents` was a completely EMPTY array** via `ConfigSettingsToolset.GetSectionPropertyValues` (container="Project", category="Engine", section="NavigationSystem") — this is Project Settings → Navigation Mesh → Supported Agents, a **separate config surface from the per-instance `RecastNavMesh` actor properties** already tried in attempts 3-4; it's the registry of which agent types can path at all, read (per the same class of behavior as this project's known `r.RayTracing` cvar) once at process/CDO init. Added one entry via `ConfigSettingsToolset.SetSectionProperties` (`Name="Default"`, `AgentRadius=34`, `AgentHeight=192`, `NavDataClass=/Script/NavigationSystem.RecastNavMesh`, `bCanCrouch/Jump/Walk/Swim=true`, `bCanFly=false`) — confirmed persisted to disk (`Config/DefaultEngine.ini` lines 150-151, `+SupportedAgents=(...)` and `SupportedAgentsMask=(bSupportsAgent0=True,...)`). **Tested live via `StartPIE` (no editor restart) — did NOT take effect:** Watcher position read twice, both times still exactly at spawn. This is consistent with `SupportedAgents` being cached at editor-process startup, same as `r.RayTracing`. **Deliberately did NOT restart the editor to verify** — doing so would end the MCP connection for the rest of that session with no in-session reconnect (a hard, previously-established constraint), and continuing other buildable work was higher-value than spending the whole session's remaining MCP access on one restart-gated test. **This is the single most promising untested lead for the next session:** restart the editor (`unreal-mcp-session` skill's restart command in CLAUDE.md), reconnect MCP, `StartPIE`, and check whether the Watcher now paths. If it still fails after a real restart, this hypothesis is exhausted too and the remaining leads are (b)/(c)/(d) above.

### Detection AI verified live end-to-end (the part that DOES work, unambiguously)
Via direct PIE property reads (not just compile): placed one `BP_WatcherCharacter` + 4 `TargetPoint` patrol markers (tagged `"PatrolPoint"`, gathered via `Actor|GetAllActorsWithTag` in `EventBeginPlay`) in `L_Sandbox_Movement`; tagged `BP_PlayerCharacter` `"Player"` and added an `AIPerceptionStimuliSourceComponent` registered for Sight+Hearing. Teleporting the player into the Watcher's forward cone produced, within the same tick: `bCanSeePlayer=true`, `DetectionMeter=1.0`, `AlertState=2` (Alert), `TargetActor` correctly pointing at the player — the full sight→detection→alert pipeline, server-authoritative, matching `04`'s spec numbers (1400/1800/35°, 1.5s fill) exactly since they're read from a new shared `DA_AIPerceptionProfile`/`AIP_WatcherProfile` Data Asset (the Blueprint-class-plus-instance pattern, so future AI types reuse the same profile class per `04 §8`'s own design intent).

## 2026-07-01 (autonomous session) — Step 2 finish: DSL enum-comparison gotchas, and re-confirmed computer-use cannot reach the Unreal Editor (fresh test, not just memory)

**computer-use re-verified blocked, not just recalled from old notes:** per the user's request to try taking over the screen on any blocker, called `request_access(["Unreal Editor","UnrealEditor","PoolHop"])` fresh this session. Result: **short-circuited before even reaching the user** — `"didYouMean":["Registry Editor"]`, none of the three names resolve to any installed/running app. Confirms the standing LESSONS finding (UnrealEditor.exe launched via `.uproject` file-association is not a Start-menu-registered app, so the resolver can't find the live window) still holds. Don't keep retrying this per-blocker within a session — the root cause (unregistered app) doesn't change between attempts.

**`DT_LoudnessActions` confirmed blocked the same way as the enums:** `DataTableTools.create`/`import_file` both *require an existing* row struct (`schema` param, must already be a `ScriptStruct` subclass of `TableRowBase`); there is no "create struct" tool anywhere in the DataTable/DataAsset toolsets. Creating a **new** Blueprint struct (`S_LoudnessAction`) is therefore not MCP-buildable — same wall as `E_AlertState`/`E_PoolTree`. **Substitute pattern (documented in CANON):** build a plain Blueprint function with an if/elif chain over the action-tag string, matching the DataTable's starter values — functionally identical to the caller, trivially swappable once the human creates the struct.

**DSL: comparing an ENUM output against a quoted string literal via `==` fails ("Could not connect pin ReturnValue to B") — enums are not strings at the pin-type level, even though the docs' quoting rule for enum *arguments* makes it look like they are.** The quoting rule (`"AlwaysSpawn"` etc.) applies to literal **arguments passed into a pin that accepts a string-like enum parameter** (auto-converted by that specific node); it does NOT mean an enum-typed pin can be compared with `==` against a bare quoted string — `==` needs two same-typed pins and there's no implicit Enum↔String conversion node inserted automatically. **Fix: use `switch <FullSwitchTypeId> value (:CaseName stmt...) ...)` instead of `==`** for enum comparisons — find the exact switch type_id via `find_node_types(graph, "FlowControl|Switch")` (for `EMovementMode` it was `Utilities|FlowControl|Switch|SwitchonEMovementMode`, NOT the docs' generic guess `Utilities|FlowControl|SwitchOnEMovementMode` — always verify, don't pattern-match from the docs' example).
- **Enum switches have no implicit `:Default` case** — only the pins for the enum's actual declared values are ever available (confirmed via the error message itself: `Unknown exec output "Default" on ...SwitchonEMovementMode. Available: [...]` lists every enumerator, no Default). Int/String/Name switches DO get a `:Default` (per the docs' own example) — enum switches don't. Plan around it: either enumerate every case you care about, or restructure so you don't need a catch-all.
- **To run two independent multi-exec branches (e.g. two separate `switch` statements) off the SAME upstream event/trigger, you must explicitly fan out with `Utilities|FlowControl|Sequence`** (pins `then_0`, `then_1`, ...) — sequential top-level DSL statements do NOT auto-chain past a multi-exec node (this reinforces the pure-node/multi-exec lesson from Step 3: only single-exec-output nodes auto-chain to "the next statement"; anything with named branches needs its consumer nested inside the branch, or fanned out via an explicit Sequence). Concretely, checking "did we just enter swimming" (switch on `NewMovementMode`) and "did we just leave swimming" (switch on `PrevMovementMode`) needed two switches fed from a `Sequence` off the same `EventOnMovementModeChanged`, not two switches written back-to-back.

**Verification win:** the swim→loudness hookup was checked live in PIE (not just compiled) by teleporting the pawn into/out of the pool's water `PhysicsVolume` — `MOVE_Swimming` detection, `bIsInWater` flag, and `CurrentLoudness` rising to `WaterBand`/decaying to `IdleFloor` all confirmed working end-to-end, physics-driven (no key input needed to trigger it, unlike sprint/vault which still need a real `unreal-input-probe` pass to exercise).

## 2026-07-01 (same session) — Step 3 pool scoring built + wired; a real Blueprint bug caught by live PIE testing: PURE Get nodes re-evaluate at each use, they are not "read once" just because a DSL `bind` reused one node

**The bug (would have shipped silently without live testing):** `Server_BankAtRisk(PS)` computed `IndividualScoreBanked += atRisk` (correct — PlayerState side matched expectations in testing), but the parallel `GameState.TeamScoreBanked += atRisk` / `TeamScoreAtRisk -= atRisk` came out **wrong** — `TeamScoreAtRisk` stayed stuck at the pre-bank value and `TeamScoreBanked` stayed 0. `get_node_infos` proved the *graph wiring* was 100% correct — one `GetIndividualScoreAtRisk` node, its single output wired to all three consumers (`IndividualScoreBanked` add, `TeamScoreBanked` add, `TeamScoreAtRisk` subtract), exactly matching the DSL source and the "bind once, reuse" rule from `get_graph_dsl_docs`.

**Root cause:** in my original statement order, `SetIndividualScoreAtRisk(PS, 0)` (the reset) ran **before** the GameState math (which lived inside a `CastToBP_PlayerGameState` `:then` block positioned later in the function). A **pure** node (no exec pins — a plain member-variable getter) has no cached/materialized value: Blueprint re-evaluates it **fresh at each consumption point**, not once at authoring time. So `SetIndividualScoreBanked` (which executes early, before the reset) read `atRisk` correctly; but `SetTeamScoreatRisk`/`SetTeamScoreatRisk` (executing later, after the reset already zeroed the source variable) re-pulled the SAME `GetIndividualScoreAtRisk` node and got **0** instead of the frozen original value. One DSL node ≠ one evaluation — "bind once, reuse" (from the docs) only guarantees you don't author a duplicate node; it does **not** freeze a pure read against a later Set on the same property within the same function.

**Fix:** reorder so **every** consumer of a soon-to-be-reset variable's pure read happens **before** the reset statement, not just the first one. Both `Server_BankAtRisk` and `Server_LoseAtRisk` had this bug (identical shape: read `IndividualScoreAtRisk` → reset it early → later re-derive a GameState delta from the same read). Fixed by nesting the reset **last**, inside the same exec branch as the GameState update, so the reset physically cannot run before every consumer of the old value has fired.

**Generalizes to:** any function that (a) reads a mutable variable, (b) resets/overwrites that SAME variable, and (c) needs the OLD value in more than one place — the fix is always "do all reads-dependent-math first, reset last," never "bind once and trust it." This will matter again for Step 5 (AlertDirector heat/detain resets) and Step 2 finish (loudness resets) — watch for this exact shape.

**Why this was caught:** direct PIE property inspection (`ObjectTools.get_properties` on the live `PlayerState`/`GameState` instances during a running PIE session, via `set_actor_transform` teleports into the pool then the stash zone) — not just "does it compile." A clean `compile_blueprint(warnings_as_errors=true)` and a correct-looking `get_node_infos` dump both said "fine"; only reading the actual runtime numbers exposed the bug. Reinforces the project's core verification discipline (see `unreal-mcp-session` skill §3) one level deeper: even *logic*, not just *assets*, needs a live runtime check, not just a clean compile.

### Smaller findings from the same build pass
- **`SpawnActorAtTransform`/`StartPIE(startTransform=...)` silently fails if the override transform collides with existing geometry** (`LogSpawn: Warning: SpawnActor failed because of collision at the spawn location`). Don't spawn-override into a trigger volume's interior for testing — spawn at the normal PlayerStart, then `ActorTools.set_actor_transform` to **teleport** the already-spawned pawn wherever you need it. Teleporting via `set_actor_transform` correctly re-triggers `UpdateOverlaps` (both Begin and End fired correctly across two separate teleports in testing) — it's just the *initial spawn* that has collision-avoidance.
- **Cross-object Blueprint accessors follow `Class|BP<ClassNameNoUnderscore>|<MemberOrFunctionName>` (find via `find_node_types` from the CALLING blueprint's own graph, never assume/guess).** Same-class self-calls to a blueprint's own custom function use a *different* prefix, `CallFunction|<Name>` — discovered when `Class|BPPoolVolume|GetCrewMultiplier` (calling itself) failed with "does not exist" but `CallFunction|GetCrewMultiplier` worked. Boolean (`bIsScoring`) member accessors strip the `b` prefix in both forms (`GetIsScoring`, not `GetbIsScoring`) — same convention as the pre-existing `BPLoudnessComponent` accessors (`GetIsSprinting` from `bIsSprinting`). Always `find_node_types` from the actual graph you're writing into rather than pattern-matching from a different blueprint's discovered names — the two prefixes are NOT interchangeable and guessing wastes a round trip (or worse, silently binds to the wrong node if a similarly-named type happens to exist elsewhere).
- **User-created Blueprint functions are never "pure" by default via these tools** — even functions with no side effects (`GetEffectiveScorePerSecond`, `GetCrewMultiplier`) get exec pins (`execute`/`then`) when called, unlike auto-generated variable Get/Set accessors (which stay pure/exec-less). No tool exposes toggling the "Pure" flag. Practical effect: never inline a call to one of your own custom functions inside an arithmetic expression in DSL — always `(bind x (CallFunction|MyFunc ...))` as its own statement first, then use `x`. Engine math/utility nodes (`Math|Float|Power`, `Array|Length`, `Array|ContainsItem`, `Networking|HasAuthority`, `Game|GetGameMode/GetGameState`, `Components|GetOwner`) ARE pure (no exec) and safe to inline.
- **`(event EventName ...)` DSL event names for engine overridable events must include the category prefix from `find_node_types`'s `AddEvent|...` result, not just the bare event name** — `(event EventActorBeginOverlap ...)` fails ("AddEvent|EventActorBeginOverlap does not exist"); the working form is `(event Collision|EventActorBeginOverlap ...)` (matching the exact `AddEvent|Collision|EventActorBeginOverlap` type_id string minus the `AddEvent|` prefix). `EventBeginPlay`/`EventTick` have no category prefix and work bare. Always `find_node_types(graph, "AddEvent|")` first rather than guessing from the docs' generic examples.
- **`ObjectTools.set_properties` cannot set a `Name`-typed property on a placed level-actor INSTANCE** (tried both `"PoolID"` and `"poolId"` casing — both silently rejected: "could not be set"). Reading the same property works fine (`get_properties` returns it correctly either casing). Root cause not identified (works fine as a plain string/int/bool/float on the same instance). Low priority — for single-pool testing the default `PoolID=None` is harmless (only affects the "is this pool newly-distinct" check, and there's only one pool so far). Revisit when placing pools B–D (need real distinct `PoolID`s for the hop-streak mechanic to mean anything) — try setting it on the CDO instead, or via the DataAssetTools route, before concluding it's a hard MCP gap.
- **A second concurrent UE process's log silently goes to `<Project>_2.log`, `_3.log`, etc.** (Windows file-lock collision when the editor already holds the primary log open) — see the entry above for the input-mapping session; same mechanism applies to any standalone-launch-while-editor-is-open scenario.

## 2026-07-01 (new session) — Crouch/Sprint re-added; the REAL IMC property is `DefaultKeyMappings`, not `Mappings` — corrects a claim in this file and the input-probe skill

**Finding that changes how to read any `InputMappingContext` from now on:** in this engine version, `UInputMappingContext`'s top-level `Mappings` property is a **legacy/always-empty shim**. The live, effective key bindings live nested at **`DefaultKeyMappings.Mappings`** (an `FInputMappingContextMappingData` struct wrapping the real `TArray<FEnhancedActionKeyMapping>` — added for the player-remappable-key-profile system). Confirmed via `ObjectTools.list_properties`/`get_properties` on `/Game/Input/IMC_Default.IMC_Default`: `get_properties(["mappings"])` → `{"mappings":[]}` (always, even with a fully-populated asset), while `get_properties(["defaultKeyMappings"])` → the real 11+ entries. The console command `GetAll InputMappingContext Mappings` (used throughout the 3 PM session to diagnose the hollow-IMC bug) **queries the dead field** — it will print `Mappings =` (empty) even on a perfectly healthy IMC. **The correct command is `GetAll InputMappingContext DefaultKeyMappings`**, which prints the full `(Mappings=((Action=...,Key=...), ...))` content. This doesn't retroactively invalidate the earlier diagnosis (the file was independently confirmed corrupt/undersized and WASD independently confirmed dead via SendInput), but the specific `GetAll ... Mappings` signal used as evidence there was measuring the wrong property the whole time — update the [`unreal-input-probe`](../.claude/skills/unreal-input-probe/SKILL.md) skill's command reference accordingly (done).

**Task:** re-added the two mappings START_HERE flagged as missing — `C → IA_Crouch`, `LeftShift → IA_Sprint` — to `IMC_Default` via `ObjectTools.set_properties` on `defaultKeyMappings` (must resend the *entire* mappings array; there's no append-only setter, only whole-property replacement — copy the existing array from `get_properties` first, mutate in memory, `set_properties` the union). Saved (`AssetTools.save_assets`) — file grew 7787 B → 8773 B, confirming real content landed (not a silent no-op).

**`compile_blueprint` + `save_assets` on an unchanged Blueprint can be a complete no-op — don't count it as "fixed" without checking the file actually changed.** Also recompiled+resaved `BP_PlayerController` per START_HERE's other open item (its BeginPlay chain was flagged as stale bytecode that never runs from a cold load). `compile_blueprint(warnings_as_errors=true)` ran clean (no `LogBlueprint` warnings), but `save_assets` produced **zero on-disk change** — file mtime identical to the last git commit, `git status` shows no diff. Re-checked the actual symptom in the fresh standalone logs from this session's verification runs: the `"CTRL BeginPlay - IMC_Default APPLIED"` print still never appears. **So this is still an open bug, not fixed** — compiling an already-"compiled" Blueprint with no graph edits doesn't dirty the package, so there was nothing for save to write. Don't declare a recompile-based fix done on the strength of "the tool call succeeded" — check `git status`/mtime for an actual diff, and re-verify the original symptom in fresh output. (Non-blocking here: the engine-level `DefaultMappingContexts` config path is what's actually proven to work, independent of this BP's own BeginPlay logic.)

**Verified on genuinely cold on-disk state** (not just editor-memory-to-disk): launched a standalone `-game` process (new PID each time — separate from the running editor, so it doesn't disturb the MCP session), drove `GetAll InputMappingContext DefaultKeyMappings` via the `send-console.ps1` pattern, and read the result from the log. **Gotcha:** when the editor is already running and holds `Saved/Logs/PoolHop.log` open, a second (standalone) process's log is silently redirected to **`Saved/Logs/PoolHop_2.log`** (Windows file-lock collision — UE appends `_2`, `_3`, ... per concurrent instance). Grepping `PoolHop.log` after launching a second instance finds nothing new; check `ls -la Saved/Logs/` for the freshest `PoolHop_2.log`/`PoolHop_N.log`. Confirmed output: `(Action=".../IA_Crouch.IA_Crouch'",Key=C)` and `(Action=".../IA_Sprint.IA_Sprint'",Key=LeftShift)` present alongside all original WASD/Jump/Look mappings, both key mappings correctly typed with no orphaned modifiers.

## 2026-07-01 (3 PM session) — "Game runs but ignores ALL input" root cause: the IMC assets on disk had EMPTY Mappings arrays

**This supersedes the "only remaining suspect is PIE keyboard focus" theory below — focus was never the problem.** Reproduced the user's bug in a cold standalone `-game` launch with REAL OS-level input (SendInput): world ticks, character idles, tilde console opens and takes typed commands (raw keyboard reaches the game fine) — but WASD/mouse/jump all dead. `showdebug enhancedinput` on screen: **"No enhanced player input action mappings have been applied to this input."**

### The mechanism (fix commit `f5c714e`)
`GetAll InputMappingContext Mappings` in the in-game console printed both IMCs **loaded but with empty `Mappings =`**. The WASD/look/jump mappings authored via MCP `set_properties` existed **only in that editor session's memory** — the on-disk `IMC_Default.uasset`/`IMC_MouseLook.uasset` serialized with zero mappings (the "save silently no-ops" corruption failure mode, one level deeper than anyone checked: everyone verified the *Blueprint graph wiring*, nobody verified the *IMC asset content on disk*). Every MCP-era PIE "verification" ran against dirty in-memory state; every cold load (the user's every Play press, and standalone) got an input system with no key bindings. Leftover `InputModifierNegate`/`SwizzleAxis` strings in the hollow uasset were stale import-table entries — **string-grepping a uasset proves references exist, NOT that arrays have elements.**
Same disease in `BP_PlayerController`: its BeginPlay chain (AddMappingContext + the `CTRL BeginPlay - IMC_Default APPLIED` print) **never runs from a cold disk load** (no log line, no Accessed None — the saved bytecode predates the wiring). Needs recompile+resave via MCP; until then the config-level fix below covers it.

### Fix 1 — restore pristine input assets by file copy (the Mannequins trick works for input too)
`C:\Program Files\Epic Games\UE_5.8\Templates\TemplateResources\High\Input\Content\` ships `IMC_Default.uasset` (7787 B vs our hollow 6220 B), `IMC_MouseLook.uasset`, and `Actions/IA_{Move,Look,MouseLook,Jump}.uasset` at the **exact same `/Game/Input/...` package paths** the project uses — copy the files over, done. Template IMC_Default has no Crouch/Sprint mappings (ours were MCP-authored) → **re-add C→IA_Crouch, LeftShift→IA_Sprint via MCP, then verify the save actually wrote** (file size changed + relaunch + `GetAll InputMappingContext Mappings` shows them).

### Fix 2 — engine-level default mapping contexts (belt-and-suspenders, now in the repo)
`Config/DefaultInput.ini` → `[/Script/EnhancedInput.EnhancedInputDeveloperSettings]` with `+DefaultMappingContexts=(InputMappingContext="/Game/Input/IMC_Default.IMC_Default",Priority=0)` (+ MouseLook at P1). The engine applies these to every local player in `UEnhancedInputLocalPlayerSubsystem::PlayerControllerChanged` — input no longer depends on any Blueprint BeginPlay. **Config gotchas that cost a round-trip each:** the struct field is **`InputMappingContext`**, NOT `MappingContext` — a wrong field name in a config array is **silently ignored**; and a failed `LoadSynchronous` on the soft path is **silently skipped** (engine code has no else-branch). When authoring config for an engine class, grep its header first for `UCLASS(... config = X ...)` (→ which Default*X*.ini) and the exact USTRUCT field names.

### The verification rule this hammers home
**"Wiring verified in the editor" ≠ "works from disk."** PIE after in-session authoring tests memory, not the asset files. The only disk-truth test is a **fresh process**: standalone `-game`, or restart the editor before PIE. (Also killed a myth: standalone `-game` does NOT exit on focus loss — that was a blind-era misdiagnosis.)

## 2026-07-01 (3 PM session) — BREAKTHROUGH: PowerShell SendInput CAN drive Enhanced Input end-to-end (no MCP, no computer-use needed)

The long-standing "MCP cannot self-test a hardware keypress" wall is **broken**. What synthetic Slate keys can't do, **OS-level `SendInput` can** — it injects into the system input stream, producing real `WM_KEYDOWN`/raw-input events that Enhanced Input consumes exactly like a physical keyboard. Proven: WASD run + mouse-look camera + Space jump, all injected from a PowerShell script, all moving the character on screen. **Full recipe captured as the [`unreal-input-probe`](../.claude/skills/unreal-input-probe/SKILL.md) skill** (scripts included). Key pieces:
- Launch `UnrealEditor.exe <proj> -game -windowed -ResX=1280 -ResY=720 -WinX=80 -WinY=80 -NoSplash`, find the window by **PID via EnumWindows** (title is `PoolHop (64-bit Development PCD3D_SM6)`; don't match by title alone — a File Explorer window named "PoolHop" collides).
- **Focus by real click** (`SetCursorPos` + `mouse_event` at window center) — clicks focus whatever is under the cursor, bypassing Windows' `SetForegroundWindow` restrictions that blocked earlier sessions.
- Send keys with **both VK and scancode** (`MapVirtualKey`); hold = down…sleep…up. Mouse-look = stepped `MOUSEEVENTF_MOVE` relative moves.
- **Observe with GDI `Graphics.CopyFromScreen`** of the window rect — unlike computer-use, GDI does NOT mask unregistered windows. Screenshot before/during/after; the third-person camera makes any movement unmissable.
- **The tilde console is drivable the same way** (it accepts these keys even though synthetic Slate keys failed): open with VK `0xC0`, type command chars as VKs, Enter. That unlocks `showdebug enhancedinput` (on-screen input-stack truth: applied contexts + live action values) and `GetAll <Class> <Prop>` (dumps ANY UObject property to the log file — e.g. `GetAll InputMappingContext Mappings` = read asset state at runtime with **no MCP connection at all**). This is a full observe/drive channel for a running game from plain PowerShell.

## 2026-07-01 (3 PM session) — unreal-mcp is session-start-only: editor closed ⇒ that Claude session can never reach it

Confirmed the LESSONS note operationally: this session started with the editor down → `ToolSearch` finds zero unreal tools all session, even after relaunching the editor+server. The fallback toolkit above (standalone launch + SendInput + GDI capture + console `GetAll`/`showdebug` + uasset string-grep + engine-source grep under `Engine\Plugins\...\Source`) was enough to root-cause and fix a runtime gameplay bug **without MCP**. Don't stall a session because MCP is gone — and always leave the editor running with `-ExecCmds=ModelContextProtocol.StartServer` for the NEXT session.

---

## 2026-07-01 (LATE PM) — The "freeze" was never a freeze: it was a MOTIONLESS A-POSE STATUE. Game runs at 120 fps.

**This supersedes the "focus throttle was the finale" framing below.** After a fresh session reconnected MCP, I measured the running game properly and the whole perf/throttle narrative collapsed: **the game renders at ~120 fps.** It was never frozen — not now, not when the user played.

### The real root cause of the "it's frozen" perception
`BP_PlayerCharacter.CharacterMesh0.AnimClass` was **`None`** (AnimationMode was already `AnimationBlueprint`, just no BP assigned). So Manny stood in a **rigid A-pose with zero idle motion**. A game running at 120 fps that shows a perfectly motionless mannequin is **visually identical to a frozen screenshot** — every time the user hit Play they saw a statue that didn't react, and reasonably called it "frozen / can't move." **Fix:** assign the restored `ABP_Unarmed` →
`set_properties(...Default__BP_PlayerCharacter_C:CharacterMesh0, '{"AnimClass": {"refPath": "/Game/Characters/Mannequins/Anims/Unarmed/ABP_Unarmed.ABP_Unarmed_C"}}')` → `compile_blueprint` → `save_assets(["/Game/_Project/Characters/BP_PlayerCharacter"])`. Manny now idles/walks. Commit `d4073d2`.

### Measuring fps reliably via MCP (correcting the "frame counter is useless" claim below)
The log `[N]` frame counter **is** usable for fps — but ONLY across two samples **< ~1 s apart** so it can't wrap 1000 ambiguously. Fire two cheap tool calls back-to-back (each logs `Running tool` with an `[N]` + `HH.MM.SS.mmm` timestamp), then `GetLogEntries(pattern:"Running tool")`: e.g. `844 → 889` across `0.379 s` = **119 fps**. Over long gaps (10–50 s) the wrap ambiguity is what made it look like noise before — that was a sampling error, not the counter lying. **Second reliable signal: frame-to-frame pixel diff.** `CaptureEditorImage` twice ~1 s apart, decode both PNGs, diff the character-region crop — thousands of changed pixels = the scene is live-rendering AND the char is animating (this is how ABP_Unarmed was verified: A-pose→arms-down + ~4800 px/frame of motion).

### The ONE thing MCP genuinely cannot test — a hardware keypress
Enhanced Input ignores synthetic Slate keys (SlateInspector `PressKey` never reaches it), and **computer-use cannot target the Unreal window** — `request_access(["Unreal Editor","UnrealEditor","PoolHop"])` returns `notInstalled` (UE launched from a `.uproject` is not a Start-menu/registered app; screenshots would mask it). So the final link *hardware W → IA_Move → movement* cannot be self-verified via MCP. Everything **up to** that link is verified: 120 fps, GameMode spawns+possesses `BP_PlayerCharacter_C` (found `UEDPIE_0_..:PersistentLevel.BP_PlayerCharacter_C_0`), controller BeginPlay runs `AddMappingContext(IMC_Default)`→`SetInputMode_GameOnly` (mouse-capture at runtime **proves** the IMC applied), and `IA_Move`'s `Triggered`/`ActionValue` pins are wired to the Move function (`get_node_infos`, node type `EnhancedInputActionIA_Move`). If real WASD still doesn't move him, the only remaining suspect is **PIE keyboard focus in "Play in Selected Viewport"** — click once inside the game view, or use Play ▾ → "New Editor Window."

### Meta-lesson (reinforces the one below): when the user says "frozen," MEASURE fps before ANY perf theory
I burned two sessions on render/throttle theories for a game that was never below 120 fps. The tell I ignored: a motionless character is not the same as a stalled engine. First question must be "is the engine actually ticking?" (frame-counter delta over <1 s, or frame-to-frame pixel diff) — not "which render feature is slow?"

---

## 2026-07-01 (PM) — The "freeze / can't move" was a STACK of issues; the finale was a focus throttle + unreliable measurement

### Meta-lesson (most important): when you cannot OBSERVE the running game, STOP guessing
After the unreal-mcp connection dropped (editor closed mid-session), I diagnosed framerate/perf **blind from logs** for a very long stretch and formed several confident-but-wrong hypotheses in a row (RTX 50-series render bottleneck, driver regression, GPU downclock, shader-compile stall). Every one was a guess over evidence I couldn't actually see. The debugging discipline (read evidence → one hypothesis → verify the mechanism) collapses without observation — restore observation FIRST. Once MCP was back, the reliable signals resolved it in minutes.

### Reconnecting unreal-mcp after it drops (it can't reconnect in-session)
The connection is made once, at Claude Code session start. If the editor closes mid-session the tools go dead and `ToolSearch` returns nothing for the rest of that session — **there is no in-session reconnect.** Recovery: relaunch the editor WITH `-ExecCmds=ModelContextProtocol.StartServer`, clear anything blocking a clean load (GameFeatureData dialog below), confirm port 8000 listening, then **restart Claude Code** — the fresh session hooks straight into the running server. State survives via git + the CLAUDE.md handoff note.

### The actual root causes (several, stacked — none was "the input")
1. **Invisible character** — `BP_PlayerCharacter.CharacterMesh0.SkeletalMeshAsset = None`. Fixed via `ObjectTools.set_properties` on the CDO (`...Default__BP_PlayerCharacter_C:CharacterMesh0`), assigning `SKM_Manny_Simple`. NOTE `set_properties` takes `values` as a **JSON string**, not an object.
2. **Broken character asset (READ THE LOAD ERRORS FIRST — the user had to point me here).** `LogAssetRegistry`/`LoadErrors` showed `SK_Mannequin` (skeleton) + `PA_Mannequin` (physics) **MISSING** and 13 zero-byte `.uasset` (Manny/Quinn materials, textures, rigs, touch UI). A skeletal mesh with no skeleton = broken character. **Restore source:** `C:\Program Files\Epic Games\UE_5.8\Templates\TemplateResources\High\{Characters,Input}\Content\` — copy the whole `Mannequins/` tree (+ `Touch/`) over the project's. Paths match exactly, `SKM_Manny_Simple` is byte-identical, and it ships a full anim set + **`ABP_Unarmed`**. Result: 0 zero-byte uassets, Manny renders correctly.
3. **The "3 fps freeze" was Unreal's not-foreground throttle, NOT a perf problem.** With the editor window not the OS-foreground app, the GPU **idles at 3% util and downclocks to 1852 MHz**; forced to foreground it jumps to **2800 MHz** (full clock) at only ~10% util — the grey box is trivial for an RTX 5080. Stripping Lumen/MegaLights/VSM/clouds did nothing because the GPU was never the bottleneck. **CONFIRMED ROOT CAUSE + FIX:** `bThrottleCPUWhenNotForeground` was **`True` at runtime** — my `.ini` edits to `[/Script/UnrealEd.EditorPerformanceSettings]` never applied (`UEditorPerformanceSettings` is `config=EditorPerProjectUserSettings`, and the Saved config written on a prior editor-close overrode my Default edit). Read the live value to confirm: `ObjectTools.get_properties("/Script/UnrealEd.Default__EditorPerformanceSettings", ["bThrottleCPUWhenNotForeground"])` → `true`. Fix it on the CDO directly: `set_properties(...Default__EditorPerformanceSettings, '{"bThrottleCPUWhenNotForeground": false}')` — takes effect immediately. **Verified end-to-end** with a slow (WalkSpeed=80) auto-walk toward the far −X wall: the pawn moved x=−1025 → −1464 during a **5 s *unfocused* wait** (before the fix it stayed frozen). The CDO value persists to the Saved config when the editor closes; `Config/DefaultEditorPerProjectUserSettings.ini` carries it for fresh clones. (`t.IdleWhenNotForeground=0` was a red herring — that's standalone-game, not PIE-in-editor.) **Diagnosis trap that cost time:** the pawn kept reaching a *perimeter wall* during warmup before the first position read, so "stationary" looked like "frozen" — to test tick-vs-blocked, slow the walk and aim at the *farthest* wall so you sample it mid-travel.

### Reliable vs UNRELIABLE runtime measurement (this cost hours)
- **UNRELIABLE — the log `[N]` frame counter.** NOT a clean monotonic per-frame counter: it resets on PIE restart and jumps around. I derived "~1-3 fps" from it repeatedly and it was noise. **Never derive fps from it.**
- **RELIABLE — the pawn's world position.** `ObjectTools.get_properties` on the Character's root (`CollisionCylinder`) `RelativeLocation`, vs the `PlayerStart` root (`CollisionCapsule`). Game state, independent of rendering/throttle. This PROVED movement works: pawn walked spawn `x=100` → `x=645` under a frozen-looking display. Use this to prove movement, not screenshots.
- **RELIABLE — `nvidia-smi`** (`utilization.gpu, clocks.current.graphics, power.draw, clocks_event_reasons.*`). Idle clock (~1852 MHz)+low util = throttled/idle; full clock (~2800 MHz) = actively rendering. This exposed the throttle.
- **Windows blocks `SetForegroundWindow` from a background/automation process**, so I cannot reliably force the game window foreground to observe un-throttled rendering — every capture was of a throttled/unfocused window (hence "display frozen" while the pawn actually moved).

### Tool notes
- **`stat unit`/`stat fps` could not be enabled from automation:** neither `ExecuteConsoleCommand("stat unit")` in BeginPlay nor typing into the editor Cmd bar rendered the overlay while throttled. Synthetic Slate keys reach neither Enhanced Input nor the game console.
- **SlateInspector is the observation/control channel** (computer-use still can't reach Unreal — not a Start-menu app, masked in screenshots): `Windows`, `Snapshot` (find refs), `Click`, `Type`, `PressKey`. The editor Cmd console is the textbox `tb2` in the bottom status bar — `Click` it to focus, then `Type` with `submit:true`.
- **GameFeatureData startup dialog** ("Add entry to PrimaryAssetTypesToScan?") blocks a clean load — fix in `Config/DefaultGame.ini`: `[/Script/Engine.AssetManagerSettings]` + `+PrimaryAssetTypesToScan=(PrimaryAssetType="GameFeatureData",AssetBaseClass=/Script/GameFeatures.GameFeatureData,bHasBlueprintClasses=False,bIsEditorOnly=False,Directories=((Path="/Game")),Rules=(Priority=-1))`.
- **Every render-cvar change forces a full shader recompile** → slow first PIE; don't confuse that with a persistent perf problem.

## 2026-07-01 — Session: "WASD still can't move" was the invisible character, NOT input

### The real bug: the character's Mesh component had `SkeletalMeshAsset = None` (Manny was invisible)
**Signature:** user hits Play, mouse gets captured ("mouse goes away" = game HAS focus + captured the cursor for mouse-look), the view looks "frozen," and pressing WASD seems to do nothing. **The input was fine the whole time.** The `CharacterMesh0` component on `BP_PlayerCharacter` had no skeletal mesh assigned (lost during the character's creation/duplication), so the possessed pawn was **invisible**. On a featureless grey-grid floor with no visible body, walking is imperceptible — it reads as a frozen game. **Fix:** `ObjectTools.set_properties` on `Default__BP_PlayerCharacter_C:CharacterMesh0` → `SkeletalMeshAsset = /Game/Characters/Mannequins/Meshes/SKM_Manny_Simple` (the mesh itself is valid, 15.8 MB; only its material instances `MI_Manny_*` are still 0-byte, so Manny renders solid **grey** via the default-material fallback — fine for grey-box). The Character base class already gives the mesh its correct relative transform (Z=-89, Yaw=-90), so no transform work needed. Verified visible in PIE. **No AnimBP exists in the project** (`find_assets ABP` → empty), so Manny is a static A-pose that slides — visible movement, no walk cycle yet (animation deferred).

### `read_graph_dsl` CANNOT traverse Enhanced Input event exec chains — it lies "empty"
**This nearly caused a wrong fix.** `read_graph_dsl` follows `then` exec pins; Enhanced Input event nodes output `Triggered`/`Started`/`Ongoing`/`Completed` (there is no `then` pin), so the reader silently can't walk them and renders EVERY input event with an **empty body** — even when they're fully wired. The UE log confirms it: `LogBlueprint: Warning: No then pin found on node ...EnhancedInputActionIA_*`. **Do NOT trust `read_graph_dsl` for input-event wiring.** Use `get_node_infos` (pin-level truth) — it correctly showed all six events wired to their handlers (Move/Aim/Jump/Crouch/Sprint). The input graph was 100% correct all along.

### Verify PIE is actually live before believing any "it doesn't work" test — `IsPIERunning` first
Multiple "can't move" tests were run against a **dead/stopped PIE** (`IsPIERunning` returned `false`; the `PlayerStart` billboard sprite was visible = editor mode, not PIE). Keys pressed into a stopped in-viewport session do nothing regardless of wiring. Also caught PIE ending on its own between calls. **Always confirm `IsPIERunning=true` (and ideally `CaptureEditorImage`) at the moment of the test.** PIE ending is a clean shutdown (`LogPlayLevel: Shutting down PIE online subsystems`), not a crash.

### Keypress-free runtime proof: the auto-walk probe
Can't send a hardware key from here, so to prove possession + movement pipeline work without input: temporarily add `EventTick → Pawn|Input|AddMovementInput(WorldDirection=(1,0,0), ScaleValue=1, bForce=true)`, start PIE, `CaptureEditorImage` before/after. The third-person camera follows the pawn, so the world visibly scrolls if movement works (it did — the character walked into the boxes). Delete the probe after. This isolates "does movement execute" from "does input arrive."

### Synthetic Slate keys do NOT drive Enhanced Input (`SlateInspectorToolset.PressKey`)
`SlateInspectorToolset` CAN find/focus the floating PIE window (`Windows`, `Snapshot`, `Click i6`) and inject key events, but Enhanced Input **polls the hardware input-device state**, so injected Slate `ProcessKeyDownEvent` keys never register with `IA_*` actions (no `Started`/`Triggered` fires). Useful for driving editor Slate UI; useless for testing Enhanced Input gameplay. Use the auto-walk probe instead.

### Tool quirks logged this session
- **`CaptureEditorImage`** returns a base64 PNG far over the inline token limit → it's written to a `tool-results/*.txt` file; extract the `"data":"..."` field with Python + `base64.b64decode` to a `.png`, then Read it. (`CaptureEditorImage` shows the whole editor incl. the floating PIE window — the way to "see" PIE without computer-use.)
- **`ObjectTools.set_properties`** takes `values` as a **JSON-encoded string** (not a `properties` object). Component transforms may report "could not be set" yet still apply — re-read to confirm.
- **computer-use still cannot target the editor** (confirmed again): `request_access` for `Unreal Editor`/`UnrealEditor`/`PoolHop` all fail (not Start-menu apps; fuzzy-matches "Registry Editor"). Use `CaptureEditorImage` + `SlateInspectorToolset` to observe/drive the editor instead.

## 2026-06-30 — Overnight: design package + Step 1 + MCP buildability spikes

### Project 42 (Gamma) process reaper does NOT target Pool Hop — verified, we're safe
The machine also runs a large trading-automation suite at `C:\Users\jackw\Desktop\42` with many scheduled `pythonw` daemons and a `Stop-StaleClaudeProcesses` reaper (`setup/scripts/_shared.ps1`). Hunted it per the debugging discipline (find the killer before assuming safety). **It cannot kill our work:** it enumerates only `claude/node/python/uv.exe`, and kills one ONLY if its command line references Project-42's `$WorkDir` (or the `tradingview-mcp`/`alpaca-mcp` servers) AND it's older than 5 min AND not on the daemon-exempt list. Verified our Claude session's `claude.exe` cmdlines contain no `Desktop\42` reference (`Refs42=no` for all), so `$isOurs` is false → skipped. `UnrealEditor.exe` isn't a candidate process type at all, and the unreal-mcp server is embedded IN the editor (not a separate python/node proc). A scan for a broad `Get-Process|Stop-Process` / `Stop-Process -Name` / `taskkill /IM` killer returned nothing — every Gamma killer is narrowly scoped (TradingView cleanup or its own grind shards). **Loop resilience (belt-and-suspenders):** the overnight heartbeat also relaunches `UnrealEditor -ExecCmds=ModelContextProtocol.StartServer` whenever port 8000 stops listening, so even an unrelated editor death self-heals.



### MCP buildability map — what the unreal-mcp toolset CAN and CANNOT author (spiked)
Ran probes before building Systems 2–5. Results (these bound the whole build plan):
- **CAN build via MCP:** Blueprint classes (`BlueprintTools.create`), member variables + replication (`add_variable`/`set_variable_replication` — RepNotify auto-creates `OnRep_`), function/event graphs + DSL logic (`write_graph_dsl`/`create_node`/`connect_pins`), components (`ActorTools.add_component`), actor placement (`SceneTools`), materials (`MaterialTools`), Data Assets (`DataAssetTools.create`), Data Tables (`DataTableTools`).
- **CANNOT build via MCP (need in-editor or a future tool):**
  - **Blueprint Enums / Structs** — no creation tool; `execute_tool_script` (ProgrammaticToolset) only allows stdlib modules (`json/math/re/time/datetime/copy`) and `execute_tool()`, **cannot `import unreal`**. → interim: use `byte`/`int` for enum-typed vars (see CANON.md), create real enums in-editor.
  - **RPC flags ("Run on Server / Reliable") on custom events** — `K2Node_CustomEvent`'s `FunctionFlags`/`bIsNetExecutedOnServer`/`bIsNetReliable` are not readable/settable via `ObjectTools` (`list_properties` returns only `errorMsg`, like the EnhancedInputAction node). → interim: build `Server_*` functions as **regular functions with a `HasAuthority` guard** — identical behavior in single-player (local player IS the server); mark them Run-on-Server in-editor as part of the Phase 2 netcode layer. The authority *logic + state location* is what the discipline requires now; the RPC *decoration* is the Phase-2 transport.
  - **Behavior Tree / Blackboard** — `aimodule_toolset...BehaviorTreeTools` is **inspect-only**. Step 4's `BT_Watcher`/`BB_Watcher` need in-editor authoring (or `execute_tool_script` can't help — no `unreal`). The AI *perception* + BP logic is buildable; the BT asset is not.
  - **UMG widget-tree authoring** — the `UMGToolSet` exists but adding widgets/bindings/animations to a `UserWidget` is unproven via MCP; treat Step 7 HUD as in-editor.
  - **Niagara / EQS** — unconfirmed; use material/decal + hand-authored fallbacks first.
**Takeaway for the loop:** autonomously buildable = Step 1 (done), Step 2 Loudness logic, Step 3 Pool scoring, Step 5/6 component logic (all with regular-function interim). NOT autonomously buildable = Step 4 BT, Step 7 UMG, and RPC-flagging. Those are the "needs the user in-editor" pile.

### computer-use CANNOT reach the Unreal Editor here (so no GUI fallback for the MCP-walled tasks)
Tried `request_access` for the editor with every plausible name (`Unreal Engine`, `UnrealEditor`, `PoolHop`, `PoolHop - Unreal Editor`) — all fail: the resolver only matches Start-menu-**installed** apps, and `UnrealEditor.exe` is launched via Epic/file-association, not a Start-menu entry, so the running window is unresolvable (fuzzy-matches to Registry Editor / ROSE Online). **Consequence:** the enum/struct/BT/UMG/RPC-flag tasks that MCP can't do also can't be done via GUI automation — they genuinely need the **user** in-editor. Leave a precise in-editor checklist rather than trying to force them.

## 2026-06-30 — Session: System 1 (movement)

### PIE froze at "Recreating Persistent SBTs" — template Lumen hardware ray tracing deadlocked
**Signature:** PIE starts fast and clean (`Play in editor total start time 0.149s`), then the whole editor freezes. The UE log stops dead at `LogRenderer: Recreating Persistent SBTs due to initializer changes ... NumGeometrySegments 0 → 512`. **This is NOT a resource problem** — verified: RAM had 11.8 GB free, all disks had hundreds of GB free, **0 ShaderCompileWorker** processes, main editor CPU moved only ~0.7 CPU-s over 4 s (idle, not compiling), GPU at 11% (not rendering-heavy), and `Get-Process ... Responding = True`. Idle CPU + idle GPU + frozen log = a **stall/deadlock**, not slow work. "SBT" = ray-tracing Shader Binding Table → the freeze is in **hardware ray tracing** init.
**Cause:** the ThirdPerson template ships `r.RayTracing=True` (Config/DefaultEngine.ini, `[/Script/Engine.RendererSettings]`) with Lumen (`r.DynamicGlobalIlluminationMethod=1`), DX12/SM6. Building the RT scene hung on first PIE.
**Fix:** set `r.RayTracing=False` in DefaultEngine.ini (a **startup** cvar — the editor must be restarted to apply). Lumen falls back to software ray tracing; a grey-box movement test needs no HWRT. Also repointed `EditorStartupMap`/`GameDefaultMap` to `L_Sandbox_Movement`. **Diagnosing a frozen editor:** read `Saved/Logs/*.log` mtime + tail (file read, not MCP), sample process CPU delta over a few seconds, and check GPU utilization — don't fire MCP calls that queue behind the blocked game thread. Recovery from the hang required force-closing the editor (all work was saved + committed, so no loss).


### Enhanced Input event nodes bind by creation type_id, NOT by the InputAction pin — this killed all input
**Signature:** PIE runs, pawn spawns, `Enhanced Input subsystem initialized` logs, but **no input works at all** — WASD, mouse-look, nothing. **Root cause:** all six `K2Node_EnhancedInputAction` event nodes on the character were internally bound to `IA_Move`. Earlier, when `find_node_types` only surfaced `IA_Move` (the others weren't indexed yet), I created IA_Move-typed nodes and `set_pin_value`'d their output `InputAction` pin to IA_Jump/Look/etc. **That does not rebind the event** — it only changes the pin display. Six duplicate IA_Move bindings collided and broke the entire input component.
**Tell:** `read_graph_dsl` / `get_node_infos` show every event's `type_id` as `...EnhancedInputActionIA_Move` even though the pins read different assets. The `type_id` is the truth.
**Fix:** once all input assets are saved, the Blueprint action DB refreshes and `find_node_types "EnhancedActionEvents|"` offers all six (`IA_Move/Jump/Look/MouseLook/Crouch/Sprint`). Delete the mis-bound nodes, `create_node` each with its correct `Input|EnhancedActionEvents|IA_<X>` type_id (now genuinely bound), and reconnect exec/data pins to the preserved downstream logic. Verify: node `type_id`s are now distinct actions + a clean `compile_blueprint(warnings_as_errors=true)`. Corrected the [unreal-mcp-blueprints](../.claude/skills/unreal-mcp-blueprints/SKILL.md) skill, which had recommended the broken pin-set workaround.

### A Blueprint that fails to compile freezes PIE on a modal — which freezes the MCP game thread
**Signature:** `StartPIE` never returns; every subsequent MCP call times out (they run on the game thread); the UE log stops dead right after `PlayLevel: Blueprint regeneration took N ms`. **Cause:** PIE compiles referenced/dirty Blueprints before play; a failure pops a modal (`title: Blueprint Asset Compilation Error`) that blocks the game thread. MCP can't reach a modal — a human must click it. Here it was the leftover template `BP_ThirdPersonCharacter` with 4 dangling `EnhancedInputAction None` nodes (from the 0-byte corruption). **Fix/prevent:** keep every Blueprint in the project compiling clean — even unused template leftovers. Remove dangling nodes (or delete the asset). Diagnose a wedged PIE by tailing `Saved/Logs/*.log` directly (file read, not MCP) — don't hammer MCP calls that will queue and time out.


### The initial commit shipped 18 `.uasset` files as literally 0 bytes
**Signature:** MCP `load_asset` fails with "Unable to load"; a Blueprint's Enhanced Input events show as `EnhancedInputActionNone` (dangling); the asset registry still "sees" the path (`find_assets` lists it) but it won't open. **Not** an LFS smudge failure — `git check-attr` showed the LFS filter applied, and `git cat-file -s HEAD:<file>` returned `0`, i.e. the blob itself was committed empty.
**Detect:** `find Content -name "*.uasset" -size 0`. Here it caught all 5 Enhanced Input assets, the Touch UI widgets, and Manny's material instances / textures / Control Rigs. `IA_Move` never existed at all.
**Fix:** recreate the asset via the right MCP tool (`DataAssetTools.create` for Input Actions/Contexts), set its properties, **then delete-and-recreate if `save_assets` silently no-ops** (see next lesson), and finally verify size on disk with `ls -la`.
**Prevention:** after any batch of asset creation, `save_assets([])` then `find ... -size 0` before committing. Never trust the in-editor state alone.
**Text files were hit too:** `Docs/00_README.md` was committed **truncated mid-sentence** (working copy on disk was intact) — the corruption wasn't limited to binaries. Worth a `git diff --stat` sanity pass on the whole tree, not just uassets.

### `save_assets` silently returns `true` but writes nothing for corrupted stub packages
A 0-byte package that's "loaded" in the editor won't re-serialize on `save_assets` — it returns success but the file stays 0 bytes (`is_dirty` reads false). **Workaround:** `AssetTools.delete` the stub, then `DataAssetTools.create` / `BlueprintTools.create` a fresh asset at the same path. The fresh asset saves normally. Re-point any Blueprint pins that referenced the old asset (they break on delete — see next).

### Blueprint asset references are hard, not soft — they break when you delete+recreate the target
After deleting and recreating an Input Action, the character's event nodes and the controller's `AddMappingContext` pins reverted to empty/None. **Re-set them** via `set_pin_value` with the asset path, then `compile_blueprint`. Verify with `get_node_infos` (not `read_graph_dsl` — see below).

### `find_node_types` only surfaces `EnhancedInputAction` events for actions already referenced in the graph
Freshly-created `IA_Crouch`/`IA_Sprint` weren't offered as `Input|EnhancedActionEvents|IA_*` node types, and `create_node` on those type_ids errored "does not exist" (Blueprint action DB indexing lag). **Workaround:** create the node using the one discoverable action event type (`...|IA_Move`), then repoint its output `InputAction` pin (index 8) to the target action with `set_pin_value`. Compile — exec pins still wire correctly even though the node's cosmetic title/pin-shape lags.

### `read_graph_dsl` mis-renders freshly-rewired nodes; trust `get_node_infos`
After rewiring input events, `read_graph_dsl` showed the events as empty-bodied `EnhancedInputActionIA_Move` stubs. `get_node_infos` on the actual nodes showed the connections were correct, and `compile_blueprint` (with `warnings_as_errors`) passed. The DSL reader is a lossy view for some node types — verify wiring via `get_node_infos` and a clean compile, not the DSL round-trip.

### DSL: member variables need explicit getters; latent/multi-out nodes need keyword pins
In `write_graph_dsl`, a bare `MyVar` only resolves to locals/params — a member variable must be `(Variables|Default|GetMyVar)` or you get "Undefined variable". And positional args bind in pin order, which put a value on `self` for `Character|LaunchCharacter` (self is arg 0). Use **keyword args** (`:LaunchVelocity ... :bZOverride true`) for anything past the first data pin. Always `get_graph_dsl_docs` before first use.

### `PhysicsVolume` cannot be a content-Blueprint parent
`BlueprintTools.create` with parent `/Script/Engine.PhysicsVolume` → "Cannot create a blueprint based on the class 'PhysicsVolume'." Volumes are brush actors that must live in a level. **For a water/pool volume:** `SceneTools.add_to_scene_from_class` a `PhysicsVolume` directly into the map, scale it (actor scale resizes the brush; default is 200³ so scale 4 → 800), then `set_properties` `{"bWaterVolume": true, "priority": 1}`. `CharacterMovementComponent` switches to `MOVE_Swimming` for free when the capsule center is inside.

### The ThirdPerson template level has a raised central mound — trace before placing
Ground is **not** flat z=0 everywhere: the spawn sits on a mound (~z=210 at origin, sloping down to the z=0 outer floor), and there are scattered raised platforms (found z=200 strips). **Always `SceneTools.trace_world` (start high, end below 0) at each intended placement XY before dropping geometry**, and probe the footprint corners for a large actor. `trace_world` returns the *distance* from start to hit, so ground_z = start_z − distance.

### CaptureViewport / big describe_toolset results overflow context — extract with Python
`CaptureViewport` returns a multi-MB base64 PNG inline; `describe_toolset` for `BlueprintTools` is ~72k chars. Both exceed the tool-result limit and get spilled to a file. **Handle it:** `python3` to `json.load` the saved file, `base64.b64decode` the `image.data` to a `.png` in scratchpad, then `Read` the PNG (Read renders images). For describe dumps, extract just the tool objects you need by short name. Pass `captureTransform` explicitly (the "optional" field is required in practice) and set `annotations.gridHeight` to your ground z for readable coordinate labels.

### Material-from-scratch via MCP (translucent unlit)
`MaterialTools.create_material` → `ObjectTools.set_properties {blendMode: BLEND_Translucent, shadingModel: MSM_Unlit, twoSided: true}` → `add_expression` a `Constant3Vector` (set `constant` LinearColor) and a `Constant` (set `r`) → `connect_to_output` to `MP_EmissiveColor` / `MP_Opacity` → `recompile`. Apply per-instance via the component's `overrideMaterials` array (don't `StaticMeshTools.set_material`, which edits the shared engine mesh). Disable a decorative plane's collision via `bodyInstance.collisionEnabled = NoCollision` so the player can pass through.

### Enhanced Input mapping JSON shape (for `ObjectTools.set_properties` on an IMC)
`mappings` is an array of `{action: {refPath}, key: {keyName: "..."}, triggers: [], modifiers: [{refPath: "/Script/EnhancedInput.InputModifier<X>"}]}`. WASD from one Axis2D action: bind `D` plain, `A` with `InputModifierNegate`, `W` with `InputModifierSwizzleAxis` (default order YXZ = swap X/Y), `S` with Swizzle+Negate. Set the array in one `set_properties` call — incremental array edits fail with "insertion points are ambiguous"; `reset_properties` the array first if you need to rebuild it.
