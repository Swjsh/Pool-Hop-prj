# Pool Hop — Project Instructions (CLAUDE.md)

Stylized online co-op stealth game in **Unreal Engine 5.8** (Blueprints-first, C++ only when forced). Sneak through sleeping suburbs at 2 AM, hop backyard pools for points, evade homeowners/cops, escape before dawn. Single **UE project** — no web backend/frontend/database.

**Read the design docs before making design decisions:** [`Docs/00_README.md`](Docs/00_README.md) → `01` (GDD) → `02` (MVP scope) → `03` (Tech architecture) → `04` (Research) → `05` (Market). Don't re-derive what's already specified there.

---

## Standing instructions (from the human — keep these satisfied every session)

1. **Build reusable skills as you go.** When you work out a non-obvious way to do something (especially via the Unreal MCP), capture it as a skill under [`.claude/skills/`](.claude/skills/) so the next session doesn't rediscover it. Existing skills:
   - [`unreal-mcp-session`](.claude/skills/unreal-mcp-session/SKILL.md) — **START HERE.** Orientation: connect to the MCP (+ the session-start-only gotcha), discover/call tools, verify against cold on-disk state, run/observe the game two ways, safety, the MCP buildability ceiling.
   - [`unreal-mcp-blueprints`](.claude/skills/unreal-mcp-blueprints/SKILL.md) — authoring Blueprint graphs/classes via MCP.
   - [`unreal-mcp-scene-building`](.claude/skills/unreal-mcp-scene-building/SKILL.md) — levels, actors, materials, screenshots, Play-In-Editor.
   - [`unreal-input-probe`](.claude/skills/unreal-input-probe/SKILL.md) — self-test real keyboard/mouse input + drive the in-game console via PowerShell SendInput (no MCP needed); the only honest test of hardware-path input.
2. **Keep a lessons log.** Append hard-won, non-obvious findings to [`Docs/LESSONS.md`](Docs/LESSONS.md) as you hit them — bugs, gotchas, tool quirks, what worked. One entry per lesson, newest first.
3. **Keep this CLAUDE.md current.** Update "Current state" below as build phases complete. Track new standing requests here.
4. **Commit + push after each meaningful chunk** (Epic's own MCP safety rule). Never `--dangerously-skip-permissions` while the Unreal MCP plugin is loaded. `execute_tool_script` runs arbitrary Python — treat as privileged, review diffs.

## The non-negotiable engineering discipline (Tech doc §2)

**All shared/authoritative state lives in GameMode/GameState/PlayerState — never as local variables on the character/pawn.** Score, loudness, alert level, banked points are server-owned even though we're single-player right now. The character holds only movement (via `CharacterMovementComponent`, which replicates for free) + local/cosmetic feedback. This is what makes co-op (Phase 2) a layering job instead of a rewrite. Do not shortcut it to move faster.

---

## Current state

**✅ REAL ASSET IMPORT (2026-07-03, later morning) — the "why isn't this styled" gap had a real cause: the Kenney City Kit (Suburban) was never actually downloaded despite `ASSET_CREDITS.md` claiming it was.** Verified via direct filesystem search (not by trusting the doc) that the houses were literal `/Engine/BasicShapes/Cube` actors with flat near-black materials, and the ground material's `BaseColor` was `(0.02,0.03,0.07)` — near-black, not grass-green. Fetched the real CC0 kit live from kenney.nl, imported 4 real building meshes + the already-staged-but-unused `ground_grass.fbx` from the nature kit, and replaced all 4 Maple Court houses (deleting the 28 old cube sub-pieces only after confirming the new mesh rendered correctly underneath). Fixed the grass's auto-imported material (looked uncolored) by swapping in an existing, already-correct `MI_Greybox_Grass` instance that had been built in a past session but never assigned to anything. Restored the 4 houses' warm window-glow `PointLight`s that got removed along with the old cubes. **Screenshot verification of the color fixes was unreliable due to viewport auto-exposure swinging wildly between near-identical camera setups — re-confirms a limitation this file already flagged in an earlier session; property-level reads (`get_material`/`get_vector_parameter`) are what's trustworthy for this class of check, not screenshots.** Full trail in `Docs/LESSONS.md`. Fence/driveway meshes imported but not yet placed. Scope: Maple Court only — the origin sandbox course's own ground/walls are untouched grey-box.

**✅ FIRST LIVE PLAYTEST OF THE OVERNIGHT BUILD (2026-07-03, morning) — 2 real bugs found and fixed, both red-proofed.** (1) The new main menu blocked visuals/mouse but NOT movement — `SetInputMode_UIOnly` alone doesn't stop Enhanced Input; needed `SetGamePaused(true)` too (the pause menu already had this right, the main menu was missed). Fixed + verified: `GameState.NightTimeRemaining` now stays frozen across a real 12s gap while the menu is up. (2) Walking off the street dropped the player into an infinite fall. `WorldSettings.KillZ` looked like the fix but isn't — the engine's default handler for it DESTROYS the pawn, worse than falling forever. Real fix: `BP_PlayerGameMode.CheckFallCatch`, ticked every frame, teleports the player back to the stash if Z drops below -300. Verified: teleported player to Z=-1000, confirmed auto-correction back to the stash on the next read. Full writeup in `Docs/LESSONS.md`.

**🌙 OVERNIGHT SESSION CONTINUED (2026-07-03, later) — player material re-skin, procedural crouch animation, and a fresh AI Watcher re-diagnosis, all committed. Full detail in `Docs/design/12_Overnight_Session_Plan.md` and `Docs/LESSONS.md`; short version below.**
- **Player re-skinned** (`MI_PlayerBody`, dark base + cyan-teal rim, distinct from the Watcher's near-black skin) — applied to `BP_PlayerCharacter`'s CDO, verified via property readback. The full Mixamo mesh+animation swap planned for this slot stays blocked on a missing editor Slate window (needed for the Python-console IK-retarget route); this material-only reskin is a substitute, not the full plan.
- **Crouch animation built**: new `CR_Crouch` Control Rig procedurally lowers the pelvis, wired into `ABP_Unarmed`'s AnimGraph and gated to zero outside of crouch via the node's own `Alpha` pin driven from native `bIsCrouched`. Compiles clean end-to-end. **Honest gap: the actual pose quality (does it look like a real crouch, or do the legs clip/stretch with no leg IK) was never visually confirmed** — screenshot framing at this world's scale couldn't resolve a 32-unit local pose change despite several attempts. Worth a human eyeball or a fresh close-up capture attempt before trusting it looks right.
- **AI Watcher bug re-tested fresh, finding REVERSED from last session**: previously sandbox-stuck/Maple-Court-fine; now Maple Court's Watcher is the frozen one (exact position+rotation unchanged 35+ real seconds, survived a level reload) while sandbox patrols fine. This confirms the bug is intermittent NavMesh-build-data state, not a property of one specific Blueprint instance — **don't trust either "Watcher X is broken" or "Watcher X is fixed" without a fresh check**. Still needs the human's one `Build → Build Paths` click; no MCP tool exists to trigger it (re-confirmed via a full toolset inventory scan).
- Both `CaptureViewport`-based visual verification attempts this session (player skin, crouch pose) hit the same wall: characters render too small/distant to judge fine pose/color detail at this project's world scale, even after repeated camera-math corrections. This is now a documented, recurring tool limitation (see LESSONS), not something to keep re-attempting the same way — a future visual-verification need should either get a much closer, carefully-computed shot on the first try or accept property-level verification.
- **§2 Menu system is now fully built** (all 3 sub-items: results screen, pause menu, main menu) — `WBP_MainMenu` added this session, spliced in front of `BP_PlayerController`'s existing `BeginPlay`/HUD-creation chain without disturbing it.
- **Final regression sweep done** (fresh PIE, after all of tonight's changes): player spawns, `GameState.NightTimeRemaining` ticks down normally, `MainMenuWidget` creates without error, zero `Accessed None`/error entries in `LogBlueprint`. Nothing broke.

**✅ Pause menu built (2026-07-03)** — `IA_Pause`→`Escape`, `WBP_PauseMenu` (Resume/Restart/Quit), toggled via `BP_PlayerController.bIsPaused`. Compiled clean, structurally verified; a real-keypress end-to-end test is the one thing left for a human/input-probe pass.

**✅ Loot system built (2026-07-03) — `E_LootRarity`, `DA_LootItem` + 2 instances (SpeedPants/GardenGnome), `PlayerState.CarriedLoot`/`BankedLoot`, `BP_LootPickup`, bank/drop spliced into the existing `Server_BankAtRisk`/`HandleDetain`. Pickup→carry step live-confirmed; carry→bank step has the identical fix applied but wasn't independently re-confirmed live (test-methodology flakiness, not a known code issue — see LESSONS). Drop-on-capture simplified to "loot is lost" rather than the fuller "respawns as a recoverable pickup" spec — flagged as a real follow-up.**

**✅ MCP RECONNECTED (2026-07-03, post-restart) — swim is now fully closed out, `MOVE_Swimming` confirmed live on 2 pools + a red-proofed self-heal shipped (see LESSONS top entry). Read [`Docs/design/12_Overnight_Session_Plan.md`](Docs/design/12_Overnight_Session_Plan.md) — it has STATUS markers per item, resume at the first non-`[x]` section.** `BP_PlayerGameMode.EventBeginPlay` now force-corrects any `StaticMeshActor` using the `/Engine/BasicShapes/Plane` mesh to `NoCollision` every level load, regardless of what the saved asset property says — this should make the swim-collision bug's recurrence moot even though its root trigger mechanism was never fully identified.

**🌙 OVERNIGHT SESSION, EARLIER (2026-07-03, late, pre-restart) — user asked to build out the rest of the game (swim fix, crouch/slide animation, stealth, menu, loot, skins) autonomously overnight without live playtesting.** Hit a real blocker early: the editor + MCP port 8000 were down when this Claude Code session started, so this session's `unreal-mcp` client never got a live handle to it (confirmed absent from the tool registry via multiple `ToolSearch` angles, not just a failed call). The editor was restarted and port 8000 is up, but **reconnecting needs a user-side Claude Code restart** — flagged via a desktop notification, not blocking the rest of the night's work. Re-diagnosed the user's "we can't swim" report MCP-free instead, via the standalone-probe technique (see `Docs/LESSONS.md` top entry): queried `bUseDefaultCollision`/`CollisionEnabled` directly via console `GetAll` in a fresh cold-launched process — **the historically-recurring collision root cause is confirmed correctly fixed right now** (all 5 water surfaces read `NoCollision` as they should). Full behavioral confirmation (`MOVE_Swimming` actually triggering) is still an open item pending MCP reconnection — see the plan doc §0. **Also surfaced: this file was 2 commits stale** — `d6a907d` (the slide mechanic is now BUILT: sprint+crouch triggers a real momentum-decay slide, reusing `ResolveSpeed`; vault/slide got real triggered motion via `AnimMontage`+`Montage_Play` stand-ins, `AM_Vault`/`AM_Slide`, since the AnimGraph itself is confirmed unreachable via MCP — see LESSONS) and `8864fdc` (docs-only) had landed but weren't reflected here. **Whoever picks this up next: check `git log` and `Docs/LESSONS.md` before trusting this section's account of "current" — that's exactly the drift this note is correcting.**

**✅ RESOLVED (2026-07-03) — the swim-collision bug that recurred across 3+ sessions is genuinely fixed now, root cause identified.** A second live human playtest (following the styling marathon below) immediately hit the exact same "standing on top of the pool" bug this file had repeatedly marked fixed. Root cause was NOT the `collisionEnabled` property everyone kept re-fixing (including two more attempts earlier this same session, both of which read back as successful in the editor and both of which were wrong) — it was `StaticMeshComponent.bUseDefaultCollision=True` on all 5 water surfaces, which makes the engine ignore the instance-level collision override entirely at runtime and pull from the shared `/Engine/BasicShapes/Plane` mesh asset's own `BlockAll` default instead. This is why every previous fix looked correct on inspection but silently did nothing — an editor property readback shows the instance's stated intent, not what the runtime physics body actually gets built from, and those two only converge when `bUseDefaultCollision` is false. Fixed properly this time (`bUseDefaultCollision=False` + `collisionEnabled=NoCollision` on all 5) and verified for real: started a fresh PIE session, read the component live (`NoCollision`, held this time), teleported the player into Pool_A, and confirmed `CharMoveComp.movementMode == MOVE_Swimming` directly. Full writeup in `Docs/LESSONS.md` (2026-07-03) including why this specific bug class is invisible to same-session editor verification and only catchable via a genuine fresh-PIE live read. Same playtest also reported the Maple Court AI patrol "just going down the street in a line" — confirmed true (all 6 waypoints sat in a 750-unit band along the street's centerline) and redesigned into a loop through all 4 lots' yards.

**✅ FIRST HUMAN PLAYTEST DONE (2026-07-02) — 6 real bugs found, all fixed same session; lane split going forward: functional fixes here, skinning/styling in the concurrent design chat.** The user played live and reported, in order: score-at-risk not counting, crouch not toggling + model not visibly crouching, vault spawning a "yellow spark explosion" instead of a subtle noise cue, swimming not working (player stands on top of the pool), a dark "block"/cylinder attached in the water, and a request for a bush + a bigger pool. Root-caused and fixed:
- **Swim + score (one root cause, `996d178`+`75d8473`):** `PoolWaterSurface`'s mesh had `BlockAll` collision — the player rested on top of the water instead of entering the `PhysicsVolume` underneath, which also meant they never reached the pool's scoring overlap. Fixed sandbox `Pool_A` AND all 4 Maple Court pools (which had regressed back to `BlockAll` despite this file previously claiming they were fixed — never actually verify a "fixed" note without a fresh property read). Verified live: `MOVE_Swimming` + `IndividualScoreAtRisk` climbing.
- **Costume cylinder (`261aeed`):** `DA_QuietShoes.AttachMesh` was still the raw engine `Cylinder` build-time placeholder. Cleared to `None`.
- **Vault FX (`9ca7ab8`):** `NS_NoiseRing`'s `Spawn Count` was still the RadialBurst template default (100) — never retuned down unlike its sibling `NS_PoolSplash`. Cut to 20 + shrunk max sprite size, keeping the same cyan→red loudness-driven color and outward reach (still visualizes hearing range), now reading as a ripple instead of a burst.
- **Crouch (`d522508`):** was hold-only (`Started→Crouch`, `Completed→UnCrouch`); rewired to a real toggle via a `Branch` on `IsCrouching`. Capsule shrink/restore verified live (90→55→90). **The "model doesn't visibly crouch" half is a genuine animation-pose gap** (the native capsule-shrink works; there's no crouch blend/pose in the AnimBP) — flagged for the design/anim chat, not fixable via Blueprint logic alone.
- **Stealth loudness ladder (`d522508`):** crouch and upright walking produced identical loudness (both fell to the same "not sprinting" branch). Added a `WalkBand` tier so crouch(quiet, at `IdleFloor`) < walk(`WalkBand`=20) < sprint(`SprintBand`=55) — matches both the user's ask and the design doc's own already-spec'd "crouch is intentionally silent" intent, which had never actually gotten its walk-tier counterpart built.
- **Bush (`56e75d4`):** `BP_BushHide` already existed but sat on an unrelated raised platform far from the played path; spawned a second instance directly between the vault wall and pool.
- **Pool enlargement — attempted, then correctly reverted.** A straightforward XY scale-up mechanically worked, but a bounds check showed the new footprint would overlap both the just-placed bush and the vault wall. Reverted rather than ship a new collision bug chasing a "would be nice" ask; a real fix needs a layout pass (repositioning neighbors), not an in-place resize — flagged as a follow-up, not done.

**Two new MCP-safety patterns earned the hard way this session (see LESSONS + the blueprints skill for the full writeup):** `get_node_type_pins()`'s auto-created preview node is NOT safe to wire into (can corrupt the whole graph's compile with a mismatched-outer `ensure`) — use explicit `create_node` for anything you'll connect to. And the zero-disk-risk recovery for a hard compile failure on an unsaved asset: `EditorLoadingAndSavingUtils.reload_packages([pkg], ASSUME_POSITIVE)` discards in-memory corruption and reloads clean from the last save, verified via a subsequent clean compile + unchanged `get_node_infos`.

**✅ MAPLE COURT LOTS A/B/C DRESSED (2026-07-02, `32cdfeb`+`17a47b8`) — fence, shed, pool chairs, and corner trees, per direct user request after the first playtest.** The user asked for a house+pool+yard layout (fence around the pool, pool chairs, a shed behind the house near the pool, corner trees to hide behind) replicated 2-3 times for "a true playtest, not just this very generic one we have now" — they'd only played the small sandbox course and hadn't seen Maple Court yet. Rather than build a parallel system, reused Maple Court's existing 4-lot infrastructure (houses+pools+patrol+scoring already wired). Built a generalized Python dressing function that derives fence/shed/chair/tree size and position from each lot's OWN pool water-surface extent + house-vs-pool Y orientation (the 4 pools are actually different sizes — score-tier variants — and Lot C's house sits on the opposite side of its pool from A/B, auto-detected and mirrored correctly). Dressed Lots A, B, C; **Lot D deliberately left alone** — it already has its own bespoke patio-wall-ring sightline puzzle from earlier work, and a redundant fence would clutter that design. New material: `MI_Greybox_Foliage` (green-toned `M_GreyboxToon` instance, for tree canopies). Verified via visual capture (all 3 lots read as coherent) + numeric bounds-checks against pre-existing geometry (houses, hedges, fences) — zero overlaps. **The user should now playtest Maple Court, not just the sandbox** — tell them how to get there (it's offset +30000 on the X axis within the same `L_Sandbox_Movement` level, reachable by walking or by a dev teleport).

**🔬 FLAGGED, NOT BUILT (2026-07-02) — a sprint-to-crouch SLIDE mechanic, at the user's explicit request but with their own hedge ("I don't know if that's necessary").** Ask: sprint+crouch → slide with momentum decay into a crouched stop; sprint+jump+crouch (mid-air) → a bigger slide from the extra momentum. This is real gameplay-feel work (a distinct movement state, velocity-preservation math in `CharacterMovementComponent`, capsule handling during the slide) that ALSO needs its own animation — the same gap already flagged for the design/anim chat on crouch (a real crouch pose was never built) and vault (no vault animation either). Building this alongside the vault/crouch animation work (rather than as an isolated Blueprint-logic pass) is the efficient path once the design chat has a character properly skinned and animated to slide. Not started — needs a deliberate design/scoping pass, not a bolt-on.

**✅ FIXED (2026-07-02, PM) — the user-reported "black screen / can't see the game" was TWO stacked lighting-authoring bugs in saved level content, now fixed and verified visually.** (1) The unbound night PP volume had a degenerate exposure lock (`AutoExposureMinBrightness=0` AND `MaxBrightness=0` both overridden, on Manual −1EV metering) crushing the whole exterior to pure black; (2) `BP_Moon`'s light color was the `nightBlueKey` palette token verbatim (0.04,0.07,0.16) — a surface color misused as a light color, starving the scene (~0.3 effective lux). Fixed: locked Histogram exposure at EV100 0/0 + bias 0; moon = nightBlueKey hue at full brightness (0.25,0.44,1.0) × intensity 3.0. **Verified with real full-color captures** (sandbox night, Maple Court overhead, home-base room — saved in `Saved/Screenshots/ForJack/` for the user). **Two doctrine corrections that come with this (see LESSONS top entry):** (a) `CaptureViewport` with an explicit transform renders fine regardless of OS focus — the "editor renders black when not OS-foreground" claim only ever applied to `CaptureEditorImage` window grabs, so **visual verification is un-blocked for all future sessions**; (b) `CaptureAssetImage` thumbnails can be stale-cached — not evidence of live render health. Separately re-confirmed the same session: **AI Watcher movement is STILL broken after a fresh editor restart + on-load nav rebuild** (both Watchers zero-velocity at spawn with healthy brains, patrol points gathered, AlertState ticking) — the `Build → Build Paths` human click flag below stands.

**🏁 STYLING/PHASE-4 LANE AUDIT: no further autonomous work found (2026-07-02) — read this before spending a cycle re-deriving it.** Cross-checked `Docs/design/08_Implementation_Roadmap.md` (the actual step-by-step build plan) and `Docs/design/09_Design_Review_Punchlist.md` (superseded, but Section C lists genuinely-still-open gaps) directly against this file's per-system status, after several prior cycles of granular bug-hunting (collision/material sweep, orphaned-node cleanup, NS_NoiseRing color lerp) had already closed out everything smaller. Findings: **(1)** Steps 0–7 + Phase 4 (home-base, Maple Court) are built, with every MCP-buildability gap resolved via a documented substitution (see CANON's "Known MCP buildability gaps") — no undone item in this range is actually MCP-buildable-but-skipped. **(2)** Step 8 ("Tune & playtest") is explicitly a **human-only** step (`08` §Step 8: "Human Play-In-Editor (MCP can't inject input)... get 2-3 friends to play") — this is the loop's own designed stopping point for autonomous work, not a gap. **(3)** Phase 4's own DoD ends in "Human feel-pass against `Docs/02` §4" — same human-only gate. **(4)** Phases 5/6 (cop, Grotto, loot, Substrate/look-dev) are explicitly gated behind Step 8 by the roadmap itself ("Do not start ahead of Step 8") — starting them now would violate the doc's own build-order discipline, not fill a gap. **(5)** The punchlist's remaining genuinely-open items (Section C: no audio spec, no results screen, no second neighborhood) are each independently confirmed out-of-MVP-scope — audio explicitly: `Docs/02_MVP_Vertical_Slice.md` line 67 lists "❌ Audio polish" under **excluded** scope, not a requirement. **(6)** Attempted a fresh visual screenshot sanity-check of Maple Court as a non-property-based verification method — blocked by the same "editor renders black when not OS-foreground" limitation already in LESSONS (not a new finding, confirmed again). **What would create new work:** the human resolving any of the 3 flagged blockers below (NavMesh Build Paths click, IK Retargeter build, or a live Play session that finds a bug) — until then, treat this lane as functionally complete and don't re-run this same audit; check `git log` for what's changed since 2026-07-02 first.

**✅ RESOLVED (2026-07-02, ~07:15) — editor closed, then came back up, and this session's MCP access came back WITH it — no Claude Code restart needed.** Historical trail: the editor cleanly shut down (not a crash — see the superseded note this replaces, preserved in git history), this session correctly stood down rather than unilaterally restarting it, the human restarted it twice (first without the MCP flag — port 8000 stayed down — then with it), and the moment port 8000 came back up (caught via a persistent `Monitor`), this SAME already-running session's next `unreal-mcp` tool call just worked. **This overturns the long-standing "no in-session reconnect" doctrine** — see `Docs/LESSONS.md` (2026-07-02, "MAJOR CORRECTION") and the corrected `unreal-mcp-session` skill §1 for the full trail and the important scope limit (this proves an *externally*-triggered restart can reconnect; a *deliberately self-triggered* restart, e.g. for the MegaLights test below, is still untested — don't over-generalize this into "restart freely now").

**✅ THE FULL STEALTH LOOP IS FUNCTIONAL (2026-07-02, PM) — catch/detain now works too: patrol → detect → chase → catch → detain → at-risk lost → respawn at stash → Watcher resumes patrol, all verified live in PIE.** The old "caught/detain unreachable, needs a human design decision" gap is closed: catch now fires on `AlertState != Unaware` + distance < the profile's own `CatchRadius` (120), sight-independent — necessary because AISense_Sight goes blind at point-blank range (reproduced: `bCanSeePlayer=false` with capsules touching), so any sight-gated catch can never fire at contact. Implemented in both `BP_WatcherController`/`_MC` TickBrains (chase acceptance also tightened 55→40 so stop distance ~108 < 120). See LESSONS for the DSL structuring gotcha that cost one iteration (an `IsValid`-in-`and` gate before the dispatch silently froze patrol; the working shape gates AFTER the dispatch with the impure cast as the null guard).

**✅ RESOLVED (2026-07-02, PM) — AI WATCHER FULLY ENABLED, ZERO HUMAN CLICKS: both Watchers verified patrolling full loops + chasing + investigating live in PIE.** The "one human click" framing below was wrong — it was THREE stacked causes: (1) the map's RecastNavMesh was a stale non-WP actor a World Partition runtime world refuses to register (fixed: fresh actor + `bIsWorldPartitioned=true` + WP nav chunks built by robot-clicking Build → Build Paths via SlateInspector); (2) all 10 patrol points floated 90-100uu above ground, beyond the agent's 50uu vertical projection extent, so patrol goals never projected (fixed: ground-traced all to z=ground+10, relocated 2 sandbox points off raised/wall geometry); (3) the arrive-advance radius (80) was smaller than the physical stop distance (accept 60 + capsule ≈ 84-94), parking Watchers at their first point forever (fixed: 150). Both TickBrains rewritten clean in the process (the sandbox one carried ~200 dead nodes from prior DSL rewrites). Three new MCP capabilities proven en route — editor menu clicking, console-command box access, self-triggered editor restart+reconnect — see LESSONS top entry. **Superseded historical flag follows:** *(original text below preserved for the record)* — needs the HUMAN: AI Watcher movement is broken project-wide, root cause found, fix is one editor click. Confirmed via an isolated MCP test (see LESSONS "regression ROOT CAUSE FOUND") that `AIController.MoveToLocation` returns `Failed` even called directly with a valid, in-bounds destination — this is a NavMesh build-data problem (tile data likely never generated for this level), not a Blueprint logic bug. Two MCP-side fix attempts (`runtimeGeneration=Dynamic`, `bForceRebuildOnLoad=true`) did not resolve it, and MCP has no tool to trigger `Build → Build Paths` or read tile/build-status to confirm. **If the user is reading this:** open the editor, toggle nav mesh display (`P` in the viewport, or the Navigation show-flag) to visually confirm it's empty over the walkable area, then `Build → Build Paths` (or `Build → Build All`) once — very likely fixes both Watchers' patrol/chase movement in one action, unblocking Step 4/5's remaining "can't reach the player" gap too. This does NOT require an editor restart (unlike the MegaLights item below), so it's safe for an agent to ask the user to do live, or for the user to do themselves at any point without disrupting an in-progress autonomous run. **Independently re-confirmed (2026-07-02, second session):** a fresh 20s wall-clock test reproduced exactly zero movement on both Watchers, and three more config-level culprits (`bGenerateNavigationOnlyAroundNavigationInvokers`, `NavigationSystemModuleConfig.bStrictlyStatic`, ground geometry's `bCanEverAffectNavigation`) were all checked and ruled out — see LESSONS. This is not a config an autonomous session is missing; it needs the actual editor Build Paths action.

**🔬 FLAGGED, NOT YET EXECUTED (2026-07-02) — needs the HUMAN, not just any autonomous session: re-test whether `Config/DefaultEngine.ini`'s Lumen/MegaLights/VSM/reflections strip is still necessary.** Strong evidence (see LESSONS 2026-07-02) it was based on the same debunked long-window frame-counter measurement as the AnimClass=None "frozen" saga below, not a genuine Blackwell perf problem. Unblocks two real Phase 4 visual-quality items: MegaLights (a config toggle, not a download — `01_Art_And_Style_Direction.md` §6 designs Maple Court's whole readability system around it) and the Satara night HDRI (`01` §5 — **already staged on disk** at `_ThirdPartyStaging/PolyHaven/satara_night_4k.exr`, not a pending download). **Why this can't just be picked up by "a session with a full turn budget":** testing needs an editor restart, and this whole multi-hour cron-recurring `/loop` run is ONE continuous Claude Code session from the MCP's perspective — per the `unreal-mcp-session` skill, there is no in-session reconnect, so restarting the editor would kill MCP access for the **entire rest of this autonomous run** (not just the current turn), recoverable only by the user manually restarting Claude Code. That's exactly the kind of hard-to-reverse, blast-radius-beyond-one-turn action that needs the user's awareness, not a unilateral autonomous call. If the user is reading this: the test plan is ready to go (LESSONS 2026-07-02, 5 steps) whenever you're at the keyboard to restart the editor yourself, or explicitly greenlight an agent to do it.

**⚠️ (SUPERSEDED / historical — wrong theories preserved for context; see the ✅ RESOLVED note below.) 2026-07-01 PM — runtime "can't move" / freeze on Play.** Diagnosed as far as possible **blind**: the unreal-mcp connection dropped when the editor was closed and could not be re-attached this session, so there was no way to *see* the running game. Established (reliable): all input/movement/mesh Blueprints are correct — verified via `get_node_infos`, the runtime log line `CTRL BeginPlay - IMC_Default APPLIED`, an `IMC_Default` dump confirming WASD→IA_Move, and an **auto-walk probe (EventTick→AddMovementInput) that visibly moved the pawn** in captures. The invisible-mesh + input-focus fixes are committed. GPU is **healthy** (`nvidia-smi`: full 2790/3090 MHz, unthrottled, 65% util, 55 °C, 154/360 W) — *not* power/thermal. Yet the user's Play still reads as frozen / can't-move. Log frame-counter *suggested* ~1-2 fps but is unreliable (sparse logging). Leading hypothesis: **RTX 5080 (Blackwell) + UE 5.8 render/shader bottleneck** — GPU waits on the render thread, and `r.D3D12.PSO.DiskCache=0`. Committed a lightweight-render profile (`788a0ba`: Lumen/MegaLights/VSM/volumetric clouds off) — did **not** resolve it; DX11 was also slow. **NEXT (needs live editor / MCP):** `StartPIE` in `PlayMode_InEditorFloating` (the one mode that rendered + moved fine when driven), then `CaptureEditorImage` a `stat unit` overlay to read Frame/Game/Draw/GPU ms and pin the real bottleneck. Gotchas found: in-viewport PIE throttles to a few fps when its window loses focus; standalone `-game` windows exit on focus loss; changing any render cvar forces a full shader recompile (slow first play). Driver 610.62 is already the latest per the user.

**UPDATE (2026-07-01 later PM) — ROOT-CAUSE asset corruption found + FIXED (the user pointed at the load errors).** The log showed `SK_Mannequin` (skeleton) and `PA_Mannequin` (physics) **MISSING**, plus **13 zero-byte `.uasset`** (Manny/Quinn materials, textures, rigs, touch UI). The character mesh `SKM_Manny_Simple` (assigned to `BP_PlayerCharacter.CharacterMesh0`) therefore had **no skeleton = broken character** — a strong candidate for the runtime "freeze"/instability. **Restored the entire pristine `Mannequins/` tree + `Input/Touch/` from `UE_5.8/Templates/TemplateResources/High/{Characters,Input}/Content/`** → project now has **0 zero-byte uassets**; skeleton/physics/materials/textures + a full anim set incl. **`ABP_Unarmed`** are present (commit after `788a0ba`). Also registered `GameFeatureData` in `Config/DefaultGame.ini` `[/Script/Engine.AssetManagerSettings]` to kill the startup "Add entry to PrimaryAssetTypesToScan?" dialog. **NEXT SESSION (MCP now reconnects — editor is up clean on port 8000): (1) assign `ABP_Unarmed` (or author a proper mannequin AnimBP) to `BP_PlayerCharacter.CharacterMesh0` AnimClass so Manny animates; (2) `StartPIE` `PlayMode_InEditorFloating`; (3) `CaptureEditorImage` a `stat unit` overlay to read the real Frame/Game/Draw/GPU ms; (4) confirm movement on screen. Re-test whether the repaired skeleton alone resolves the "can't move"/perf symptom before chasing the RTX50 render angle again.**

**✅ RESOLVED (2026-07-01 late PM — MCP reconnected, diagnosed live). The game was NEVER frozen — it runs at ~120 fps.** The entire perf/throttle narrative above was wrong. Measured live: two tool calls 0.379 s apart advanced the frame counter `844→889` = **119 fps** (confirm with tight, <1 s samples to avoid the counter's 1000-wrap ambiguity). **The "freeze" was a motionless A-pose statue:** `BP_PlayerCharacter.CharacterMesh0.AnimClass` was `None`, so Manny stood perfectly still — a 120 fps game showing a rigid, non-idling mannequin looks *identical* to a frozen frame. **FIX (commit `d4073d2`):** assigned `ABP_Unarmed` to `CharacterMesh0.AnimClass`; verified live — the pose changed A-pose→relaxed idle and the character pixels move frame-to-frame (~4800 px/frame), i.e. he's now visibly breathing/animating and will walk when moving. **Everything else verified working:** GameMode spawns+possesses `BP_PlayerCharacter_C` (found the live PIE pawn), controller BeginPlay applies `IMC_Default`→`SetInputMode_GameOnly` (runtime mouse-capture proves the IMC is live), `IA_Move` `Triggered`/`ActionValue` wired to the Move function (`get_node_infos`). `bThrottleCPUWhenNotForeground` is currently `False` (fine). **The one link MCP cannot self-test:** a *hardware* WASD press (Enhanced Input ignores synthetic keys; computer-use can't target the UE window — `request_access` returns `notInstalled`). If real WASD still doesn't move him despite the game being visibly alive, the only remaining suspect is **PIE keyboard focus in "Play in Selected Viewport"** — click once in the game view, or Play ▾ → "New Editor Window." **Measurement rule (LESSONS):** confirm fps via <1 s frame-counter delta OR frame-to-frame pixel diff *before* any perf theory.

**✅ RESOLVED (2026-07-01 3 PM session, commit `f5c714e`) — the "game ignores my keyboard/mouse" bug is FIXED; input verified end-to-end with real OS-level input.** The "PIE keyboard focus" suspicion above was wrong. **Root cause: the on-disk `IMC_Default`/`IMC_MouseLook` assets had EMPTY `Mappings` arrays** — the WASD/look/jump bindings authored via MCP existed only in that editor session's memory (silent-save failure); every cold-from-disk launch (the user's every Play press) had zero key bindings while the world ran fine at 120 fps. Proven WITHOUT MCP (new [`unreal-input-probe`](.claude/skills/unreal-input-probe/SKILL.md) skill): standalone `-game` + **PowerShell SendInput** (OS-level input DOES drive Enhanced Input — unlike synthetic Slate keys) + GDI window screenshots + in-game console `GetAll InputMappingContext Mappings` (showed both IMCs loaded-but-empty). **Fixes:** (1) restored pristine template `IMC_Default`/`IMC_MouseLook`/`IA_{Move,Look,MouseLook,Jump}` (same `/Game/Input/` package paths) from `TemplateResources\High\Input\Content\`; (2) engine-level `DefaultMappingContexts` (IMC_Default P0, IMC_MouseLook P1) in `Config/DefaultInput.ini` `[/Script/EnhancedInput.EnhancedInputDeveloperSettings]` — every local player gets the contexts with zero Blueprint dependency. **Verified on a cold standalone launch: WASD run (run anim plays), mouse-look camera, Space jump — all work.** Known remainder: `BP_PlayerController`'s BeginPlay chain still never executes from a cold load (stale saved bytecode — no log line, no script errors; harmless now that config applies the contexts). **NEXT MCP SESSION: (1) re-add Crouch (`C`→IA_Crouch) + Sprint (`LeftShift`→IA_Sprint) mappings to the restored IMC_Default via `set_properties`, then verify ON DISK via cold standalone + `GetAll InputMappingContext Mappings` (never trust a save-success again); (2) recompile+resave BP_PlayerController (or strip its now-redundant AddMappingContext in favor of the config route); (3) sweep every `_Project` Blueprint for the same stale-bytecode disease with a cold-load smoke test.**

**✅ RESOLVED (new session) — Crouch/Sprint re-added + verified on cold disk.** Added `C→IA_Crouch` and `LeftShift→IA_Sprint` to `IMC_Default` (property is actually `defaultKeyMappings`, not the always-empty legacy `mappings` — see LESSONS), saved (file grew 7787→8773 B). Verified on a genuinely fresh standalone process via `GetAll InputMappingContext DefaultKeyMappings` (the corrected console command — LESSONS explains why the old `...Mappings` command always reads empty regardless of asset health): both new mappings present alongside the original WASD/Jump/Look bindings. `IMC_MouseLook` also confirmed intact.
**⚠️ Still open (non-blocking): `BP_PlayerController`'s BeginPlay chain still does not fire on a cold load.** Ran `compile_blueprint(warnings_as_errors=true)` — clean, no errors — but `save_assets` produced **zero file diff** (git shows no change, mtime unchanged since the last commit), meaning nothing was actually dirtied/rewritten. Confirmed the underlying symptom is unchanged: the `"CTRL BeginPlay - IMC_Default APPLIED"` print did not appear in either fresh standalone log from this session's verification runs. **Not a blocker** — the engine-level `DefaultMappingContexts` config (Config/DefaultInput.ini) applies the IMCs independently of this Blueprint's own BeginPlay logic, and that path is what's actually verified working (WASD/Crouch/Sprint/Jump all confirmed above). Root cause not yet found; a future session should inspect why this BP's on-disk bytecode doesn't execute BeginPlay from a cold load (the graph has an `Event BeginPlay` + a `CreateWidget` call + subsystem lookups — worth `get_node_infos` on the full chain) rather than re-attempting a no-op recompile.

**Phase 1 — Systems Sandbox (MVP).** Build order per [`Docs/02_MVP_Vertical_Slice.md`](Docs/02_MVP_Vertical_Slice.md) §5. Build **only** the current system; park everything else.

- **System 1 — Player Controller & Movement** — ✅ **built + verified** (as far as possible without a human keypress).
  - Core framework: `BP_PlayerGameMode/GameState/PlayerState/PlayerController` (`_Project/Core`), `BP_PlayerCharacter` (`_Project/Characters`, from the ThirdPerson template for working camera/anim).
  - Movement verbs on `BP_PlayerCharacter`: walk, crouch (`MaxWalkSpeedCrouched`), sprint (`WalkSpeed`/`SprintSpeed` → `MaxWalkSpeed`), jump, vault (`TryVault` trace + `LaunchCharacter` gated before Jump), swim (level `PhysicsVolume` `bWaterVolume=true` + water surface).
  - Test map `L_Sandbox_Movement`: flat grey-box course (vault wall, crouch doorway, swim pool), GameMode override → `BP_PlayerGameMode`.
  - **Input fully repaired** — two bugs fixed (both in LESSONS): the template's input assets were committed 0-byte, and the six input events were all mis-bound to IA_Move (a `set_pin_value` on the InputAction pin does NOT rebind — must delete+recreate with the correct `Input|EnhancedActionEvents|IA_X` type). Now each event binds its correct action (verified via `get_node_infos`).
  - PIE verified clean: spawns `BP_PlayerCharacter`, `MaxWalkSpeed=600` from BeginPlay, `MOVE_Walking`, no hang.
  - **Movement PROVEN working (2026-07-01).** The user's persistent "can't move" was NOT input — it was that the character's `CharacterMesh0` had `SkeletalMeshAsset = None` (Manny invisible), so walking on a featureless grid was imperceptible + read as "frozen." **Fixed:** assigned `SKM_Manny_Simple` → Manny now renders (grey, materials still 0-byte; A-pose, no AnimBP). Proved movement keypress-free via an auto-walk probe (EventTick→AddMovementInput → the pawn visibly walked into the boxes). All six input events confirmed correctly wired+bound via `get_node_infos` (NOT `read_graph_dsl` — it can't traverse Enhanced Input `Triggered` pins and falsely reports every event empty; see LESSONS). Controller adds `IMC_Default` on BeginPlay (graph confirmed). A live hardware WASD press is the only thing MCP can't self-drive (Enhanced Input ignores synthetic Slate keys). **AnimBP now assigned** (`ABP_Unarmed` → `CharacterMesh0.AnimClass`, commit `d4073d2`) — Manny idles/animates instead of standing in a frozen A-pose (this was the real cause of the "it's frozen" reports; see the ✅ RESOLVED note above). **Remaining polish:** fix the 0-byte `MI_Manny_*` materials (grey is fine for the sandbox).
- **Step 1 — Authoritative framework state** — ✅ built. 16 server-authoritative vars on `BP_PlayerGameState`/`BP_PlayerState` (canonical names per `Docs/design/CANON.md`), correct replication, compiled clean.
- **Step 2 — LoudnessComponent** — ✅ **finished this session** except `ReportNoise` (deferred to Step 4, needs an AI to hear it). `BP_LoudnessComponent`: `CurrentLoudness` (RepNotify) + tuning, `AddLoudness`/`GetLoudness01`, `TickLoudness` (asymmetric rise/decay, pre-existing), plus new `Server_ReportAction`/`Server_SetSustainedSource`/`GetActionInstantBump`. **The `DT_LoudnessActions` DataTable can't be built** — `DataTableTools.create` requires an *existing* row struct, and creating a new Blueprint struct (`S_LoudnessAction`) is confirmed not possible via MCP (same wall as the enums). Substituted a Blueprint if/elif lookup function (`GetActionInstantBump`) with the same starter values from `06 §2.6` — swappable for a real DataTable later once the human creates the struct (add to the in-editor checklist). Wired on `BP_PlayerCharacter`: Sprint Started/Completed → `Server_SetSustainedSource("Sprint", …)`, `TryVault` success → `Server_ReportAction("Action.Vault")`, new `EventOnMovementModeChanged` → `Server_SetSustainedSource("Water", …)` on entering/leaving `MOVE_Swimming`. **Verified live in PIE** (swim path): teleporting into the pool's water sets `bIsInWater=true` and `CurrentLoudness` climbs to `WaterBand` (45); teleporting out resets both correctly. Sprint/vault are graph-verified (`get_node_infos`) but not yet exercised with real input — good candidates for the next `unreal-input-probe` pass.
- **Step 3 — Pool scoring + banking** — ✅ **built + wired + verified live in PIE (new session).** `BP_PoolVolume` (`ReceiveActorBeginOverlap`/`EndOverlap` → occupancy + `PoolScoreComp.Server_EnterPool`/`Server_ExitPool`; `ReceiveTick` → half-life decay/recovery of `CurrentDecayMult`; `GetEffectiveScorePerSecond`/`GetCrewMultiplier` functions). `BP_PoolScoringComponent` (`ReceiveTick` → per-tick accrual via `GameMode.GetHopStreakMultiplier`/`Server_AddScore`; `OnRep_bIsScoring` broadcasts a new `OnScoringStateChanged` dispatcher for the future HUD). Scoring rules on `BP_PlayerGameMode`: `Server_AddScore`, `GetHopStreakMultiplier`, `Server_BankAtRisk`, `Server_LoseAtRisk`, `Server_RegisterPoolVisit` (new, tracks "is this PoolID new" via a new `PlayerState.VisitedPoolIDs` array — see CANON's "Step 3 variable additions"), `Server_OnReachStash`. New `BP_StashZone` actor (overlap → bank). One pool (`Pool_A`) + one stash zone placed in `L_Sandbox_Movement`, co-located with the existing swim `PhysicsVolume`. **Verified via live PIE**: teleported the pawn into the pool (`ActorTools.set_actor_transform` — spawn-transform override fails on collision, but a post-spawn teleport correctly re-triggers overlaps), watched `PlayerState.IndividualScoreAtRisk`/`GameState.TeamScoreAtRisk` climb in lockstep, teleported to the stash, confirmed bank correctly zeroes at-risk and credits banked on both PlayerState and GameState. **Caught and fixed a real bug in the process** (not just a "looked right" bug — a live-PIE-only catch): pure Get nodes re-evaluate at each use, so reading-then-resetting-then-re-reading the same variable across a function silently used the POST-reset value for later consumers; see LESSONS "pure node re-evaluation" entry — this is now a documented Blueprint-authoring gotcha in the `unreal-mcp-blueprints` skill. **Not yet done (sandbox-specific; B/C/D variants exist in Maple Court instead — see below):** pools B/C/D in this map specifically, `Server_LoseAtRisk` has the same fix applied as `Server_BankAtRisk` but isn't independently exercised yet (no catch/detain mechanic exists until Step 4/5). **`PoolID` on `Pool_A` — resolved.** The "can't be set on a placed instance" limitation was a missing `Instance Editable` flag on the class, fixed and verified on Maple Court's Pool B in an earlier session (LESSONS 2026-07-01) — but the original sandbox `Pool_A` instance itself was never revisited afterward and was still reading `PoolID="None"` until this session, when it was set to `"Pool_A"` and verified via readback. **Water surface material — resolved (2026-07-02).** A material-consistency sweep across all 5 pools (found via `find_actors` with an `actor_type` class filter, not name-substring — the name search alone missed `Pool_A` since its Label doesn't literally contain "PoolVolume") found `Pool_A`'s water surface was the ONE holdout still on the pre-art/theme-pass `M_WaterPlaceholder`, while all 4 Maple Court pools correctly use `MI_Greybox_Pool`. Swapped to match; verified via property readback. **⚠️ Potentially significant fix, needs a real-movement playtest to confirm (2026-07-02): the same sweep found all 4 Maple Court water surfaces had blocking collision (`collisionProfileName=BlockAll`) while `Pool_A`'s surface correctly had `NoCollision`.** Each plane sits at a flat Z well inside its pool's basin bounds (e.g. `Pool_MC_A` basin Z:[-88,168], water plane at Z=20) — squarely in a player's entry path, not off to the side. This plausibly explains why every pool-scoring verification this whole session used `set_actor_transform` (teleport, which can bypass collision resolution) rather than real movement: the scoring LOGIC was genuinely proven correct, but a real player walking/swimming toward a Maple Court pool may have been physically blocked by its own water surface the whole time. Fixed by matching `Pool_A`'s confirmed-correct `NoCollision` config on all 4. **Could not live-verify the before/after difference with real movement** without touching the excluded `BP_PlayerCharacter` — this is a pattern-match fix backed by the project's own documented recipe (`unreal-mcp-scene-building` skill: water surfaces get collision disabled), not something confirmed via an actual playtest. **The movement/gameplay lane or the human should confirm Maple Court pool entry now works** as part of any next real Play session.
- **Step 4 — AI Watcher (detection + movement)** — ⚠️ **movement regression: ROOT CAUSE FOUND, needs ONE HUMAN CLICK to fix (2026-07-02) — see LESSONS "regression ROOT CAUSE FOUND" entry for the full evidence chain.** Confirmed via an isolated test (a temporary custom event calling `AIController.MoveToLocation` directly with a hardcoded, valid, in-bounds destination, completely bypassing `TickBrain`) that **`MoveToLocation` returns `Failed`** — this is a NavMesh build-data problem, not a Blueprint logic bug. `SupportedAgents` config matches exactly, bounds volumes geometrically cover the area, `runtimeGeneration=Dynamic` and `bForceRebuildOnLoad=true` are both set (tried as fixes, neither resolved it) — the remaining explanation is that navigable tile data was simply never generated/baked for this level, and MCP has no tool to trigger `Build → Build Paths` (the editor action that would fix this) or to even read tile-count/build-status to confirm. **Next session with the human: open the editor, toggle nav mesh display (`P` in viewport or the Navigation show-flag) to visually confirm it's empty, then `Build → Build Paths` once — very likely fixes AI movement across both Watchers project-wide in one action.** Perception (below) was NOT re-verified this pass — only movement was tested via this method; it may still be fine, per earlier live-PIE checks. **Original entries, unverified as of the flag above:** ✅ **perception + movement both confirmed working.** `BT_Watcher`/`BB_Watcher` confirmed not MCP-buildable (inspect-only toolset, no Blackboard creation tool) — substituted a Blueprint state machine: `BP_WatcherController.TickBrain` (detection meter fill/decay + Unaware/Suspicious/Alert transitions + per-state movement dispatch, called every tick) replaces the BT; see CANON's "Step 4 substitution" for the full mapping. Built: `DA_AIPerceptionProfile`/`AIP_WatcherProfile` (shared tuning Data Asset per `04 §8`'s own reuse intent), `BP_WatcherController` (AIPerceptionComponent with Sight 1400/1800/35° + Hearing 1200 configured directly via `ObjectTools.set_properties` on the instanced sense-config sub-objects — a newly-confirmed MCP capability), `HandlePerceptionUpdate` (tag-filtered, HasAuthority-guarded sight/hearing handling), `BP_WatcherCharacter` (Manny placeholder + `ABP_Unarmed`, `AIControllerClass`/`AutoPossessAI` set). Player tagged `"Player"` + `AIPerceptionStimuliSourceComponent` added. One Watcher + 4 patrol `TargetPoint`s placed in `L_Sandbox_Movement`. **Verified live in PIE**: teleporting the player into the Watcher's cone produces `bCanSeePlayer=true`, `DetectionMeter=1.0`, `AlertState=2` (Alert), correct `TargetActor` lock — the full sight→detection→alert pipeline works, server-authoritative, exactly on-spec. **✅ Movement NOW WORKS** (status change from prior "blocked" — the `SupportedAgents` config fix from the previous session took effect after an editor restart between sessions): the Watcher was observed live, patrolling and moving from its spawn point on its own. Do not re-investigate the old NavMesh issue — see LESSONS. **Not built yet**: EQS search (spec allows deferring this). **The cosmetic pawn `AlertState` mirror is now built** — see the readability-layer entry below (`WBP_WatcherStateIcon`).
- **Step 5 (AlertDirector, sensor light, caught/escape/heat)** — 🔨 **heat/alert system fully verified working; caught/detain built correctly but currently unreachable in play (design gap, not a bug).** `BP_AlertDirectorComponent` (attached to `BP_PlayerGameMode`): heat accrual from player loudness + decay (`TickHeat`), detection-driven heat bumps via a poll-based watch on the Watcher's `AlertState` (`PollWatcherAndPush` — see CANON's "Step 5 substitution" for why poll-not-push was necessary), pushes `GameState.NeighborhoodHeat`/`AlertLevel` from thresholds. **Both heat-bump tiers (Suspicion +8, Alert +25) verified live and match spec exactly** — an earlier "wrong magnitude" reading was pure measurement error (decay racing the MCP test round-trips, not a code bug — see LESSONS). `BP_WatcherCharacter` overlap → `GameMode.HandleDetain(PS, RespawnDelay)` when `AlertState==Alert`: sets `bDetained`, `Server_LoseAtRisk`, resets the Watcher's own meter/state, delays via `SetTimerByFunctionName` (Unreal's `Delay` node isn't MCP-creatable — see LESSONS) then teleports to the stash and clears `bDetained`. **`HandleDetain`/`FinishDetain` are graph-verified correct, but the catch can never actually fire yet**: fixed one real bug this session (chase `AcceptanceRadius=120` was larger than the physical catch range, stopping the Watcher ~50 units short — lowered to `55`), but even after that fix the Watcher's sight breaks at close range during the final approach, dropping `AlertState` before contact. **This is a real AI-perception/hysteresis design gap needing a human decision — see LESSONS for 3 candidate fixes.** `BP_SensorLight` (box trigger + spotlight, off by default) placed in the sandbox: player overlap alerts the nearest Watcher (`HeardNoise`/`LastKnownLocation`) and bumps heat — **all effects verified working live, INCLUDING the light itself, which is now fully closed out.** The earlier "`bVisible` reads false after SetVisibility" report was a false alarm: `HandleSensorOverlap` turns the light on immediately then schedules a 4s-delayed `TurnOffLight` (via `SetTimerByFunctionName`) that turns it back off — any test slower than ~4 seconds between trigger and read (easy to hit across several MCP round-trips) reliably caught the auto-off state and misread it as broken. Re-tested this session with a minimal single-round-trip read: `bVisible` reads `true` immediately after trip, `false` again ~4s later — full on/off cycle confirmed working exactly as designed (see LESSONS).
- **Movement Polish (`07_Movement_And_UI.md` §2) — ✅ built + verified live in PIE.** Scope note: this whole block was built by the movement/gameplay lane while a separate concurrent session worked styling/assets — see the coordination note below.
  - **`DA_MovementTuning`** (PrimaryDataAsset, `_Project/Data/`): all ground/vault/swim/air/hide tuning numbers from `07 §2.6`, consumed by a new `BP_PlayerCharacter.ApplyMovementTuning` (called from `EventBeginPlay`) that writes them onto `CharMoveComp` + the character's own `WalkSpeed`/`SprintSpeed`/vault vars. **Verified live**: `CharMoveComp.MaxAcceleration/BrakingFrictionFactor/CrouchedHalfHeight/AirControl/MaxSwimSpeed/BrakingDecelerationSwimming` all match the data asset's tuned values (not engine defaults) after `BeginPlay`.
  - **New input actions** `IA_Interact`/`IA_Dive`/`IA_SwimUpDown` (`Content/Input/Actions/`, per CANON) + `IMC_Default` bindings (`E`/`Left Ctrl`/`Space`+`Left Ctrl`-negate) — verified on **cold disk** via a fresh standalone process (`GetAll InputMappingContext DefaultKeyMappings`), not just dirty editor memory.
  - **`BP_BreathComponent`** (on `BP_PlayerState`): drains `Air` while `bIsSubmerged` past `AirGraceOnSubmerge`, refills after `AirRefillDelay`, sets `bBreathCritical` at threshold, forces a non-punitive surface + brief `bWinded` at `Air==0`. New `PlayerState.bWinded`/`bIsInWater` + `Server_SetInWater`/`Server_SetSubmergeIntent`. `BP_PlayerCharacter` wires `IA_Dive` (toggle submerge intent) and `IA_SwimUpDown` (direct vertical `AddMovementInput` while swimming), polling `IsSwimming` every tick to keep `bIsInWater` in sync. **Verified live**: entering water alone no longer drains Air (a real bug caught only by live property reads — first draft mistakenly gated on `bIsInWater` instead of `bIsSubmerged`, see LESSONS).
  - **`BP_BushHide`** (`_Project/Gameplay/`): `HideVolume` overlap sets/clears the character's local `bIsInBushVolume`; exiting the volume auto-clears `PlayerState.bIsHidden` via new `Server_SetHidden`. `IA_Interact` sets `bIsHidden=true` while in a bush and not swimming. **Verified live**: clean, zero-displacement enter/exit toggling (an early false "collision is blocking" reading turned out to be a stale/wrong Z coordinate in the test itself, not a bug — see LESSONS, an important teleport-testing gotcha). **⚠️ Two real issues found + partially fixed (2026-07-02), reopening this as not-fully-closed:** following the Maple Court pool water collision fix, checked `BushMesh` (the visual mesh) and found it had `BlockAllDynamic` collision — would block a real player from ever walking INTO the bush, the exact same bug shape as the pools (fixed to `NoCollision`, matching the pools' corrected pattern). Also had no material override, the same predates-art/theme-pass gap as the vault wall/crouch doorway (fixed to `MI_Greybox_Cover`). **The "HideVolume floats 300uu above BushMesh" claim above was a misdiagnosis (2026-07-02, movement/gameplay lane) — false alarm, closing this out.** `HideVolume` is `BP_BushHide`'s root component, so its `relativeLocation` (300) IS the actor's own world transform (confirmed identical to `get_actor_transform`'s own reading), not an additive offset stacked on top of it — the same root-component-vs-relative-offset confusion the styling lane independently hit and generalized the same day via `BP_ItemPickup_QuietShoes`'s `PickupVolume` (see LESSONS). Re-derived properly: `HideVolume` world Z = 300 (root, unparented), `BushMesh` (child, relative Z=0) world Z = 300 + 0 = 300 — both sit correctly on the actual ground at that XY (traced at Z=210, not the flat main-area's Z=0; this platform is simply on a different part of the grey-box course), matching `get_actor_bounds`' combined actor bounds exactly (210–390). No reposition needed; this was never broken. A follow-up live-movement confirmation of the BlockAllDynamic→NoCollision fix (walking a real, non-teleported player into the bush/pools) was attempted via a temporary auto-walk probe on `BP_PlayerCharacter` but proved unreliable to control precisely — teleporting a Character while its own `AddMovementInput`-driven simulation is actively running doesn't reliably stick (see LESSONS). Probe fully removed, `BP_PlayerCharacter` reverted clean. **Still genuinely open: needs a human real-WASD playtest to confirm the collision fix**, same as the Maple Court pools below.
  - **`BP_HedgeSqueeze`** (`_Project/Gameplay/`): `SqueezeChannel` overlap sets the character's `SqueezeSpeedMultiplier` (from `SqueezeSpeedScale`, default `0.45`), auto-crouches on entry (restoring prior crouch state on exit), reports one `Action.HedgeSqueeze` loudness bump (new case added to `LoudnessComponent.GetActionInstantBump`). **Verified live**: `MaxWalkSpeed` reads exactly `WalkSpeed * 0.45` while inside, restores exactly on exit. **Material gap fixed (2026-07-02):** `HedgeHalfA`/`HedgeHalfB` had no material override (same predates-art/theme-pass pattern), now `MI_Greybox_Cover`. Their `BlockAllDynamic` collision is correct by design here (two solid hedge blocks forming the squeeze gap, not a pass-through volume like the bush) — left untouched.
  - **Speed resolution (`07` §2.5)** — new `BP_PlayerCharacter.ResolveSpeed`, called every tick, implements `MaxWalkSpeed = (sprint ? SprintSpeed : WalkSpeed) * SqueezeSpeedMultiplier` (crouch speed is handled natively by the engine's own `MaxWalkSpeedCrouched`, no manual resolution needed). Sprint intent (`bWantsToSprint`) is tracked via a **second, independent `IA_Sprint` listener** added purely for this, rather than risking edits to the pre-existing Sprint input handler (Unreal allows multiple listener nodes on one InputAction — see LESSONS). **Verified live** for the squeeze multiplier path (`135 = 300 * 0.45` exactly); the sprint branch is graph-verified (same formula, compiled clean) but not exercised via a real keypress in this session — `ObjectTools.set_properties` cannot write ANY property (not just `RepNotify` ones) on an actor instance that has already run `BeginPlay`, so this class of gap now has a documented, generalized cause (see LESSONS) rather than being case-by-case.
  - **Not built**: the lateral input-lock/soft-guide-through-the-channel polish for hedge squeeze, and the cosmetic rustle SFX/wobble/underwater post-process — all deliberately deferred as polish, matching this project's "don't gold-plate ahead of the MVP playtest" discipline.

**Styling/assets/character-models/houses lane (2026-07-01, concurrent session — runs in parallel with the movement/gameplay lane above, coordinated via an explicit handoff):**
- **✅ STYLING MARATHON — first full pass complete (2026-07-02 PM).** User handed this session the whole visual pass directly ("style the game... houses, pools, siding, light poles, bushes, trees, characters... loop until complete") and stepped away; worked it autonomously as a sequence of committed+pushed chunks, all live-verified via property readback (not just compile-clean), finishing with a project-wide material-name sweep to confirm no gaps were missed. Done: **(1)** real Kenney nature-kit meshes (`SM_Tree_Oak`/`PineTallA`/`Default`, `SM_Bush_{Plain,Large,Detailed}`, `SM_Rock_LargeA`) replacing Maple Court's 6 grey-box cube trees and `BP_BushHide`'s cube bush, new `MI_Greybox_{Bark,Stone,Foliage}` materials. **(2)** 5 grey-box street lights (pole+head+`SpotLight`) along Maple Court's road, built to `design/01`'s exact spec (70° cone, radius 900, 5000lm, `LampWarm`), new `MI_Greybox_LampWarm`. **(3)** House siding variety — Houses B/C/D given distinct tones (`MI_Siding_SlateBlue`/`Sage`/`Terracotta`, House A keeps original cream) plus window/door/foundation-trim geometry on all 4, derived from each house's existing window-light position rather than guessed. **(4)** The `design/01`-specced "Pool glow" `PointLight` (radius 300, `PoolTurquoise`) — existed on **zero** of the 5 pools before this pass, now on all 5. **(5)** Fences (15 panels), sheds (3 bases) moved off borrowed materials (`MI_Greybox_Cover`/`MI_HomeBase_Wall`) onto their own new tones (warm wood / weathered wood). **(6)** Pool chairs upgraded TWICE — first a material-only fix (12 primitive pieces → `MI_Greybox_PoolChair`), then fully replaced with a real Kenney `loungeChairRelax` mesh (6 instances, oriented to face each lot's pool), reusing the kit's own metal/carpet/wood materials already proven on the home-base sofa. **(7)** A project-wide material-name sweep (searching for `Prototype`/`Placeholder`/`BasicShape` substrings across every static mesh component, not just re-checking known spots) found and fixed a real, previously-undocumented gap: **20 sandbox primitives (boundary walls + interior ramp/step cluster) AND the entire main ground `Floor` actor (4000×4000, the single largest visible surface in the level)** were still on Unreal's stock `MI_PrototypeGrid_Gray` — missed by the earlier "re-skin 33 leftover template props" pass. Re-swept after fixing: zero remaining placeholder materials project-wide. **(8)** Full PIE regression check after all changes — GameMode spawns, HUD creates, zero errors. See `Docs/LESSONS.md` (2026-07-02, "Styling marathon") for: the `MaterialInstanceConstantFactoryNew.initial_parent` gotcha (use `duplicate_asset` instead), a verification-methodology note (night-mode screenshots read wildly differently by camera distance/angle — trust property readback over screenshot exposure-hunting), the "search by what a placeholder IS, not where you expect it" sweep lesson, and a real scale bug (a hand-rolled `FbxImportUI` import needed ~0.23x scale, not the usual ~16x Kenney convention — the constant is a property of the *import call-site*, not the kit; always re-verify via `get_actor_bounds()` after any new import+scale). **Characters**: the AI Watcher is already fully re-skinned (BigVegas + dark `MI_WatcherBody`, see below) — nothing further to do there since `BP_PlayerCharacter` is off-limits (movement/gameplay lane owns it) and no other character class exists yet. **Deliberately deferred, not a gap**: retiling the 15 fence panels to real Kenney fence meshes (would need per-panel segment subdivision to avoid stretched picket proportions — meaningfully riskier than the low-risk material/mesh swaps done here; flagged as its own follow-up task rather than rushed).
- **Art/theme layer — ✅ built, with 2 retroactive gaps found and closed (2026-07-02).** `DA_ArtPalette` (11 `LinearColor` tokens per `design/01` §2.1), `M_GreyboxToon` master material (polar-angle-free banded toon shading + fresnel rim + emissive param) + 4 MIs (`Ground`/`Cover`/`Pool`/`Stash`), night mood rig (moon `DirectionalLight`, `SkyLight`, `PostProcessVolume` tuned per `design/01` §2.3) on the existing `L_Sandbox_Movement` actors — but "existing actors" turned out to have missed the *very original* Step 1 grey-box props specifically (they predate even the art/theme pass itself, evidently outside whatever actor set that pass swept). Found and fixed via two targeted material-consistency checks, not a blind re-audit: **Pool_A's water surface** (still `M_WaterPlaceholder` instead of `MI_Greybox_Pool`, fixed the cycle before) and **the vault wall + crouch-doorway frame** (2 posts + lintel, still the raw engine `BasicShapeMaterial` default instead of `MI_Greybox_Cover`, fixed this cycle — verified via property readback on all 4 pieces, see `git log` for the exact commit). This closes out the complete original Step 1 course (vault wall, crouch doorway, swim pool — all three now correctly styled); no further original-course geometry is un-styled as far as targeted checks have found.
- **Readability layer — ✅ fully built AND wired live end-to-end, including the vision-cone decal itself.** `M_VisionCone` deferred-decal material: polar angle mask (atan2-based) clipped to `PeripheralVisionHalfAngle`, 3-color/opacity/pulse-speed driven by an `AlertState` scalar param, plus a `DetectFill` scalar param (0-1) that sweeps a hard-edged `ColorAlert`-red fill from the apex outward — reuses the existing forward-axis distance node (already computed for the angle mask) as the radial-fraction input, so no new UV math was needed; blended additively onto the existing final emissive via one new `Lerp`, so the angle-mask/pulse logic underneath is untouched. `NS_NoiseRing` Niagara built and its spawn trigger wired (see below). **`PlayerState.DetectionAlpha`** is populated by `BP_WatcherController.TickBrain` (movement/gameplay lane) whenever a valid target exists — **live-verified end-to-end (2026-07-02, movement/gameplay lane)**: teleporting the player to mid-range dead-on the patrolling Watcher's forward axis drove `DetectionMeter` 0→1 and `PlayerState.DetectionAlpha` 0→1 in exact lockstep, and teleporting away decayed both back to 0 together. **The decal itself is now built and live-wired (styling lane, this session):** `BP_WatcherCharacter` got a new `VisionConeDecal` (`DecalComponent` using `M_VisionCone`, projected downward under the Watcher) + a `VisionConeMID` (`MaterialInstanceDynamic`, created `OnBeginPlay`) + `UpdateVisionConeParams(AlertState, DetectFill)` pushing both via `SetScalarParameterValueByInfo`; `BP_WatcherController.TickBrain` now calls it every tick with its own live `AlertState`/`DetectionMeter` (spliced in right AFTER the existing `SetDetectionMeter` node, not at the function's front, so the read isn't one tick stale — see LESSONS "pure node re-evaluation" family). **Verified live in PIE**: `VisionConeMID` confirmed non-null post-BeginPlay; placing the player in the Watcher's sight cone drove `AlertState` 0→2 and `DetectionMeter` 0→1 through the exact exec path the splice sits in, proving the Cast doesn't dead-end the function and the material receives live, correct values every tick. **Rendered-visual confirmation done (2026-07-02, PM — now that lighting renders):** live PIE capture confirms the `VisionConeDecal` **renders on the ground as a fan** narrowing to the Watcher's feet (and bleeds slightly onto adjacent walls, a normal large-decal-box effect), and a direct read of the MID's `AlertState` scalar param proved it **tracks the controller's live state in real time** (read 2 while alerted, 0 after decay) — so the material receives correct per-frame values on-screen, not just in the data path. **One honest residual:** the exact rendered *hue* per alert level couldn't be nailed down because the session's navmesh debug overlay (`show Navigation`, left on from AI-movement testing) projects the same striped green onto that ground and could not be fully suppressed across the editor-vs-PIE-viewport show-flag boundary from MCP — it does NOT appear in a normal Play (it's a debug-session toggle), so the cleanest color check is still a human Play. Screenshots saved under `Saved/Screenshots/ForJack/08–11`. **⚠️ CORRECTION + FIX (2026-07-02, later): the "fan narrowing to the Watcher's feet" and "bleeds onto adjacent walls" above were the BUG, not a feature — and the "navmesh green" that confounded the color read was actually THIS decal.** The `VisionConeDecal` has `relativeRotation (0,0,0)` + `relativeLocation (0,0,0)` + a large `decalSize (128,256,256)`, so it projects the (greenish-at-AlertState-0) material as a box **onto the Watcher's own body** and nearby walls instead of a clean floor cone in front. Confirmed by hiding it in the editor viewport (nav-viz off) → the Watcher's dark skin appeared instantly. **Decal is now HIDDEN on both Watchers (CDO + placed instances, `bVisible=false`, holds through PIE ticks)** — the overhead `WBP_WatcherStateIcon` + HUD `DetectionBar` carry alert readability; the decal's geometry+material-UV rework is a flagged focused follow-up (fiddly to do blind — see LESSONS). **SPIKED + DECLINED (2026-07-02, via the non-PIE capture loop):** assigned `M_VisionCone` to the placed decal, tried both `relRot pitch=-90` and `pitch=+90` with a forward/ground offset + floor-slab size, un-hid, and captured — the cyan fan projected onto a nearby WALL (not the floor) in BOTH pitch signs, because the decal box intersects sandbox wall geometry (inherent deferred-decal bleed) and the fan-mask/projection orientation didn't resolve in 2 iterations. Reverted (never saved — disk untouched). Conclusion: not worth the rabbit-hole for a feature already redundantly covered by the icon + HUD bar; a real fix likely needs a **mesh-based** ground cone (flat translucent-unlit fan quad on the floor, oriented to watcher facing) rather than a deferred decal, OR careful per-map box tuning to avoid walls — a genuine focused task, not a general-cycle item. **MESH APPROACH VALIDATED (2026-07-02, next cycle) — foundation built, integration parked:** created `M_VisionConeFan` (`_Project/AI/Materials/`, Surface/Translucent/Unlit/TwoSided, params `FanColor`+`FanOpacity`) and proved a **flattened Engine `Cone` mesh renders as a clean flat floor fan** — recipe: `relRot pitch=-90` (lays the cone axis horizontal), `scale=(0.05, 2, 2)` (the 0.05 flattens the now-vertical LOCAL-X → a flat slab; local Y=lateral width, local Z=length), no collision, `M_VisionConeFan`. One orientation note for the finish: at pitch −90 the apex ends up FORWARD and the wide base at the watcher — **flip it** (yaw 180 or pitch +90) so the narrow apex sits at the eye and it widens forward. Validated visually via the non-PIE capture loop (test actor removed, no debris; the "big cyan square" seen in those caps was just the sandbox pool water surface, not vision debris). **Remaining integration (the focused-task part, NOT done):** (1) add a persistent `StaticMeshComponent` to `BP_WatcherCharacter` + `_MC` — **there is NO `add_component` in BlueprintTools**; components here are added via the editor-Python `SubobjectDataSubsystem` API (the same path the decal/icon used), which is fiddly; (2) parent it to the root so it follows facing, apply the recipe transform; (3) create a `FanColor` MID and drive it from `AlertState` (re-add a fan-color line to the now-icon-only `UpdateVisionConeParams`). Until someone does that focused pass, readability stays on the overhead icon + HUD `DetectionBar` (both working); `M_VisionConeFan` is committed as the ready foundation. **✅ DONE (2026-07-02, `013e8db`) — the mesh vision cone SHIPPED on both Watchers.** The focused integration got done: added a persistent `VisionFanMesh` `StaticMeshComponent` to `BP_WatcherCharacter` + `_MC` via the editor **`SubobjectDataSubsystem`** Python API (`k2_gather_subobject_data_for_blueprint` → `add_new_subobject` → `rename_subobject` → set mesh/material/xform on the template → compile+save — this is the general "add a persistent SCS component to a BP" recipe, since BlueprintTools has NO `add_component`; see LESSONS). Config: flattened Engine `Cone` + `M_VisionConeFan`, `relRot pitch=-90 yaw=180` (apex at the eye, widening forward), `scale (0.05,3,5)`, `relLoc (150,0,-88)` on the floor, no collision, parented to root → follows the Watcher's facing. Verified via non-PIE overhead capture: a clean cyan FOV cone, apex at the Watcher, widening along +X, **no wall-bleed** (the decal's fatal flaw). **✅ v2 DONE (`94e863d`) — the cone reddens on detection.** Both Watchers' `UpdateVisionConeParams` now append `SetColorParameterValueOnMaterials(VisionFanMesh, "FanColor", lerp(cyan (0.1,0.65,1), red (1,0.12,0.12), InDetectFill))` — the ground FOV cone smoothly shifts cyan→red as the detection meter fills. Used the component's `SetColorParameterValueOnMaterials` (handles the dynamic instance internally — **no explicit MID var / BeginPlay wiring needed**, much simpler than the decal's MID approach). Color line is LAST in the function so the load-bearing icon path runs first (learned pattern); compile-clean, `read_graph_dsl` confirms the icon path intact. v1 already proved `FanColor` drives the cone color (rendered cyan); the visual redden-on-detection is a human-playtest observation (needs a detected player). **The readability layer is now fully complete: ground FOV cone (direction + detection color) + overhead `?`/`!` icon (alert state) + HUD `DetectionBar` (detection amount) — three complementary signals, each with a distinct job.** **Final pre-playtest visual QA of the sandbox movement course done (2026-07-02, non-PIE captures in `Saved/Screenshots/ForJack/qa_sandbox_*`) — CLEAN: correct night exposure (same rig as Maple-Court-clean, no home-base-style blown PP), Pool_A water cyan, grey-box props (vault wall / crouch doorway / hedge squeeze / cubes) silhouetted + readable from player eye level, vision cone rendering in-course. No fixes needed. With this, the whole styling/Phase-4 lane is genuinely exhausted — every buildable+MCP-reachable item is done; the real next step is the human Step 8 feel-playtest (needs the user) or Phase 5 (gated behind it).** Screenshots `12–16`. **The Maple Court Watcher (`BP_WatcherController_MC`/`BP_WatcherCharacter_MC`) now has the identical decal build too** — ported and independently verified live (`DetectionMeter` observed rising 0→0.33 then decaying back to 0 across ticks, proving the splice runs every frame). **`WBP_WatcherStateIcon` (the overhead `?`/`!` icon, `04 §11`) — ✅ built (new session), closing a genuine gap distinct from the vision cone.** The spec calls for BOTH a ground-projected vision cone AND an overhead icon as complementary signals; only the cone existed before this. New `WidgetComponent` (`StateIconWidget`, Screen space, 64×64, 120uu above head) on both `BP_WatcherCharacter` and `BP_WatcherCharacter_MC`, `WidgetClass=WBP_WatcherStateIcon` (a `TextBlock` showing `?`/`readWarn`-yellow when Suspicious, `!`/`readAlert`-red when Alert, `Collapsed` when Unaware — colors from `DA_ArtPalette` for consistency with the cone). Wired by extending the already-tick-verified `UpdateVisionConeParams` splice (no new `TickBrain` changes) to also `SetAlertState` on the pawn's own long-stuck mirror variable (see LESSONS — this had been blocked on a search-index-lag issue since an early session) and push it to the icon via `GetUserWidgetObject→Cast→UpdateIcon`. **Verified live in PIE on both instances**: the mirror updates in lockstep with the controller (0→1 on sight-cone entry), no `Accessed None`/cast-failure warnings in the log for either Blueprint. **First confirmed use of `WidgetComponent`** (a 3D-world-attached UMG widget, distinct from `WBP_HUD`'s screen overlay) via this MCP surface — worked on the first attempt using the same component-adding path as the vision-cone decal. Both AI instances are done; the readability layer is fully closed out, including the icon that was missing.
- **`NS_NoiseRing` (Niagara) — ✅ FX asset built AND wired to spawn (movement/gameplay lane picked up this handoff).** `Content/_Project/UI/FX/NS_NoiseRing`, based on the engine's `RadialBurst` template pruned to its one `OmnidirectionalBurst` sprite-burst emitter (already had every module the spec needed — spawn burst, size-over-life, color-over-life, sprite renderer — zero modules built from scratch). Retuned `Lifetime` to `0.6s` and `Color` to the `ReadWarn` amber palette token; `bHasErrors:false`, 331 KB on disk. **Resolves the "(unconfirmed) Niagara" flag** — `NiagaraToolsets` is real and MCP-buildable, with one real gotcha: a too-minimal template (`MinimalLightweight`) hit a hard wall (`AddModule` can't create a missing script STAGE, only add modules to an existing one) — the fix was starting from a template that's a complete effect and deleting what's unwanted, not building up from empty (see LESSONS). **Spawn wiring done**: `BP_LoudnessComponent.Server_ReportAction` now calls `Niagara|SpawnSystemAtLocation` at the owner's location whenever an instant loudness bump occurs (any bump > 0 — Vault/SplashEnter/SensorTrip/FenceClimb/HedgeSqueeze). Verified via exec-chain reachability + a clean error/warning-free log scan (the effect's own 0.6s auto-destroy is too fast to catch via direct property polling — see LESSONS for the verification pattern). **Velocity retuned to match `HearingRadius`** (this session): the ring's `AddVelocity.Velocity Speed` range was `75`-`500` u/s, which over the `0.6s` lifetime (and the `Drag` module's `0.75` coefficient) only visually reached ~150-250 units — underselling `AIP_WatcherProfile.hearingRange` (`1200`) by roughly 4x. Retuned to `1800`-`2600` u/s so the visible reach roughly tracks the real hearing radius. Verified via `GetSystemCompileState` (`UpToDate`, no errors) + an independent `GetDynamicInputChain` readback. **Loudness-driven color lerp — done (2026-07-02).** Added `User.RingColor` (LinearColor) as a Niagara User Parameter and rebound `InitializeParticle.Color` to it as a **Linked** input (`NiagaraToolset_System.SetStackInputData`, the same mechanism Niagara uses internally for `Emitter.Age`/`Engine.Owner.Scale`), replacing the static amber literal. `Server_ReportAction` now captures `SpawnSystemAtLocation`'s returned `NiagaraComponent` and calls `SetNiagaraVariable(LinearColor)` on it with `Math|Color|Lerp` between amber and `DA_ArtPalette.readAlert` red, driven by `GetLoudness01()` read post-bump. Verified via independent `GetStackInputData` readback + `GetSystemCompileState` (`UpToDate`, zero errors) + exec-chain tracing on the Blueprint side (same verification class as the rest of this asset — the 0.6s auto-destroy is too fast for direct property polling). **While tracing the Blueprint's exec chain for this edit, discovered and fixed a real bug of a different kind**: `write_graph_dsl` had left 17 fully-orphaned dead nodes in `Server_ReportAction` from 2-3 prior sessions' rewrites of this same function — silently invisible to `compile_blueprint`, only found by tracing reachability from `FunctionEntry`. Cleaned them up (verified the live 18-node chain was untouched via `read_graph_dsl` round-trip + recompile). This generalizes to any function edited via DSL more than once across sessions — see LESSONS and the `unreal-mcp-blueprints` skill's new entry. **Still not done**: ring shape/spawn-location fine-tuning (cosmetic, low priority).
- **`NS_PoolSplash` (Niagara) — ✅ built and wired to `BP_PoolVolume`'s overlap, verified live.** Per `01_Art_And_Style_Direction.md` §4.3 (chunky white droplet burst on pool entry, tied to the `SplashEnter` loudness moment). Same build technique as `NS_NoiseRing`: pruned from the `RadialBurst` template down to its single `OmnidirectionalBurst` emitter (removed `RibbonTrailFollower`/`Ribbon_Trail_Leader` via `RemoveEmitter`, which needs the full `NiagaraExt_StackItemReference` struct, not a bare emitter-name string), retuned `Lifetime` 1/2.25→0.3/0.6s and `Uniform Sprite Size` 2/6→4/10, `Spawn Count` 100→40, color left at template white. Wired into `BP_PoolVolume.EventActorBeginOverlap` via `Niagara|SpawnSystemAtLocation` at the overlapping actor's location. **Caught and fixed a near-miss bug while wiring this**: the first attempt tried to add the spawn call as a second `connect_pins` off the SAME already-connected output pin feeding the existing scoring `IfThenElse_0` — `connect_pins` silently REPLACED that connection instead of adding a parallel branch (it does not support exec-pin fan-out, unlike the graphical editor), which would have silently killed pool scoring entirely. Caught via immediate `get_node_infos` re-verification (not the tool's `null` success return) and fixed by splicing linearly instead (`break_pins` → `Event → SpawnSystemAtLocation → IfThenElse_0`). Verified live in PIE: `PlayerState.IndividualScoreAtRisk` still climbs correctly (0→51) on pool entry, proving the original scoring chain survived the fix. Full writeup + the general pattern in `Docs/LESSONS.md` (2026-07-02) and the `unreal-mcp-blueprints` skill.
- **`UMGToolSet` discovered live and confirmed working** — the long-standing "UMG needs the human" ceiling is lifted; see CANON's "Known MCP buildability gaps" and LESSONS.
- **`WBP_HUD` (Step 7) — ✅ built end-to-end via MCP, verified live in PIE.** Simplified/MVP-scoped from `07_Movement_And_UI.md` §3's full spec (skipped: wristwatch, screen-edge detection arcs, vignette+heartbeat — all explicitly deferred polish, not needed for the core "can I read my state" loop). Built: `LoudnessBar`/`AirBar` (`ProgressBar`, color-banded green/amber/red, Air auto-hides unless submerged), `ScoreBankedValue`/`ScoreAtRiskValue` (`TextBlock`, split from their static labels to sidestep needing a text-concat node), `AlertText` (`?`/`!`/`!!` colored by `AlertLevel`), `HidePipText` (shows only while `bIsHidden`). Two functions (`UpdatePlayerHUD`/`UpdateGameHUD`, split to avoid nested-cast depth) called from `EventTick`, bound to the REAL canonical vars per `CANON.md`'s drift table (`CurrentLoudness`/`IndividualScoreAtRisk`/`TeamScoreBanked`/`AlertLevel`/`Air`/`bIsSubmerged`/`bBreathCritical`/`bIsHidden` — not `07`'s stale short names). Wired into `BP_PlayerController.BeginPlay` (spliced in front of the existing chain via `break_pins`, not touching the pre-existing touch-controls logic) — `CreateWidget(WBP_HUD)` → `AddToViewport`. **Verified live**: a `PrintString` marker + `LogsToolset.GetLogEntries` confirmed `BeginPlay` fires and the HUD's `EventTick` ran every frame with zero `Accessed None`/runtime errors for the rest of the PIE session — this also means `BP_PlayerController`'s BeginPlay is NOT permanently stuck non-firing as earlier sessions suspected, at least in PIE (cold-disk confirmation still the honest next check, per LESSONS). Several new widget-specific DSL/node-discovery gotchas documented in LESSONS (own-class variable accessor prefix is `Variables|WBP_HUD|Get...` not `Variables|Default|Get...`; TextBlock's real type_id is `Widget|SetText(Text)`; a Blueprint can only have one `EventBeginPlay` node; widget `EventTick` needs the `UserInterface|` category prefix the bare-name DSL sugar doesn't supply). **`DetectionBar` added (new session)** — a genuine gap found by cross-referencing `04_AI_Watcher.md` §11's `WBP_DetectionBar` spec against the actual widget tree: nothing was reading `PlayerState.DetectionAlpha` even though the concurrent session had just verified that value works end-to-end. New `ProgressBar` (stacked above `AirBar`/`LoudnessBar`) + `UpdateDetectionBar` function (own `GetOwningPlayer→GetPlayerState→Cast→GetDetectionAlpha` chain, 3-color banding — `floodWhite` neutral/`readWarn` yellow/`readAlert` red, matching the spec's "no fine gradient" intent), added as a third call after `UpdateGameHUD` in `EventTick` rather than surgically editing the large existing `UpdatePlayerHUD`. Compiles clean, zero runtime errors live in PIE — but a live 0→nonzero value transition wasn't actually observed this session (the Watcher's perception didn't trigger across several reposition attempts, an environment/timing quirk unrelated to this new code since the upstream `DetectionAlpha` pipeline itself was independently proven by the other session one cycle earlier). Worth a visual eyeball next Play session. **Not built**: `NS_NoiseRing` Niagara, the wristwatch/edge-detection/vignette elements named above.
- **`SkeletalMeshTools.import_file` discovered live and confirmed working** — raw `.fbx` → real `SkeletalMesh` import is possible via MCP (another "needs the human" assumption that was wrong). **Gotcha: import one character at a time with a save between each** — rapid-fire imports with `import_materials=true` cross-contaminate material slot assignments across characters (see LESSONS for the full diagnosis).
- **Watcher SKINNED dark (2026-07-02, PM) — character-differentiation skin pass, distinct threat vs player.** New `MI_WatcherBody` (`_Project/AI/Materials/`, dup of `MI_Greybox_Cover` → `M_GreyboxToon`): near-black charcoal `BaseColor (0.015,0.018,0.028)` + deep `ShadowColor` + cool moonlit `RimColor (0.14,0.18,0.30)` so the silhouette edge reads at night, no emissive. Applied to both material slots of both Watchers' `CharacterMesh0` (CDO + placed instances — the CDO-only edit does NOT reach placed instances, see LESSONS), keeping the animating Manny mesh + `ABP_Unarmed` (no mesh swap = no regression). Confirmed in PIE: the Watcher is a dark menacing figure, clearly distinct from the light Mannequin player — the `05_Characters` §1.2 "heavy dark silhouette contrasts the light/low player" read, via the doc's own endorsed `OverrideMaterials` path. `BP_PlayerCharacter` left untouched (movement lane). **Update (2026-07-02): the Watchers were subsequently upgraded from skinned-Manny to a fully-retargeted animating Mixamo `SK_BigVegas` — the "Mixamo models are human-blocked" caveat is overturned, see the Mixamo bullets below.**
- **4 Mixamo characters imported** (mesh+skeleton only, no materials yet) to `Content/ThirdParty/Mixamo/Characters/`: `SK_BigVegas` (Watcher body — **now retargeted + wired + animating, see next bullet**), `SK_Kaya`/`SK_Josh`/`SK_Michelle` (player-body candidates, still unwired — `BP_PlayerCharacter` is the movement lane's file). Note: Mixamo's own skeleton ≠ the UE Mannequin skeleton, so existing `ABP_Unarmed` animations don't play on these directly — they need retargeting.
- **✅ Mixamo `SK_BigVegas` RETARGETED + wired onto BOTH Watchers + verified animating (2026-07-02) — the "IK Retargeter needs a human" ceiling is BROKEN via editor Python.** Overturns the immediately-prior "IK Rig/IK Retargeter NOT MCP-buildable, full stop" claim: the *dedicated toolset* truly doesn't exist, but the **editor Python console — reachable by typing `py "abs/path.py"` into the status-bar Cmd box (SlateInspector ref `tb2`) — has the FULL `unreal` API** (`IKRigController`, `IKRetargeterController`, `IKRetargetBatchOperation`, asset factories), so IK rigs + retargeters + batch anim retargeting ARE buildable, zero human clicks. Built `IK_Manny`/`IK_BigVegas` (auto-generated matching humanoid retarget chains) + `RTG_Manny_To_BigVegas` (`_Project/AI/Retarget/`), batch-retargeted `ABP_Unarmed` + 21 anims → `ABP_Unarmed_BV` + sequences (`_Project/AI/BigVegas/`), wired `SK_BigVegas` + `ABP_Unarmed_BV` + 4-slot `MI_WatcherBody` dark skin onto `BP_WatcherCharacter` + `_MC` (CDO + placed instances). **The one non-obvious blocker — a retargeted ABP carries the source skeleton's post-process Control Rig and it stamps a T-POSE over the correct animation:** `ABP_Unarmed`'s final AnimGraph node runs `CR_Mannequin_FootIK` (Manny foot IK), which can't characterize the BigVegas skeleton → outputs ref pose. **Fixed by setting that ControlRig node's `Alpha` pin `1.0 → 0.0`** (blends the foot-IK out, base locomotion passes through). **Verified via Simulate-mode `CaptureViewport`** (the viewport DOES render off-foreground during Simulate — a genuine way to eyeball a pose without a human): silhouette went arms-out (T-pose) → arms-down (natural idle). Result: the Watcher is now a distinct heavyset dark silhouette vs the light-Manny player, and it animates. **Full pipeline + all four gotchas (AssetData-not-loaded-objects for batch retarget, the ControlRig T-pose, off-screen bone reads lie / Simulate-capture works, `save_loaded_asset` no-ops without `only_if_is_dirty=False`) in LESSONS (2026-07-02, top entry).** Commits `c5d50b2` (retarget+wire) + `16ef0a0` (Alpha=0 fix). The 3 player-body Mixamo meshes remain unwired (player char off-limits); if adopted later, same pipeline applies. `BP_WatcherCharacter`/`BP_WatcherCharacter_MC` are the styling lane's to edit (only `BP_PlayerCharacter`/`BP_PlayerState`/`BP_BreathComponent` are off-limits).
- **✅ RESOLVED (2026-07-02) — the per-tick `Accessed None ... VisionConeMID` error is eliminated; both Watchers' `UpdateVisionConeParams` are now icon-only (`ad300fb`).** Diagnosis first: live reads showed `VisionConeMID` IS reliably created by BeginPlay in fresh PIE sessions (valid in two independent runs) — the errors clustered only in the **post-crash recovery churn**, so it was a crash-recovery-specific race, not a normal cold-load bug, and on a decal that's hidden/mis-built anyway. Fix: rewrote both `BP_WatcherCharacter` + `_MC`'s function to `SetAlertState → Cast → UpdateIcon` only, dropping the (pointless-while-hidden) decal scalar-param updates — structurally error-proof since no `VisionConeMID` access remains. Both compile clean. The decal's alert-param plumbing gets re-added with the decal's own rework (project-down + UV re-align). Note: the DSL rewrite left harmless disconnected orphan nodes in these two functions (zero compile warnings, zero behavior impact — see LESSONS; the decal rework clears them). A DSL `(if (IsValid …) …)` guard was tried first and does NOT lower to a working branch — see the LESSONS entry.
- **Costume system (Step 6) — ✅ data layer built AND integrated, verified live in PIE.** `DA_Costume` PrimaryDataAsset (`CostumeID`/`DisplayName`/`MeshOverride`/`AttachMesh`/`AttachSocket`/`LoudnessModifierMult`/`SwimSpeedMult`/`bIsDefault`) + 2 instances (`DA_SwimTrunks` default, `DA_QuietShoes` −20% loudness) built by the styling lane. **Integration completed by the movement/gameplay lane**: `PlayerState.EquippedCostume` (RepNotify) added, `BP_CostumeComponent` attached to `BP_PlayerCharacter` as `CostumeComp`, `Server_EquipCostume`/`ApplyCostumeVisual`/`GetEquippedCostume` bodies implemented, `BeginPlay` defaults to `DA_SwimTrunks`, new `BP_LoudnessComponent.LoudnessModifier` folded into `TickLoudness`'s target-loudness calc, `BP_ItemPickup_QuietShoes` built. **Verified live in PIE end-to-end**: default equip syncs all three state points on `BeginPlay`; picking up Quiet Shoes drops `LoudnessModifier` to exactly `0.8` and updates `PlayerState.EquippedCostume`, pickup self-destructs. **`AttachedMeshComp` spawn/attach logic now wired (new session), closing this gap.** `ApplyCostumeVisual`'s "Is Not Valid" branch (fires on every first equip, since `AttachedMeshComp` starts null) was a dead end — only the re-equip case (swap mesh on an already-existing component) worked. New: `IsValid(Costume.AttachMesh) → AddComponentByClass(StaticMeshComponent) → SetStaticMesh → SetRelativeLocation(0,0,-85) → SetAttachedMeshComp`. No dedicated "AttachToComponent" node was reachable via this MCP surface despite several guessed type_ids, so this uses `AddComponentByClass`'s default root-auto-attach + a manual offset approximating foot height, rather than a true socket-follow (won't track foot-bone animation) — **Settled (2026-07-02): a true socket-follow is NOT reachable via this MCP surface, full stop.** Re-investigated the `bManualAttachment`/`RelativeTransform` pins the type-pins preview hides — inspected the real, already-created node instance directly (`get_node_infos`, not the preview) and confirmed both pins exist, but neither takes a socket name; `RelativeTransform` is just an offset (equivalent to the `SetRelativeLocation` already used), and `bManualAttachment` only toggles auto-vs-manual root attachment. A further `find_node_types` sweep (filtered `"Socket"` and `"AttachTo"`, using the new component's own output as context) surfaced only socket-query functions (`GetSocketTransform`/`GetAttachSocketName`/`DoesSocketExist`) — no attach-to-socket call exists in this tool's node catalog. The manual-offset approach already in place is the ceiling for this system without a genuinely new MCP capability; don't re-attempt this. Also populated `DA_QuietShoes.AttachMesh`/`AttachSocket` (both were `None` — an engine `Cylinder` placeholder + `foot_l_Socket`, confirmed to exist on `SKM_Manny_Simple`), since the logic had nothing to spawn without them. **Verified live in PIE end-to-end**: picking up Quiet Shoes correctly creates a `StaticMeshComponent_0` on the player with the right mesh and offset, and `CostumeComp.attachedMeshComp` correctly references it. See LESSONS for a critical MCP finding hit while building the surrounding system (a stub function built as a hidden Custom Event silently resisted all function-editing tools). **`PickupMesh` collision/material fixed (2026-07-02):** same collision-blocking pattern as the pools/bush — `BlockAllDynamic` would have prevented a real player from ever reaching the pickup's overlap trigger; fixed to `NoCollision` + `MI_Greybox_Cover` (was the raw engine default). A suspected `PickupVolume` position bug turned out to be a misdiagnosis (it's the actor's root component, so its `RelativeLocation` legitimately equals the actor's world position) — see LESSONS for the full root-component gotcha and how a resulting accidental 300uu actor displacement was caught and reverted.
- **Maple Court neighborhood — 🔨 core layout + all 4 pools + patrol built, verify-pending.** Built at a large world-space offset (+30000 X) inside `L_Sandbox_Movement` rather than a separate `L_MapleCourt` map, because **Level/`.umap` assets cannot be created or duplicated via any MCP tool** (confirmed gap — `AssetTools.duplicate`/`exists`, `SceneTools.load_level` all mishandle Level assets; see LESSONS). This inherits the sandbox's already-tuned GameMode override + night lighting rig for free. Splitting it into a real separate map later is a normal in-editor "Move Selected Actors to New Level" operation once someone confirms that path, or a good next MCP spike.
  - **Ground/street/playground/stash + Pools A/B/C** built and scored (`Pool_MC_A/B/C`, rates 10/13/15) in an earlier chunk of this session.
  - **Pool D ("the money pool") built**: patio wall ring (5 walls, 2 gaps) + hedge-squeeze on gap 2, warm streetlight over gap 1, deep basin, `Pool_MC_D` at rate 20 (highest tier). Matches `02_Neighborhood_MapleCourt.md` §3.5's "greed vs. safety" sightline puzzle.
  - **Verticality props built** (`SB_Deck_C_MC`, `SB_AC_Unit_D_MC`, `SB_RampToRoof_B_MC`, `SB_ShedRoof_B_MC` per §3.6) — landed correctly but got committed under the concurrent session's `2daad3f` (a shared `save_assets` flush swept them in; see LESSONS "cross-session git hygiene" entry). Content verified correct regardless of which commit carries it. **Collision/material-checked (2026-07-02), clean, no fix needed:** as part of the pool/bush/pickup collision-bug sweep, checked all 4 for the same bug pattern — all correctly have `BlockAll` (appropriate here, players climb ONTO these, unlike the water/bush pass-through cases) and proper materials (`MI_Greybox_Cover`/`MI_Greybox_Ground`, thematically sensible per prop). Good evidence Maple Court's own construction batch is internally consistent; the bugs found this session were all in either pre-art-pass geometry or specialized systems built in separate passes, not in Maple Court's main build.
  - **Patrol built and isolated**: 6 `SB_WP*_MC` waypoints (§4) tagged `PatrolPoint_MC`, a duplicated `BP_WatcherController_MC`/`BP_WatcherCharacter_MC` pair (Tag pin repointed from the shared `"PatrolPoint"` literal so this Watcher can't merge waypoints with the original sandbox Watcher — see LESSONS for why that would otherwise happen), a second `NavMeshBoundsVolume` covering the whole Maple Court region (the original only covers the sandbox). Patrol-point gathering verified correct live in PIE (exactly 6 MC points, zero sandbox contamination, and the sandbox Watcher's own 4 points are equally unaffected). **✅ Movement now confirmed working for BOTH Watchers** (as of that test) — caught as a side effect of this session's vision-cone-decal PIE testing (fresh editor process, per the `c3213aa` restart requirement already noted below): the sandbox Watcher moved x=100→530 chasing/patrolling, and the Maple Court Watcher independently moved x=32900→32669 the same way, both while its `DetectionMeter` correctly rose and decayed tick-by-tick. **⚠️ (2026-07-02) A later, more rigorous re-test contradicted this** — see LESSONS "regression suspected, UNRESOLVED" and the flag on the Step 4 entry above. Don't treat either result as final; re-test fresh with a real-wall-clock-verified window before relying on Watcher movement.
  - **4 grey-box house shells built** — one behind each pool lot (`SB_House{A,B,C,D}_Base/Roof_MC`), addressing the user's original "houses" request which had been completely unaddressed until now (per CANON's own gap note, `Kenney City-Kit-Suburban` is NOT staged on disk — grey-box is the correct, deliberate interim, not a shortcut). Two-tier cube silhouette (base + overhanging roof cap) using the already-built `MI_HomeBase_Wall` (warm cream walls) + `MI_Greybox_Cover` (dark roof) materials — no new assets needed. One warm `WindowGlow`-toned `PointLight` per house, aimed at the pool-facing wall, for a "someone lives here" night read. Positions surveyed from the actual placed pool-basin bounds (not eyeballed/re-derived from memory) and confirmed clear of the ground plane's real extent (`X:[26900,33900] Y:[-2600,2600]`) via `trace_world` before placing. **Not built**: actual door/window cutouts, interior, driveway — pure exterior silhouette, matching the "grey-box now, Kenney suburban kit later" discipline. **Collision/material-checked (2026-07-02), clean:** all 8 pieces (4 bases + 4 roofs) correctly `BlockAll` (houses are meant to be solid, unlike the pass-through cases) with proper materials already assigned as documented above. Extends the same finding as the verticality props — Maple Court's own construction batch (pools, verticality, houses now all checked) is internally consistent; this session's bugs were isolated to pre-art-pass geometry and a few specialized-system props.
  - **`SB_DogPen_Fence` placeholder placed (new session)** — the §6 stretch table's grey-box marker (Cube, `MI_Greybox_Cover`, matching the Pool D patio-wall style) for the future Phase-5 dog-pen environmental threat, at doc coords (1600,−1200,90)+the Maple Court X-offset, ground-traced first, tagged `Phase5Placeholder`. Left empty per spec — no mechanic, just marks the spot. **Not yet built**: stretch pools E/F (§6, explicitly optional/deferred, skipped for the same reason).
  - **Collision/material bug-hunt CLOSED OUT project-wide (2026-07-02).** Checked the last two unaudited categories — `SB_PatioWall` ×5 (Pool D's ring) and playground props (`SB_PlaygroundFence_N/S/W_MC`, `SB_PlaygroundPad_MC`, `SB_MonkeyBars_MC`, `SB_Slide_MC`) — both clean: correctly `BlockAll` (solid-by-design, same category as `BP_HedgeSqueeze`'s intentional barrier, not the pool-water/bush/pickup pass-through class) with correct `MI_Greybox_Cover`/`MI_Greybox_Stash` materials, no fixes needed. Combined with the already-clean verticality props and house shells (above), every Maple Court prop category is now audited. See LESSONS for the `find_actors` name-substring miss caught mid-check (missed `MonkeyBars`/`Slide`, recovered via a `bounds`-box query).
- **Home-base starting room — ✅ built.** Per `design/01` §7 (Phase 5 spec). Enclosed room at world offset X=-6000 (own region — no overlap with sandbox/Maple Court): floor/ceiling/4 walls (south doorway gap), warm `MI_HomeBase_Wall`/`MI_HomeBase_Floor` (M_GreyboxToon instances, cream/wood tones). 6 Kenney furniture props (sofa, coffee table, rug, TV cabinet, CRT screen, floor lamp) dressed couch-facing-TV. New `M_CRT` material: scanline-emissive (Sine-banded, TVGlow-blue) screen + matching `RectLight` key + warm floor-lamp `PointLight` + soft ceiling fill. **Needed a second, bounded `PostProcessVolume` to override `AutoExposureBias`** — the level's one existing PP volume is `bUnbound` with Manual exposure biased for the night exterior, which crushed the room to black regardless of local light intensity; this is the standard/correct fix, not a workaround (see LESSONS for the full diagnosis, plus two other real bugs caught: Kenney FBX imports landing at ~1/16 scale, and `set_actor_transform` silently zeroing unset location/rotation on a partial-xform call). Verified visually via `CaptureViewport` (camera positioned *inside* the new PP volume's bounds — outside it, the fix invisibly doesn't apply). **⚠️→✅ Exposure over-corrected, caught + fixed by a visual QA pass (2026-07-02, `9fe1f10`):** that PP volume's `AutoExposureBias` had been left at **+3.0** (8× brightening, to escape the dark exterior lock) — combined with the room's own 1500–3000 cd lights it blew the whole room to solid warm-white (no detail visible). A Simulate + `CaptureViewport` QA capture caught it; dropped bias to **0.0** → room now reads as a cozy warm living room (orange sofa, table, rug, CRT TV, floor lamp all visible; before/after in `Saved/Screenshots/ForJack/qa_homebase_*`). Lesson: adding a bias to lift an interior out of a locked exterior exposure is easy to overshoot — always re-capture and check for blown highlights. **Not built**: the doc's animated CRT flicker (static glow only, deliberately deferred as polish).
- **Coordination note:** two Claude sessions are working this project concurrently (styling/assets here, movement/hiding/swimming in the other). Established via an explicit handoff: stage specific files before `git commit` (never `-A`, which sweeps up the other session's in-progress work into a misleading commit message), check `IsPIERunning()` before touching PIE state (a block can be the *other* session's test, not yours), and each lane stays out of the other's core files (`BP_PlayerCharacter`/`BP_PlayerState`/`BP_BreathComponent` = gameplay lane; materials/data assets/new components/third-party imports = styling lane).

**Autonomous-build ceiling (spiked, in LESSONS):** MCP's *dedicated toolsets* can build variables/graphs/components/materials/actors/UMG widget trees but not Blueprint enums/structs, Run-on-Server RPC flags, Behavior Trees, or the built-in `Delay` node. **BUT the ceiling is much lower than it looks: the editor Python console (full `unreal` API) is reachable via the status-bar Cmd box (`py "abs/path.py"`, SlateInspector ref `tb2`)** — this is how IK Rigs/IK Retargeters/anim retargeting got built (2026-07-02) despite having no dedicated toolset, and it can drive most editor operations a human could. Interim substitutes still in use where a graph edit is simpler: enums→`byte`, RPCs→regular `HasAuthority` functions, `Delay`→`SetTimerByFunctionName`. Computer-use still can't reach the editor window here, but the Python console largely obviates it. The AI-Watcher Behavior Tree was substituted with a Blueprint state machine (`TickBrain`); the HUD (Step 7) turned out MCP-buildable via UMG.

**Project 42 reaper:** verified safe — it only kills processes referencing `Desktop\42`'s workdir; ours don't, and the editor isn't a candidate. The loop also self-heals the editor+MCP if port 8000 drops (LESSONS).

**Rendering:** hardware ray tracing is **disabled** (`r.RayTracing=False` in DefaultEngine.ini) — the template's Lumen HWRT deadlocked PIE at render init. Lumen runs in software. Revisit for real neighborhoods (Phase 4+). `EditorStartupMap`/`GameDefaultMap` → `L_Sandbox_Movement`.

**Historical note:** Steps 0–8 of the original implementation roadmap (`08_Implementation_Roadmap.md`) are long complete — see "Current state" above for what's actually live now. The old session-handoff doc (`Docs/design/START_HERE.md`) was deleted 2026-07-03 as redundant with this file; the current work queue lives in [`Docs/design/12_Overnight_Session_Plan.md`](Docs/design/12_Overnight_Session_Plan.md).

**New design doc added (2026-07-01, brainstorm + cited research, not built yet):** [`Docs/design/11_Loot_And_Extraction.md`](Docs/design/11_Loot_And_Extraction.md) — a Phase 5+ forward-spec for found-in-the-world loot (common field trinkets like speed-boosting pants, one ultra-rare nightly "Trophy" collectible per neighborhood à la a garden gnome), carried at-risk and banked at the stash like score, small situational stat nudges via a sibling of the CostumeComponent pattern. Grounded in cited extraction/loot-design research (Hunt: Showdown's carry-broadcasts-position mechanic, Deep Rock Galactic's visible push-your-luck resource, Risk of Rain 2/Hades' situational-not-stacking itemization, WoW's pity-timer RNG floor). Wired into `CANON.md` (new names/enum), `08_Implementation_Roadmap.md` (Phase 5 step block), `01_Art_And_Style_Direction.md` (3 new rarity palette tokens, deliberately kept separate from the alert-state colors), and `Docs/01_Game_Design_Document.md` §5.6. **Do not build ahead of the Step 8 MVP playtest** — this is parked, specified-not-built, same discipline as the rest of Phase 5.

**Restart the editor + MCP (if the server is down / port 8000 not listening):**
```powershell
Get-Process UnrealEditor,CrashReportClientEditor -EA SilentlyContinue | Stop-Process -Force; Start-Sleep 5;
Start-Process "C:\Program Files\Epic Games\UE_5.8\Engine\Binaries\Win64\UnrealEditor.exe" -ArgumentList @("C:\Users\jackw\Desktop\PoolHop\PoolHop.uproject","-ExecCmds=ModelContextProtocol.StartServer")
```
The editor does NOT auto-start the MCP server; the `-ExecCmds` arg does. Poll `Test-NetConnection 127.0.0.1 -Port 8000`.

**Known issues:** none currently open from the original template corruption — re-verified this session (`find Content -name "*.uasset" -size 0` across the whole project returns zero results; `SKM_Manny_Simple`'s two material slots resolve to the real `MI_Manny_01_New`/`MI_Manny_02_New` instances, parented to `M_Mannequin`, on both `BP_PlayerCharacter` and `BP_WatcherCharacter`). This stale note previously said Manny's materials/textures/rigs and the Touch UI widgets were still 0-byte — that was true before the `Mannequins/`+`Input/Touch/` tree restore (see the 2026-07-01 "ROOT-CAUSE asset corruption found + FIXED" note above) but was never corrected afterward. Manny renders in proper mannequin skin materials, not grey.

---

## Conventions

- **Our content** lives under `Content/_Project/{Core,Characters,Components,AI,Systems,Gameplay,UI,Data,Maps}`. Imported/marketplace content stays in `Content/ThirdParty/` or the stock template folders (`Content/ThirdPerson`, `Content/Characters/Mannequins`, etc.). Never edit template folders in place — duplicate into `_Project/` first.
- **Naming:** `BP_` Blueprints, `IA_`/`IMC_` Enhanced Input, `M_`/`MI_` materials, `L_` maps, `SB_` sandbox grey-box props.
- **Blueprints-first.** Drop to C++ only for perf hot-paths or Blueprint-inaccessible features. No `Source/` module exists yet.
- **No automated test harness** — testing is manual Play-In-Editor against [`Docs/02_MVP_Vertical_Slice.md`](Docs/02_MVP_Vertical_Slice.md) §4 success criteria. This is a deliberate project decision, not an oversight — don't scaffold unit tests.

## Documentation taxonomy

This project predates the global `markdown/`-subfolder convention and uses `Docs/` instead. Keep it consistent:
- **Design docs** (vision, scope, architecture, research): `Docs/NN_Name.md` (numbered).
- **Build lessons / gotchas:** `Docs/LESSONS.md`.
- **Architecture overview:** `ARCHITECTURE.md` (root anchor).
- **Skills:** `.claude/skills/<name>/SKILL.md`.
- Root markdown is limited to the conventional anchors (`README`-style docs live in `Docs/`, plus `CLAUDE.md`, `ARCHITECTURE.md`).

## MCP workflow (Claude ↔ Unreal 5.8)

The `unreal-mcp` server drives the running editor. Key habits (full detail in the skills):
- Load tool schemas with `ToolSearch` / `describe_toolset`; `BlueprintTools`' schema is too big to fetch whole — parse the saved JSON with Python to extract the tools you need.
- Prefer `write_graph_dsl` for Blueprint logic; call `get_graph_dsl_docs` first. Fall back to `create_node`/`connect_pins` for surgical edits to existing graphs.
- **Save + verify on disk** after asset work (`save_assets`, then `ls -la` / `find ... -size 0`). Some saves silently no-op on corrupted stub packages — see LESSONS.
- Commit before starting and after each chunk. Origin: `https://github.com/Swjsh/Pool-Hop-prj` (main), Git LFS for binaries.
